# Growth Agent Run — 2026-07-08

⚠ **Needs operator (non-urgent, no action forced):**
- **ch2 quota watch** (digest `auto:false`): 8500/9000 spent by 09:00 local, `daily_limit_hit:false`.
  Same pattern as 07-06/07 — the drip clears it after the 07:00 UTC reset. Monitor only; no
  budget/drip change made (no evidence of a wall, publishes are landing).
- Reminder from last night's code fix (`7791b9b`): `MANAGER_PUBLIC_BASE_URL` is still unset in
  `.env`, so the portal-reconnect redirect fix is inert until you set it. Only matters for the
  next OAuth reconnect via the portal.

## Observed (key numbers)
| | ch1 Owera Software | ch2 Rodrigo Recio |
|---|---|---|
| oauth | connected ✓ | connected ✓ |
| published (total) | 119 (was 111) | 103 (was 97) |
| failed / stuck / review | 0 / 0 / 0 | 0 / 0 / 0 |
| board inventory | 2.0 d — at capacity | 2.0 d — at capacity |
| analytics measured | 110 / 119 | 93 / 103 |
| subscribers | 9 | 54 (+1) |

- CTR/impressions still 0 on every measured video — R8 stays signal-scarce.
- BGM pool 15→**20** this run (variety batch, below); `error_runs_24h` empty.

## Triage
**Found a real content bug: the Portuguese channel published English videos.**
- Videos **495–497** (t10 "Dicas de CrewAI para Desenvolvedores", published 07-07) have fully
  **English scripts narrated by the pt-BR voice** (verified: video 495's script text is English,
  `creation_config.voice = pt-BR-AntonioNeural`). Published videos are untouchable per guardrails —
  they stay. Root cause + code fix below (Engagement section).
- **Rejected draft 541** (t10, "Your CrewAI Crew Ignores Context…" — the 4th English idea, auto-refilled
  01:57 today, caught before render). Observed after-state: `{"id": 541, "status": "rejected"}`.
- Refilled the freed slot through the **fixed** live app: `POST /api/topics/10/generate {"count":1}`
  → draft **543 "Seus Agentes CrewAI Alucinam Porque Você Ignora Isso"** (Portuguese ✓). This is
  also the live end-to-end proof of today's code change.
- **BGM variety batch** (queued from yesterday's watch list: pool unchanged since 06-27 by file
  dates, demand real — 14 videos published across channels since yesterday):
  `POST /api/music/generate {"count":5}` → 5 tracks, 0 errors; pool count **15 → 20** (re-read
  from `/api/agent/state.bgm_pool` after).
- Everything else clean: zero failed/rejected/stuck/review rows, no oauth/cooldown entries, no
  music-gen errors.

## Learn
- **H5 / t4 re-steer verdict slips to 07-09:** videos 491–493 are still 52–60h old (not 72h) —
  yesterday's "matures ~07-08" was optimistic. Early flag: the entire recent ch2 cohort
  (487–497, 07-05→07-07) shows 0 views/0 impressions so far. That's within normal analytics lag,
  but **if it's still all-zero when mature tomorrow, the question becomes distribution, not
  content** — worth a dedicated look.
- **t13 "Agentes LLM em Produção" (ch2, long, weight 2) is a confirmed loser:** 6 mature videos,
  views 0–1 each, avp ≤13.2. Demoted **weight 2 → 1** (small step, not 0 — cohort is small and the
  channel is tiny). Observed after-state: `{"id":13, "weight":1}`. t2 (the other w2 long topic)
  keeps its weight: avg 3.2 views / 22.3 avp / 41s avd is materially better.
- Mature leaderboards otherwise unchanged in shape: ch2 concrete-topic pattern holds
  (MacBook-trava 95.5 avp / eGPU 79.5 / alugar-cluster 54.2 vs abstract-infra 12–27); ch1 leaders
  remain loop-friendly rewatch shorts on t3/t15. No other weight changes warranted.
- **Root-cause insight (the language bug):** `generate_ideas` and `worker._generate_script` never
  stated an output language anywhere — the LLM inferred it from the topic/title text. Every prompt
  example pattern in `generate_ideas` is English, so a Portuguese-named topic could still tip into
  English titles; an English title then deterministically produced an English script; the pt-BR
  voice narrated it anyway. Language was only ever implicit in the render-profile voice id.

## Engagement
**Baseline** (golden set on pre-change HEAD, 4/4 clean renders `visible=True`, frames read —
note: the label dir accumulates frames across runs; only frames matching today's printed beat
lists were scored):

| Lever | Score | Note |
|---|---|---|
| R1 hook | 2 | All 4 at t=0, pain-first ("Your RAG serves garbage" / "Toda API nova = mais código"). |
| R8 thumb/title | 2 | No title echo; genuine gaps ("Your Retrieval Is Lying" vs "Fix Your Slow RAG…"). |
| R2 info gain | 2 | **All 4 subjects now carry a code beat** (07-07 rule 2b holding), varied mixes, grounded stats. |
| R3 mobile clarity | 2 | Diagrams fill the frame, edge labels beside connectors with halo, cmp full-width stacked. Two recurring nits (2nd day): code renderer flattens `def`/`return` indentation (ch2-code); MCP hub-and-spoke rendered as a linear chain (Modelo IA→Banco→Arquivos→APIs). |
| R4–R7 / R10 / R11 | 2 | Word-synced 80–96w scripts, early turns, quotable closes, specific CTAs, per-topic accents, shorts tight. |

**Targeted: language-fit (fallback clause) → shipped `0911bed`.** The rubric baseline was strong
across levers, and the English-on-ch2 bug was unambiguously the highest-leverage engagement defect
of the day (a PT audience served EN narration = zero retention + channel-identity damage; 3 videos
already shipped wrong). Change (5 files, one purpose):
- `video_gen.language_from_voice()` + `channel_language()` — resolve the channel's language from
  its render-profile voice id.
- `generate_ideas(language=…)` — hard rule to write titles in the channel language; threaded
  through `autofill_loop`, `topics.generate`, `trends.adopt`.
- `worker._generate_script()` — pins script language to the voice, so a stray wrong-language title
  can never flip the narration language again (defense in depth for the exact 495–497 mode).

**Gate (all passed):**
- Direct behavior: `generate_ideas(t10, language="Brazilian Portuguese")` → 3 PT titles;
  `_generate_script("Stop Chaining AI Agents — Do This Instead", pt-BR voice)` → fully PT script.
- Render gate: `--only ch2-code` (PT hook "Suas integrações quebram toda semana", beats
  hook,lst,term,cmp,diagram,code,stat,stmt,cta) and `--only ch1-code` control (EN unchanged) —
  both `visible=True`, no fallback, no lever dropped.
- `verify_storyboard` **40/40**.
- **Made live:** committed → `launchctl kickstart` → `/api/dashboard` 200 → live API call produced
  PT draft 543 (observed in DB).

**Settled: R3 cmp-stack (`2092365`, 07-05) → PROMOTED** (SIGNAL-SCARCE: no cc-carrying video is
mature yet, so no retention cohort). Render-judge: today's baseline ch1-code `b1_cmp` and
ch2-concept `b5_cmp` both full-width stacked cards, VS badge in flow, no collision.

**Still running:** R3 elabel `7ec526f` (settle ~07-09 — today's ch2-code diagram already shows
"MCP" labels clean beside connectors), R2 code-beat `160afde` (re-check ~07-10), language-fit
`0911bed` (new).

**Next run's engagement target (pre-registered):** the code-beat indentation flattening — 2nd
consecutive baseline showing `return` flush with `def` in a Python snippet (syntactically wrong on
screen for a developer audience). Linear-chain diagram semantics is the runner-up.

## Monetization
Lower Tier for both; nothing crossed 100%. Binding constraints unchanged (Shorts views / watch
hours ~0%); ch2 subscribers remains the closest milestone (10.8%, +1 sub). Shorts volume ×
retention stays the strategy; today's language fix protects exactly that on ch2. No override of
the analytics-driven plan.

| Metric | ch1 current / needed / % | ch2 current / needed / % |
|---|---|---|
| Subscribers | 9 / 500 / 1.8% | 54 / 500 / **10.8%** |
| Watch hours | 2.2 / 3000 / 0.1% | 1.9 / 3000 / 0.1% |
| Shorts views | 1364 / 3M / 0.0% | 553 / 3M / 0.0% |

## Channel actions (all logged via the API)
1. `POST /api/videos/541/reject` — English draft on ch2 (observed: `rejected`).
2. `POST /api/topics/10/generate {"count":1}` — refill through fixed pipeline → PT draft 543.
3. `PATCH /api/topics/13 {"weight":1}` — demote loser long-form topic (observed: `weight:1`).
4. `POST /api/music/generate {"count":5}` — BGM variety batch (observed: pool 15→20, 0 errors).
5. Trend research/adoption **skipped by the capacity gate** — both boards `at_capacity:true`
   (10/10 pending each). Watching bench unchanged: trends 25 (*Rodar IA local em 2026*, ch2, s88)
   and 26 (*Agent memory and state in production*, ch1, s84) stay first in line for a free board.

## Watch next run
- **Settle R3 elabel (`7ec526f`)** — render-judge re-check (evidence already positive today).
- **H5 / t4 re-steer:** 491–493 mature by 07-09 — compare avp vs the ~24.6 abstract-era baseline.
- **Recent-cohort zero-views flag:** if the 07-05→07-07 ch2 cohort (487–497) is still 0 views /
  0 impressions once mature, investigate distribution (not content) — e.g. impressions pipeline.
- **Language-fit follow-through:** confirm the next t10-produced video renders PT end-to-end and
  autofill generates no new EN drafts on ch2 (check any new drafts' language during triage).
- **R2 code-beat (`160afde`)** — render-judge re-check ~07-10; later attribute via
  `creation_config` beat mix.
- **Indentation nit = pre-registered target** (see Engagement).
- **ch2 token:** watch date 07-12/13 post-Production-publish; expect NO revocation.
- Quota ch2: keep monitoring 8500/9000 pattern; act only if `daily_limit_hit` flips.

**Commits:** `0911bed` (language pinning fix) + this report/experiment-log commit.
