# Growth Agent Run — 2026-07-06

⚠ **Needs operator (standing advisory, not blocking): publish the ch2 Google app to
Production.** ch2 OAuth is connected and publishing normally today, but the Testing-mode
token has died every ~7 days — **expected next revocation ≈ 2026-07-12.** Publishing the
app in the Google Cloud Console ends the weekly reconnect cycle.

## Observed (key numbers)
| | ch1 Owera Software | ch2 Rodrigo Recio |
|---|---|---|
| oauth | connected ✓ | connected ✓ |
| published (total) | 110 (was 101) | 93 (was 87) |
| published today | 5 / 5 budget | 5 / 5 budget |
| approved awaiting drip | 1 | 4 |
| failed / stuck / review | 0 / 0 / 0 | 0 / 0 / 0 |
| board inventory | 2.0 d — at capacity | 2.0 d — at capacity |
| analytics measured | 100 / 110 | 78 / 93 |
| subscribers | 9 | 53 |

- Quota 8500/9000 (shared), `daily_limit_hit:false` — both channels exhausted their 5-video
  publish budgets by 09:00 local, so the reading is budget-gated throughput, not a stall.
  Resets 07:00 UTC 07-07; next publish ETA 00:00 UTC 07-07.
- CTR/impressions still 0 everywhere — R8 stays signal-scarce.
- BGM pool 15/15 (`is_low:false`); `error_runs_24h` empty.

## Triage
**No operational issues found.** Issues digest: zero failed/rejected/stuck/review rows, no
OAuth or cooldown entries, no music-gen errors. The only two entries are the `quota`
monitors (`auto:false`, "monitor") covered above — no action taken, self-resets at 07:00 UTC.
No remediation API calls were needed this run.

## Learn
- **ch2 concrete-topic pattern keeps holding** (mature leaderboard): MacBook-trava 95.5 avp,
  MacBook+eGPU 79.5, alugar-cluster 54.2 vs abstract infra 12–27. The 07-05 t4 re-steer is
  visibly working at the idea level — new t4 videos 491–493 all lead with concrete pain
  ("Docker roda seu modelo local mas explode em produção"; "Sua GPU fica ociosa metade do
  treino") — but all are <24h old, so no retention verdict yet (H5 still open).
- **ch1**: t15 (Copilot Credits) still the mature retention leader among high-view topics;
  top mature avp entries are loop-friendly shorts (avp >100% = rewatches: 225.1, 139.4,
  125.0). Shorts remain the discovery vehicle on both channels (avg views 14.1 vs 2.2 ch1;
  17.7 vs 3.2 ch2). No weight changes warranted today.
- **Experiments settled: none matured.** R3 diagram-fill (`9268c6f`, shipped 07-04) hits
  72h ~07-07; R3 cmp-stack (`2092365`, 07-05) ~07-08. Both left `running` — cohorts too
  young to judge, and inventing a verdict is worse than waiting.
- Hypotheses: (H6, new) diagram beats whose edge labels are legible (today's change) read
  better on mobile and lift avp on diagram-carrying videos. (H4/H5 carried, above.)

## Engagement
**Baseline** (golden set on HEAD, 4/4 clean renders `visible=True`, frames read):

| Lever | Score | Note |
|---|---|---|
| R1 hook | 2 | Hook beat at t=0 on all 4; pain-first ("Right docs, dumb answers" / "Sua RAG busca lixo"). |
| R8 thumb/title | 2 | 0/4 title-echo; genuine curiosity gaps ("Why Your Answers Feel Dumb" vs "Fix Your Slow RAG in 5 Lines"). |
| R2 info gain | 2 | Real code, stats, diagrams; varied 8–10 beat mixes, ≤2 statements. |
| R6 payoff | 2 | Quotable closes ("Se o chunk não faz sentido sozinho, sua RAG também não vai."). |
| **R3 mobile clarity** | **1** | **Weakest.** cmp-stack (07-05) and diagram-fill (07-04) both hold, but the diagram connector line struck straight through its edge label on BOTH diagram baselines ("context" ch1-concept — also clipping the Store box border; "protocolo" ch2-code). |

**Targeted R3 → shipped `7ec526f`.** In `storyboard.py`: portrait edge labels now render
beside the vertical connector (start-anchored at +18px, vertically centered in the gap)
instead of centered on it, and `.elabel` gets a `paint-order:stroke` halo so any edge line
passes behind the glyphs. Landscape placement untouched; no other beat's CSS in scope.
- **Gate (all three passed):** after-renders `--only ch1-concept` + `--only ch2-code` —
  ch1-concept's "store" label sits cleanly beside the line, nothing clipped, no
  strike-through (the exact defect in the baseline frame); ch2-code's regenerated diagram
  came back without edge labels this render, confirming no regression on the unlabeled
  path; all other frames unchanged-good (cmp still full-width stacked). Both renders
  `visible=True`, no fallback; `verify_storyboard` **40/40**.
- **Made live:** committed → `launchctl kickstart` restart → `/api/dashboard` 200
  (observed).
- Known pre-existing nit (unchanged, both baseline and after): the last edge's arrowhead
  can appear detached in an extracted frame because edges are still line-draw animating at
  capture time — frame-timing, not a layout defect. Left alone.
- Logged as a new R3 experiment in `run/experiments.jsonl` (`status:running`, predicted
  `avg_view_pct` up; render-judge re-check ~07-09).

## Monetization
Lower Tier for both; nothing crossed 100%. Binding constraint unchanged on both channels
(Shorts views / watch hours at ~0%); ch2 subscribers is the closest milestone (10.6%).
Shorts volume × retention stays the lever — today's R3 fix targets exactly that. No
override of the analytics-driven plan.

| Metric | ch1 current / needed / % | ch2 current / needed / % |
|---|---|---|
| Subscribers | 9 / 500 / 1.8% | 53 / 500 / **10.6%** |
| Watch hours | 1.9 / 3000 / 0.1% | 1.5 / 3000 / 0.1% |
| Shorts views | 1238 / 3M / 0.0% | 550 / 3M / 0.0% |

## Channel actions
**None this run — by design.** Both boards `at_capacity:true` (capacity gate: no idea
generation, no trend adoption), zero triage rows, BGM pool at target. Trends 25 (*Rodar IA
local em 2026*, ch2, score 88) and 26 (*Agent memory and state in production*, ch1, score
84) remain `watching` for the first board that frees up. Trend research skipped this run:
both channels are at capacity so nothing could be adopted, and the watching bench already
holds two high-scoring candidates matched to each channel's proven pattern.

## Watch next run
- **Settle R3 diagram-fill (`9268c6f`)** — matures 07-07: compare ch1 avp of post-07-04
  videos vs before (retention data is flowing, so SIGNAL-RICH is possible).
- **Settle R3 cmp-stack (`2092365`)** ~07-08 (shorts carrying cmp beats).
- **Settle R3 elabel fix (`7ec526f`)** ~07-09 — render-judge re-check that labels stay
  clean (SIGNAL-SCARCE).
- **H5 / t4 re-steer:** videos 491–493 mature ~07-08/09 — compare their avp vs the ~24.6
  abstract-era t4 baseline.
- **Quota window:** drip resumes 00:00 UTC but quota resets 07:00 UTC with only 500 units
  headroom — confirm the publish loop defers cleanly (no `failed` rows from a quota
  breach) between 00:00 and 07:00 UTC.
- **ch2 token: expected revocation ≈ 07-12** — if `oauth` flips, lead with the reconnect
  escalation + Production-publish durable fix.
- **BGM staleness:** pool has been 15/15 since at least 07-05; if unchanged by ~07-08,
  generate a variety batch of 5.
- CTR/impressions: still waiting; R8 re-settles on real CTR when they flow.

**Commits:** `7ec526f` (R3 elabel fix) + this report/experiment-log commit.
