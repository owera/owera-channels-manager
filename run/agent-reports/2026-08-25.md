# Growth agent — 2026-08-25

## ⚠ Needs operator

1. **PAUSE ch1 (hard review overdue since 08-01).** Still **12 subscribers** vs the 15-sub bar. **New attribution:** v1032 t6 short “Stop Loading Every Tool Upfront — Your AI Agent Chokes Picking One” (23 views / 27.6% avp / SHORTS=22 / YT_SEARCH=1 / age 99h) now carries `subscribers_gained=1`. Lifetime video-attributed converters are **five** (v558 MCP, v367 t6, v43 t1, v96 t5, **v1032 t6**). The 11→12 move is no longer unattributed. 12 still does not clear 15. **Recommendation stands: PAUSE ch1** (or explicitly override with a new bar). Agent continues **zero ch1 strategy moves** (no weight/trend adopt/generate for growth). Pipeline hygiene only.
2. **ch2 metadata backfill** (`db7a254` plan) still awaits `--apply` if still desired.
3. **BGM pool path** remains external (`MoneyPrinterTurbo/resource/songs`). Count 25 (target 15); wipe risk still escalates to silent renders.
4. **Flood at 11:42 UTC (contained this run).** An unattributed API actor adopted 10 watching ch2 trends and bulk-produced the bench (pending 10→90) ~18 min before this run. Topics 33–42 parked w=0; 80 videos rejected (subjects retained). Guard shipped so adopt now 409s at capacity. Operator: do not unpark t33–42 and do not click Adopt on a full board.

---

## Observed

- **Now:** 2026-08-25 ~12:00 UTC. Kill switch off, `scheduler_paused: false`, tree clean at start, `main`. Last full growth run: 2026-08-24.
- **ch1:** published **5/5** today, quota **8750/9000** (`daily_limit_hit: false`). Mix = **1 long + 4 shorts** (v1056 t5 long first @ 07:00 `__qk0n-1W48`, then v1059 t6, v1060 t6, v1058 t6, v1069 t18). **Twentieth consecutive 1L+4S day** (08-06…08-25).
- **ch2:** published_today **0** at observe (window opens **14:00 UTC**). rendered_today **5**. **08-24 did publish 1L+4S** (v1054 t2 long first @ 14:01 `povqQQqrg7A`, then v1037/1061/1062/1063 **all t4**) — yesterday’s 1L + 4×t4 prediction held.
- **ch2 subscribers: 64** (unchanged). Lifetime converters still **seven** (v542/524/810/421/684/315 + v1022). v1022 still 3 views / 65.1% avp / YT_SEARCH=3 / +1.
- **ch1 subscribers: 12.** Subs-per-video on the mature leaderboard: **5 lifetime +1s** (v1032 is the new row). None on the post-07-13 Copilot-credits wedge (t15 still 0 subs on 46 videos).
- **BGM pool:** 25 (min 5 / target 15; `is_low: false`). No `music_gen` errors. No `error_runs_24h`. Last generate was 2026-08-24; not stale. No refresh this run.
- **Boards after triage (API `by_format`):**
  - ch1 draft `{long:0, short:0}` · queued `{long:2, short:8}` · approved `{long:0, short:2}` (1013/1014 t7 leftover)
  - ch2 draft `{long:0, short:0}` · queued `{long:1, short:9}` · approved `{long:1, short:11}` (v1055 t2 long leading)
  - Both `at_capacity: true`, `days_of_inventory: 2.0`.
  - Observe-at-start was ch2 queued **90** (see Triage). After-state is the quote above.
- **Language (ch2):** 08-24 titles all PT-BR ✓ (1054 rollback produção; 1037 Ryzen iGPU; 1061 GPU integrada; 1062 40GB/7GB; 1063 R$500 cloud). Leading approved long v1055 also PT-BR (“CI/CD para ML vs Software Tradicional…”). No EN leak.
- **traffic_json (via leaderboard API, no DB dive):** v542 SHORTS=385; v785 SHORTS=164 + YT_SEARCH=6; v558 SHORTS=104 + YT_SEARCH=12; v948 SHORTS=64 + **EXT_URL=9** (unchanged vs 08-17…08-24) + NOTIFICATION=3; v884 YT_SEARCH=22 + SHORTS=77; v1022 YT_SEARCH=3; **v1032 SHORTS=22 + YT_SEARCH=1**. SHORTS / YT_SEARCH dominate. **No BROWSE / SUGGESTED keys** on the top samples. No invented CTR.

### Publish log (settle evidence)

| day | ch1 | ch2 |
|-----|-----|-----|
| 08-17 | 5 (1L+4S) long 972 first | 5 (1L+4S) long 955 first + 4×t4 |
| 08-18 | 5 (1L+4S) long 962 first + 3×t18 + t6 | **5 (1L+4S)** long 957 first + 4×t4 |
| 08-19 | **5 (1L+4S)** long 959 first + t18 + 3×t6 | **5 (1L+4S)** long 956 first + 3×t4 + t27 |
| 08-20 | **5 (1L+4S)** long 961 first + t18 + 3×t6 | **5 (1L+4S)** long 958 first + **4×t4** (1020–1023, **v1022 converted**) |
| 08-21 | **5 (1L+4S)** long 960 first + t18 + 3×t6 | **5 (1L+4S)** long 982 first + **4×t4** (1024–1027) |
| 08-22 | **5 (1L+4S)** long 963 first + 4×t6 | **5 (1L+4S)** long 1038 first + t4 1028 + 3×t27 |
| 08-23 | **5 (1L+4S)** long 999 first + 3×t6 + t18 | **5 (1L+4S)** long **1052** first + **4×t4** |
| 08-24 | **5 (1L+4S)** long 1005 first + 3×t18 + t6 | **5 (1L+4S)** long **1054** first + **4×t4** (1037, 1061, 1062, 1063) |
| 08-25 | **5 (1L+4S)** long 1056 first + 3×t6 + t18 | pending window (0 at observe; 1L+11S banked, **v1055 leading then 4×t4**) |

## Monetization

| metric | ch1 current | ch1 needed | ch1 % | ch2 current | ch2 needed | ch2 % |
|---|---|---|---|---|---|---|
| subscribers | 12 | 488 | 2.4% | 64 | 436 | 12.8% |
| watch hours | 7.6 | 2992.4 | 0.3% | 12.0 | 2988.0 | 0.4% |
| Shorts views | 4454 | 2,995,546 | 0.1% | 4370 | 2,995,630 | 0.1% |

**Binding constraint both channels: subscribers** (lower tier). No milestone crossed (no metric hit 100% for the first time). ch1 just attributed a 5th sub on t6 without moving the channel total off 12 — prefer analytics (keep t4 as ch2 converter lead; ch1 frozen) over pure milestone math.

## Triage

Digest at start: **not clean**. `failed=[]`, `pipeline_starved` / stuck / oauth / bgm_pool_low / music_gen / `error_runs_24h` empty. Quota monitor + **board_overflow on t33–t42** (8 pending each) + ch2 `days_of_inventory: 18.0` / queued **90**.

**Incident (11:42–11:43 UTC, JobRuns 20349–20428):** 10 `trend_adopt` then `bulk-produced via API`. Topics 33–42 created (weight 1, 8 ideas each). ch2 queued by topic at observe: t27×4 + t2×1 + t4×5 + **t33–t42 ×8 = 80**. ch1 also bulk-produced its 10 drafts → queued (horizon-sized, not a growth; left in place).

| Issue | Action | Proof |
|---|---|---|
| t33–t42 flood (80 queued) | `PATCH /api/topics/33..42 {"weight":0}` then `POST /api/videos/{id}/reject` ×80, reason `flood adopt 2026-08-25 — displaced; subject retained; topic parked` | park read-back: all 10 `weight=0`. reject: 80/80 `status=rejected`, 0 fail. After: ch2 queued **10** by topic `{27:4, 2:1, 4:5}`, draft 0, rejected 80 (all t33–t42). `board_inventory` ch2 pending 10 / days 2.0 / `at_capacity: true`. `board_overflow: []` |
| ch1 10 queued (2L+8S) | none — this is the previous draft bench, not new topics | queued `{long:2, short:8}` = horizon; mix 2 t5 + 4 t6 + 3 t18 + 1 t7 |
| quota ch1 8750/9000 | monitor only | full 5-publish day (5×1750=8750); `daily_limit_hit: false` |
| failed / stuck / oauth / bgm_low / pipeline_starved / error_runs_24h | none | clean lists |

After-state board (API): ch1 queued `{long:2, short:8}` · approved `{long:0, short:2}`; ch2 queued `{long:1, short:9}` · approved `{long:1, short:11}` (v1055 leading). Failed list stays empty. Rejected list is 80 brand-new rows — **not deleted** (age < 7d).

## Learned

1. **ch2 08-24 mix prediction held:** 1L+4S, long v1054 first, then **4×t4**. This is the 72h settle for `rebalance-short-for-long`.
2. **ch1 11→12 is now attributed** to v1032 t6 (LangChain tips, SHORTS=22). Not the Copilot wedge. Freeze still holds (12 < 15).
3. **Adopt had no board-horizon guard** — generate and autofill already stop at `budget × horizon`; adopt did not. That is how 10 clicks exploded the bench. Guard live-verified this run.
4. **Winners (mature):** ch2 t4 23.1 avg views + **5 lifetime subs** (the only converter that still moves); t28 25.8 @ 59.5% avp / 0 subs; t27 19.6 avg / 42.9 avp / **0 subs**. ch1 t18 MCP 14.1 avg + 1 sub; t6 now **2 lifetime subs** (v367 + v1032); t15 high-avp loops, 0 subs on 46 videos.
5. **Hypotheses this run:** (a) ch2 08-25 window publishes 1L+4S with v1055 leading then 4×t4; (b) 14:00 today is the first live “do not drain” for `rebalance-headroom-zero` (queued shorts exist, `approved_longs` will hit 0, `rendered_today==5`); (c) a later at_capacity adopt (SPA or agent) 409s and the bench does not grow.

`rebalance-short-for-long` (shipped 08-22, `d2d158f`): **PROMOTED** — 72h+, two live days (08-23 v1052, 08-24 v1054).

`rebalance-headroom-zero` (shipped 08-23, `22056ea`): still <72h for a live 14:00-with-queued-shorts. Today’s 14:00 is the first real shot (ch2 queued=10, rendered_today=5). Leave `running`.

`queue-plan-mix` (shipped 08-24, `6005571`): live queue-plan this noon labels every keeper `render budget full (5/5)` — no “next to render” to quote while the budget is spent. Leave `running`.

## Acted (channels)

- **Parked t33–t42 weight 0.** Proof: PATCH read-back `weight=0` on all 10.
- **Rejected 80 flood videos** (ids 1084–1163 on t33–t42). Keepers left queued: 1048–1051 t27, 1073 t2 long, 1074–1078 t4. Cap for delete of old failed was not used (these are new rejected).
- **No weight changes** on converters. t4=4 / t27=3 / t2=2. ch1 frozen.
- **Trends logged (no adopt/displace — boards at capacity; t33–t42 parked so a new topic cannot displace a w4/w3 keeper):**
  - **#73** ch2 score **86** (was 85) adopted/parked refresh — Qwen3.8-27B Ollama **732.2K** (was 688.4K, +44K/day). Proof: `POST /api/trends` → id 73, status=adopted, topic 33.
  - **#82** ch2 score **84** adopted/parked refresh — Muse Glimmer still **161.1K** (flat). Proof: id 82, topic 35.
  - **#78** ch2 score **83** (was 84) adopted/parked refresh — Nemotron **130.9K** (flat). Proof: id 78, topic 34.
  - **#81** ch2 score **81** adopted/parked refresh — Ollama 0.32.15 still latest stable (0.33 is rc3 as of 08-24). Proof: id 81, topic 36.
  - **#83** ch2 score **76** adopted/parked refresh — Ollama 0.33 still rc3; docs now say `ollama launch claude-desktop` is no longer supported. Proof: id 83, topic 40.
  - **#77** ch1 score **70** reject refresh — Copilot Max credits — *is* the wedge, rejected because ch1 is frozen. Proof: id 77, status=rejected.
  - **#84** probe trend: created watching, adopt 409’d, then PATCH rejected (`live 409 probe 2026-08-25 — discarded`).
- **No produce / no generate / no displace** after the restore. Auto_produce owns the overnight mix.
- **No BGM generate** — pool 25, refreshed yesterday.
- **Seeding kit:** `run/seeding/2026-08-25.md` (operator posts; agent does not). Features v1032 (new ch1 converter) + yesterday’s t4 1037/1062 + t2 long 1054.

## Engagement

**Baseline `baseline`** — 4/4 clean, `visible=True`, none `used_fallback`. ~239s. Scores from frames + scripts + beat timings:

| Lever | score | evidence |
|---|---|---|
| R1 hook (P1) | 2 | Pain-first on frame + opener ≤~12w: “Your RAG returns garbage, and you don't know why.” / “Your AI forgets you the second you close the tab.” / “Cansado de reescrever a mesma integração toda semana?” / “Sua RAG responde qualquer coisa, menos o que você perguntou.” Hook cards: “Your RAG returns garbage” / “Your AI forgets you instantly” / “A mesma integração de novo?” / “Sua RAG responde qualquer coisa”. Hook at t=0 all 4. |
| R8 thumb/title (P2) | 2 | Gaps vs titles (hook “Your search returns garbage answers” vs title “Fix Your Slow RAG…”; “Goldfish memory, fixable tonight” vs “Why Your AI Agent Forgets…”; PT “Um Só Conecta Tudo” / “Cortou a frase no meio”). Zero title-word echo. |
| R2 info gain (P3) | 2 | Varied mixes (ch1-code: hook/stmt/diagram/term/stat/code/lst/quote/cta + `top = rr.rank(query, hits)`; ch1-concept: Feels like memory vs Actual memory + list; ch2-code fanout MCP → Ferramentas/Dados/Sistemas + `@mcp.tool()` indented `return db.query(q)`; ch2-concept `split(text, by="section", overlap=50)` + 3-item list all visible). |
| R6 payoff (P4) | 2 | Quotable closes (“Retrieval finds the neighborhood; reranking finds the house.” / “Memory isn't magic—it's engineering.” / “Integre uma vez, use pra sempre.” / “Chunk ruim é lixo entrando; lixo entrando é lixo saindo.”) |
| R5 retention (P5) | 2 | Open loops early on all 4 (“Here's the truth: your vector search is fine” / “That's not a bug” / “o problema nunca foi a ferramenta” / “O problema não é o modelo”). |
| R3 clarity (P6) | 2 | Code legible + indented (ch2-code `return db.query(q)` under `def buscar(q):`; ch1-code five-line rerank). CMP stacked + VS in flow (Feels like memory vs Actual memory; Sem MCP vs Com MCP). Fanout on ch2-code truthful. List midpoint frames show all items (ch2-concept: parágrafos / seções / ideias completas). |
| R4 pacing (P7) | 2 | Hook at t=0. Min beat 1.68s (ch1-code stat + ch2-concept term — floor holding). Max hold **6.905s** (ch2-code lst) — yesterday’s 9.01s cmp drag is gone this render. No sub-1.7s flashes. Drift still owns the long holds. Prompt-level R4 is exhausted — do not ship another R4 prompt rule. |
| R7 CTA (P8) | 2 | Follow/Siga + named next value 4/4 (“Cut RAG latency with caching” / “Exact memory setup tomorrow” / “Amanhã: seu 1º servidor MCP” / “Amanhã: o tamanho exato”). Spoken ask present on all 4 scripts. |

**All-2 baseline (22nd consecutive judged day) → non-engagement path (fallback clause).** No prompt change. The pipeline bug (adopt ignoring the horizon) was clearly the higher-leverage work.

**Shipped instead (pipeline-robustness):** `POST /api/trends/{id}/adopt` 409s when the channel bench is already at `daily_render_budget × board_horizon_days`, and otherwise clamps `idea_count` to remaining seats. Same guard generate and autofill already had.

Lifecycle (written before the change, as required):

```
draft ─produce→ queued ─render_loop._submit_new→ rendering ─render_loop._advance→
   rendered → (review | approved)
approved ─publish_loop→ publishing → published
```

adopt creates DRAFT and QUEUED rows (and a Topic). The 409 path creates **neither** — it does not set `Video.status`. Publish still only consumes APPROVED. Render still only consumes QUEUED.

| | at_capacity (pending = horizon) | 2 seats left, idea_count=8 produce_count=3 |
|---|---|---|
| adopt before | new topic + 8 videos (3 queued) | new topic + 8 videos |
| adopt after | **409**, trend stays watching | 2 videos, produce_count clamped to 2 |

Proof: `PYTHONPATH=. .venv/bin/python tests/verify_trends.py` → **29/29**. `uv run python -c "import app.main"` ok. Manager restarted (`launchctl kickstart`); `GET /api/dashboard` = 200. Live `POST /api/trends/84/adopt` against ch2 pending=10 → **409** `board at capacity (horizon reached) — displace drafts first`; pending stayed 10; max topic id stayed 42; trend 84 stayed watching then was PATCH-rejected as a discarded probe.

### Experiments settled / opened

- Settled: **`rebalance-short-for-long` PROMOTED** (see experiments.jsonl).
- Left running: `rebalance-headroom-zero` (first live 14:00-with-queue is today, after this run exits); `queue-plan-mix` (budget-full at noon, no “next to render”).
- Opened: `adopt-board-cap` commit `21fab8c`, status=running. Settle from the next at_capacity adopt attempt — it must 409 and the bench must not grow.

## Watch next run

1. Operator ch1 pause yes/no (still the binding decision). 12 subs does not clear the 15-sub bar — do not unfreeze. v1032 is the 11→12 attribution; do not treat t6 as a reason to revive t15.
2. ch2 08-25 publish composition after 14:00 UTC — expect **1L+4S** with t2 long **v1055** leading then **4×t4** (1064, 1065, 1066, 1067).
3. First live “do not drain” for `rebalance-headroom-zero`: today 14:00 ch2 has 10 queued and `rendered_today==5`. After v1055 publishes, `approved_longs` hits 0. Those 10 should still be queued at the next noon (not five produce-demote JobRuns). The long 1073 is already queued, so even a demote would be a no-op for mix — quote the JobRun list anyway.
4. `queue-plan-mix` live signal still needs a noon where a long is queued behind lower-id shorts **and** `approved_longs==0` **and** render budget is not already spent. Today budget was full so every card said `render budget full`.
5. Failed list stays empty. The 80 rejected t33–t42 rows age toward the 7-day delete cap — do not delete before that.
6. Do not unpark t33–t42. Qwen3.8 (#73, 732.2K) is the only watching-class term still rising; serve it as a **t4 episode via displacement** if a w1-or-lower draft exists, not by unparking t33.
7. BGM count stays ≥ min (25, above target 15). Next refresh only if stale again *and* renders landed.
8. Quote whether v948 EXT_URL grows past 9 (seeding signal). Quote whether v1032 views/avp grow now that it has a +1.
9. A SPA/agent adopt against an at_capacity board must 409 (the `adopt-board-cap` settle signal).

## Commit

`21fab8c` (adopt board-capacity 409). Report follows in the next commit.
