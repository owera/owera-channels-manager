# Growth agent — 2026-07-27 (Monday)

**Commits:** 6091ab9 (delete-audit JobRun), + this report/log commit.

> ## ⚠ Needs operator
> 1. **Seeding kits: posted or not? (4th day asking.)** EXT_URL is still ch1 3 / ch2 6
>    cohort-wide. One word ("yes"/"no") changes how I read that number and whether I
>    keep drafting link-posts or switch to comment-only seeding.
> 2. **The ~35-video ch1 deletion (07-25/26) intent is still unconfirmed** — I'm
>    operating on "backlog cleanup". Note: as of today, any delete (UI or API) writes
>    an audit JobRun, so a repeat will be visible in /api/runs with per-video detail.
> 3. Quota 8750/9000 daily on both channels — same deterministic ceiling as yesterday
>    (5 publishes + analytics). No action needed; flagging closeness.

## The day's main read: yesterday's autofill fix works live — the mix is restored
Both watch-items from 07-26 landed **positive, DB/JobRun-verified**:

1. **Publish mix (directive 2) restored on both channels.** ch1 published 1 long +
   4 shorts with **t15 Copilot-wedge leading 3 of the 4 shorts** (videos 774–776 — the
   drafts generated in yesterday's rebalance; t15's first publishes since ~07-18), plus
   one t6 short (747). ch2: 1 long (724) + 4 shorts (744/745/777/778), all PT-BR titles
   (language integrity clean).
2. **Autofill's first live weight-first walk refilled the right topics.** JobRuns
   10541/10542 (00:13): "auto-refilled 5 ideas for 'Fundamentos da Engenharia de IA'"
   (ch2 t4, w3) and "auto-refilled 5 ideas for 'GitHub Copilot Credits 2026'" (ch1 t15,
   w3) — weight-DESC, no id-order dump into longs, no overshoot (board_overflow empty).
   Bench now: ch1 = 5× t15 shorts + 5× t5 longs; ch2 = 5× t4 shorts + 5× t2 longs.
3. **Bonus verify — last night's render-budget fix (8acbc1a, code agent) holds:**
   exactly **5 render-starts per channel** today (JobRun count by channel), vs the 8/5
   overshoot it fixed. BACKLOG #15 stays closed.

The board-mix experiment (ed719f8) stays `running` per protocol (settles ≥72h,
07-29) — but its named signal has fired on day 1.

## Triage
Digest: 16 entries, none actionable today. The 12 `rejected` rows are yesterday's
deliberate board rebalance (`suggested_action:"leave"`, 35h old — delete gate is >7d).
`publish_starved` ch1 3 / ch2 4 approved is the known auto-produce steady state
(tonight's 00:00 produce+render refills; verified it did exactly that last night).
No failed / stuck / oauth / error_runs_24h / board_overflow; BGM 20 tracks (min 5),
not stale-eligible for a refresh top-up beyond target. **No remediation actions taken —
nothing needed one.**

## Observed (key numbers)
- **Subs: ch1 10, ch2 60 — both flat for the entire week** (channelmetric daily history
  07-20→07-27 shows no movement on either). This is the Growth review headline below.
- **Traffic snapshot is 24h stale by timing:** the analytics pass runs ~12:16 UTC and
  this run started at 12:00, so latest traffic_json is the 07-26 12:16 capture (ch1
  SHORTS 819 / SEARCH 98 / EXT_URL 3 / SUBSCRIBER 6; ch2 SHORTS 1349 / SEARCH 244 /
  EXT_URL 6 / SUBSCRIBER 2). No fresh delta to read today; tomorrow's run reads two
  days of movement.
- **Leaderboard notables (mature only):** ch2 589 "Por que seu Ollama roda LENTO mesmo
  com GPU" — avp **76.3%**, 109 views: the tool-shaped-title + hardware-reality vein
  keeps outperforming. ch2 search terms stay tool-shaped (qlora 3, langgraph 3, fine
  tuning 2, claude code 2, cuda vs rocm). ch1's only mature sub-gainer remains 558
  (MCP long, avp 58.9).
- **ch1 subs-per-video (wedge measure):** mature 07-20→07-24 cohort still 0 subs
  gained; the *restarted* wedge cohort (t15, first publishes today) matures ~07-30.

## Monetization
| Metric (Lower tier) | ch1 current / needed / pct | ch2 current / needed / pct |
|---|---|---|
| Subscribers | 10 / 490 / **2.0%** | 60 / 440 / **12.0%** |
| Watch hours | 4.3 / 2995.7 / 0.1% | 7.4 / 2992.6 / 0.2% |
| Shorts views | 2771 / ~3M / 0.1% | 2470 / ~3M / 0.1% |

Subscribers remain the binding constraint on both channels; no metric crossed a
threshold. Conversion work (wedge + CTA machinery) stays the priority.

## Actions taken
1. **ch2 publish windows ACTIVATED** (the planned single-variable change, done after
   reading the mix restoration): `PATCH /api/channels/2 {"publish_windows":
   "11:00-22:00","publish_tz":"America/Sao_Paulo"}`. Verified: API + DB read-back both
   show the values; exercised `_window_ok`/`next_window_open` against the live row —
   04:30 BRT → blocked, 12:00 BRT → allowed, next-open from a dead hour = 14:00 UTC
   (= 11:00 BRT sharp); ch1 (no windows) unaffected. Today's ch2 publishes had already
   run 07:02–09:05 UTC (04:02–06:05 BRT) *before* activation — tomorrow's publish
   times are the live read. ch1 deliberately left unset (global audience).
2. **Code change (1 of ≤2): DELETE /api/videos/{id} now writes an audit JobRun**
   (`kind:"delete"`, status/subject/yt-id snapshot in detail) — closes the
   observability gap that made the operator's ~35-video deletion invisible to
   /api/runs. Verified: new `tests/verify_video_delete.py` (8 checks) exercises the
   real endpoint via TestClient on an isolated DB — delete → 204, row gone, exactly one
   JobRun with video_id/channel_id/detail; missing-id delete → 204 and **no** JobRun.
   Adjacent suites green: quota 39, health 17, issues 74, publish 70, storyboard 40,
   autofill 10. Manager restarted, dashboard 200. *Live-path caveat:* no
   guardrail-eligible video existed to delete against production (rejected rows are
   35h < 7d), so the live confirmation will be the first real delete appearing in
   /api/runs — the endpoint-level test is the behavioral proof.
3. **Trend log:** persisted 2 candidates as `watch` (both boards at_capacity — adoption
   barred today): #38 "Claude Opus 5 launch (2026-07-24)" (score 85, ch1 — adopt as an
   Opus-5-vs-Copilot wedge comparison when bench space frees) and #39 "Kimi K3 2.8T
   open MoE" (score 74, ch2 — hardware-reality-check angle, t4's vein). No adoption, no
   idea generation, no weight changes (weights already positioned; digest clean).
4. **Seeding kit** `run/seeding/2026-07-27.md`: ch2 TabNews dual-monitor-VRAM post +
   r/brdev memory-bandwidth comment + adjacent-channel comment; ch1 Copilot
   stack-trace-credits comment (r/LocalLLaMA / r/ChatGPTCoding), dev.to junior-burn
   post, LangChain-vs-CrewAI adjacent comment. All feature today's videos.
5. **Settled R7 spoken-CTA experiment** (below) and updated `run/experiments.jsonl`.

## Engagement
**Baseline (full golden set, 4/4 clean renders, visible=True, no fallback):**
| Lever | score | evidence |
|---|---|---|
| R1 hook (P1) | 2 | 4/4 pain-first ≤11w at t=0 ("Your RAG returns garbage", "Sua RAG responde lixo"); holds 4.4–6.6s |
| R8 thumb/title (P2) | 2 | anchored hooks, zero title echo 4/4 |
| R2 info gain (P3) | 2 | 8–9 varied beats/subject; real code (valid indented Python on ch1-code), diagram, stat, cmp |
| R6 payoff (P4) | 2 | quotable closes 4/4 ("Uma integração pra governar todas as outras") |
| R5 retention (P5) | 2 | truth-pivot mid-script, no sag |
| R3 clarity (P6) | 2 | stacked cmp cards intact, VS in flow, nothing clipped |
| R4 pacing (P7) | **1** | min beat 1.88s everywhere (floor holds) BUT ch2-code b2 cmp held **10.39s** — the known content-level drag (07-13: 10.12s, 07-16: 10.44s). Drift tween present on all >5.5s beats (2/2/3/3 per subject) as designed |
| R7 CTA (P8) | 2 | spoken follow-ask + named next-value 4/4; visual card states the same promise |

**Shipped engagement change: none — deliberate no-op, fallback used on code.** R4 is
the only weak lever and it's explicitly parked ("prompt-level pacing exhausted;
revisit only with a content-aware mechanism" — 07-16 verdict), its mitigation (drift)
has a running experiment that settles **tomorrow**, and shipping another R4 change
today would confound that settle. The change cap went to the delete-audit fix (1 of 2
used).

**Settled:** **R7 spoken-CTA (f79fa33, 07-24) → PROMOTED** (72h, SIGNAL-SCARCE — subs
flat all week, no conversion cohort to measure; render-and-judge is the evidence).
07-27 baseline: 4/4 closes carry the spoken imperative + named next-value in-language,
visual cta states the same promise ("Cut RAG latency in half" / "Amanhã: 1º servidor
MCP"). No word-count or clipped-sub regression. Kept in code; re-settle on
subscribers_gained when a cohort actually converts.

**Running:** R4 long-hold drift (70f5320) settles ~07-28 — today's evidence positive
(every >5.5s beat carries exactly one drift tween, layouts intact). board-mix
(ed719f8) settles ~07-29 — day-1 signal fired in full (see top).

## Growth review (Monday)
- **Subs delta, week 07-20→27: ch1 +0 (10), ch2 +0 (60).** Worst week since the
  offensive started — ch2 had been +6/week before the plateau.
- **What the week actually tested:** nothing it was supposed to. The wedge (the ch1
  strategy) was dark 07-18→26 from the autofill bug; ch2's publishes landed 04:00–06:05
  BRT all week (audience-dead); seeding posted-status unknown. The conversion machinery
  (R1 hook cap, R7 spoken CTA, publish order) is all shipped and *holding on renders* —
  but the distribution-side preconditions were broken the whole window.
- **Traffic-source mix:** SHORTS-feed remains the only real discovery channel
  (ch1 819 / ch2 1349 cohort views); search is small but real and tool-shaped on ch2
  (+~18/day pace pre-plateau); BROWSE/SUGGESTED ≈ 0. Directive-7 falsifier still NOT
  triggered (SHORTS feed = algorithmic testing), but the flat week weakens it — one
  more flat week at restored preconditions forces the distribution escalation.
- **Series:** t15 numbered-series cadence restarts today (3 published); ch2's hardware
  vein is a de-facto series and produced the week's best retention (589: avp 76.3).
- **The single biggest bet for the coming week:** the restarted t15 wedge cohort
  publishing into live hours (ch2 windows active; ch1 wedge-led), measured by
  per-video `subscribers_gained` before the **08-01 ch1 review**. Secondary: adopt
  Opus 5 (#38) into the ch1 wedge the moment bench space frees.

## Watch next run (07-28)
1. **ch2 publish times must land ≥ 14:00 UTC (11:00 BRT)** — first live read of the
   window gate; ch1 unchanged (~07:00 UTC). If ch2 publishes at 07:xx UTC again, the
   window gate is broken — investigate before anything else.
2. **R4 drift experiment settles** — re-render-judge per protocol.
3. Day-2 mix read (1L+4S, t15 leading) toward the board-mix settle on 07-29.
4. Two days of traffic delta to read (today's pass was 16 min after run start).
5. If ch1/ch2 bench space frees, adopt trend #38 (Opus 5) into the wedge.
6. Wedge cohort first subscribers_gained reads ~07-30; **08-01 ch1 review is Saturday.**
