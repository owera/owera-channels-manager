# Growth Agent Run — 2026-07-04

⚠ **Needs operator — reconnect Channel 2 (Rodrigo Recio) OAuth.**
The refresh token is still **expired** (`token missing/expired for channel 'rodrigo-recio' —
reconnect required`). ch2 has published **0 videos since 2026-07-01 09:05 UTC** (~75h) and cannot
upload until you re-run the OAuth consent flow (Channels → reconnect `rodrigo-recio`). The 8
previously-failed uploads were already retried and now sit safely in **`approved`** (9 total),
waiting for a live token — nothing is wrong with the files; they'll publish automatically once
reconnected. **Durable fix (see memory):** this token dies every ~7 days because ch2's Google app
is likely in *Testing* status — publish the app to *Production* in the Cloud Console to stop the
recurring revocation, rather than reconnecting every week.

## Observed (key numbers)
| | ch1 Owera Software | ch2 Rodrigo Recio |
|---|---|---|
| oauth | **connected** ✓ | **expired** ✗ (reconnect) |
| published (total) | 100 | 78 |
| published today | 5 (daily budget hit) | **0** |
| last successful publish | 2026-07-04 09:04 UTC | **2026-07-01 09:05 UTC** |
| quota spent today | 8500 / 9000 | 0 / 9000 |
| approved / queued / failed | 1 / 0 / 0 | **9** / 0 / 0 |
| board inventory | 2.0 d — **at capacity** | 2.0 d — **at capacity** |
| analytics `measured` | **90 / 100** (new!) | **73 / 78** (new!) |

- **Analytics are now flowing** on both channels (was `measured:0` yesterday). **CTR is still 0
  everywhere** — impressions aren't populating yet, so CTR-driven levers (R8) stay signal-scarce —
  but **retention is now live** (`avg_view_pct`, `average_view_duration`), which is real signal.
- **BGM pool:** 15 / 15, `is_low:false` — healthy, no action.

## Triage
Clean day operationally except the one standing escalation.
- **ch2 OAuth expired → escalated** (`auto:false`, "operator must reconnect"). Recurring; root
  cause is the Testing-mode app (see ⚠ block + memory `ch2-oauth-recurring-revocation-testing-mode`).
  I cannot fix this from the API. ch1 re-checked `connected`.
- **9 ch2 videos in `approved`** — correct parked state (publish loop skips a channel whose
  `oauth_status != connected`, so they wait rather than re-stall). **Not touched, not deleted**
  (good content, <7d old). They publish on reconnect.
- **ch1 quota 8500/9000** — informational (`auto:false`, "monitor"); ch1 hit its 5-video daily
  budget, near-cap is expected. No action.
- No `failed` / `rejected` / stuck / `error_runs_24h`; BGM healthy. `summary.needs_operator:2`
  (the oauth + the quota-monitor note); nothing else actionable.

## Learn
Analytics maturing changed the picture — this is the first run with real retention signal.

- **Shorts dominate reach on both channels** (ch1 short 14.4 avg views vs long 2.0; ch2 short 14.1
  vs long 3.1). Long-form barely gets discovered. For the monetization constraints (subscribers +
  Shorts views), **Shorts are the vehicle** — consistent with current weighting.
- **Retention is the weak spot, and ch2 is worse than ch1** (short `avg_view_pct` 36.4% ch1 vs
  **21.4%** ch2; `avd` ~10-14s on ~35s shorts → viewers drop in the first third). Same pipeline,
  so this is an **engagement-quality** problem, not a topic problem → points the daily loop at
  retention levers (hook / info-gain / pacing / dead-screen), not topic steering.
- **ch1 winner, underweighted:** t15 *GitHub Copilot Credits 2026* — 14.7 avg views **and** the
  best retention among high-view topics (avp **64.55%**), sitting at weight 1. Weighted up (below).
  t3 *Prompt & Context* has the most views (16.0) but below-average retention (avp 29.85%); it's the
  volume driver, kept at weight 3.
- **Hypotheses (falsifiable):** (H1) filling the mid-body diagram frames lifts `avg_view_pct` by
  cutting dead-screen where viewers drop — shipped as this run's gated change, settle on the ch1
  cohort next week. (H2) t15's high retention will hold as its weight-2 refills mature. (H3) ch2's
  low retention is a script/pacing gap specific to the PT prompts — investigate next run now that
  ch2 has 73 measured videos.

## Engagement
**Baseline** (golden set rendered on HEAD, frames read against `run/engagement-rubric.md`):

| Lever | Score | Note |
|---|---|---|
| R1 hook | **2** | Hook beat on screen at t=0; pain-first spoken lines ("Right docs, dumb answer"; "Sua RAG responde com lixo"). |
| R8 thumb/title | **2** | 0/4 thumbnail hooks echo the title; genuine curiosity gaps. |
| R2 info gain | **2** | Real diagrams, real Python, real stats, tradeoff compares; varied beat mix. |
| R6 payoff | **2** | Quotable closes ("Intelligence isn't remembering everything — it's writing the right things down"). |
| **R3 mobile clarity** | **1** | **Weakest.** Diagram/code/stat beats cluster low leaving ~40-55% of the frame empty; the ch1-code `cmp` "VS" badge intermittently collides with column text. |

**Targeted R3 → shipped `9268c6f`.** `_diagram_svg` portrait branch: narrowed the viewBox
(1000→760 units, node 560→600) so the vertically-stacked flow fills more of the 9:16 frame. `.dsvg`
is width-fit, so a taller viewBox aspect renders taller; nodes also read larger on mobile. Isolated
to the diagram renderer — no other beat renderer touched.
- **Gate (all three passed):** after-render `--only ch1-code`/`ch1-concept` → the ch1-code diagram
  now fills **~73% of frame width** (was ~51%) and more vertical space, nothing clipped; both renders
  `visible=True`, no fallback; `verify_storyboard` **40/40**. Target lever R3 up; no other lever can
  drop (change is code-isolated to diagram geometry). **Made live** (committed → manager restarted →
  dashboard 200). Logged as R3 experiment (`run/experiments.jsonl`), `status:running`.
- **Experiment settled:** **R8** (thumbnail curiosity-gap, `62ecec6d`, shipped 07-01) → **PROMOTED**.
  Signal-scarce (measured CTR still 0), so re-render-judged: today's baseline shows 0/4 title-echo,
  all genuine gaps — the change holds. Kept in code; re-settle on measured CTR once impressions flow.

## Monetization
Lower Tier for both; nothing crossed 100% this run. All content steering already gated by board
capacity, so the table is for tracking (no new action beyond the t15 weight).

| Metric | ch1 current / needed / % | ch2 current / needed / % |
|---|---|---|
| Subscribers | 9 / 500 / **1.8%** | 52 / 500 / **10.4%** |
| Watch hours | 1.8 / 3000 / 0.1% | 0.9 / 3000 / 0.0% |
| Shorts views | 1126 / 3M / 0.0% | 436 / 3M / 0.0% |

Binding constraint both channels: Shorts views / watch hours (both ~0%). Subscribers is the closest
proxy metric (ch2 10.4%). Shorts volume + retention is the lever — which is exactly what the
engagement loop (retention) and Shorts weighting target. No override of the analytics-driven plan.

## Channel actions
- **t15 *GitHub Copilot Credits 2026* → weight 2** (was 1). Data-backed: 14.7 avg views + avp
  64.55% (best retention among high-view topics). Read back confirmed `weight:2`. Steers future
  refills only (safe at board capacity — no ideas generated now).
- **No idea generation, no trend adoption** — both channels `at_capacity:true` (2.0 d ≥ 2-d
  horizon). Correctly skipped per the board-capacity gate.
- BGM pool healthy — no generation.

## Watch next run
- **Did the operator reconnect ch2?** If `oauth_status` is `connected`, confirm the 9 approved
  videos publish (last success advances past 07-01 09:05 UTC). If still `expired`, re-lead with ⚠
  and push the *publish-to-Production* durable fix again.
- **Settle the R3 diagram-fill experiment** once the ch1 cohort of new diagram videos matures
  (≥72h + `measured`): did `avg_view_pct` move on videos rendered after `9268c6f`? (SIGNAL-RICH now.)
- **Investigate ch2's low retention (H3)** — 73 measured videos; compare `creation_config` (beat
  mix / hook style) of ch2's few higher-avp videos vs the low ones to find the PT-specific gap.
- **CTR:** watch for impressions to start populating so R8 can settle on real CTR, not render-judge.
- Consider the intermittent `cmp` "VS"-badge/text overlap as a small next R3 fix (noted, not shipped
  — kept this run to one change).

**Commit:** `9268c6f` (diagram fill). Channel action (t15 weight) logged in `/api/runs`.
