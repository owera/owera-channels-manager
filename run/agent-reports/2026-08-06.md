# Growth agent — 2026-08-06

## ⚠ Needs operator

1. **ch1 pause decision (hard review overdue since 08-01).** Still **10 subscribers** vs the 15-sub bar. Lifetime: ~265 published, conversion remains near-zero despite real Shorts-feed distribution (v785 = 173 views, heavily SHORTS; still 0 subs). Recommendation stands: **PAUSE ch1** (or explicitly override with a new bar) — do not let it drift. Agent continues **zero ch1 strategy moves** until you decide.
2. **Stranded ch1 t15 approved shorts 826–828 (weight 0).** Still approved, never reach the front under weight-desc. If you want the wedge series finished, bump t15 weight; if pause wins, reject them.
3. **ch2 metadata backfill** (`db7a254` plan) still awaits `--apply` if still desired.
4. **BGM pool was empty at run start** (0 tracks; `resource/songs` recreated today with only the 10 tracks this run generated). External path dependency — if MoneyPrinterTurbo’s songs dir gets wiped again, every render ships silent. Worth pinning/backup outside that tree.

---

## Observed

- **Now:** 2026-08-06 ~20:45 UTC. Kill switch off, `scheduler_paused: false`, tree clean at start. First full growth run since **2026-08-03** (Grok runner; Claude OAuth had killed 08-04/05/06 morning runs — supervisor note earlier today).
- **Both channels published 5/5 today; quota 9000/9000** (full day — monitor only, not a bug).
  - ch1: 1 long (t5) + 4 shorts (t6) — correct mix **today**.
  - ch2: 1 long (t2) + **4 t27 shorts** (862–865) — displacement experiment live.
- **BGM pool:** was **0/5 (is_low:true)** → generated **10 tracks** → **10/5 is_low:false**. Proof: `POST /api/music/generate {"count":10}` returned 10 files; `GET /api/music` count 10; issues `bgm_pool_low` cleared.
- **Boards:** both `at_capacity:true` (10 drafts / horizon 2.0). No failed/stuck/oauth. 3 ch2 rejected (displacement 08-03, age ~116h, `suggested_action:leave` — not yet 7d).
- **Language (ch2):** all 08-04→08-06 titles PT-BR ✓ (no EN leak).
- **ch1 subs-per-video:** still essentially 0 on the post-CTA cohort; lifetime sub-gainers remain the thin historical set (e.g. v558 MCP, +1). **ch1 = 10 subs.** **ch2 = 61 subs** (unchanged vs 08-03).

### Publish log (settle evidence)

| day | ch1 | ch2 |
|-----|-----|-----|
| 07-28 | 5 (1L+4S) | **4** |
| 07-29 | 5 | 5 |
| 07-30 | 5 | 5 |
| 07-31 | 5 | **4** |
| 08-01 | 5 (1L+4S) | **5** |
| 08-02 | 5 (0L+5S) | **5** |
| 08-03 | **5 (5L+0S)** | **5** |
| 08-04 | 5 (0L+5S) | **5** |
| 08-05 | **5 (5L+0S)** | **5** |
| 08-06 | 5 (1L+4S) | **5** |

ch2 has not missed a 5th publish since the 08-01 quota-reserve ship. ch1 still has all-long / all-short days when the approved pool is long-heavy — fixed this run (see Engagement / Acted).

## Monetization

| metric | ch1 current | ch1 needed | ch1 % | ch2 current | ch2 needed | ch2 % |
|---|---|---|---|---|---|---|
| subscribers | 10 | 490 | 2.0% | 61 | 439 | 12.2% |
| watch hours | 6.2 | 2993.8 | 0.2% | 10.5 | 2989.5 | 0.4% |
| Shorts views | 3339 | 2,996,661 | 0.1% | 3376 | 2,996,624 | 0.1% |

**Binding constraint both channels: subscribers** (lower tier). ch2 remains the only compounding asset. No milestone crossed. Prefer analytics (ch2 local-AI hardware shorts) over pure milestone math — already aligned.

## Triage

Digest at start: 6 issues (3 rejected leave, 2 quota monitor, **bgm_pool_low auto**). After:

| Issue | Action | Proof |
|---|---|---|
| `bgm_pool_low` (0 tracks, need 15) | `POST /api/music/generate {"count":10}` (cap/run) | before count=0 → after count=10; `is_low:false`; bgm_pool_low empty in re-read |
| rejected 839–841 (disp. 08-03, age 116h) | leave (auto) | age &lt; 7d; subjects retained |
| quota both channels 9000/9000 | monitor only | full 5-publish day by design |
| failed / stuck / oauth / starved / music_gen errors | none | clean |

## Learned

1. **#24 quota-coexistence → PROMOTED.** ch2 5/5 for six straight days post-ship; pre-fix missed 5ths on 07-28/07-31. Analytics still runs (batch success 17:22–17:23 UTC).
2. **#25 trend-displacement → still RUNNING.** Mechanism worked: t27 w4 led ch2 short slots 08-05 (859–861) and 08-06 (862–865); drafts 888–891 on bench; approved 866 waiting. **Predicted metric not yet measurable** — 859/860/861 published 08-05, metrics still 0 / no traffic_json (analytics lag + &lt;72h). Settle ≥08-08 when cohort is mature; do not invent views.
3. **ch1 mix defect diagnosed on the publish log:** long-first reserved only *slot 1*; slots 2–5 were pure weight-desc, so w2 longs beat w0 shorts → 5L/0S days (08-03, 08-05). Symmetric 0L/5S days when no long was approved.
4. **Winners (mature):** ch2 t27 32.3 avg views / still best short genre; t4 26.6; t28 25.6 @ 60% avp. ch1 t18 MCP 36.2 avg views + the recent sub-gainer; t15 high avp loops but 0 subs on volume.
5. **Hypotheses this run:** (a) daily-mix cap restores ch1 1L+4S whenever both formats sit approved; (b) t27 859–865 cohort shows SHORTS traffic + higher avg_views than same-window t4 once mature; (c) operator pause decision is the binding ch1 move — content levers alone have not moved subs past 10.

## Acted (channels)

- **BGM refill:** 10 tracks generated (see Triage).
- **Trends logged (watch only — no adopt/displace):**
  - #48 ch2 score 82 — “Claude Code + Ollama local (sem Claude Max)” — strong fit, deferred until t27 flood has metrics.
  - #50 ch2 score 76 — “Qwen3.5-27B/35B coding local (Q8 vs Q4)” — partial overlap with adopted #45.
  - #49 ch1 score 68 — “Claude Code MCP security risks (Mitiga)” — below bar; pause recommendation blocks strategy moves.
- **No weight changes, no generates, no produces** — both boards at capacity; ch1 strategy frozen; t27 already over-fed pending signal.
- **Seeding kit:** `run/seeding/2026-08-06.md` (operator posts; agent does not).

## Engagement

**Baseline `baseline-0806`** — 4/4 clean, `visible=True`, none `used_fallback`. Scores from frames + scripts:

| Lever | score | evidence |
|---|---|---|
| R1 hook (P1) | 2 | Pain-first openers at t=0: “Your RAG spits garbage” / “Your AI agent has amnesia” / “Chega de reescrever integrações” / “Sua RAG responde lixo” |
| R8 thumb/title (P2) | 2 | Gaps hold (e.g. title rerank vs hook “Wrong answers, buried at the top”; PT hook ≠ title echo) |
| R2 info gain (P3) | 2 | Varied mixes; code beats present (cohere.rerank / @mcp.tool / split_by_meaning); diagrams sequential + fanout |
| R6 payoff (P4) | 2 | Quotable closes (“Retrieval finds candidates; reranking finds answers” / “Lixo entra, lixo sai — o chunking é a porta”) |
| R5 retention (P5) | 2 | Open loops early on all 4 |
| R3 clarity (P6) | 2 | Code indented, nothing clipped on read frames |
| R4 pacing (P7) | 2 | Hook at 0; no flash beats observed |
| R7 CTA (P8) | 2 | Follow/Siga + named next value 4/4 (“Cut RAG latency with caching” / “Servidor MCP do zero” / “Amanhã: tamanho de chunk ideal”) |

**All-2 baseline fifth consecutive judged day → non-engagement code path (fallback clause).**

### Shipped: daily-mix cap after first long (`publish_loop._next_approved`)

- **Files:** `app/services/publish_loop.py`, `tests/verify_publish.py`
- **Change:** once `published_long_today > 0`, pick from the **short** pool first; fall through to any approved only if no short remains (soft cap — no long starve).
- **Gate (synchronous):**
  - Crafted 08-03 shape (5× w2 long + 3× w0 short): order **L, S, S, S, L** (not LLLLL).
  - Long-only after a long published → still drains longs.
  - `tests/verify_publish.py` **74/74** (4 new mix checks).
  - `tests/verify_storyboard.py` **40/40**.
  - `import app.main` ok; `launchctl kickstart` manager; `GET /api/dashboard` **200**.
- **Lifecycle map:** only changes *which* `APPROVED` row `publish_loop` selects next — still APPROVED → publishing → published. Does not invent paths; re-render still QUEUED; re-publish still APPROVED.
- **Live effect watch next run:** ch1 tomorrow should be 1 long + 4 shorts **if** both formats are in the approved pool after tonight’s render (currently approved = only 826–828 t15 shorts; longs will come from auto-produce).

**No prompt/rubric lever change this run** — shipping an unproven polish on an all-2 baseline would violate the gate. Experiment logged as `daily-mix-cap`.

### Experiments settled / opened

| id | lever | verdict |
|---|---|---|
| #24 08-01 | quota-coexistence | **PROMOTED** (ch2 5/5 × 6 days) |
| #25 08-03 | trend-displacement | **RUNNING** — wait mature 859–861 metrics ≥08-08 |
| #26 08-06 | daily-mix-cap | **RUNNING** — watch ch1 mix 08-07+ |

## Watch next run

1. Operator ch1 pause yes/no.
2. ch1 publish composition 08-07 (should not be 5L if shorts approved).
3. t27 859–865: views, avp, traffic_json SHORTS vs t4 same window — settle #25.
4. BGM count stays ≥ min (if dir wiped again → escalate path hygiene).
5. ch2 sub movement attribution if channel counter moves.

## Commit

`a2ba90f` — Growth agent 2026-08-06: refill BGM, cap daily mix 1L+4S, promote #24
