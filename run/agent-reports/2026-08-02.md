# Growth agent — 2026-08-02

## ⚠ Needs operator — pause decision still open; your bulk-produce was seen

Yesterday's hard-review report recommended **PAUSE ch1** (10 subs vs the 15 bar). No
explicit answer yet — but the access log shows what happened instead: **at 17:41 UTC
on 08-01 (14:41 São Paulo), ~6h after the recommendation, you bulk-produced every
draft on both boards from the UI** (`POST /api/videos/produce` ×2) — 20 videos,
including the five shorts of **t15, the wedge topic parked at weight 0**. I read that
as an implicit "keep ch1 running", but directive 9 forbids drift: please either
(a) `PATCH /api/channels/1 {"paused":true}`, or (b) say what ch1 should try next.
Until then I am making **no ch1 strategy moves** (no weight changes, no new topics).

Mechanical consequence you should expect (not a bug, see Observed): ch1 published
**5 shorts / 0 long** today and will run long-heavy on 08-03 while your bulk queue
drains; the normal 1L+4S mix self-restores 08-04.

Carry-over: ch2 metadata backfill (`db7a254`, plan file 07-28) still awaits `--apply`.

## Observed

- **Now:** 2026-08-02 ~12:00 UTC (Sunday). Kill switch absent, scheduler running, tree
  clean. Boards both `at_capacity:true` (10 pending / 2.0 days) → no idea generation,
  no trend adoption this run.
- **Operator bulk-produce forensics (the run's main discovery):** the 20 rows queued
  08-01 17:41 UTC had **no JobRun trail** — `produce_bulk` (and single API produce)
  never logged one; attribution took the uvicorn access log + DB reconstruction.
  Composition: ch1 = 5× t15 shorts (created 07-31, pre-parking) + 5× t5 longs;
  ch2 = 3× t2 longs + 2+5× t4 shorts. The scheduler behaved correctly throughout:
  auto-produce no-op'd at 08-02 00:00 (headroom −5: the queue was already over
  budget) — its `Topic.weight > 0` filter and long-slot reservation were never
  bypassed by the *scheduler*, only pre-empted by the manual queue. Render submission
  is id-order, so ch1's five oldest queued rows (the t15 shorts) rendered; ch1's
  approved pool held no long at publish time → **5S/0L day** (824/825 t15 + 3 t6
  published 07:00–09:04 UTC; directive-2 miss, first since 07-26).
- **ch1:** 245 published, **10 subs** (flat; 0 sub-gainers since 07-15; 245 videos →
  10 subs lifetime). Tomorrow: 834-838 (5 t5 longs) render at 00:00; approved pool =
  3 t15 shorts → 08-03 publishes ~2L+3S (bulk-queue drain), then normal mix 08-04.
- **ch2:** 220 published, **60 subs** (flat ~13 days). **#24 day-1 evidence is exact:**
  08-01 published **5/5** with the long (753, t2) leading at 14:01 UTC — first 5/5 after
  the 07-31 4/5 quota day. Quota-day arithmetic: **8999/9000** used (5×1750 publish
  + 8u analytics at 17:13 + 241u at 05:13 — the reserve throttled both passes to the
  exact remainder). Today's window opens 14:00 UTC; approved pool 11 (3 t2 longs +
  8 shorts, all titles PT-BR ✓ directive 6).
- **Publish log 07-30→08-02:** ch1 5/5/5/5 · ch2 5/4/5/(window pending today).
- **Seeding watch (kits paused):** 778 flat at 53 views, 683 flat at 31 — 6th
  consecutive flat EXT_URL day; pause holds.
- **Quota digest:** ch1 8750/9000 = the designed cost of a full 5-publish day
  (5×1750), not a fault. Monitor only.

## Monetization

| metric | ch1 current | ch1 needed | ch1 % | ch2 current | ch2 needed | ch2 % |
|---|---|---|---|---|---|---|
| subscribers | 10 | 490 | 2.0% | 60 | 440 | 12.0% |
| watch hours | 5.2 | 2994.8 | 0.2% | 9.8 | 2990.2 | 0.3% |
| Shorts views | 3195 | 2,996,805 | 0.1% | 3120 | 2,996,880 | 0.1% |

Subscribers remain the binding constraint on both; ch2 remains the only compounding
asset. No milestone crossed.

## Triage

Digest at run start: 13 issues (12 expired rejected + ch1 quota monitor), 1 needs-operator
(the standing pause decision). Disposition:

- **Deleted 10 of 12 expired rejected rows** (07-26 board-rebalance batch, age 179.7h >
  168h floor, `suggested_action:"delete"`): ids 757-765 (ch2) + 771 (ch1), all 204.
  **Proof:** audit JobRuns 13026-13035 written, `rejected` counts now ch1=2/ch2=0
  (was 3/9). **772/773 remain** (10/run cap) — delete tomorrow.
- **ch1 `quota` (escalate-class):** 8750/9000 is by design on a full publish day —
  no action, dissolves if ch1 pauses.
- Zero failed / stuck / oauth rows; `error_runs_24h` empty; `pipeline_starved` empty
  (correctly — see #23 settle below); BGM 20/15 healthy and stable — no generation
  (pool unchanged in days but variety fine; demand exists, threshold not met).

## Learned

1. **The operator is an unmodeled actor in the pipeline, and their actions were the
   only unlogged ones.** Both forensic fires this fortnight were manual UI actions
   with no JobRun: mass-delete (07-26 → fixed 07-27) and now bulk-produce (08-01 →
   fixed today, `4e673ae`). The transition audit is now complete: every video
   lifecycle mutation writes a JobRun.
2. **A manual bulk queue defeats mix scheduling for exactly `queue_size/budget` days**
   — auto-produce's long-slot reservation and weight ordering only govern what *it*
   queues; id-order render submission then drains the manual queue as-is. Don't
   "fix" this in code: it's the operator exercising direct control, now visible in
   /api/runs. Expect and annotate the off-mix days instead of misdiagnosing them.
3. **The quota reserve packs the day to the unit** (8999/9000 with 5/5 published) —
   the two consumers now coexist by arithmetic, not luck.

**Hypotheses:** (a) 08-03 ch1 publishes off-mix (~2L+3S) from the bulk-drain, then
08-04 returns to 1L+4S with zero intervention; (b) ch2 publishes 5/5 today through
08-04 (settles #24); (c) if the operator intends "keep ch1", the next manual queue
action will now show up as `bulk-produced via API` rows in /api/runs — watch for it.

## Engagement

**Baseline `baseline-0802`** (full golden set, 4/4 clean, `visible=True`, none
`used_fallback`) — **third consecutive all-2 baseline:**

| Lever | score | evidence (read from frames + scripts) |
|---|---|---|
| R1 hook (P1) | 2 | 4/4 pain-first ≤10w openers at t=0 ("Your RAG returns garbage even with the right documents" / "Você escreve a mesma integração de novo. E de novo.") |
| R8 thumb/title (P2) | 2 | zero title echo, real gaps 4/4 ("Right docs, wrong answers" vs "Fix Your Slow RAG…"; "Goldfish memory, fixed" vs "…Forgets Everything Between Chats") |
| R2 info gain (P3) | 2 | truthful diagrams both directions (ch1-code sequential top20→reranker→top3 for a real pipeline; ch2-code fanout MCP→Banco/Arquivos/APIs); varied mixes, ≤2 stmt |
| R6 payoff (P4) | 2 | quotable closes 4/4 ("The model doesn't learn you. It reads you, then forgets." / "Chunk ruim é lixo entra, lixo sai") |
| R5 retention (P5) | 2 | open loop in all 4 ("That's not a retrieval problem—it's a ranking problem") |
| R3 clarity (P6) | 2 | nothing clipped in any frame read; lists ≤4-word rows; code ≤30 chars |
| R4 pacing (P7) | 2 | min beat 1.68s (floor), max hold 8.06s (carries drift tween); **content fits the slot even at the floor** — the 1.68s diagram is 3 short nodes, the 2.06s code beat is 3 tiny lines, the 2.17s list is 2 items (frames read to confirm) |
| R7 CTA (P8) | 2 | spoken imperative + named next value + matching card, both languages ("Siga →" / "Amanhã: tamanho ideal de chunk" matching "…amanhã eu mostro o tamanho de chunk ideal") |

**Targeted: none** — no weak lever for the third run straight. The change slot went to
the produce-audit fix (playbook non-engagement fallback), which today's forensics made
the clear highest-leverage work: **`4e673ae`** — both produce endpoints now write
per-video JobRuns detail-tagged `via API`. Verified synchronously:
`tests/verify_produce_audit.py` 15/15 (per-video rows, ids, API tag vs auto-produce,
refused/no-op calls write nothing); all 18 verify suites green; manager restarted
(dashboard 200); live no-op probe `{"produced":0}` wrote no JobRun. Not logged in
`experiments.jsonl` — no growth signal to settle; the synchronous suite is the proof.

**Settled: experiment #23 (`14f6471`, starvation-digest projection) → PROMOTED** (72h,
DB publish log). Zero false alarms 07-30→08-02 with zero missed-for-pipeline-reasons
publish days and zero manual produces by the agent; the exact pre-fix false-positive
shape recurred live today (ch1 approved=3 at noon) and the digest correctly stayed
silent (projection: 5 queued longs + 5 producible drafts cover tomorrow). The 07-31
ch2 4/5 was the quota defect (#24's domain), not starvation.

**Still running:** #24 (quota reserve — day-1 evidence exact; settles 08-04 on the
publish log).

## Acted

1. **Code (1 of ≤2): `4e673ae`** — produce-endpoint audit JobRuns + 15-check suite
   (see Engagement/Triage). Restarted live; behavior verified in-process and probed live.
2. **Deleted 10 expired rejected videos** (triage, audit rows quoted above).
3. **Settled #23 → promoted** in `run/experiments.jsonl`.
4. **No channel API actions** — boards at capacity (idea/trend gate), no weight
   changes (ch1 awaits the operator; ch2 mix healthy and self-correcting).
5. **Trend research: light pass, nothing adopted** — both boards at capacity make
   adoption impossible today; #45/#46/#47 stay queued for when boards open.
6. **No seeding kit** — paused per the 08-01 pre-commitment (6th flat EXT_URL day).

## Watch next run (08-03)

1. **Operator decision on ch1** — pause or a stated direction; any new manual queueing
   is now directly visible (`bulk-produced via API` in /api/runs).
2. **ch1 08-03 mix will be off** (~2L+3S bulk-drain incl. the last 3 t15 shorts) —
   expected; do NOT misread it as an autofill/auto-produce regression. Normal mix
   should return 08-04 unaided; if it doesn't, THEN investigate.
3. **#24 settle prep:** confirm ch2 published 5/5 on 08-02 (753-style long lead at
   14:0x) and again 08-03; final settle 08-04.
4. **Delete 772/773** (last expired rejected rows).
5. **t15 goes dark after 08-03** — its last 3 videos publish, bench empty, weight 0.
   Only the operator revives it.
6. **EXT_URL stays on watch** with kits paused; movement without seeding = organic.
