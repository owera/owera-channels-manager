# Growth agent — 2026-08-28

## ⚠ Needs operator

1. **PAUSE ch1 (hard review overdue since 08-01).** Still **13 subscribers** vs the 15-sub bar. Video-attributed converters remain **five** (v558 MCP, v367 t6, v43 t1, v96 t5, v1032 t6). The 12→13 move is still **unattributed**. **Recommendation stands: PAUSE ch1** (or explicitly override with a new bar). Agent continues **zero ch1 strategy moves** (no trend adopt / generate for growth). Hygiene: t3 stays w=0, t5 long-anchor stays w=2.
2. **ch2 metadata backfill** (`db7a254` plan) still awaits `--apply` if still desired.
3. **BGM pool path** remains external (`MoneyPrinterTurbo/resource/songs`). Count 30 after today's refresh (target 15); wipe risk still escalates to silent renders.

---

## Observed

- **Now:** 2026-08-28 ~12:00 UTC. Kill switch off, `scheduler_paused: false`, tree clean at start, `main`. Last full growth run: 2026-08-27.
- **ch1:** published **5/5** today, quota **8750/9000** (`daily_limit_hit: false`). Mix = **1 long + 4 shorts** (v1165 t5 long first @ 07:01 `o7BwF2tvk34`, then v1174/1175/1176/1177 **4×t15**). **Twenty-first consecutive 1L+4S day** (08-06…08-28).
- **ch2:** published_today **0** at observe (window opens **14:00 UTC**). rendered_today **5**. **08-27 did publish 1L+4S** (v1164 t2 long first @ 14:01 `tsHmhamta6E`, then v1074/1075/1076/1077 **all t4**) — yesterday’s 1L+4×t4 prediction held.
- **ch2 subscribers: 64** (unchanged). Lifetime converters still **seven** (v542/524/810/421/684/315 + v1022). v1022 still 4 views / 47.82% avp / YT_SEARCH=4 / +1.
- **ch1 subscribers: 13.** Subs-per-video on the mature leaderboard: **still 5 lifetime +1s**. None on the post-07-13 Copilot-credits wedge (t15 still 0 subs on 46 measured / 50 published). Do not invent which video converted 12→13.
- **BGM pool:** 25 → **30** this run (min 5 / target 15; `is_low: false`). No `music_gen` errors. Last generate was 08-24; refreshed 5 tracks today (renders landed).
- **Boards (API `by_format`):**
  - ch1 draft `{long:1, short:9}` · queued `{long:0, short:0}` · approved `{long:0, short:2}` (1072 t18 + 1083 t6)
  - ch2 draft `{long:1, short:9}` · queued `{long:0, short:0}` · approved `{long:1, short:11}` (v1179 t2 long leading)
  - Both `at_capacity: true`, `days_of_inventory: 2.0`.
- **Language (ch2):** 08-27 titles all PT-BR ✓ (1164 Fica Mais Lento; 1074 2 tokens; 1075 conta de cloud; 1076 MacBook Air 8GB; 1077 12GB vira 4GB). Leading approved long v1179 also PT-BR (“Sua Fatura de Egress na Nuvem Explode com IA”). No EN leak.
- **traffic_json (via leaderboard API, no DB dive):** v542 SHORTS=385; v785 SHORTS=164 + YT_SEARCH=6; v558 SHORTS=104 + YT_SEARCH=12; v948 SHORTS=64 + **EXT_URL=9** (unchanged vs 08-17…08-27) + NOTIFICATION=3; v884 YT_SEARCH=22 + SHORTS=77; v1022 YT_SEARCH=4; v1032 SHORTS=22 + YT_SEARCH=1. SHORTS / YT_SEARCH dominate. **No BROWSE / SUGGESTED keys** on the top samples. No invented CTR.
- **Midnight 08-28 auto_produce CONFIRMED:** JobRuns **21424–21428** ch1 (1165 t5 long + 1174–1177 t15) and **21429–21433** ch2 (1179 t2 long + 1078/1180–1182 t4). Autofill **21455** t15 +5, **21454/21456** t4 +4/+1.

### Publish log (settle evidence)

| day | ch1 | ch2 |
|-----|-----|-----|
| 08-21 | **5 (1L+4S)** long 960 first + t18 + 3×t6 | **5 (1L+4S)** long 982 first + **4×t4** |
| 08-22 | **5 (1L+4S)** long 963 first + 4×t6 | **5 (1L+4S)** long 1038 first + t4 1028 + 3×t27 |
| 08-23 | **5 (1L+4S)** long 999 first + 3×t6 + t18 | **5 (1L+4S)** long **1052** first + **4×t4** |
| 08-24 | **5 (1L+4S)** long 1005 first + 3×t18 + t6 | **5 (1L+4S)** long **1054** first + **4×t4** |
| 08-25 | **5 (1L+4S)** long 1056 first + 3×t6 + t18 | **5 (1L+4S)** long **1055** first + **4×t4** (1064–1067) |
| 08-26 | **5 (1L+4S)** long 1079 first + 2×t18 + 2×t7 | **5 (1L+4S)** long **1073** first + **4×t27** (998/1000/1001/1003) |
| 08-27 | **5 (1L+4S)** long 1068 first + t7 + 3×t6 | **5 (1L+4S)** long **1164** first + **4×t4** (1074–1077) |
| 08-28 | **5 (1L+4S)** long **1165** first + **4×t15** | pending window (0 at observe; plan **1179 t2 long @14:00 then 1078/1180–1182 all t4**) |

## Monetization

| metric | ch1 current | ch1 needed | ch1 % | ch2 current | ch2 needed | ch2 % |
|---|---|---|---|---|---|---|
| subscribers | 13 | 487 | 2.6% | 64 | 436 | 12.8% |
| watch hours | 7.8 | 2992.2 | 0.3% | 12.5 | 2987.5 | 0.4% |
| Shorts views | 4533 | 2,995,467 | 0.2% | 4497 | 2,995,503 | 0.1% |

**Binding constraint both channels: subscribers** (lower tier). No milestone crossed (no metric hit 100% for the first time). Prefer analytics (keep t4 as ch2 converter lead; ch1 frozen) over pure milestone math.

## Triage

Digest: **not clean** (`summary.clean: false`, `total_issues: 81`, `needs_operator: 1`). `failed=[]`, `pipeline_starved` / stuck / oauth / bgm_pool_low empty. Quota monitor + 80 rejected (flood, age 72.3h, `suggested_action: leave`).

| Issue | Action | Proof |
|---|---|---|
| 80 rejected t33–t42 (age 72.3h) | leave | `suggested_action: leave`; age < 7d. Do not delete. |
| quota ch1 8750/9000 (`auto: false`) | monitor only | full 5-publish day (5×1750=8750); `daily_limit_hit: false` |
| `error_runs_24h` | none | empty list |
| failed / stuck / oauth / bgm_low / pipeline_starved / board_overflow | none | empty lists |
| t3 parked / t5 long-anchor | no change | GET `/api/topics/3` `weight=0`; GET `/api/topics/5` `weight=2` `content_format=long` (post-restart still) |

Failed list stays empty. The 80 rejected rows are **not deleted**.

## Learned

1. **ch2 08-27 mix prediction held:** 1L+4S, long v1164 first, then **4×t4**.
2. **Midnight 08-28 auto_produce held:** queued ch1 long **v1165** (published 07:01) + 4 t15; ch2 long **v1179** + 4 t4. Yesterday’s hypothesis (b) confirmed.
3. **Today’s ch2 14:00 plan is 1L + 4×t4** (1179 then 1078/1180–1182). Leftover t27 (1002/1004/1048…) sit behind w4.
4. **Winners (mature):** ch2 t28 25.8 avg views @ 59.5% avp / 0 subs; t4 21.9 avg + **5 lifetime subs** (the only converter that still moves); t27 20.3 avg / 44.5 avp / **0 subs**. ch1 t15 22.3 avg / 62.3 avp / 0 subs on 46 measured; t6 13.4 avg + **2 lifetime subs**; t18 11.4 avg + 1 sub.
5. **Hypotheses this run:** (a) ch2 08-28 window publishes 1L+4S with v1179 leading then 4×t4; (b) midnight 08-29 auto_produce queues one ch1 long (approved_longs=0 after today’s 1L, draft 1166 t5 waiting); (c) a later at_capacity adopt still 409s (already confirmed today — used to promote `adopt-board-cap`).

`queue-plan-mix` (shipped 08-24, `6005571`): 96h+ but live noon queue is `{}` both channels (budget already spent). Leave `running`.

`adopt-board-cap` (shipped 08-25, `21fab8c`): **PROMOTED**. Live `POST /api/trends/84/adopt` → **409** `board at capacity (horizon reached) — displace drafts first`; pending stayed 10. Third consecutive confirmation.

`R2` code-ok retry (shipped 08-26, `8188c17`): 48h. Re-render-judged on today’s baseline: **all 4 golden subjects carry a code beat**. Leave `running`.

`R4` max-hold (shipped 08-27, `ca37863`): 24h. Residual found (penultimate dump blocked by CTA-at-8s) — follow-up shipped this run. Leave original `running`.

## Acted (channels)

- **No produce / no generate / no displace.** Boards at capacity; ch2 lowest-weight draft is t2 long w2 (must not displace) then t27 w3 / t4 w4 — no w1-or-lower seat for a ≥75 trend.
- **Trends logged (no adopt):**
  - **#73** ch2 score **89** (was 88) — Qwen3.8 Ollama library **1M** (was 924.7K). Proof: id 73, status=adopted, topic 33 parked.
  - **#83** ch2 score **84** — Ollama **0.33.1** still latest stable; **0.33.2-rc1** 2026-08-27. Proof: id 83, topic 40 parked.
  - **#82** ch2 score **83** — Muse Glimmer **172.2K** (was 169.8K, +2.4K, stalled vs Qwen). Proof: id 82, topic 35.
  - **#85** ch1 score **76** **rejected** — Copilot Business/Enterprise promo credits end **2026-09-01** (4 days). *Is* the wedge; rejected because ch1 is frozen. Proof: id 85, status=rejected, `adopted_topic_id: null`.
  - **#86** ch2 score **74** (was 72) **watching** — Ornith 1.5 official Ollama **102.4K** (crossed 100K). Proof: id 86, status=watching, topic=null.
- **Live 409 check:** `POST /api/trends/84/adopt` → http 409, both boards pending=10.
- **BGM generate 5:** count 25→30, `generated: 5`, `errors: []`. Proof: GET `/api/music` count=30; new files `techno_1787919408269.wav` … `techno_1787919409084.wav`.
- **Seeding kit:** `run/seeding/2026-08-28.md` (operator posts; agent does not). Features 08-27 t4 1074/1077 + t2 long 1164 + v1032 converter.

## Engagement

**Baseline `baseline`** — 4/4 clean, `visible=True`, none `used_fallback`. ~236s. Scores from frames + scripts + beat timings:

| Lever | score | evidence |
|---|---|---|
| R1 hook (P1) | 2 | Pain-first on frame + opener: “Your RAG returns garbage, and you can't figure out why.” / “Your AI agent has amnesia every single time you open it.” / “Você tá cansado de escrever a mesma integração toda semana?” / “Sua RAG responde qualquer coisa, menos o certo.” Hook cards at t=0: “Your RAG returns garbage” / “Your agent has amnesia” / “A mesma integração toda semana?” / “Sua RAG responde errado”. |
| R8 thumb/title (P2) | 2 | Gaps vs titles (hook “Wrong answers, delivered fast” vs title “Fix Your Slow RAG…”; “Its Memory Wipes Every Session” vs “Why Your AI Agent Forgets…”; PT “Uma conexão substitui todas” / “Seu texto cortado no lugar errado”). Zero distinctive title-word echo. |
| R2 info gain (P3) | 2 | **R2 code-ok HOLDS.** 4/4 mixes include a code beat: ch1-code `ce.score` + `rank(...)[:3]`; ch1-concept `mem.save`/`mem.recall`; ch2-code `mcp.tool`/`mcp.serve`; ch2-concept `split(text, by="section", overlap=50)` indented. Fanout on ch2-code (Modelo → Bancos/Arquivos/Serviços). CMP stacked + VS. |
| R6 payoff (P4) | 2 | Quotable closes (“Retrieval gets you close; reranking gets you correct.” / “It forgets because you never gave it a place to remember.” / “Pare de reinventar o encanamento a cada projeto.” / “Chunk ruim entra, lixo sai. Simples assim.”) |
| R5 retention (P5) | 2 | Open loops early on all 4. |
| R3 clarity (P6) | 2 | Code indented (ch2-concept `by="section"` under `split(`; ch1-concept `fact)` / `user_id)`). CMP stacked + VS in flow. Fanout on ch2-code. List midpoint shows items. Edge labels beside connectors (`save`/`recall` on ch1-concept). |
| R4 pacing (P7) | **1** | **Weakest lever.** No sub-1.7s flashes on start-delta (min 1.80). **Penultimate DRAG:** ch2-code list **8.61s visual** (8.72 start-delta) with CTA already at **8.00s** — yesterday’s `_TAIL_MAX=8.0` blocked the dump. ch2-concept cmp at 7.50s (cap holding). |
| R7 CTA (P8) | 2 | Follow/Siga + named next value 4/4 (“Cache scores, halve latency” / “Exact memory setup tomorrow” / “MCP do zero, passo a passo” / “Amanhã: tamanho ideal de chunk”). Spoken ask present on all 4 scripts. |

**Targeted R4 residual** (weakest lever; P7). Not another prompt rule — uncap the last-beat absorber so a penultimate 8s+ card can dump into the CTA.

**Shipped (`2521280`):** last-beat successor in `align_storyboard` max-hold is **uncapped** (`new_next = wanted`). Mid-body successors still cannot exceed `_MID_MAX`. Last beat itself is still not shortened.

Lifecycle: this change does not set `Video.status`. `compose()` still returns HTML or None; render_loop still consumes QUEUED. No state-machine touch.

| | ch2-code (penultimate list) |
|---|---|
| baseline max | **8.72s** list / CTA **8.00s** |
| unit-exercised after | **7.50s** list / CTA **9.10s**, `valid=True` |

**Gate (after ch2-code + after ch2-concept):**
- ch2-code after: mix hook/term/cmp/diagram/**code**/stmt/lst/cta; `@mcp.tool()` / `def buscar` / indented `return db.query(q)`; fanout Servidor MCP → API/Banco/Ferramenta; CTA “Siga → Amanhã: seu 1º servidor MCP”; hook “A mesma integração, de novo?”; `visible=True`, no fallback. Penultimate lst **3.21s** (targeted leak not present on this sample). Residual: code beat **8.46s** with successor already at 7.50 (known successor-at-cap case, not this change).
- ch2-concept after: mix hook/stmt/stat/lst/cmp/**code**/quote/cta; `split(text, by="section", overlap=50)` indented; CTA “Siga → Tamanho ideal e overlap amanhã”; hook “Sua RAG responde lixo”; `visible=True`, no fallback. Quote/CTA 3.97/7.10s. Residual: lst **8.41s** with successor cmp at 7.50.
- Targeted leak **closed** on exact timings. Overall R4 still has the successor-at-cap residual (do not claim 1→2 for the whole lever). R1/R2/R7/R8 unchanged. verify_storyboard **234/234**. `uv run python -c "import app.main"` ok. Manager restarted (`launchctl kickstart`); `GET /api/dashboard` = 200.

### Experiments settled / opened

- **PROMOTED:** `adopt-board-cap` (`21fab8c`) — 409 confirmed 08-26/27/28, pending stayed 10.
- Left running: `queue-plan-mix` (noon queue still `{}`); `R2` code-ok (holds on fresh baseline, 48h); `R4` max-hold `ca37863` (24h; residual follow-up today).
- Opened: `R4` last-beat uncap commit `2521280`, status=running.

## Watch next run

1. Operator ch1 pause yes/no (still the binding decision). 13 subs does not clear 15 — do not unfreeze. Keep t3 at 0.
2. ch2 08-28 publish composition after 14:00 UTC — expect **1L+4S** with t2 long **v1179** leading then **4×t4** (1078/1180–1182).
3. Midnight 08-29: ch1 `approved_longs=0`, draft **1166** t5 long. Expect auto_produce to queue **one long** into tomorrow’s 07:00 slot (long-reserve). Quote the JobRun.
4. `queue-plan-mix` live signal still needs a noon where a long is queued behind lower-id shorts **and** `approved_longs==0` **and** render budget is not already spent.
5. Failed list stays empty. The 80 rejected t33–t42 rows age toward the 7-day delete cap (age 72.3h today) — do not delete before that (~08-30).
6. Do not unpark t33–t42. Qwen3.8 (#73, 1M) is still the only watching-class term rising; serve it as a **t4 episode via displacement** if a w1-or-lower draft exists, not by unparking t33.
7. BGM count stays ≥ min (30, above target 15). Next refresh only if stale again *and* renders landed.
8. Quote whether v948 EXT_URL grows past 9 (seeding signal). Quote v1022 views/avp (4 / 47.8% today). Quote v1032.
9. Re-render-judge the R4 last-beat uncap on a fresh baseline (penultimate mid-body ≤7.5s when successor is CTA; successor-at-cap residual may still show).

## Commit

`2521280` (R4 last-beat uncap). Report `de67b3a`.
