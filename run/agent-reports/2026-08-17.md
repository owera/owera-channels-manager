# Growth agent — 2026-08-17

## ⚠ Needs operator

1. **PAUSE ch1 (hard review overdue since 08-01).** Still **10 subscribers** vs the 15-sub bar. Lifetime attributed converters remain four videos (v558 MCP +1, v367 t6 +1, v43 t1 +1, v96 t5 +1). Post-CTA / post-wedge volume still ~0 subs (v785 ≈174 views / 148% avp / SHORTS=164 → 0). **Recommendation stands: PAUSE ch1** (or explicitly override with a new bar). Agent continues **zero ch1 strategy moves** (no weight/trend adopt/generate for growth). Pipeline hygiene only.
2. **ch2 metadata backfill** (`db7a254` plan) still awaits `--apply` if still desired.
3. **BGM pool path** remains external (`MoneyPrinterTurbo/resource/songs`). Count holds at 15 = target; wipe risk still escalates to silent renders.
4. **6 failed renders still sitting** after today's cap of 5 requeues (all `suggested_action: requeue`, `render timed out`). Next run should drain the rest (v961/963/973/975/976/977). Yesterday's 5 requeues *did* render overnight — the hung-retry path is working; these remaining rows just need the same kick.

---

## Observed

- **Now:** 2026-08-17 ~12:00 UTC. Kill switch off, `scheduler_paused: false`, tree clean at start, `main` == `origin/main`. Last full growth run: 2026-08-16.
- **ch1:** published **5/5** today, quota **9000/9000** (full 5-publish day, monitor only, `daily_limit_hit: false`). Mix = **1 long + 4 shorts** (v972 t5 long first @ ~07:00, then v988 t6, v942 t18, v990 t18, v991 t18). **Twelfth consecutive 1L+4S day** (08-06…08-17). v942 is yesterday's requeued MCP timeout — recovered and published same morning.
- **ch2:** published_today **0** at observe (window opens **14:00 UTC**). rendered_today **5**. Approved = **1 long + 11 shorts** (v955 t2 long + 5×t4 + 5×t27 + leftover t10) — ready for 1L+4S. **08-16 did publish 1L+4S** (v981 t2 long first @ ~14:00, then v930/931/966/967 **all t4**). The 08-16 t4-w4 rebalance showed up on the publish side.
- **ch2 subscribers: 63** (unchanged vs 08-13/08-16). Video-attributed `subscribers_gained` still the same six known converters. Do not invent which video converted.
- **ch1 subscribers: 10.** Subs-per-video on the mature leaderboard: **4 lifetime +1s**, none on the post-07-12 CTA / post-07-13 wedge cohort.
- **BGM pool:** 15 / min 5 / target 15 (`is_low: false`). No `music_gen` errors. No `error_runs_24h`. Last generate was yesterday — not stale; no refresh this run.
- **Boards (API `by_format` at observe, before triage):**
  - ch1 draft `{long:2, short:8}` · queued `{long:0, short:0}` · approved `{long:1, short:1}`
  - ch2 draft `{long:2, short:8}` · queued `{long:0, short:0}` · approved `{long:1, short:11}`
  - Both `at_capacity: true`, `days_of_inventory: 2.0`.
- **t4 vs t27 after overnight autofill (watch item from 08-16):** ch2 drafts t2=2 / **t4=3 (new 1010–1012)** / t27=5 (1000–1004 leftover). t4 *did* refill; t27 still holds more leftover inventory from the old w4 era. Publish-side is what matters: 08-16 shorts were 4×t4.
- **Language (ch2):** 08-16 titles all PT-BR ✓ (981 Feature Store; 930 GPU nuvem; 931 Android; 966 internet; 967 Mac 16GB). No EN leak.
- **traffic_json (via leaderboard API, no DB dive):** v542 SHORTS=385; v785 SHORTS=164 + YT_SEARCH=6; v558 SHORTS=104 + YT_SEARCH=12; v948 SHORTS=10 + **EXT_URL=9** (new vs 08-16); v891 YT_SEARCH=13 + SHORTS=5; v884 YT_SEARCH=19 + SHORTS=77. SHORTS / YT_SEARCH dominate. **No BROWSE / SUGGESTED keys** on the top samples. No invented CTR.

### Publish log (settle evidence)

| day | ch1 | ch2 |
|-----|-----|-----|
| 08-11 | 5 (1L+4S) long 893 first | 5 (1L+4S) long 911 first |
| 08-12 | 5 (1L+4S) long 894 first | 5 (1L+4S) long 932 first |
| 08-13 | 5 (1L+4S) long 895 first | 5 (1L+4S) long 952 first |
| 08-14 | 5 (1L+4S) long 897 first | **4 (1L+3S)** long 953 first |
| 08-15 | 5 (1L+4S) long 896 first | 5 (1L+4S) long 954 first |
| 08-16 | 5 (1L+4S) long 971 first | **5 (1L+4S)** long 981 first + 4×t4 |
| 08-17 | **5 (1L+4S)** long 972 first | pending window (0 at observe; 1L+11S banked, v955 leading) |

## Monetization

| metric | ch1 current | ch1 needed | ch1 % | ch2 current | ch2 needed | ch2 % |
|---|---|---|---|---|---|---|
| subscribers | 10 | 490 | 2.0% | 63 | 437 | 12.6% |
| watch hours | 7.3 | 2992.7 | 0.2% | 11.4 | 2988.6 | 0.4% |
| Shorts views | 4218 | 2,995,782 | 0.1% | 3986 | 2,996,014 | 0.1% |

**Binding constraint both channels: subscribers** (lower tier). No milestone crossed (no metric hit 100% for the first time). Prefer analytics (ch2 local-AI hardware shorts that convert; ch1 frozen) over pure milestone math.

## Growth review

Monday checkpoint (vs 2026-08-10 report).

| | 08-10 | 08-17 | Δ |
|---|---|---|---|
| ch1 subs | 10 | 10 | **0** |
| ch2 subs | 61 | 63 | **+2** (moved 08-10→08-13; flat since) |
| ch1 watch hours | 6.4 | 7.3 | +0.9 |
| ch2 watch hours | 10.9 | 11.4 | +0.5 |
| ch1 Shorts views | 3453 | 4218 | +765 |
| ch2 Shorts views | 3690 | 3986 | +296 |

- **Traffic-source mix:** still SHORTS + YT_SEARCH. No named BROWSE / SUGGESTED on the leaderboard samples. Directive-7 falsifier (BROWSE+SUGGESTED ~0 across a long cohort) has been true since the 07-23 hard review — this is **channel-level distribution**, not a missing prompt. Escalate with the ch1 pause case; do not publish more of the same into a feed that is not testing us.
- **Seeding:** 08-14/08-15 kits never written. 08-16 kit written; operator post status unverified. **v948 EXT_URL=9** is the week's only new external spike (21 views / 169% avp) — possible 08-13 kit landing, not proven.
- **Series:** no numbered “Parte N” in the last 8 days of titles on either channel. Directive-3 is slipping. Do not invent a series this run while ch1 is frozen and ch2's converter (t4) is already leading the shorts.
- **Biggest bet next week:** keep **t4 (w4)** as the ch2 converter lead; do not open another t27 topic while t27 is 0 subs. Operator must decide ch1. Drain the last 6 failed renders.

## Triage

Digest at start: 14 issues (11 failed requeue + 2 rejected delete + 1 quota monitor). `pipeline_starved` / stuck / oauth / bgm_pool_low / music_gen / `error_runs_24h` all empty.

Yesterday's 5 requeues (watch item): **4/5 rendered overnight**. v962 → approved (t5 long, has `video_path`); v942 → **published** today; v944 → approved (t18, has `video_path`); v955 → approved (t2 long, has `video_path`); v956 stayed draft (mix-rebalance, expected). v946 was *not* in yesterday's 5 and stayed failed until today.

| Issue | Action | Proof |
|---|---|---|
| failed v946 (ch1 t18 MCP, timeout 132h) | `POST /requeue` | after: `status=queued`, `error=None`, `retry_count=0` |
| failed v957 (ch2 t2 long, timeout 108h) | `POST /requeue` | after: `status=queued` (the 1 long reserve). `GET /api/videos?status=queued` lists v957. |
| failed v958 (ch2 t2 long, timeout 108h) | `POST /requeue` | after: `status=draft` (mix-rebalance — ch2 already had 1 queued long). Subject retained, `error=None`. Recovered off `failed`. |
| failed v959 (ch1 t5 long, timeout 108h) | `POST /requeue` | after: `status=queued`, `error=None` |
| failed v960 (ch1 t5 long, timeout 108h) | `POST /requeue` | after: `status=draft` (mix-rebalance — ch1 already had 1 queued long v959). Subject retained, `error=None`. |
| remaining failed (6) | leave (cap 5) | digest after: v961/963/973/975/976/977 still `failed` / `suggested_action: requeue` |
| rejected v922, v928 (ch2, age 180h / 180h) | `DELETE` | after: `GET /api/videos/922` and `/928` → **404**. `rejected: []` in digest. |
| quota ch1 9000/9000 | monitor only | full 5-publish day by design (5×1750); `daily_limit_hit: false` |
| stuck / oauth / bgm_low / pipeline_starved / error_runs_24h | none | clean lists |

After-state board (API): ch1 queued `{long:1, short:1}` (959 + 946) · draft `{long:3, short:8}`; ch2 queued `{long:1, short:0}` (957) · draft `{long:3, short:8}`. `rendered_today=5` both — requeues will not start until the UTC-midnight budget reset. Watch that overnight, not now.

## Learned

1. **Yesterday's timeout requeues rendered.** 4/5 recovered files overnight under `43cfe3d`. No new `failed` rows since 08-15. The remaining 6 are pre-fix leftovers, not a live hang bug — drain, don't recode.
2. **t4 w4 led 08-16 shorts (4/4).** Predicted autofill “t4 before t27” is only partial on the *draft* bench (t4=3 new, t27=5 leftover) but the *publish* order did the job. Leave weights.
3. **Winners (mature):** ch2 t4 26.2 avg views + **4 lifetime subs** (still the only converter); t28 25.8 @ 59% avp / 0 subs; t27 17.2 avg / 41.3 avp / **0 subs** (v948 EXT_URL=9 + v891 SEARCH=13 are distribution without conversion). ch1 t18 MCP 24.7 avg + the only recent sub-gainer; t15 high-avp loops, 0 subs on 46 videos.
4. **Hypotheses this run:** (a) the 5 requeued timeouts render overnight the same way yesterday's did; (b) ch2 08-17 window publishes 1L+4S with v955 leading then t4 shorts; (c) operator pause remains the binding ch1 move.

No running experiments in `experiments.jsonl` to settle (all prior lines already `promoted` as of 08-16).

## Acted (channels)

- **Requeue 5 + delete 2** — see Triage. Cap hit. 6 failed remain.
- **No weight changes.** t4=4 / t27=3 / t2=2 already match conversion. ch1 frozen.
- **Trends logged (no adopt/displace — boards at/over capacity after requeue; every ch2 draft is w2+ so a new w1 topic cannot displace; ch1 frozen):**
  - **#73** ch2 score **82** watch refresh — “Qwen3.8-27B local GGUF (Ollama / llama.cpp) 2026-08”. Proof: `POST /api/trends` → id 73, status=watching.
  - **#76** ch2 score **80** watch — “gpt-oss 20B/120B local MXFP4 (Ollama / llama.cpp)” (new family, not another Qwen-27B). Proof: id 76, status=watching.
  - **#69** ch2 score **80** watch refresh — “vLLM OOM vs llama.cpp RAM-spill on 24GB”. Proof: id 69.
  - **#77** ch1 score **70** reject — “Copilot Max $100 / $200 credits (Aug 2026)” — *is* the wedge, rejected because ch1 is frozen. Proof: id 77, status=rejected.
- **No produce / no generate / no displace.** Queued after requeue = tomorrow's first render slots. t4 bench has 3 drafts.
- **No BGM generate** — pool 15 = target, last refresh yesterday.
- **Seeding kit:** `run/seeding/2026-08-17.md` (operator posts; agent does not).

## Engagement

**Baseline `baseline`** — 4/4 clean, `visible=True`, none `used_fallback`. ~225s. Scores from frames + scripts + beat timings:

| Lever | score | evidence |
|---|---|---|
| R1 hook (P1) | 2 | Pain-first on frame + opener ≤11w: “Your RAG returns garbage” / “Your agent forgets everything” / “Toda API = integração nova” / “Sua RAG responde tudo errado”. Hook at t=0 all 4. |
| R8 thumb/title (P2) | 2 | Gaps vs titles (hook “Your Retrieval Returns Garbage” vs title “Fix Your Slow RAG…”; PT “Uma conexão substitui todas” / “Você corta no lugar errado”). Zero title-word echo. |
| R2 info gain (P3) | 2 | Varied mixes (ch1-code: hook/stat/term/lst/code/cmp/stat/quote/cta + indented `r = co.rerank(`; ch2-code fanout Servidor MCP → Claude/GPT/Gemini + indented `@mcp.tool()` / `return db.query(q)`). ch2-concept is concept-only (no code) which fits the chunking subject. |
| R6 payoff (P4) | 2 | Quotable closes (“The retrieval wasn't broken—the ranking was.” / “An AI without memory isn't an assistant—it's a stranger…” / “Pare de costurar integração e comece a padronizar.” / “Contexto ruim gera resposta ruim, sempre.”) |
| R5 retention (P5) | 2 | Open loops early on all 4 (“Here's the truth: your vector search is good enough…” / “that forgetting isn't a bug, it's the default” / “E isso é insano” / “O problema quase nunca é o modelo”). |
| R3 clarity (P6) | 2 | Code legible + indented; CMP stacked + VS in flow (Cohere vs BGE; Sem MCP vs Com MCP; O modelo vs O chunking); diagram elabel “past context” beside the Vector-DB→Model arrow, no strikethrough. |
| R4 pacing (P7) | 2 | Hook at t=0; **zero beats under 1.80s or over 8s** (yesterday's residual 8.79s holds are gone on this render). min 1.80–2.07s, max 7.08–7.49s; drift tweens present on every >5.5s hold. Do not ship another R4 prompt rule. |
| R7 CTA (P8) | 2 | Follow/Siga + named next value 4/4 (“The chunking mistake, tomorrow” / “Build the memory layer” / “Amanhã: seu 1º servidor MCP” / “Amanhã: tamanho de chunk exato”). Spoken ask present on all 4 scripts. |

**All-2 baseline (14th consecutive judged day) → non-engagement path (fallback clause).** No prompt change. Recurring render-timeouts are already owned by `43cfe3d` (live; yesterday's 5 requeues produced files). Today's work is the next 5 requeues + 2 deletes, not another timeout-heuristic. Shipping unproven polish on an all-2 set would violate the gate.

### Experiments settled / opened

None. No running lines left to settle. No new engagement experiment (no file touch).

## Watch next run

1. Operator ch1 pause yes/no (still the binding decision).
2. ch2 08-17 publish composition after 14:00 UTC — expect **1L+4S** with t2 long v955 leading then t4 shorts from the 1L+11S bank.
3. Overnight render of requeued v946 / v957 / v959 (and whether mix-rebalanced drafts v958 / v960 become the next long auto-produce, not lost subjects).
4. Drain the last 6 failed (v961/963 t5, v973/975/976/977 t6). After that the timeout backlog should be empty.
5. BGM count stays ≥ min (now 15 = target).
6. #76 gpt-oss: adopt only as a t27 episode via displacement if a w1-or-lower draft exists — not a new topic while t27 converts 0.
7. Quote whether v948 EXT_URL grows (seeding signal) and whether any new ch2 video attributes a subscriber.

## Commit

`929aa39` — Growth agent 2026-08-17: drain 5 more timeouts; Monday review
