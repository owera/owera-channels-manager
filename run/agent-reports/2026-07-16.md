# Growth agent — 2026-07-16

No `⚠ Needs operator` items today. Pipeline is clean.

## Observed
- **ch1 moved off 9 subscribers: now 10** (first movement since 2026-07-02). ch2: **59 subs**
  (needs ~2/day to hit the 75 checkpoint on 07-23). Watch hours: ch1 3.0h, ch2 4.2h.
- **Publish-order fix (5858fd0) delivered exactly as predicted** (07-15 watch item): today ch1's
  first slot was the t5 long (630, CrewAI vs LangChain) and **all 4 short slots were t15 wedge
  videos (641–644)** — the operator-approved wedge cohort is finally reaching the audience, 7+
  days sooner than FIFO would have. ch2: first-slot long 672 (t2) + 4× t4 hardware shorts
  (655–658). Daily mix (directive 2) held on both channels; all ch2 titles PT-BR (directive 6 ✓).
- **ch2 SHORTS-feed distribution is real and concentrated in hardware (t4)**: video 542
  ("32GB de RAM… VRAM") = 401 views, **387 from the SHORTS feed**, +1 sub; 524 ("7B ou 13B")
  = 94 views, 84 SHORTS, +1 sub. The algorithm is actively testing t4 — the t4/t27 weighting is
  doing its job. (Directive 1: quoting traffic_json, not CTR.)
- **ch1 conversion (directive 9 metric)**: lifetime subs-per-video still 3/145 gainers — 43, 96,
  367, all long-form comparisons; 96 converted via YT_SEARCH with 9 watch-minutes on 3 views.
  The wedge cohort (t15) published only this morning — first conversion data ~07-19.
- ch1's 07-12→07-15 pre-wedge cohort (t18/t20 shorts): 15 metric rows, **0 measured views** so
  far — young (<96h) but worth watching; if it stays ~0 while t15 moves, that's more evidence
  for the wedge.
- Quota: both channels at 8750/9000 spent with all 5/5 publishes done — yesterday's 9000/9000
  self-reset confirmed (07-15 watch item ✓). playlist_add: 12 successes + 2 auto-created
  playlists since yesterday, zero errors — the recurring 404 is gone (07-15 watch item ✓).
- BGM pool 20/15 target, healthy. Boards: both channels `at_capacity` (2.0 days inventory) →
  idea generation and trend adoption skipped per the capacity gate.

## Monetization
| Channel | Metric | Current | Needed (lower tier) | Pct |
|---|---|---|---|---|
| ch1 Owera | subscribers | 10 | 490 | 2.0% |
| ch1 Owera | watch hours | 3.0 | 2,997 | 0.1% |
| ch1 Owera | shorts views | 1,961 | 2,998,039 | 0.1% |
| ch2 Rodrigo | subscribers | 59 | 441 | 11.8% |
| ch2 Rodrigo | watch hours | 4.2 | 2,995.8 | 0.1% |
| ch2 Rodrigo | shorts views | 1,587 | ~2,998,413 | 0.1% |

Binding constraint everywhere is subscribers → conversion-first strategy stands (wedge on ch1,
t4 hardware series on ch2). No budget/topic override from milestone math.

## Triage
- Digest: 10 items, 0 auto-fixable. 8 `rejected` rows are deliberate `leave` (parked-t3 clears +
  board-overflow trims from prior runs). 2 quota entries are `monitor` (near-cap is by design —
  budgets exhausted normally, publishes completed). No failed/stuck/oauth/error-run items.
  **No remediation actions were needed.**

## Engagement (the run's core)
**Baseline (4/4 clean renders, all visible=True, no fallback):** R1 hook **2** (4/4 pain-first
at t=0: "Your RAG grabs junk" / "Your AI forgot you already" / "Uma integração nova a cada
API?" / "Sua RAG busca lixo") · R2 **2** (code+diagram+stat in every subject; MCP `@mcp.tool()`
snippet correct) · R3 **2** (code indented + line-highlighted, diagram edge labels clean beside
connectors) · **R4 pacing 1 — only weak lever**: ch1-concept **tail floor FAILED** (code 1.19s /
quote 1.04s / cta 1.16s — four beats bunched in the last 3.4s defeat the two-beat floor, whose
guard checks a predecessor that is itself bunched), ch1-code term 1.09s flash, ch2-code 1.66s
flash + 10.44s cmp drag; ch2-concept clean (2.9–6.1s) · R6 **2** (quotable closes) · R7 **2**
(Follow/Segue + concrete next-payoff 4/4) · R8 **2** (anchored hooks, zero title echo —
1f70497 holding, settles tomorrow).

**Shipped — `14b1979` (R4, deterministic beat floor in `align_storyboard`):** every beat now
gets a soft spacing floor (2.0s for the last two, 1.8s mid), granted in a backward pass only
while all predecessors can still fit their own floors — so a bunched tail steals time from the
first beat with slack, and genuinely too-short clips keep pure word-sync (the 4s unit cases are
byte-identical). Gate: unit-exercised on all three of today's exact failing timing sets
(ch1-concept quote 1.04→1.88s, cta 1.16→2.0s; zero beats under 1.68s; slack beats untouched);
after-render on ch1-concept through the real pipeline: min beat 1.68s, cta 2.0s, no new drag,
hook/CTA frames intact (R1/R7 unchanged), visible=True, no fallback; `verify_storyboard`
40/40. Manager restarted, dashboard 200 → **live in production**. Logged as running experiment
(settles ~07-19). Residual: the drag side (a single 10.4s cue span the LLM won't split) is
content-level and deliberately NOT addressed by this change.

**Settled — `1ba57be` (07-13, R4 18-word cue gap): PROMOTED with residual.** Kept (directionally
safe; 07-14/07-15 renders were cleaner), but today's baseline proves the prompt rule does not
reliably hold — ch2-code regressed to a 10.44s drag (pre-change: 10.12s). Honest conclusion:
prompt-level pacing levers are exhausted; the flash half now belongs to the deterministic floor
above; the drag half stays open pending a content-aware idea.

## Actions
- Logged **trend #32** (watch, score 78): "Copilot promo credits expire end of August 2026" —
  GitHub moved Copilot to usage-based AI Credits on 06-01; Business/Enterprise promo credits
  vanish end of August (10–50x bill jumps reported). Feeds t15 (wedge lead) directly; queued as
  the first t15 idea batch when board inventory frees. No adoption (board at capacity).
- **Seeding kit #6** → `run/seeding/2026-07-16.md`: ch2 TabNews quantization/VRAM post (658) +
  r/brdev VRAM comment (655) + LinkedIn cloud-bill post (657); ch1 dev.to Copilot-credits post
  (641) + LinkedIn pricing-comparison post (643) + HN angle; 3 adjacent-channel comment drafts.
  Kits #1–#5 still show no EXT_URL in traffic_json → highest-leverage single item remains the
  TabNews hardware post.
- No weight/budget/topic changes: current weights (ch1 t15=3/t6=2/t5=2; ch2 t4=3/t27 up) are
  exactly what today's data endorses, and both boards are at capacity.
- Code changes this run: 1 of 2 cap used (`14b1979`).

## Watch next run
- **R8 experiment `1f70497` settles 07-17** — re-judge anchored thumbnail hooks on fresh renders.
- **First t15 wedge conversion data ~07-19**: per-video `subscribers_gained` on 641–644/630.
  Also check whether ch1's new sub (9→10) attributes to any video once metrics refresh.
- **`14b1979` floor in production**: tomorrow's baseline should show zero sub-1.68s beats on all
  4 subjects; drag (≥8.5s spans) may persist — that's the known residual, don't re-ship a prompt
  rule for it.
- ch2 slope to 75: 59 today, checkpoint 07-23 — if <65 by Monday, lead the growth review with
  cadence/seeding options (the unposted TabNews seed is the biggest untried lever).
- ch1 pre-wedge cohort (t18/t20, 07-12→07-15): still 0 measured views? Compare against t15's
  first numbers as a natural A/B for the wedge thesis.
