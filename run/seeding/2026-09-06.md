# Seeding kit — 2026-09-06

Operator posts; agent does not. Feature: ch1's search-led Copilot commit-message short (98 views / YT_SEARCH=86, rewatches) plus the first t15 convert; ch2 today's Chrome/Discord VRAM leak.

## ch2 Rodrigo Recio (PT-BR)

### 1. TabNews — post
**Título:** Chrome e Discord na GPU dedicada comem a VRAM do modelo
**Corpo:**
Se o `nvidia-smi` mostra 2–3 GB ocupados com o Ollama parado, o inquilino não é o GGUF. É o Chrome (hardware acceleration) e o Discord (overlay). Os dois pedem a NVIDIA por padrão no Windows. O modelo que “cabe” em 8 GB na planilha não cabe com o browser aberto.

Teste de 20 segundos: fecha o Chrome, `nvidia-smi` de novo. Se a VRAM caiu 1+ GB, o gargalo era o cliente, não o quant. `Hardware acceleration` off no Chrome; Discord → Advanced → hardware accel off. Aí sim mede o 7B.

Short de hoje (t4):
https://youtu.be/pgjYTrdywgc

### 2. r/brdev — post/comment
**Título / ângulo:** Quantizar pra 4-bit não é o atalho de RAM que o tweet promete
**Corpo:**
Q4 corta pesos. O KV cache do contexto não corta na mesma proporção. Um 7B Q4 com `num_ctx=8k` ainda estoura 8GB se o keep-alive segura o modelo + o cache. `ollama ps` na hora do generate: se a VRAM não cai quando você mata o chat, o inquilino é o keep-alive, não o GGUF.

https://youtu.be/Ch7Phhu6alA

### 3. LinkedIn (PT) — post curto
**Corpo:**
Alugar RTX 4090 por 6 meses já compra a placa. A conta só fecha se você precisa do burst (um treino, um deadline) e devolve. Se a placa fica ligada 8h/dia “porque o modelo está no ar”, você está pagando ouro por idle.

Amanhã o short (t4, 14:00 UTC): aluguel vs compra, no número.

## ch1 Owera Software (EN)

### 1. r/LocalLLaMA — comment on a fresh Copilot / git / commit-message thread
**Draft:**
Copilot autocomplete inside the git commit box is real, and almost nobody turns it off. Every `git commit` with the pane open re-sends context. That is why a 30-second short on this is pulling search (`github copilot`, `ez commit`, `vscode copilot agent`) and getting rewatches — 98 views, YT_SEARCH=86 of them.

If your commit messages started sounding like the model, check whether Copilot is bound to the SCM input, not just the editor.

https://youtu.be/wF8qVBRoLB0

Formatter-on-save sibling that actually converted a subscriber at 21 views:
https://youtu.be/yS-Gv9wkhy0

### 2. dev.to — short post
**Title:** Copilot is autocompleting your git commit messages (and billing the context)
**Body:**
Completions in the commit box are not “free.” They consume the same AI credits as chat once the file + diff are in context. The silent loops (formatter-on-save, suggestions in comments, `@workspace` on a one-line fix, commit-message autocomplete) are how a Pro allotment disappears on a Tuesday.

One repo, one pane, Copilot off in SCM input, format-on-save off, suggestions off in comments. Then look at the meter.

Search-led: https://youtu.be/wF8qVBRoLB0
The convert: https://youtu.be/yS-Gv9wkhy0

### 3. HN — skip
Not a Show HN. The long that shipped this morning (LangChain vs CrewAI fallback models) is a comparison, not a release. Save the slot.

## Comment drafts (adjacent channels)
- A Copilot commit / SCM thread: "Autocomplete in the git commit box re-sends context. Search is already sending people here: https://youtu.be/wF8qVBRoLB0"
- A Copilot billing / formatter-on-save thread: "This one converted at 21 views: https://youtu.be/yS-Gv9wkhy0"
- An Ollama VRAM / Chrome GPU thread: "Fecha o Chrome, mede de novo. https://youtu.be/pgjYTrdywgc"

## Yesterday's seeds — status
- 09-05 kit operator-post status unverified (agent does not post).
- v948 EXT_URL still = 9 (frozen since 08-17).
- v960 EXT_URL still = 1 (converter, unchanged).
- v1190 YT_SEARCH still 59 / views 74 — still search-led, still 0 subs.
- v1210 20→21 views, YT_SEARCH 4→5, still 1 sub. No new EXT_URL movement attributed to prior kits.
