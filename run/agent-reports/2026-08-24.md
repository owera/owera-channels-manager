# Growth agent — 2026-08-24

## ⚠ Needs operator

1. **PAUSE ch1 (hard review overdue since 08-01).** Still **12 subscribers** vs the 15-sub bar (unchanged vs 08-23). Lifetime video-attributed converters remain **four** (v558 MCP +1, v367 t6 +1, v43 t1 +1, v96 t5 +1) — the 11→12 move is still **unattributed**. Post-CTA / post-wedge volume still ~0 attributed subs (v785 ≈174 views / 148% avp / SHORTS=164 → 0). **Recommendation stands: PAUSE ch1** (or explicitly override with a new bar). Agent continues **zero ch1 strategy moves** (no weight/trend adopt/generate for growth). Pipeline hygiene only.
2. **ch2 metadata backfill** (`db7a254` plan) still awaits `--apply` if still desired.
3. **BGM pool path** remains external (`MoneyPrinterTurbo/resource/songs`). Count 25 (target 15); wipe risk still escalates to silent renders.

---

## Observed

- **Now:** 2026-08-24 ~12:00 UTC. Kill switch off, `scheduler_paused: false`, tree clean at start, `main`. Last full growth run: 2026-08-23. Monday → Growth review below.
- **ch1:** published **5/5** today, quota **8750/9000** (`daily_limit_hit: false`). Mix = **1 long + 4 shorts** (v1005 t5 long first @ 07:01 `uaoX9WIHbxs`, then v1045 t18, v1046 t18, v1047 t18, v1057 t6). **Nineteenth consecutive 1L+4S day** (08-06…08-24).
- **ch2:** published_today **0** at observe (window opens **14:00 UTC**). rendered_today **5**. Approved = **1 long + 11 shorts** (v1054 t2 long + 4×t4 1037/1061–1063 + 6×t27 998–1004 + leftover v709 t10 from July). **08-23 did publish 1L+4S** (v1052 t2 long first @ 14:01 `4HdWXUPoeT8`, then v1029/1034/1035/1036 **all t4**) — yesterday’s 1L + 4×t4 prediction held.
- **ch2 subscribers: 64** (unchanged vs 08-21…08-23). **New attributed converter:** v1022 t4 short “2 GPUs mas o modelo usa só uma?” (3 views / 65.1% avp / YT_SEARCH=3 / age 92.5h) — t4 `subscribers_gained` **4 → 5**. Lifetime converters now **seven** (v542/524/810/421/684/315 + **v1022**). The 08-21 63→64 move now has a candidate on the leaderboard; total channel subs did not move again.
- **ch1 subscribers: 12.** Subs-per-video on the mature leaderboard: **still 4 lifetime +1s**, none on the post-07-12 CTA / post-07-13 wedge cohort. Do not invent which video converted the 5th.
- **BGM pool:** 20 → **25** this run (generated 5; min 5 / target 15; `is_low: false`). No `music_gen` errors. No `error_runs_24h`. Last generate was 2026-08-20 (4 days + overnight renders) so variety was refreshed. Proof: `POST /api/music/generate {"count":5}` → `generated: 5, errors: []`; `GET /api/music` count 25.
- **Boards (API `by_format` at observe):**
  - ch1 draft `{long:2, short:8}` · queued `{long:0, short:0}` · approved `{long:0, short:2}` (1013/1014 t7 leftover)
  - ch2 draft `{long:2, short:8}` · queued `{long:0, short:0}` · approved `{long:1, short:11}`
  - Both `at_capacity: true`, `days_of_inventory: 2.0`.
- **Language (ch2):** 08-23 titles all PT-BR ✓ (1052 pipeline/schema; 1029 GPU 24GB modelo 8GB; 1034 dois modelos VRAM; 1035 4-bit alucina; 1036 cabo PCIe). 08-24 leading approved long v1054 also PT-BR (“Rollback de Modelo em Produção…”). No EN leak.
- **traffic_json (via leaderboard API, no DB dive):** v542 SHORTS=385; v785 SHORTS=164 + YT_SEARCH=6; v558 SHORTS=104 + YT_SEARCH=12; v948 SHORTS=64 + **EXT_URL=9** (unchanged vs 08-17…08-23) + NOTIFICATION=3; v884 YT_SEARCH=22 + SHORTS=77; v1022 YT_SEARCH=3 (the new converter — search, not Shorts). SHORTS / YT_SEARCH dominate. **No BROWSE / SUGGESTED keys** on the top samples. No invented CTR.
- **Overnight mix (JobRuns 19968–19977 produce @ 00:00):** ch1 auto-produced 1005 t5 long + 1045–1047 t18 + 1057 t6 (the 5 that published this morning). ch2 auto-produced 1054 t2 long + 1037 t4 (Friday’s leftover demote, recovered) + 1061–1063 t4. No demote JobRuns — queued was already empty at yesterday’s 14:00, so `rebalance-headroom-zero` was a vacuous no-op as predicted.

### Publish log (settle evidence)

| day | ch1 | ch2 |
|-----|-----|-----|
| 08-17 | 5 (1L+4S) long 972 first | 5 (1L+4S) long 955 first + 4×t4 |
| 08-18 | 5 (1L+4S) long 962 first + 3×t18 + t6 | **5 (1L+4S)** long 957 first + 4×t4 |
| 08-19 | **5 (1L+4S)** long 959 first + t18 + 3×t6 | **5 (1L+4S)** long 956 first + 3×t4 + t27 |
| 08-20 | **5 (1L+4S)** long 961 first + t18 + 3×t6 | **5 (1L+4S)** long 958 first + **4×t4** (1020–1023, **v1022 converted**) |
| 08-21 | **5 (1L+4S)** long 960 first + t18 + 3×t6 | **5 (1L+4S)** long 982 first + **4×t4** (1024–1027) |
| 08-22 | **5 (1L+4S)** long 963 first + 4×t6 | **5 (1L+4S)** long 1038 first + t4 1028 + 3×t27 |
| 08-23 | **5 (1L+4S)** long 999 first + 3×t6 + t18 | **5 (1L+4S)** long **1052** first + **4×t4** (1029, 1034, 1035, 1036) |
| 08-24 | **5 (1L+4S)** long 1005 first + 3×t18 + t6 | pending window (0 at observe; 1L+11S banked, **v1054 leading then 4×t4**) |

## Monetization

| metric | ch1 current | ch1 needed | ch1 % | ch2 current | ch2 needed | ch2 % |
|---|---|---|---|---|---|---|
| subscribers | 12 | 488 | 2.4% | 64 | 436 | 12.8% |
| watch hours | 7.5 | 2992.5 | 0.2% | 11.9 | 2988.1 | 0.4% |
| Shorts views | 4407 | 2,995,593 | 0.1% | 4335 | 2,995,665 | 0.1% |

**Binding constraint both channels: subscribers** (lower tier). No milestone crossed (no metric hit 100% for the first time). ch2 t4 just added an attributed sub (v1022) without moving the channel total off 64 — prefer analytics (keep t4 as converter lead; ch1 frozen) over pure milestone math.

## Growth review

Monday checkpoint (vs 2026-08-17 report).

| | 08-17 | 08-24 | Δ |
|---|---|---|---|
| ch1 subs | 10 | 12 | **+2** (moved 08-23, still unattributed) |
| ch2 subs | 63 | 64 | **+1** (moved 08-21; v1022 now carries a +1 on the leaderboard) |
| ch1 watch hours | 7.3 | 7.5 | +0.2 |
| ch2 watch hours | 11.4 | 11.9 | +0.5 |
| ch1 Shorts views | 4218 | 4407 | +189 |
| ch2 Shorts views | 3986 | 4335 | +349 |

- **Traffic-source mix:** still SHORTS + YT_SEARCH. No named BROWSE / SUGGESTED on the leaderboard samples. v1022 converted on **YT_SEARCH=3** (not Shorts) — same search-shaped conversion as older t4 winners v421/v315. Directive-7 falsifier (BROWSE+SUGGESTED ~0) still true; do not publish more of the same into a feed that is not testing us on ch1.
- **Seeding:** kits written 08-17…08-23; operator post status unverified. **v948 EXT_URL still = 9** — the week’s only external spike, unchanged for 7 days. Seeding traction unproven.
- **Series:** no numbered “Parte N” in this week’s titles on either channel. Directive-3 is still slipping. Do not invent a series this run while ch1 is frozen and ch2’s converter (t4) is already leading the shorts — a theme_prompt series experiment can wait for a w1 draft / operator unfreeze.
- **Biggest bet next week:** keep **t4 (w4)** as the ch2 converter lead (now 5 attributed subs / 134 videos, including a *this-week* converter). Do not open another t27 topic while t27 is 0 subs. Operator must decide ch1.

## Triage

Digest at start: 1 issue (quota monitor only). `failed=[]`, `pipeline_starved` / stuck / oauth / bgm_pool_low / music_gen / `error_runs_24h` / `rejected` all empty. `summary.clean: false` only because of the quota row.

Timeout backlog remains empty (closed 08-20).

| Issue | Action | Proof |
|---|---|---|
| quota ch1 8750/9000 | monitor only | full 5-publish day (5×1750=8750); `daily_limit_hit: false` |
| failed / stuck / oauth / bgm_low / pipeline_starved / error_runs_24h / rejected | none | clean lists |

After-state board (API, unchanged by triage): ch1 queued `{long:0, short:0}` · approved `{long:0, short:2}` (1013/1014 t7); ch2 approved `{long:1, short:11}` (v1054 leading). Failed list stays empty.

## Learned

1. **ch2 08-23 mix prediction held:** 1L+4S, long v1052 first, then **4×t4**. Weight-first + long-reserve did what they said. Today’s plan: **1054 @ 14:00 then 1037/1061/1062/1063 all t4** (`GET /api/videos/publish-plan?channel_id=2`).
2. **t4 converted again.** v1022 (published 08-20, matured 08-24, 3 views / 65% avp / YT_SEARCH=3) is the 7th lifetime ch2 converter and the 5th on t4. Small-n, search-shaped — do not overfit, but it is a real `subscribers_gained` row that did not exist yesterday.
3. **ch1 still frozen at 12.** 11→12 remains unattributed. Freeze holds; 12 does not clear 15.
4. **Winners (mature):** ch2 t4 23.1 avg views + **5 lifetime subs** (the only converter that still moves); t28 25.8 @ 59.5% avp / 0 subs; t27 21.0 avg / 45.5 avp / **0 subs**. ch1 t18 MCP 14.5 avg + the only recent sub-gainer; t15 high-avp loops, 0 subs on 46 videos.
5. **Hypotheses this run:** (a) ch2 08-24 window publishes 1L+4S with v1054 leading then 4×t4; (b) `rebalance-headroom-zero` first live “do not drain” still needs a 14:00 where shorts are QUEUED and `approved_longs` just hit 0 with `rendered_today==budget`; (c) operator pause remains the binding ch1 move.

`publish-plan-mix` (shipped 08-21, `de23128`): **PROMOTED** — 72h+, three live days (see experiment log).

`rebalance-short-for-long` (shipped 08-22, `d2d158f`): 08-23 publish log CONFIRMED (v1052 long first + 4×t4). Still <72h — leave `running`, settle ~08-25.

`rebalance-headroom-zero` (shipped 08-23, `22056ea`): overnight recovered 1L+4S without demotes because queued was already empty at 14:00. First live “do not drain” still unobserved. Leave `running`.

## Acted (channels)

- **No requeue / retry / delete.** Failed list already empty. Cap not hit.
- **No weight changes.** t4=4 / t27=3 / t2=2 already match conversion (t4 just added a 5th sub). ch1 frozen (t15 remains w=0 as part of the freeze; not revived).
- **Trends logged (no adopt/displace — boards at capacity; every ch2 draft is w2+ so a new w1 topic cannot displace; ch1 frozen):**
  - **#73** ch2 score **85** (was 84) watch refresh — “Qwen3.8-27B local GGUF”. Ollama **688.4K** (was 632.2K, +56K/day). Proof: `POST /api/trends` → id 73, status=watching.
  - **#82** ch2 score **84** (was 85) watch refresh — “Muse Glimmer 30B”. Ollama **161.1K** (was 157.7K; slowing). Proof: id 82, status=watching.
  - **#78** ch2 score **84** (was 86) watch refresh — “Nemotron 3.5 Lightning”. Ollama **130.9K** (was 129.2K; stalled). Proof: id 78, status=watching.
  - **#81** ch2 score **81** watch refresh — “Ollama 0.32.15 TTFT halved”. Still latest stable (v0.33.0 is rc2). Proof: id 81, status=watching.
  - **#80** ch2 score **80** watch refresh — “Ollama default num_ctx silently truncates 128K models”. Proof: id 80, status=watching.
  - **#83** ch2 score **76** watch **new** — “Ollama 0.33 Claude Desktop local-model toggle (rc2)”. Concrete t4 episode once it stables; still rc. Proof: `POST /api/trends` → id 83, status=watching.
  - **#77** ch1 score **70** reject refresh — “Copilot Max $100 / $200 credits (Aug 2026)” — *is* the wedge, rejected because ch1 is frozen. Proof: id 77, status=rejected.
- **No produce / no generate / no displace.** Auto_produce owns the overnight mix. ch1 approved 0L+2S is the known noon gap; rendered_today=5 so a manual produce cannot fill today.
- **BGM generate 5** — pool 20→25. Proof above.
- **Seeding kit:** `run/seeding/2026-08-24.md` (operator posts; agent does not). Features v1022 (new t4 converter) + yesterday’s t4 1029 + t2 long 1052.

## Engagement

**Baseline `baseline`** — 4/4 clean, `visible=True`, none `used_fallback`. ~245s. Scores from frames + scripts + beat timings:

| Lever | score | evidence |
|---|---|---|
| R1 hook (P1) | 2 | Pain-first on frame + opener ≤~11w: “Your RAG returns garbage, and you know it.” / “Your AI forgets you the second you close the tab.” / “Você vive escrevendo integração pra cada API nova.” / “Sua RAG responde qualquer coisa, menos o que você perguntou.” Hook cards: “Your RAG returns garbage” / “Your AI forgets you instantly” / “Uma integração por API? Chega” / “Sua RAG responde qualquer coisa”. Hook at t=0 all 4. |
| R8 thumb/title (P2) | 2 | Gaps vs titles (hook “Your retrieval is dumping garbage” vs title “Fix Your Slow RAG…”; “Its Memory Resets Every Time” vs “Why Your AI Agent Forgets…”; PT “Um Só Padrão Conecta Tudo” / “Você Está Cortando Errado”). Zero title-word echo. |
| R2 info gain (P3) | 2 | Varied mixes (ch1-code: hook/stat/cmp/term/code/diagram/stat/quote/cta + `scores = ce.predict(pairs)`; ch1-concept: Chat→Store→Retrieve→Model + `notes = db.load(user)`; ch2-code fanout MCP → Banco/Arquivos/APIs + `@mcp.tool()` indented; ch2-concept cmp O modelo vs Seu chunking + 3-item list all visible at midpoint). |
| R6 payoff (P4) | 2 | Quotable closes (“Retrieval finds candidates; reranking finds the truth.” / “Your agent doesn't remember—it re-reads.” / “Integração boa é aquela que você não precisa reescrever nunca mais.” / “Chunk ruim entra, resposta ruim sai — sempre.”) |
| R5 retention (P5) | 2 | Open loops early on all 4 (“The problem isn't your embeddings” / “it's not broken, it's designed that way” / “A verdade? Você não precisa disso.” / “O problema quase nunca é o modelo”). |
| R3 clarity (P6) | 2 | Code legible + indented (ch2-code `return db.run(sql)` under `def query(sql):`; ch1-code CrossEncoder snippet). CMP stacked + VS in flow (Signal vs Noise; O modelo vs Seu chunking). Fanout on ch2-code truthful. List midpoint frames show all items (ch2-concept lst: rasga frases / separa pergunta e resposta / joga contexto fora). |
| R4 pacing (P7) | 2 | Hook at t=0. Min beat 1.80s (ch1-code code — floor holding). **Known residual returned:** ch2-code cmp **9.01s DRAG**; ch2-concept lst 8.45s + 8.35s. Prompt-level R4 is exhausted (07-13/07-16 verdicts) — do not ship another R4 prompt rule. Drift still owns the long holds. Yesterday’s 10.98s ch1-code diagram drag is gone this render (5.55s). |
| R7 CTA (P8) | 2 | Follow/Siga + named next value 4/4 (“Cut RAG latency in half” / “Build the memory layer tomorrow” / “Amanhã: seu 1º MCP do zero” / “Chunk size e overlap ideais”). Spoken ask present on all 4 scripts. |

**All-2 baseline (21st consecutive judged day) → non-engagement path (fallback clause).** No prompt change. The only soft spot is the known R4 residual (9.01s cmp hold), which prior verdicts already closed at the prompt layer. Recurring render-timeouts are gone (failed=[]). Shipping unproven polish on an all-2 set would violate the gate.

**Shipped instead (pipeline-robustness):** `GET /api/videos/queue-plan` walks `_queued_candidates` order (long-first when no approved long, shorts-first when a long is banked) instead of FIFO-by-position/id. Dual of `publish-plan-mix` (de23128) for the render side of the board.

Lifecycle (written before the change, as required):

```
draft ─produce→ queued ─render_loop._submit_new→ rendering ─render_loop._advance→
   rendered → (review | approved)
approved ─publish_loop→ publishing → published
```

queue-plan is **read-only** — it does not set `Video.status`. It only labels queued cards. `_submit_new` still consumes QUEUED in `_queued_candidates` order; this change makes the label match that consumer. Publish still only consumes APPROVED.

| | no approved long, lower-id shorts + long queued | approved long banked, lower-id long + short queued |
|---|---|---|
| FIFO queue-plan (before) | short labeled **next to render**; long **budget full** if slots=1 | long labeled **next to render** |
| mix queue-plan (after) | **long next to render**; shorts budget-full if slots=1 | **short next to render** |

Proof: `PYTHONPATH=. .venv/bin/python tests/verify_render.py` → **129/129** (was 121; +8 mix-order cases). `uv run python -c "import app.main"` ok. Manager restarted (`launchctl kickstart`); `GET /api/dashboard` = 200. Live `GET /api/videos/queue-plan?channel_id=1|2` = `{}` (queued empty at observe — no live “next to render” to quote). Watch next run.

### Experiments settled / opened

- Settled: **`publish-plan-mix` PROMOTED** (see experiments.jsonl).
- Left running: `rebalance-short-for-long` (08-23 publish confirmed, <72h); `rebalance-headroom-zero` (live 14:00 drain still unobserved).
- Opened: `queue-plan-mix` commit `60055710eb2ead634bcbdf36329fac9526985a93`, status=running. Settle from the next noon a long is queued behind lower-id shorts — the plan’s first “next to render” should be that long.

## Watch next run

1. Operator ch1 pause yes/no (still the binding decision). 12 subs does not clear the 15-sub bar — do not unfreeze. Quote whether any leaderboard row now shows `subscribers_gained` for the 11→12 move.
2. ch2 08-24 publish composition after 14:00 UTC — expect **1L+4S** with t2 long **v1054** leading then **4×t4** (1037, 1061, 1062, 1063). This is more evidence for `rebalance-short-for-long` (settle ~08-25).
3. First live “do not drain” for `rebalance-headroom-zero`: a 14:00 where shorts are still QUEUED and `approved_longs` just hit 0 with `rendered_today==budget`. Those shorts should still be queued at the next noon (not five produce-demote JobRuns).
4. First live `queue-plan-mix` signal: a noon where ch2 (or ch1) has a queued long behind lower-id shorts and `approved_longs==0`. `GET /api/videos/queue-plan` should label the long “next to render”.
5. ch1 noon approved shorts=2 (t7 leftover) + 0 longs is thin; drafts already have 2 t5 longs + 3 t6 + 4 t18. Auto_produce after midnight should refill without a manual produce.
6. Failed list stays empty.
7. BGM count stays ≥ min (25, above target 15). Next refresh only if stale again *and* renders landed.
8. #83 Ollama 0.33 Claude Desktop (76, rc2) / #73 Qwen3.8 (85, 688.4K): adopt only as a t4/t27 episode via displacement if a w1-or-lower draft exists — not a new topic while t27 converts 0.
9. Quote whether v948 EXT_URL grows past 9 (seeding signal). Quote whether v1022 views/avp grow now that it has a +1.

## Commit

`6005571` (queue-plan mix order). Report this commit.
