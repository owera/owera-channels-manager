# Growth agent — 2026-07-24

**Commits:** a8c15bd (playlist recreate loop removed) · 0fd9d9a (auto-produce)

> ## ⚠ Needs operator
> **1. Your channels have accumulated duplicate one-video playlists (12+ in 3 days) — cleanup is
> optional but they're visible to viewers.** Cause: the 07-21 "playlist placeholder fix" (d978127)
> was built on a wrong diagnosis. 13-char `PL…` ids are a REAL id format YouTube returns from
> `playlists.insert` (verified today: `PLLdeDcM9G5vY` is live and public, and item-adds into it
> succeed) — the len==34 validity check declared every such id invalid, so **every publish of one
> of 17 topics created a brand-new playlist** (50 quota units each). Fixed in code today (loop
> removed; details below). I cannot delete playlists (guardrail) — if you want the dupes gone,
> delete the older empty ones per topic in YouTube Studio; the app now keeps using each topic's
> newest one, which works.
> **2. Carried from 07-23, still unanswered:** are the seeding kits being posted? EXT_URL is still
> ~0 (ch1 2 / ch2 5 views, unchanged). And directive-3 (named "Parte N" series) remains
> unimplemented since 07-09.

## The two structural fixes (both live, both verified)

### 1. Playlist recreate loop — killed (`publish_loop.py`, `youtube.py`, `tests/verify_publish.py`)
The starvation detector's cousin from 07-21 had the opposite failure: a fix that misfires on
healthy state. Evidence trail, all observed today:
- Playlist rows 39–50: created 07-22→07-23, one per publish of an affected topic, all 13-char,
  each logged "auto-created playlist" + a **successful** item-add seconds later.
- Public fetch of newly-minted `PLLdeDcM9G5vY` → real, live playlist titled "Context Engineering
  2.0". So the ids the check called "placeholders that 404" are valid.
- The pre-existing add-404 self-heal (`is_playlist_missing` → drop mapping → recreate next
  publish) already handles genuinely dead playlists — it's the only truthful invalidity test.
**Change:** removed the pre-upload `is_valid_playlist_id` drop (and the helper — its premise is
disproven; length cannot distinguish valid from invalid). Rewrote the tests to pin the correct
behavior: a 13-char stored id is kept and used (no create call), and an add-404 still drops the
mapping for recreate. `verify_publish` 30/30.
**Live verification note:** none of today's remaining publishes hit an affected topic (ch1's
budget is spent; ch2's queue is t2/t4/t10, all 34-char), so the live no-recreate observation
lands next time an affected topic publishes — **watch next run:** expect playlist_add success
with NO "auto-created playlist" JobRun and a stable `topic.playlist_id`.

### 2. Auto-produce — the render pipeline can no longer starve on a full bench
(`render_loop.py` + new `tests/verify_render.py`)
This morning the 07-23 detector fired again on BOTH channels (`render_starved`: 10 drafts, 0
queued) and **ch2 was at 0 approved, publish_starved after publishing only 1 video** — the
second consecutive day production existed only because this agent ran. Detection without repair
just converts a silent stall into a daily chore, so I closed the gap in the loop itself.

**Lifecycle trace (required for state-machine changes):** the new `_auto_produce` makes exactly
one transition, DRAFT → QUEUED — the same one `POST /api/videos/{id}/produce` makes. QUEUED is
consumed by `render_loop._submit_new` (renders it), which already enforces
`rendered_today < daily_render_budget` and render_concurrency; everything downstream
(RENDERING → approved → publish_loop) is untouched. No other loop reads DRAFT, so nothing else
can double-consume the rows it promotes.

**Policy:** per non-paused channel, promote at most
`daily_render_budget − rendered_today − (queued+rendering)` drafts per tick; weight-0/inactive
topics are never touched (t3's parked drafts stay parked); if the APPROVED buffer has no
long-form, one long is queued first (so `published_long_today`'s reserved daily long slot can
always be filled — directive-2), then shorts weight-first, longs backfill. One `produce` JobRun
logged per promotion.

**Verified:** 10 new checks in `tests/verify_render.py` (headroom math incl. in-flight and
spent-budget cases, paused/parked exclusions, long-buffer guarantee both directions). Full suite
**444 checks green** across 11 files. Imports OK, manager restarted, dashboard 200. **Live
negative check observed:** post-restart ticks with 10 fresh drafts/channel and both budgets
spent → **0 `produce` JobRuns, all 20 drafts still drafts** — it correctly declines when there
is no headroom. The positive live case fires at tomorrow's day-start (headroom 5/channel):
**watch next run** — the morning render batch should exist WITHOUT this agent producing anything.

## Observed (key numbers)
- **Subs: ch1 10, ch2 60** (unchanged from yesterday). Binding constraint: subscribers, both.
- **Conversion, 07-09→ cohort (latest snapshot per video, captured today 12:18):**
  ch1 **1 sub / 75 measured** (0.013/video); ch2 **3 subs / 71 measured** (0.042/video).
- **Traffic mix (cohort ≥07-09):** ch2 SHORTS 1310 (+74 vs yesterday), YT_SEARCH 211 (+33 — search
  is ch2's fastest-growing source; terms: langgraph, claude code, "como ser um bom programador").
  ch1 totals **byte-identical to yesterday** (SHORTS 626, SEARCH 69) — with ch1's quota at
  8750/9000, I can't distinguish "flat views" from "quota-capped partial refresh"; flagging, not
  concluding.
- **Published today:** ch1 5/5 (701–704 shorts + 718 long — the 4+1 mix held). ch2 2 so far
  (713, 714) with 4 approved banked and 3 drip slots left — recovers from yesterday's dry day.
- BGM pool 20/min 5 (healthy). Both channels `oauth: connected`. Language integrity ch2: all 7
  titles published since yesterday noon are PT-BR — no EN leak.
- Playlist-fix memory item ("verify live next run"): verified — and it failed the verification;
  see fix #1.

## Monetization
| Metric (Lower tier) | ch1 current / needed / pct | ch2 current / needed / pct |
|---|---|---|
| Subscribers | 10 / 490 / **2.0%** | 60 / 440 / **12.0%** |
| Watch hours | 4.2 / 2995.8 / 0.1% | 6.9 / 2993.1 / 0.2% |
| Shorts views | 2545 / ~3M / 0.1% | 2402 / ~3M / 0.1% |

No metric at 100%; binding constraint subscribers on both. (ch2 watch hours +0.9h day-over-day —
the long-form drumbeat is compounding, slowly.)

## Triage
- **`render_starved` ×2 + `publish_starved` (ch2 at 0 approved)** — the detector's first real
  catch. Fixed now: produced 10 drafts (ch1 705–708 + 719 long; ch2 709/714/715 shorts +
  717/721 t2 longs — only 3 shorts were on ch2's bench, so 3+2 to fill its 5 slots).
  **After-state observed:** all 10 → queued → rendering → **approved**; ch2's 714 published
  12:06, ending publish starvation; digest re-read shows `render_starved` GONE (only the
  borderline ch2 `publish_starved` 4<5 remains, which tomorrow's auto-produce cycle refills).
  Root cause closed permanently by fix #2.
- **Playlist recreate loop** — found via the memory item's "verify live" follow-up; fixed as
  fix #1 above.
- **Quota ch1 8750/9000** (`daily_limit_hit:false`, auto:false) — monitor only; consistent with
  5 publishes × ~1600 + analytics. No budget change.
- **`error_runs_24h`:** 2× "recovered orphaned render — re-queued" — the 02:15 code-agent restart
  orphaned in-process renders; the startup recovery re-queued them (working as designed). No action.
- No failed/rejected/stuck/oauth entries. Autogen refilled both benches to 10 drafts after I
  produced — the idea loop is healthy.

## Learned
1. **Both of this week's pipeline failures were self-inflicted control logic, not YouTube.**
   The starvation was a gate conflating bench-full with work-in-flight; the playlist loop was a
   validity heuristic contradicted by the platform. Pattern for future fixes: prefer *behavioral*
   truth signals (did the API call succeed) over *structural* heuristics (does the id look right).
2. **ch2's search channel is real and growing** (+33 search views in a day, 211 total, 16% of
   cohort when SHORTS is 81%): concrete-hardware and tool-name terms (langgraph, claude code).
   Hypothesis: ch2 titles that lead with a searchable tool name outperform pure-pain titles on
   search pickup — checkable in `traffic_json` per-video within a week.
3. **ch1 conversion is still the gap** (0.013 subs/video vs ch2 0.042). The 08-01 review is 8
   days out; the R1+R7-carrying cohort (first published 07-23/07-24) matures 07-26→27 — that
   cohort's `subscribers_gained` is the last big evidence drop before the pause decision.

## Engagement
**Baseline (full golden set, 4/4 clean, visible=True, no fallback):**
| Lever | score | note |
|---|---|---|
| R1 hook (P1) | **2** | all 4 openers pain-first ≤10w ("Your RAG returns garbage…" 9w; "Sua RAG responde qualquer coisa…" 8w); hook card at t=0 |
| R8 thumb/title (P2) | **2** | anchored hooks, no title echo ("Wrong answers buried in results" / "Seus documentos cortados no lugar errado") |
| R2 info gain (P3) | **2** | 8–10 varied beats; real diagrams/code/stats (PT diagram Modelo→MCP→Banco→Tools→Serviços) |
| R6 payoff (P4) | **2** | quotable closes on 4/4 ("Retrieval finds the neighborhood; reranking finds the house") |
| R5 retention (P5) | 2 | open loops, no sag (scripts read clean) |
| R3 clarity (P6) | 2 | code ≤30 chars/line, highlighted lines, nothing clipped |
| R4 pacing (P7) | **1** | ch1-code hook held 8.46s (24% of a 35.5s short) — the documented prompt-exhausted DRAG residual; floors hold everywhere (min 1.68s), no flashes |
| R7 CTA (P8) | **2** | overnight spoken-ask change (f79fa33) confirmed in all 4: spoken imperative + named next-value, visual sub states the same promise ("Follow → Live reranker pipeline tomorrow" / "Siga → Tamanho ideal de chunk amanhã") |

**No new engagement lever shipped this run — deliberately.** The weakest lever (R4 drag) is the
known content-aware-mechanism residual (logged 07-19: prompt-level attempts exhausted), the R7
spoken-CTA experiment shipped ~10h ago is `running` and must settle alone for clean attribution,
and two live structural bugs were unambiguously the higher-leverage work (step-4 fallback). Both
code-change slots went there. **Settled:** nothing (R7 07-24 is <72h old; next settle ≈ 07-27).

## Actions taken
- Produced 10 drafts (ids above) → all 10 rendered → approved; ch2 publishing resumed (714 @12:06).
- Code #1: playlist recreate loop removed (`publish_loop`/`youtube` + rewritten tests).
- Code #2: `_auto_produce` in the render loop + `tests/verify_render.py` (10 checks).
- Full suite 444 checks green; manager restarted; dashboard 200; live negative check on
  auto-produce observed (0 promotions at zero headroom).
- Seeding kit `run/seeding/2026-07-24.md` (ch2: Mac-M3 TabNews post + r/brdev SSD/VRAM comment;
  ch1: chunking for r/LocalLLaMA, circuit-breakers dev.to, CrewAI-vs-rewrite LinkedIn; 3
  adjacent-channel comment drafts).
- No weight/topic/trend/settings changes (both boards at capacity; t18 bump still maturing).

## Watch next run (07-25)
1. **Auto-produce's first live day-start cycle:** expect ~5 promotions/channel logged as
   `produce` JobRuns near Pacific midnight, morning renders, and NO `render_starved`. If the
   digest fires anyway, the fix missed — investigate before hand-producing.
2. **Playlist no-recreate live proof** on the next affected-topic publish (any of the 17): no
   "auto-created playlist" JobRun; `topic.playlist_id` stable across the publish.
3. **ch1 traffic freshness:** if cohort totals are STILL byte-identical, it's the quota cap
   starving ch1's analytics refresh — consider whether the newest-first pass needs a per-channel
   floor (would be a code change).
4. **R7-spoken settles ≈07-27**; the R1+R7 cohort matures 07-26→27 — first real
   `subscribers_gained` read before the 08-01 ch1 review.
5. ch2 should publish 5/5 today (4 approved + tomorrow's batch); verify no dry morning.
