# Growth agent 2026-09-23

Preflight: kill switch absent. `scheduler_paused` false. Branch `main` at `94404ca`, up to date with origin. Untracked `.craft-handoff/` (a Sep 22 patch bundle) was left untouched and was not staged. No tracked files were dirty at start.

Code commit: `PENDING_SHA`. Manager restart and `GET /api/dashboard` after that commit are recorded below once observed.

## Needs operator

ch1 will not publish today unless you approve a review. `default_skip_gate` is false, so a clean render lands in `review` with `craft_review=pending` and the publish loop will not pick it up. The only row past 48h was ch2 v1274, which was approved this run. These two ch1 renders are clean and 22h old (under the stuck threshold), so they were left:

- **1296** `Chat marked no emails. Prod sent them. · Agent memory 11` — title matches the subject, script opens on that sentence, `craft_review=pending`
- **1312** `It forgot the brief you already gave it. · Agent memory 10` — same

OAuth is fine on both channels (`connected`). This is not a reconnect.

## Monetization

Lower tier. Neither channel has `tier_achieved`. Binding constraint on both is **subscribers**. No metric hit 100% for the first time. ch1 channel subs **15 → 16**. That +1 is not on any video's `subscribers_gained` (shorts still 5, longs still 4, nine video-attributed converters). Do not hang the +1 on a title.

| metric | ch1 current | ch1 needed | ch1 % | ch2 current | ch2 needed | ch2 % |
|---|---|---|---|---|---|---|
| subscribers | **16** | 484 | **3.2%** | **70** | 430 | **14.0%** |
| watch hours | 11.2 | 2988.8 | 0.4% | 23.2 | 2976.8 | 0.8% |
| Shorts views | 5922 | 2,994,078 | 0.2% | 5791 | 2,994,209 | 0.2% |

Shorts views fell versus yesterday's report (ch1 6323 → 5922, ch2 5903 → 5791). Watch hours ticked down (11.4 → 11.2, 23.3 → 23.2). Prefer the analytics over milestone math: do not revive t15.

ch1 subs-per-video, lifetime, unchanged: t5 long **3**, t15 **2** on 86 measured, t6 **2**, t18 **1**, t1 **1**. t26 Agent memory still **0** on 8 measured (64.8% avp). t7 CrewAI **0** on 13 (66.9% avp).

## Triage

Digest at start: **not clean** (`summary.clean: false`, `total_issues: 22`, `needs_operator: 0`). `oauth` / `quota` / `cooldown` / `bgm_pool_low` / `stuck_publishing` / `stuck_rendering` / `pipeline_starved` / `craft_gate_blocked` empty. `error_runs_24h`: 13× `grok.Timeout` after 600s, 2× orphaned-render requeue, 1× the same timeout as a terminal error. Some renders still succeeded (`rendered_today=5` on each channel), so this is a flake, not a dead CLI. Not treated as an OIDC outage.

| Issue | Did | Proof (read-back) |
|---|---|---|
| failed **1297** (t26, age 22.1h, retry 2/2, no file, grok timeout, `suggested_action: requeue`) | `POST /requeue` | GET 1297 `status=queued` `retry_count=2` `error` empty. Next timeout is terminal (`retry_count < 2` is already false). |
| rejected **1264** (age 179.6h, displaced, not a HOLD, `suggested_action: delete`) | `DELETE` | GET 1264 **404** `video not found` |
| rejected **1257** (HOLD, eighty-four vs `$84`, age 188.3h) | leave | GET 1257 still `rejected` |
| rejected **1260** (HOLD, Gate B 5.70s, age 227.6h) | leave | GET 1260 still `rejected` |
| CoS STOP rows 1275, 1285–1293, 1295, 1304, 1305, 1309 (`suggested_action: leave`, <7d) | leave | not deleted |
| stuck_review **1274** (ch2 t4, age 59.5h, llama.cpp, PT title matches subject) | `POST /approve` after a local `publish_craft_block_reason` returned None | GET 1274 `status=approved` `craft_review=pass` `approved_at=2026-09-23T12:06:06` |
| title-pattern **1306, 1307, 1308, 1310, 1311** (Shipping, CoT or unsuffixed titles, `craft_review=fail`, `auto: false` but suggested reject) | `POST /reject` | GET 1306 and 1308 `status=rejected` `craft_review=fail`. 1307, 1310, 1311 returned `rejected` on the POST. |
| drafts **1321, 1325** (subject was catalog-planning CoT glued to a real claim) | `PATCH` subject to the claim only | GET 1321 `O agente me cobrou $47 no terminal. · Claude Code 10`. GET 1325 `Bateria trava a GPU em 30W, não é engenharia. · IA 199`. Both still `draft`. |
| t15 / t3 / t5 | no weight change | t15 `weight=0` `active=false` (operator stop). t5 inactive. t3 weight 0. |

Approved queue before this run was empty on both channels, which is why `published_today=0` at noon. v1274 is the only new approved row. ch2's window is 11:00–22:00 America/Sao_Paulo, so the upload itself is later today. Not waited on.

BGM pool: count **30**, min 5, target 15, `is_low: false`. No tracks generated.

## Learned

Board: both channels `at_capacity: true`, `days_of_inventory: 2.0`, 10 drafts, 0 queued. `rendered_today=5` equals `daily_render_budget`, so nothing was produced.

**Winners (mature):** ch2 t28 28.4 avg views / 60.4% avp / 0 subs; t4 23.6 avg / 64.4% avp / **6 subs**; t27 19.1 avg / 53.9% avp / 0 subs. ch1 t15 20.7 avg / 51.2% avp / **2 subs on 86 measured** (was 25.7 avg yesterday — the cohort got quieter, it did not start converting). t26 18.9 avg / 64.8% avp / 0 subs. t6 16.5 avg / **2 subs**. t5 long 4.1 avg / **3 subs**, still parked.

**Losers / stopped:** the Copilot bill formula. Operator reject reason on the 09-22 batch: "CoS STOP: Copilot Credits / bills angle killed by Rodrigo 22 Sep — formula nonsense." Recent bill uploads are not being tested. v1272 (`U5GvoXiCxFM`, age 29h), v1273 (`fpKl8I2PTK4`, age 22.4h), v1270 (age 53h), v1268 (age 77h) are all **0 views**. v1247 (`_XGUAQrwE6s`), yesterday's discovery leader at 305 views / SHORTS=276 / 0 subs, today reads **views=0, has_data=true, traffic={}**, captured 2026-09-23T02:09:51. v1251 similarly 2 → 0. v1208 is still 101 views / YT_SEARCH=88, so this is not a global wipe. Cause not investigated past the API row.

**Traffic:** no BROWSE and no SUGGESTED on the rows inspected. EXT_URL still frozen (v948=9, v1245=1, v1230=1). v1253 (age 262h) still SHORTS=77 / YT_SEARCH=6 / **0 subs**. v1244 and v1210 still the ch1 converters (1 sub each) and they are not the new bill series.

**Language:** new ch2 titles checked are PT-BR, including the review batch 1274–1283 and the cleaned drafts. No EN leak found this run.

**Hypotheses carried in:** (a) midnight 09-23 rendered without BlockingIOError — **held**, the 24h errors are grok timeouts, not EAGAIN. (b) ch1 07:00 would publish the queued short — **missed**. Nothing was approved at 07:00; v1273 and v1294 had already published earlier (age ~22h) and the new renders sat in review. (c) ch2 v1274 would publish ~00:18 UTC — **missed**. It has been in review since 2026-09-21T00:30. Approved this run instead. (d) v1247 stays ~305 and 0 subs — **views did not stay**. Now 0 in the API. (e) v1251 stays a miss — still 0 subs, views now 0.

**Hypotheses next run:** (a) v1274 publishes inside the ch2 window and the YouTube title stays the llama.cpp sentence, not CoT. (b) ch1 still publishes 0 unless 1296 or 1312 was approved. (c) The next autofill idea for t4 or t28 does not start with "Vou conferir o catálogo" (this run's strip; the running app only has it after the restart below). (d) v1297 either renders or fails once and stays `failed` (retry_count already 2). (e) v1247 does not silently return to 305; if it stays 0, say so rather than quoting last week's SHORTS=276.

`queue-plan-mix` (08-24, `6005571`): still no approved long, queues empty at noon. Leave `running`.

`blockingio-transient` (09-05, `48bdaa9`): no BlockingIOError in the 24h digest. Retry path still not live-exercised. Leave `running`.

`R1 hook-fill` (09-20, `99b7e3f`): **72h+**, but the settle render did not finish. Production hooks on videos that do store beats are still short (v1272 hook 2.825s, v1273 3.46s, v1294 4.775s) against the pre-change 13.9s on v1267. That is consistent with the change and not a cohort verdict. `avg_view_pct` on that cohort is still too young or zero. Leave `running`.

`R1` CoT strips from 09-21 (`5f6b8b0`) and 09-22 (`9853db8`): under 72h. Leave `running`. The 09-23 leak is a different surface (idea subjects, not the script preamble).

## Act

No weight changes. t15 stays off. No idea generation and no trend adoption: both boards are at the horizon, and a new weight-1 topic cannot displace t26/t7/t45/t46 drafts on ch1 or t4/t27/t28 drafts on ch2.

Trends logged (no adopt):

| id | score | status | term |
|---|---|---|---|
| 116 | 70 | watching | Grok 4.7 in Copilot — reason rewritten: do not feed the killed Credits wedge |
| 117 | 70 | watching | Ollama v0.34.3 stable (2026-09-19) |
| 118 | 48 | rejected | Ollama v0.34.4-rc0 |
| 119 | 78 | watching | AgentLSD task contamination — already t46 draft 1319 |
| 120 | 66 | watching | Copilot runtime Rust port — not a bill, and Shipping is not an allowed series label |
| 121 | 74 | watching | eval sandbox answer-key — same draft 1319 |

Unnamed llama.cpp nightlies (b11124 and friends, several per hour) were not logged.

Long-form stays parked (t5 inactive, no long drafts). That contradicts the July "1 long per day" line. The later operator stop and the empty long bench win. Not unparked.

## Engagement

Rubric baseline **did not score**. `run/rubric_review.py --label baseline` was started in the foreground; ch1-code died with `GrokCLIError: grok.Timeout: grok -p timed out after 600s` after the header printed, and ch1-concept had started the same call when the process was killed at ~12 minutes so it would not sit on a 4×600s timeout beside the live render queue. No frames were read. No lever is scored 2/1/0 this run. Do not treat any previous day's scores as today's.

The fail-open bug in front of that gate: autofill saved planning text as the subject.

- 1321 was `Vou conferir o catálogo para não repetir título nem o número da série.O agente me cobrou $47 no terminal. · Claude Code 10`
- 1325 was `Vou conferir o catálogo da série para o próximo número e um ângulo que ainda não foi usado.Bateria trava a GPU em 30W, não é engenharia. · IA 199`

Both already carry an allowed series suffix (`Claude Code`, `IA`), so `_lock_patterned_opener` would speak the planning sentence. Shipping failed the other way: `SERIES_LABELS` does not include `Shipping` or `Agent traps`, the opener lock never fired, and five renders (1306–1311) shipped CoT titles (`The hook has to…`, `The first line has to…`). Those were rejected, not retitled. **Allowlist is the next code change.** It was not this run's change. Golden subjects are unpatterned, so that allowlist would not have moved a rubric score anyway.

**Shipped (not a render-gate ship):** `video_gen._strip_idea_cot`, applied inside `generate_ideas`. Leading catalog-planning sentences are removed; a line that is only planning is dropped; a clean title is unchanged.

Proof, not a frame:

- `PYTHONPATH=. .venv/bin/python tests/verify_video_gen.py` → **103/103**.
- Isolated call on the exact 1321/1325 strings returned the two claims above, the pure-CoT line returned `None`, and the Agent traps draft subject was unchanged.
- Live rows after PATCH, quoted under Triage.
- `uv run python -c "import app.main"` printed `import-ok`.

Render gate: **not run** after the edit. A golden render cannot show an idea-parser change, and grok `-p` was already timing out. Not claimed as an R1 frame-score increase.

## Watch next run

1. Add `Shipping` and `Agent traps` to `craft.SERIES_LABELS` / `SPOKEN_TITLE_RE`, with a `spoken_title_ok` check on `· Shipping 2` and `· Agent traps 1`. Until then every clean Shipping / Agent traps render fails the title gate the way 1306 did.
2. After today's restart, the next t4/t28 autofill subject must not start with `Vou conferir o catálogo`.
3. v1274 should publish; quote the YouTube id. v1297 should not loop forever.
4. v1247 views. Quote the API number. Do not reuse 305.
5. grok `-p` 600s timeouts (13 in 24h). If the next rubric or the next long compose dies the same way, that is the pipeline bug, not another hook tweak.
6. Settle hook-fill only when a rubric render actually finishes.

## Seeding

`run/seeding/2026-09-23.md`. ch2 features v1253 (`rWdbIYunDB0`, 70B on CPU, SHORTS=77). ch1 features v1294 (`9UtC9SSHOZo`, Agent memory), not a bill short, and not r/LocalLLaMA. Prior EXT_URL counts did not move.
