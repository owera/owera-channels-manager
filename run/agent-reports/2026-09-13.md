# Growth agent — 2026-09-13

## ⚠ Needs operator

1. **PAUSE ch1 (hard review overdue since 08-01).** Subs **14** (< 15; unchanged vs 09-12). Video-attributed converters remain **eight**, all mature. t15 still **2 mature converts** on 77 measured (v1210 formatter-on-save + v1244 unbounded test loop). The 08-24→14 climb is attributed, but the bar is still missed. Recommend **PAUSE ch1** (or explicitly override with a new bar). Agent did **not** pause the channel or unpark t5.
2. **Scheduler is unpaused** (`GET /api/settings` → `scheduler_paused: false`). This run acted (token-json publish fix + retry + produce + trends).
3. **ch2 metadata backfill** (`db7a254` plan) still awaits `--apply` if still desired.
4. **BGM pool path** remains external (`MoneyPrinterTurbo/resource/songs`). Count 30 (target 15).

---

## Observed

- **Now:** 2026-09-13T12:00 UTC (Sunday). Kill switch off, `scheduler_paused: false`, tree clean at start, `main`. Last full growth run: **2026-09-12**. Channel budgets unchanged (`daily_render_budget=1` / `daily_publish_budget=1`).
- **Cadence:** both channels still budget=1. Directive-2 mix (4 shorts + 1 long) is structurally impossible. Longs stay parked (ch1 t5 `active=false` `weight=0`; ch2 t2 `weight=0`).
- **ch1:** published_today **0** at observe — **09-12 hypothesis (b) MISSED** (v1251 did not finish `publishing`→`published` yesterday). Cause: ch1 `token.json` had a valid JSON object plus **173 trailing bytes**; `json.loads` raised `JSONDecodeError: Extra data`. That exception escaped `_publish_one` **after** the row was already committed `publishing`, so `tick()` rollback did not undo it. Recovery re-queued **5×900s** then marked failed. Same fate overnight for **v1252**. `oauth_status=connected`. `paused=false`. Analytics for ch1 was also skipped (`analytics unavailable … Extra data`). After this run: token healed, **v1251 PUBLISHED** `oUXDtGWtQM4` at 2026-09-13T12:17:37Z. published lifetime **409**. Title kept `Your regex rewrite billed $61. · Copilot Credits 76`.
- **ch2:** published_today **0** at observe (window 14:00). **v1253 t4** published 2026-09-12T14:01:00 (`rWdbIYunDB0`) — **09-12 hypothesis (a) held**. Overnight **v1254 t4** rendered (`used_fallback=false`, beat_count=10, duration 41.14s, title `Você pagou o Colab Pro e acha que é engenharia. · IA 177`) and skip-gate auto-approved (craft-gate B=7.5 holding — **09-12 hypothesis (c) held on ch2**). publish-plan **2026-09-13T14:00:00Z**. published lifetime **384**. `oauth_status=connected`. Language: newest titles PT-BR ✓. No EN leak.
- **ch2 subscribers: 69** (unchanged). Video-attributed converters still **nine** (v542/524/810/421/298/684/315/755/1022). The unattributed 68→69 still has no new `subscribers_gained=1` row.
- **ch1 subscribers: 14** (unchanged). Subs-per-video: **8 lifetime +1s**, all mature. t15 still **2 mature converts on 77 measured**.
- **BGM pool:** 30 (min 5 / target 15; `is_low: false`). No `music_gen` errors. No generate this run.
- **Boards after triage:**
  - ch1 draft `{long:0, short:1}` · queued `{long:0, short:1}` · approved `{long:0, short:1}` — queued **v1255** t15 (`render budget full (1/1)`, eta 2026-09-14T00:00); approved **v1252** @ 2026-09-14T07:00 (1251 published this run)
  - ch2 draft `{long:0, short:1}` · queued `{long:0, short:1}` · approved `{long:0, short:1}` — queued **v1256**; approved **v1254** @14:00
  - both `at_capacity: true` (horizon 2 at budget 1). `board_overflow` []. `pipeline_starved` [].
- **traffic_json (via leaderboard API, no DB dive):** v542 SHORTS=389; v948 SHORTS=64 + **EXT_URL=9** (frozen) + NOTIFICATION=3; **v1190 t15** (age 363h, 74 views, avp 106.6): **YT_SEARCH=59 / SHORTS=14**, search_terms `copilot:5` — still 0 subs. **v1210 t15** (age 315h, 22 views, avp 37.4): **SHORTS=16 / YT_SEARCH=6**, **subscribers_gained=1**. **v1208 t15** (age 316h, 100 views, avp 326.6): **YT_SEARCH=87 / SHORTS=10**, search_terms `github copilot:3`, `ez commit:2` — 0 subs. **v1244 t15** (age 205.8h, 51 views, avp 21.6): **SHORTS=49 / YT_SEARCH=2**, **subscribers_gained=1**. **v1245 t15** (age 197.2h, 46 views, avp 43.0): **SHORTS=29 / YT_SEARCH=16 / EXT_URL=1**, search_terms `copilot:1`, **0 subs**. **v1229 t4** (age 166h, 74 views, avp 36.6): **SHORTS=72**, 0 subs. **v1230 t4** (age 142h, 113 views, avp 31.4): **SHORTS=100 / YT_SEARCH=8 / SUBSCRIBER=1**, **0 subs**. **v1238 t15** (age 125h, 64 views, avp 161): **SHORTS=61**, 0 subs. **v1243 t4** (age 94h, 65 views, avp 47.1): **YT_SEARCH=36 / SHORTS=28**, search_terms `ollama:1` — 0 subs, new search pull. **No BROWSE / SUGGESTED keys** on the top samples. No invented CTR. v1250/v1247/v1248/v1253 still 0 views (under 72h / analytics lag).

### Publish log

| day | ch1 | ch2 |
|-----|-----|-----|
| 08-31 | **5 (0L+5S)** 5×t15 — long 1213 TTS-failed | **5 (1L+4S)** long **1212** first + **4×t4** |
| 09-01 | **5 (0L+5S)** 4×t15 + t18 — long 1213 fallback→review | **5 (0L+5S)** 4×t4 + t27 |
| 09-02 | **5 (0L+5S)** 4×t15 + t6 — v1213 good render→review again | **1 (1L+0S)** long **1223** first @14:01; budgets cut 14:28 |
| 09-03 | **0** (BlockingIOError burst) | **0** |
| 09-04 | **1** v1244 t15 @22:29 (after unpause) | **1** v1217 t4 @20:19 (after unpause) |
| 09-05 | **1** v1245 t15 @07:00 | **1** v1228 t4 @14:00 (`pgjYTrdywgc`) |
| 09-06 | **1** v1213 t5 **long** @07:01 (`52yYnOpbeA0`) | **1** v1229 t4 @14:00 (`Cz4rg_P7FPQ`) |
| 09-07 | **1** v1227 t15 @07:00 (`NkrI4EpbXTQ`) | **1** v1230 t4 @14:00 (`v0A8KU9CBIM`) |
| 09-08 | **1** v1238 t15 @07:00 (`iOK7ILg6C1k`) | **1** v1242 t4 @14:00 (`lt2rk_0Bb6c`) |
| 09-09 | **1** v1239 t15 @07:00 (`2btb_bSKaYM`) | **1** v1243 t4 @14:00 (`zriBDmenVZk`) |
| 09-10 | **1** v1247 t15 @07:00 (`_XGUAQrwE6s`) | **1** v1246 t4 @14:00 (`ggJ0JBzfvGM`) |
| 09-11 | **1** v1250 t15 @07:00 (`rqcoMPQbonU`) | **1** v1248 t4 @14:00 (`jV5IzNMTOiA`) |
| 09-12 | **0** (v1251 stuck-publishing 5×900s → failed) | **1** v1253 t4 @14:01 (`rWdbIYunDB0`) |
| 09-13 | **1** v1251 t15 @12:17 (`oUXDtGWtQM4`) — recovered this run | **0** at observe; plan **v1254 t4 @14:00** |

## Monetization

| metric | ch1 current | ch1 needed | ch1 % | ch2 current | ch2 needed | ch2 % |
|---|---|---|---|---|---|---|
| subscribers | 14 | 486 | 2.8% | 69 | 431 | 13.8% |
| watch hours | 10.9 | 2989.1 | 0.4% | 21.6 | 2978.4 | 0.7% |
| Shorts views | 5881 | 2,994,119 | 0.2% | 5541 | 2,994,459 | 0.2% |

**Binding constraint both channels: subscribers** (lower tier). No milestone crossed (no metric hit 100% for the first time). ch1 shorts_views **5881** (flat vs 09-12); ch2 5483→**5541** (+58). ch1 watch hours 10.9 (flat); ch2 20.2→**21.6** (+1.4). Subs flat 14 / 69. Prefer analytics (keep t4 as ch2 converter lead; ch1 t15 still **2 mature** attributed — do not unfreeze parked topics) over pure milestone math.

## Triage

Digest at start: **not clean** (`summary.clean: false`, `total_issues: 13`, `needs_operator: 2`). `oauth` / `quota` / `cooldown` / `bgm_pool_low` / `stuck_publishing` / `stuck_rendering` / `pipeline_starved` / `craft_gate_blocked` / `title_pattern_blocked` empty. **Two failed rows (auto:false):** v1251 + v1252, `suggested_action: retry`, error `upload repeatedly stalled: stuck 'publishing' past 900s on 5 attempts`. **Scheduler unpaused — remediations taken.** After: `failed: []`, `needs_operator: 0`, rejected **1249** only (`leave`, <7d).

| Issue | Did | Proof (read-back) |
|---|---|---|
| 7 rejected >7d (1231, 1234–1237, 1240, 1241) | `DELETE` | GET 1231 → **HTTP 404**; GET **1249 still `rejected`**. |
| failed 1251/1252 (auto:false, retry cap exhausted) | root-cause fix then `POST /retry` | Isolated: `_loads_token_info` ignores trailing bytes; `_publish_one` JSONDecodeError → APPROVED (not PUBLISHING); retry_count 5→0. Live: GET 1251 after retry → `status=approved` `retry_count=0`; then **`status=published` `yt_video_id=oUXDtGWtQM4` `published_at=2026-09-13T12:17:37Z`**. GET 1252 → `status=approved` `retry_count=0`. Dashboard ch1 `published_today=1` `published=409` `quota_spent_today=1998`. |
| ch1/ch2 queued=0 < budget=1 | `POST /produce` v1255 + v1256 | GET 1255 → `status=queued`, subject `Copilot billed $88 while you slept. · Copilot Credits 78`. GET 1256 → `status=queued`, subject `Você subiu o vLLM na 4090 e acha que é engenharia. · IA 178`. queue-plan both `render budget full (1/1)`, eta 2026-09-14T00:00. |
| t3 parked / t5 long-anchor | no change | t3 `weight=0`; t5 `weight=0` `active=false` |

Midnight 09-13 render (09-12 hypothesis c): **v1252** last_attempt 2026-09-13T00:13:43, retry_count=0, used_fallback=false, beat_count=11 — **render succeeded**. Skip-gate did **not** get to auto-approve a publish: the token Extra-data stall failed it at upload. **v1254** (ch2) rendered used_fallback=false and **did** auto-approve (B=7.5). BlockingIOError did not recur.

Did **not** re-apply the abandoned 2026-07-02 `http=AuthorizedHttp` timeout (commit `acb30ea` reverted it because it hung all uploads at 0%). httplib2 already has timeout=60 via `build_http()`. The stall was never a socket hang — it was an uncaught JSON parse.

## Learned

1. **09-12 hypothesis (a) held:** v1253 published at 14:01:00 on 09-12 (`rWdbIYunDB0`).
2. **09-12 hypothesis (b) missed:** v1251 did not finish publishing yesterday — Extra-data token JSON, not craft-gate B.
3. **09-12 hypothesis (c) split:** midnight rendered 1252/1254 at retry_count=0, used_fallback=false. ch2 skip-gate auto-approved 1254 (B=7.5). ch1 1252 never reached skip-gate-on-publish; it stalled on get_service.
4. **Winners (mature):** ch2 t28 27.8 avg views @ 59.7% avp / 0 subs; t4 22.8 avg + **5 lifetime subs**; t27 21.0 avg / 54.6 avp / **0 subs**. ch1 t15 23.2 avg / 57.5 avp / **2 subs on 77 measured**; t26 18.8 avg / 64.8 avp / 0 subs; t7 15.8 avg / **66.9 avp**; t6 16.2 avg + **2 lifetime subs**; **t5 long 2 lifetime subs** at low avg views. Conversion still lives on t4 (ch2) and t6/t5/t15 (ch1). **v1208** still the strongest recent search pull (100 / YT_SEARCH=87) — 0 subs. **v1230** still 113 views / SHORTS=100 / 0 subs — volume ≠ conversion. **v1243** 65 views / YT_SEARCH=36 — search is testing t4 “Ollama na RTX”.
5. **Hypotheses this run:** (a) ch2 14:00 today publishes v1254 t4 (budget=1; now approved). (b) Midnight 09-14: 1255 (ch1) and 1256 (ch2) render success without BlockingIOError **and** skip-gate auto-approves (B=7.5). Quote v1255 title — must keep `· Copilot Credits 78`. (c) ch1 09-14 07:00 publishes v1252; title keeps `· Copilot Credits 77`.

`queue-plan-mix` (shipped 08-24, `6005571`): 20d+. Queues non-empty but all shorts; `rendered_today=1` so every card is `render budget full`. Predicted case (long queued behind lower-id shorts, `approved_longs==0`) still never appeared. Longs remain parked. Leave `running`.

`blockingio-transient` (shipped 09-05, `48bdaa9`): 8d / 72h+. Retry path still not live-exercised (midnight success at retry_count=0). Leave `running`.

`R1` 12w hook lock (shipped 09-11, `407198b`): 2d, <72h. Leave `running`.

`craft-gate-B` (shipped 09-12, `38e06a5`): 1d, <72h. ch2 v1254 auto-approved (B=7.5). Leave `running` for a second overnight.

## Acted (channels)

- **Deletes:** 1231, 1234–1237, 1240, 1241 (rejected >7d). 7/10 cap.
- **Retry:** 1251 + 1252 (after token-json fix + manager restart). 1251 published; 1252 approved for 09-14 07:00.
- **Produce:** 1255 (ch1 t15, patterned subject) + 1256 (ch2 t4, patterned subject). queued=0 < budget=1 both channels.
- **No generate / displace / weight / adopt.** Both boards at_capacity; remaining drafts are t15 w5 (ch1 1257) and t4 w5 (ch2 1258) — cannot displace equal-or-higher.
- **Trends persisted (not adopted):**
  - **#94** refresh: Ollama **0.34.0 still latest** (GitHub 09-09; TechSpot 09-10). Score 90 hold, watching, ch2. GET `/api/trends/94` `status=watching` `score=90.0` `updated_at=2026-09-13T12:18:21`. Sibling of parked t40; do not unpark / do not adopt (t4 w5 drafts).
  - **#92 Copilot Business promo** refresh: score **86** hold, watching, **serves t15 wedge**. Oct-1 seat-charge still upcoming (~18d; changelog 09-03). GET `/api/trends/92` `updated_at=2026-09-13T12:18:21`.
  - **#98 Copilot agentic autofix** refresh: score **82** hold, watching, **serves t15 wedge**. GET `/api/trends/98` `updated_at=2026-09-13T12:18:21`.
  - **#97 vLLM 0.29.0** refresh: still latest stable (PyPI 09-09); **v0.29.1rc0** appeared 09-12 (RC, not stable). Metal plugin v0.29.0. Score 80, watching. GET `/api/trends/97` `updated_at=2026-09-13T12:18:21`. Sibling of parked t37; do not adopt.
  - **#86 Ornith 1.5** refresh: no new official spike. Score 80, watching. GET `/api/trends/86` `updated_at=2026-09-13T12:18:21`.
  - **#99** refresh: OpenCode + Ollama + sbx on Mac. Score 68 (<75 adopt bar), watching, ch2. GET `/api/trends/99` `updated_at=2026-09-13T12:18:21`. How-to, not a release. Do not adopt.
- **Seeding kit:** `run/seeding/2026-09-13.md` (operator posts; agent does not). Features v1244/v1210 (mature t15 converts) + v1208 (search pull) + ch2 v1243 (65 views / YT_SEARCH=36) / v1230 (113 views).

## Engagement

**Baseline `baseline --only ch1-code`** — full-set skipped this run (publish outage outranked; last run’s `ch1-concept` also did not finish). **ch1-code completed today:** `visible=True`, `used_fallback=false`, 98w / 36.62s.

| Lever | score | evidence |
|---|---|---|
| R1 hook (P1) | **0** (golden-set flake) | frame0 + title + first spoken are grok CoT: “I'll check the workspace for series naming so the endcard matches the channel”. Object widget **RAG** still on frame0. **Production is fine:** v1251 title = patterned subject. Do not treat this flake as a title-lock regression. |
| R8 thumb/title (P2) | 1 | Title lock did its job (title = sentence 1) — the sentence was garbage. |
| R2 info gain (P3) | 2 | Mix hook/stat/cmp/term/code/cmp/stat/quote/cta. Code beat: `CrossEncoder("ms-marco")` / `ce.predict` / `argsort(scores)[:5]` / `keep = hits[idx]` indented. CMP stacked + VS (Vector search vs Rerank). |
| R6 payoff (P4) | 2 | Quote card “Don't retrieve more — rank what you already found”. |
| R5 retention (P5) | 2 | Open loop (neighbors ≠ answers → five-line rerank). |
| R3 clarity (P6) | 2 | Code readable, indented, nothing clipped. CMP full-width stacked. |
| R4 pacing (P7) | 2 (not re-timed this render; no mid-body 8s+ reported) | CTA present; visible=True. |
| R7 CTA (P8) | 2 | Chip “Subscribe · Copilot Credits” / micro “same series”. Zero Follow. |

**Target:** not a prompt lever. Pipeline-robustness outranked: ch1 published 0 yesterday and had 0 approved at observe because get_service crashed on trailing-byte token JSON. Playbook fallback: “if a recurring bug is clearly the higher-leverage work this run, fix that instead.”

**Change:** `youtube._loads_token_info` takes the first JSON object and ignores trailing bytes (heals on a valid load). `get_service` maps unreadable tokens to `NeedsConnect`. `_publish_one` catches non-`NeedsConnect` get_service crashes and reverts to **approved** (does not leave PUBLISHING). `POST /retry` resets `retry_count` to 0.

**Gate (isolated + live):**

| | before | after |
|---|---|---|
| `_loads_token_info` clean object + 173 trailing bytes | `json.loads` Extra data | first object, `trailing=True`; `_load_creds` returns valid creds and rewrites clean JSON |
| `_publish_one` + JSONDecodeError from get_service | PUBLISHING committed, tick rollback, 900s recovery | **APPROVED**, JobRun `get_service failed` |
| POST /retry with retry_count=5 | stays 5 → next stall gives up immediately | **retry_count=0** |
| live v1251 | failed, 5×900s | **published** `oUXDtGWtQM4` 12:17Z |

verify_youtube **212/212**, verify_publish **237/237**, verify_lifecycle_audit **61/61**. `uv run python -c "import app.main"` ok. Manager restarted, `GET /api/dashboard` 200. ch1 `fetch_identity` → `UCSH53DmossqLTUZdh2PE1Hg` / Owera Software (bounded 45s).

**Shipped.** Did **not** retune HTTP timeout via `http=AuthorizedHttp` (reverted 2026-07-02, `acb30ea`). Did **not** ship a grok-preamble strip (would be an ungated R1 prompt change on a one-off flake; production titles are clean).

**Settled:**
- `R1` title lock (09-10, `fae2dc7`) → **promoted** (production titles keep `· Copilot Credits nn`; golden-set CoT leak is a script-prefix residual).
- `publish-token-json` (this run) → **promoted** same-day (v1251 published).

## Watch next run

1. **PAUSE ch1 yes/no.** 14 still does not clear 15. Quote whether the operator overrode. Keep t3 at 0.
2. Did v1254 publish at 14:00? Quote `yt_video_id` / `published_today`.
3. Midnight 09-14: quote render JobRuns / last_attempt on **1255** (ch1) and **1256** (ch2). Expect success at retry_count=0 **and** skip-gate auto-approve (B=7.5). Quote v1255 **title** — must keep `· Copilot Credits 78`. Quote v1252 publish at 07:00 — must keep `· Copilot Credits 77`.
4. 1249 is <7d (`suggested_action: leave`). Resume deleting when it ages past 7d.
5. `queue-plan-mix` still wants a noon where a long is queued **behind lower-id shorts** AND `approved_longs==0` AND render budget is not already spent. Unlikely while t5/t2 stay parked.
6. BGM count stays ≥ min (30, above target 15).
7. Quote whether v948 EXT_URL grows past 9. Quote v1022. Quote **v1190** views / YT_SEARCH (74 / 59 today). Quote **v1210** (22 views / 1 sub). Quote **v1208** views / YT_SEARCH (100 / 87 today). Quote **v1244** (51 views / 1 sub / 205.8h). Quote **v1245** (46 views / 0 subs / YT_SEARCH=16 / EXT_URL=1). Quote **v1229** (74 views / SHORTS=72 / 0 subs). Quote **v1230** (113 views / SHORTS=100 / 0 subs). Quote **v1238** (64 views / avp 161 / SHORTS=61 / 0 subs). Quote **v1243** (65 views / YT_SEARCH=36). Quote whether the unattributed ch2 +1 (69) ever attaches to a video. Quote **v1251** once it has age_hours ≥ 72.
8. Next-run candidate (not shipped today): strip grok CoT preamble from `_generate_script` so sentence 1 cannot be “I'll check the workspace…”. Do not ship another R3 change while the 09-11 R1 12w lock is still running.
9. Do not unpark t33–t42. Do not unpark t5/t2 unless the operator restores budget>1 and wants the 1L mix back. Do not unpark t40 for Ollama 0.34 stable (#94, score 90). Do not adopt #96 Unsloth Desktop (score 70), #97 vLLM 0.29 (score 80, t37 parked; 0.29.1 is RC), #98 Copilot agentic autofix (score 82, t15 w5 drafts — serve as a t15 episode), or #99 OpenCode+Ollama+sbx (score 68).
10. Do not drop `MID_BEAT_MAX_S` back to 3.0 without also dropping `_MID_MAX` and a gated R4 after-render.
11. Do not re-apply `http=AuthorizedHttp` to `build()`.

## Commit

`3468d0d` — token-json publish fix + triage + report + seeding.
