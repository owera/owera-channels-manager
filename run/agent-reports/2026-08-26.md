# Growth agent — 2026-08-26

## ⚠ Needs operator

1. **PAUSE ch1 (hard review overdue since 08-01).** Still **13 subscribers** vs the 15-sub bar (12→13 since 08-25). Video-attributed converters remain **five** (v558 MCP, v367 t6, v43 t1, v96 t5, v1032 t6) — the 12→13 move is **unattributed** on the leaderboard (no new `subscribers_gained` row). 13 still does not clear 15. **Recommendation stands: PAUSE ch1** (or explicitly override with a new bar). Agent continues **zero ch1 strategy moves** (no weight/trend adopt/generate for growth). Pipeline hygiene only.
2. **ch2 metadata backfill** (`db7a254` plan) still awaits `--apply` if still desired.
3. **BGM pool path** remains external (`MoneyPrinterTurbo/resource/songs`). Count 25 (target 15); wipe risk still escalates to silent renders.

---

## Observed

- **Now:** 2026-08-26 ~12:00 UTC. Kill switch off, `scheduler_paused: false`, tree clean at start, `main`. Last full growth run: 2026-08-25.
- **ch1:** published **5/5** today, quota **8750/9000** (`daily_limit_hit: false`). Mix = **1 long + 4 shorts** (v1079 t5 long first @ 07:01 `Kt8x2-Ww15c`, then v1070 t18, v1071 t18, v1013 t7, v1014 t7). **Twenty-first consecutive 1L+4S day** (08-06…08-26). Leftover t7 1013/1014 finally drained.
- **ch2:** published_today **0** at observe (window opens **14:00 UTC**). rendered_today **5**. **08-25 did publish 1L+4S** (v1055 t2 long first @ 14:01 `dmxdDuYssSQ`, then v1064/1065/1066/1067 **all t4**) — yesterday’s 1L + 4×t4 prediction held.
- **ch2 subscribers: 64** (unchanged). Lifetime converters still **seven** (v542/524/810/421/684/315 + v1022). v1022 still 3 views / 65.1% avp / YT_SEARCH=3 / +1.
- **ch1 subscribers: 13.** Subs-per-video on the mature leaderboard: **still 5 lifetime +1s**. None on the post-07-13 Copilot-credits wedge (t15 still 0 subs on 46 videos). Do not invent which video converted 12→13.
- **BGM pool:** 25 (min 5 / target 15; `is_low: false`). No `music_gen` errors. `error_runs_24h`: one analytics `HttpError 500` on youtubeanalytics (backendError, video z0L4VS6FGUA) — transient YouTube, not a code bug. Last BGM generate 08-24; not stale. No refresh this run.
- **Boards (API `by_format`):**
  - ch1 draft `{long:2, short:3}` · queued `{long:0, short:5}` · approved `{long:1, short:1}` (1068 t5 long + 1053 t7 leftover)
  - ch2 draft `{long:1, short:4}` · queued `{long:0, short:5}` · approved `{long:1, short:11}` (v1073 t2 long leading)
  - Both `at_capacity: true`, `days_of_inventory: 2.0`.
- **Language (ch2):** 08-25 titles all PT-BR ✓ (1055 CI/CD ML; 1064 1 pergunta; 1065 RTX 30%; 1066 HF maior; 1067 KV cache). Leading approved long v1073 also PT-BR (“Batch Inference vs Tempo Real…”). Queued 1074–1078 t4 all PT-BR. No EN leak.
- **traffic_json (via leaderboard API, no DB dive):** v542 SHORTS=385; v785 SHORTS=164 + YT_SEARCH=6; v558 SHORTS=104 + YT_SEARCH=12; v948 SHORTS=64 + **EXT_URL=9** (unchanged vs 08-17…08-25) + NOTIFICATION=3; v884 YT_SEARCH=22 + SHORTS=77; v1022 YT_SEARCH=3; v1032 SHORTS=22 + YT_SEARCH=1. SHORTS / YT_SEARCH dominate. **No BROWSE / SUGGESTED keys** on the top samples. No invented CTR.

### Publish log (settle evidence)

| day | ch1 | ch2 |
|-----|-----|-----|
| 08-18 | 5 (1L+4S) long 962 first + 3×t18 + t6 | **5 (1L+4S)** long 957 first + 4×t4 |
| 08-19 | **5 (1L+4S)** long 959 first + t18 + 3×t6 | **5 (1L+4S)** long 956 first + 3×t4 + t27 |
| 08-20 | **5 (1L+4S)** long 961 first + t18 + 3×t6 | **5 (1L+4S)** long 958 first + **4×t4** (v1022 converted) |
| 08-21 | **5 (1L+4S)** long 960 first + t18 + 3×t6 | **5 (1L+4S)** long 982 first + **4×t4** |
| 08-22 | **5 (1L+4S)** long 963 first + 4×t6 | **5 (1L+4S)** long 1038 first + t4 1028 + 3×t27 |
| 08-23 | **5 (1L+4S)** long 999 first + 3×t6 + t18 | **5 (1L+4S)** long **1052** first + **4×t4** |
| 08-24 | **5 (1L+4S)** long 1005 first + 3×t18 + t6 | **5 (1L+4S)** long **1054** first + **4×t4** |
| 08-25 | **5 (1L+4S)** long 1056 first + 3×t6 + t18 | **5 (1L+4S)** long **1055** first + **4×t4** (1064–1067) |
| 08-26 | **5 (1L+4S)** long 1079 first + 2×t18 + 2×t7 | pending window (0 at observe; plan **1073 t2 long @14:00 then 998/1000/1001/1003 all t27**) |

## Monetization

| metric | ch1 current | ch1 needed | ch1 % | ch2 current | ch2 needed | ch2 % |
|---|---|---|---|---|---|---|
| subscribers | 13 | 487 | 2.6% | 64 | 436 | 12.8% |
| watch hours | 7.7 | 2992.3 | 0.3% | 12.2 | 2987.8 | 0.4% |
| Shorts views | 4479 | 2,995,521 | 0.1% | 4431 | 2,995,569 | 0.1% |

**Binding constraint both channels: subscribers** (lower tier). No milestone crossed (no metric hit 100% for the first time). ch1 12→13 is unattributed; prefer analytics (keep t4 as ch2 converter lead; ch1 frozen) over pure milestone math.

## Triage

Digest: **not clean** (`summary.clean: false`, `total_issues: 82`, `needs_operator: 1`). `failed=[]`, `pipeline_starved` / stuck / oauth / bgm_pool_low empty. Quota monitor + 80 rejected (flood, age 24.3h, `suggested_action: leave`).

| Issue | Action | Proof |
|---|---|---|
| 80 rejected t33–t42 (age 24.3h) | leave | `suggested_action: leave`; age < 7d. Do not delete. |
| quota ch1 8750/9000 (`auto: false`) | monitor only | full 5-publish day (5×1750=8750); `daily_limit_hit: false` |
| `error_runs_24h` analytics HttpError 500 | none — YouTube backendError, count=1 | not a recurring code signature |
| failed / stuck / oauth / bgm_low / pipeline_starved / board_overflow | none | empty lists |
| t33–t42 parked w=0 | left parked | topics 33–42 still `weight=0` |

Failed list stays empty. The 80 rejected rows are **not deleted**.

## Learned

1. **ch2 08-25 mix prediction held:** 1L+4S, long v1055 first, then **4×t4**. Third consecutive 1L+4×t4-or-mix day with a t2 long leading.
2. **`rebalance-headroom-zero` live 14:00 “do not drain” CONFIRMED** (see Engagement / experiments). 1074–1078 still queued; zero demote JobRuns after 14:00.
3. **Today’s ch2 14:00 plan is 1L + 4×t27**, not 4×t4. The approved short bank is leftover t27 (998/1000/1001/1003, w3). Queued t4 1074–1078 (w4) render overnight and jump ahead tomorrow via weight-desc. Not a bug — quote it as the 08-26 mix.
4. **Winners (mature):** ch2 t4 22.8 avg views + **5 lifetime subs** (the only converter that still moves); t28 25.8 @ 59.5% avp / 0 subs; t27 20.1 avg / 43.7 avp / **0 subs**. ch1 t15 22.2 avg / 61.9 avp / 0 subs on 46 videos; t6 13.5 avg + **2 lifetime subs**; t18 12.2 avg + 1 sub.
5. **Hypotheses this run:** (a) ch2 08-26 window publishes 1L+4S with v1073 leading then 4×t27; (b) overnight 08-27 00:00 demotes 1 queued t4 (headroom=0, approved_longs will be 0 after 14:00, long draft 1164 waiting) and auto_produce queues the long; (c) a later at_capacity adopt still 409s.

`rebalance-headroom-zero` (shipped 08-23, `22056ea`): **PROMOTED**.

`queue-plan-mix` (shipped 08-24, `6005571`): still budget-full at noon (every card `render budget full (5/5)`). Leave `running`.

`adopt-board-cap` (shipped 08-25, `21fab8c`): no new at_capacity adopt attempt this run (agent did not adopt; no flood). Leave `running`.

## Acted (channels)

- **No weight changes.** t4=4 / t27=3 / t2=2. ch1 frozen. t33–t42 stay parked.
- **No produce / no generate / no displace.** Boards at capacity; no w1-or-lower draft to displace for a ≥75 trend. Auto_produce owns the overnight mix.
- **Trends logged (no adopt):**
  - **#73** ch2 score **87** (was 86) — Qwen3.8-27B Ollama **853.6K** (was 732.2K, **+121K/day**). Proof: `POST /api/trends` → id 73, status=adopted, topic 33 parked.
  - **#82** ch2 score **84** — Muse Glimmer **167.6K** (was 161.1K, +6.5K, still stalled vs Qwen). Proof: id 82, topic 35.
  - **#78** ch2 score **83** — Nemotron **134.6K** (was 130.9K, +3.7K). Proof: id 78, topic 34.
  - **#83** ch2 score **82** (was 76) — **Ollama 0.33.0 is now latest stable** (GitHub assets 2026-08-26; was rc3 yesterday). Proof: id 83, topic 40 parked. Do not unpark (no w1 draft).
  - **#81** ch2 score **70** (was 81), momentum fading — 0.32.15 superseded by 0.33 stable. Proof: id 81, topic 36.
  - **#85** ch1 score **74** **rejected** — Copilot Business/Enterprise promo credits end **2026-09-01**. *Is* the wedge; rejected because ch1 is frozen. Proof: id 85, status=rejected, `adopted_topic_id: null`.
- **No BGM generate** — pool 25, refreshed 08-24.
- **Seeding kit:** `run/seeding/2026-08-26.md` (operator posts; agent does not). Features 08-25 t4 1067/1066 + t2 long 1055 + v1032 converter.

## Engagement

**Baseline `baseline`** — 4/4 clean, `visible=True`, none `used_fallback`. ~541s. Scores from frames + scripts + beat timings:

| Lever | score | evidence |
|---|---|---|
| R1 hook (P1) | 2 | Pain-first on frame + opener ≤~12w: “Your RAG returns garbage, and you don't know why.” / “Your AI agent has amnesia, and it's not its fault.” / “Você escreve a mesma integração de novo todo mês.” / “Sua RAG responde qualquer coisa, menos o que importa.” Hook cards at t=0: “Your RAG returns garbage” / “Your AI agent has amnesia” / “A mesma integração toda vez” / “Sua RAG responde tudo, menos o certo”. |
| R8 thumb/title (P2) | 2 | Gaps vs titles (hook “Wrong answers buried on page two” vs title “Fix Your Slow RAG…”; “Goldfish memory, fixable tonight” vs “Why Your AI Agent Forgets…”; PT “Um conector no lugar de mil” / “Você Cortou no Lugar Errado”). Zero title-word echo. |
| R2 info gain (P3) | **1** | **Weakest high-priority lever.** ch1-code has code (`CrossEncoder` + indented `best = top[:4]`) + cmp stacked. ch1-concept has `store.get`/`retrieve`/`llm`. **ch2-code MCP explainer mix hook/term/cmp/diagram/stmt/cmp/lst/cta — ZERO code/command.** ch2-concept chunking also no code. Rule 2b (07-07) is prompt-only and the few-shot example was a chunking board with no snippet, so the model copied it. Fanout diagram on ch2-code still truthful (Servidor MCP → API/Banco/Ferramenta/IA). |
| R6 payoff (P4) | 2 | Quotable closes (“Retrieval finds candidates; reranking finds answers.” / “Your agent doesn't forget. It was never built to remember.” / “Pare de reinventar o encanamento…” / “Chunk ruim não tem embedding que salve.”) |
| R5 retention (P5) | 2 | Open loops early on all 4. |
| R3 clarity (P6) | 2 | Code indented (ch1-code `best = top[:4]`; ch1-concept three-line memory). CMP stacked + VS in flow. Fanout on ch2-code. ch2-concept list midpoint shows all 3 (seções / títulos / parágrafos inteiros). Residual: ch1-code 3.63s list midpoint shows 2/3 rows (step cap is for long lists). |
| R4 pacing (P7) | 2 | Hook at t=0. Min beat 1.693s (ch1-code stmt — floor holding). Max hold **7.217s** (ch2-concept term). No sub-1.7s flashes. No 8s+ drag. Drift still owns the long holds. Do not ship another R4 prompt rule. |
| R7 CTA (P8) | 2 | Follow/Siga + named next value 4/4 (“Cut reranker latency in half” / “Permanent memory setup tomorrow” / “Amanhã: seu 1º servidor MCP” / “Amanhã: o tamanho exato”). Spoken ask present on all 4 scripts. |

**Targeted R2** (weakest high-priority lever; P3, above R6). Prompt-level 2b violated on the two PT subjects — same class as the R7 follow-verb force.

**Shipped (`8188c17`):** when `code`/`command` is allowed, `_code_ok` retries once if the draft has neither; keep the retry only if it adds one. Rule 2b is now a MUST. Few-shot example includes a code beat when Phase-B types are on (the old example was a chunking board *without* code — the smoking gun for ch2-concept). Vacuous when those types are not allowed.

Lifecycle: this change does not set `Video.status`. `compose()` still returns HTML or None; render_loop still consumes QUEUED. No state-machine touch.

| | ch2-code (MCP) | ch2-concept (chunking) |
|---|---|---|
| baseline mix | hook,term,cmp,diagram,stmt,cmp,lst,cta (**no code**) | hook,cmp,stat,term,stmt,stmt,lst,quote,cta (**no code**) |
| after mix | hook,lst,term,diagram,**code**,lst,stmt,cta | hook,cmp,stat,lst,term,stmt,term,**code**,quote,cta |

**Gate (after2 ch2-code + after ch2-concept):**
- ch2-code after2: `mcp.connect("stdio")` / `mcp.call("query_db")` indented; min beat 2.517s; max **7.693s**; CTA “Siga → Servidor MCP do zero”; fanout Servidor MCP → Banco/Planilha/API/Arquivo; `visible=True`, no fallback. (First `after` render had a 10.105s cmp drag — R4 would have dropped; re-rendered once, after2 holds R4.)
- ch2-concept after: `split(doc, by="paragraph", overlap=50)`; min 2.167s; max 6.942s; CTA “Siga → Amanhã: tamanho exato de chunk”; `visible=True`, no fallback.
- R2 1→2 both. R1/R7/R8/R3 unchanged. verify_storyboard **218/218**. `uv run python -c "import app.main"` ok. Manager restarted (`launchctl kickstart`); `GET /api/dashboard` = 200.

### Experiments settled / opened

- Settled: **`rebalance-headroom-zero` PROMOTED** (see experiments.jsonl).
- Left running: `queue-plan-mix` (budget-full at noon, no “next to render”); `adopt-board-cap` (no new at_capacity adopt this run).
- Opened: `R2` code-ok retry commit `8188c17`, status=running.

## Watch next run

1. Operator ch1 pause yes/no (still the binding decision). 13 subs does not clear 15 — do not unfreeze. 12→13 is unattributed; do not treat it as a t6 or t15 revival.
2. ch2 08-26 publish composition after 14:00 UTC — expect **1L+4S** with t2 long **v1073** leading then **4×t27** (998, 1000, 1001, 1003). t4 1074–1078 stay queued (budget full) and should lead 08-27 shorts (w4 > t27 w3).
3. Midnight 08-27 is the live “demote 1 short, queue the long” half of `rebalance-headroom-zero`: after v1073 publishes, `approved_longs=0`, queued 5 t4, long draft **1164**. Expect one demote JobRun at 00:00 and auto_produce of 1164. Quote the JobRun list.
4. `queue-plan-mix` live signal still needs a noon where a long is queued behind lower-id shorts **and** `approved_longs==0` **and** render budget is not already spent.
5. Failed list stays empty. The 80 rejected t33–t42 rows age toward the 7-day delete cap — do not delete before that.
6. Do not unpark t33–t42. Qwen3.8 (#73, 853.6K, +121K/day) is still the only watching-class term rising; serve it as a **t4 episode via displacement** if a w1-or-lower draft exists, not by unparking t33. Ollama 0.33 is now stable (#83) — same displacement rule, do not unpark t40.
7. BGM count stays ≥ min (25, above target 15). Next refresh only if stale again *and* renders landed.
8. Quote whether v948 EXT_URL grows past 9 (seeding signal). Quote whether 12→13 gets a video row. Quote v1032 views/avp.
9. A SPA/agent adopt against an at_capacity board must 409 (the `adopt-board-cap` settle signal).
10. Re-render-judge the R2 code-ok retry on a fresh baseline (ch2-code must still carry a snippet).

## Commit

`8188c17` (R2 code-ok retry). Report `4dc9d81`.
