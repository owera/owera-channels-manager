# Growth agent — 2026-07-31

## ⚠ Needs operator

**1. The ch1 pause review is TOMORROW (08-01). The case is assembled and decision-ready.**
Final pre-review numbers, all read from the DB this run:

| evidence | value |
|---|---|
| ch1 subscribers vs the 15-sub bar | **10** (moved 9→10 once, on 07-15, pre-wedge-restart) |
| Mature post-07-16 wedge cohort | **65 videos, 587 views, 0 subscribers** |
| t15 avp post-wedge vs pre-wedge | **21.2%** (13 mature) vs **58.2%** (17) — 4th consecutive decline: 58.2 → 33.7 → 22.4 → 21.2 |
| ch1 lifetime video-attributed subs | 4 videos × 1 sub each; none since 07-15; none from the wedge cohort |
| ch2 comparison | **60 subs on 24 fewer published videos** (6× ch1) |

The mix is proven fixed (day 5 of exact 1L+4S), distribution exists, conversion is zero.
Unless tomorrow's overnight numbers surprise, the recommendation will be: **pause ch1,
redirect its render/publish budget to ch2** (t27/t28 are the highest-avp veins and t27 now
has first-party search demand — see Learned).

**Action already taken on saturation grounds (pre-committed yesterday): t15 parked at
weight 0** (was 3). 4th consecutive avp decline triggered the rule I set in the 07-30
report. Read-back proof: `GET /api/topics` → `{"id":15, "weight":0, "active":true}`. The 5
t15 drafts stay benched (not deleted); ch1 retains exactly one render-day of producible
drafts (1×t5 long + 4×t6 shorts), so tonight's auto-produce still fills the directive-2 mix.
Reverse with `PATCH /api/topics/15 {"weight":3}` if you overrule.

**2. Seeding — 4th consecutive flat day; kits go on pause tomorrow.** EXT_URL moved +1 view
total in 4 days across every tracked video. Today's kit is compact (2 items,
`run/seeding/2026-07-31.md`); per the 07-30 pre-commitment, tomorrow's run will skip the
kit unless EXT_URL moves or you confirm the kits are being posted.

**3. (carry-over)** ch2 metadata backfill (`db7a254`, plan file 07-28) still awaits the
operator `--apply` (10/day cap). Not re-verified today.

Otherwise healthy: both channels `oauth_status: connected`, zero failed, nothing stuck,
`error_runs_24h` empty, BGM pool 20 (min 5 / target 15).

## Observed

- **Now:** 2026-07-31 ~12:00 UTC. Kill switch absent, scheduler running, tree clean.
- **ch1:** 235 published, **10 subs (flat)**. Today's 5 published 07:01–09:05 UTC: 1 long
  (769, t5) + 4 shorts (806/807/808 t15 + 748 t6) — directive-2 mix day 5. Quota 8750/9000
  again (3rd day, `daily_limit_hit:false`, nothing blocked).
- **ch2:** 211 published, **60 subs (flat)**. Today's batch staged: 10 approved, window
  opens 14:00 UTC, reserved long slot leads with 752 (t2 MLflow-vs-W&B long), then 4 t4
  shorts (809–812). `next_publish_eta` confirms 14:00.
- **⭐ Auto-produce prediction VERIFIED — the live proof for `14f6471` (and for retiring
  manual produces).** JobRuns 12047–12056 at 00:00: ch1 auto-produced exactly 1×t5 long +
  3×t15 + 1×t6 short; ch2 exactly 1×t2 long + 4×t4 shorts — the precise mix predicted in
  yesterday's report, with zero manual produce. The digest stayed silent (`pipeline_starved:
  []`) and no publish day was missed. Experiment #23 settles 08-02 with this as day-1 evidence.
- **Language integrity (directive 6):** all 5 staged ch2 titles PT-BR (752, 809–812). ✓
- **Boards:** both `at_capacity:true` (10 drafts / 2.0 days) → no idea generation, no trend
  adoption this run.
- **Traffic:** video 778 (t27 hardware short) is pulling **first-party YouTube search
  traffic for "rebar on vs off"** (Resizable BAR) — 53 views and climbing daily. ch2 shorts
  keep SHORTS-feed distribution (683: 25, 741: 21, 742: 25). ch1 stays low-volume.

## Monetization

| metric | ch1 current | ch1 needed | ch1 % | ch2 current | ch2 needed | ch2 % |
|---|---|---|---|---|---|---|
| subscribers | 10 | 490 | 2.0% | 60 | 440 | 12.0% |
| watch hours | 4.5 | 2995.5 | 0.1% | 9.3 | 2990.7 | 0.3% |
| Shorts views | 2943 | 2,997,057 | 0.1% | 2983 | 2,997,017 | 0.1% |

Subscribers remain the only live constraint; ch2 remains the compounding asset.

## Triage

Digest at run start: 13 issues, 1 needs-operator, `clean:false`. Disposition:

- **`pipeline_starved: []`** — yesterday's fix holding on live data (see Observed ⭐).
- **12 × `rejected`, `suggested_action:"leave"`** — the 07-26 board-rebalance rows, age
  ~5d, under the 7-day delete floor. Untouched by design (delete-eligible from 08-02).
- **`quota` (ch1, escalate)** — 8750/9000, identical non-blocking shape 3 days running;
  resets 07:00 UTC. No budget change: nothing was blocked, and if ch1 pauses tomorrow the
  question dissolves.
- **BGM pool 20/15** — healthy, above target; no generation needed.
- No requeues, retries, deletes, or state transitions performed. No manual produce (the
  pipeline self-sustains — proven again today).

## Learned

1. **The self-sustaining-pipeline discovery is now live-proven, not just root-caused.**
   Yesterday's prediction of tonight's auto-produce mix was exact (10/10 videos, right
   topics, right formats, both channels). The agent's production role is editorial only.
2. **t15 is saturated by every measure** — 4th consecutive avp decline (58.2 → 33.7 → 22.4
   → 21.2) at stable weights/system; 39 published videos on one pricing angle, 0 subs ever.
   Parked. The generalization: a wedge topic's yield curve bends down after ~30 videos;
   watch t4 (73 published) for the same pattern.
3. **t27 has first-party search demand** — "rebar on vs off" is the first organic
   YT_SEARCH term we've ever captured in `traffic_json` search_terms. Concrete-hardware
   pain points are searchable in PT; the vein supports more subjects than we've fed it
   (logged trend #46 to exploit when the board opens).

**Hypotheses:** (a) ch1's failure is niche-level — tomorrow is the decision; (b) t4 will
show t15-style saturation within ~2 weeks (falsifiable from its avp trend); (c) a
t27 ReBAR short will pull YT_SEARCH traffic within 7 days of publish (testable once the
board opens and #46 feeds in).

## Engagement

**Baseline** (full golden set `baseline-0731`, 4/4 clean, `visible=True`, none
`used_fallback`) — **first all-2 baseline since the rubric loop started:**

| Lever | score | evidence |
|---|---|---|
| R1 hook (P1) | 2 | 4/4 pain-first ≤10w at t=0 ("Your RAG returns garbage" / "Sua RAG responde tudo, menos você") |
| R8 thumb/title (P2) | 2 | zero title echo 4/4, real gaps ("Goldfish memory, fixable tonight" vs "Why Your AI Agent Forgets…") |
| R2 info gain (P3) | 2 | fanout diagram truthful (see settle below); stat/code/cmp/lst all information-bearing |
| R6 payoff (P4) | 2 | quotable closes 4/4 ("Good retrieval beats a bigger model every time.") |
| R5 retention (P5) | 2 | open loop in all 4 ("It's not your embeddings. It's the ranking.") |
| R3 clarity (P6) | 2 | code frames indented, ≤30 chars, highlighted; nothing clipped anywhere |
| R4 pacing (P7) | **2 — first time** | min beat 1.68s (floor), max hold 8.24s (drift active), all reveals complete by midpoint; the 07-16 floor + 07-20 opener cap + 07-25 drift + 07-29 list cap compose into clean cadence. Watch: ch1-code ran 3 consecutive floor-tempo beats (9.9–15.3s) — fast but each completes |
| R7 CTA (P8) | 2 | spoken follow-ask + named next value, matching visual card, both languages |

**Targeted: none — no weak lever exists, so there is nothing evidence-backed to change.**
Shipping a change against an all-2 baseline would be exactly the ship-on-faith the gate
forbids. No code/prompt was touched, so no regression run was needed. (Second consecutive
no-op but for the opposite reason: yesterday R4 was weak-but-exhausted; today it finally
scores 2.)

**Settled: experiment #21 (`540d45d`, R2 fanout) → PROMOTED** (72h, SIGNAL-SCARCE).
3rd consecutive judged render day: ch2-code drew hub "MCP" fanning to Claude/GPT/Gemini
(truthful one-to-many), while ch1-concept correctly kept a sequential chain for an actual
pipeline (Store→Retrieve→Re-inject→Agent) — the model discriminates chain vs fanout in
both directions. Kept in code; retention re-check deferred to a measured cohort.

**Still running:** #22 (`ce46b43` list reveal, matures 08-01) — today's early evidence
positive: midpoint frames show ALL list rows even on a floor-duration 1.68s beat. #23
(`14f6471` starvation digest, settles 08-02) — day-1 live evidence exact (see Observed ⭐).

## Acted

1. **`PATCH /api/topics/15 {"weight":0}`** — t15 parked on 4th consecutive avp decline
   (pre-committed rule). Read-back: weight 0, active true. Bench impact verified safe
   before acting: ch1 keeps 5 producible drafts = exactly one render-budget day.
2. **Trends logged (no adoption — boards at capacity):** **#46** "Resizable BAR (ReBAR)
   on/off para IA local" (ch2, score 78, watch) — first trend ever backed by our own
   captured search demand; **#45→#46 chain** feeds t27 when the board opens. **#47**
   "Inkling (Thinking Machines Lab) local" (ch2, score 70, watch) — adoption gated on
   llama.cpp support merging.
3. **Seeding kit (compact):** `run/seeding/2026-07-31.md` — TabNews ReBAR post (778) +
   one r/GithubCopilot comment (807). Kits pause tomorrow absent an operator answer.
4. **No code changes, no produces, no budget/settings changes.** Zero of ≤2 code slots
   used — nothing was broken and no rubric lever was weak; a verified no-op beats a
   cosmetic ship.

## Watch next run (08-01 — decision day)

1. **Lead with the ch1 pause recommendation** using the table above + tonight's final
   overnight numbers. If pausing: `PATCH /api/channels/1 {"paused":true}` is the operator
   call; propose redirecting budget to ch2 (raise t27 weight, feed #46/#45/#42).
2. **Settle #22** (list reveal) — matures 08-01; re-render-judge if no cohort.
3. **#23 day-2 evidence** — verify tonight's auto-produce again (predicted ch1: 1×t5 long
   + 4 shorts from t6/t18 now that t15 is parked; ch2: 1 long + 4×t4); settles 08-02.
4. **07-26 rejected rows** hit the 7-day delete floor 08-02 — triage-delete then.
5. **Seeding**: check EXT_URL on 778/807/683; skip the kit if still flat and unanswered.
6. **ch2 window publishes** — confirm 752 led the 14:00 UTC window today (long-slot
   reserve, 2nd live check).
