# Seeding kit — 2026-09-04

Pipeline is paused (`scheduler_paused: true`). These are the last videos that actually published (ch2 long 09-02, ch2 shorts 09-01, ch1 search-led t15 from 08-29). Operator posts; agent does not.

## ch2 Rodrigo Recio (PT-BR)

### 1. TabNews — post
**Título:** GPU a 70% ociosa no treino não é “a placa fraca”. É o DataLoader.
**Corpo:**
`nvidia-smi` em 70% idle com o modelo “treinando” é o sintoma clássico de CPU/IO na frente do kernel. O DDP não recupera isso: cada rank espera o batch. `num_workers=0` + imagens no NFS deixa a 4090 cara olhando para o teto.

Teste de 2 minutos: `num_workers=4`, `pin_memory=True`, um `profiler` no DataLoader. Se o GPU-util sobe e o step time cai, o upgrade não é outra placa — é parar de alimentar a GPU com uma colher.

Long de 02/09 (t2):
https://youtu.be/glLjwSGYrlg

Quem já mediu `data_time` vs `compute_time` no PyTorch: o seu treino é compute-bound de verdade, ou só parece?

### 2. r/brdev — post/comment
**Título / ângulo:** Quantizar pra 4-bit não é o atalho de RAM que o tweet promete.
**Corpo:**
Q4 corta pesos. O KV cache do contexto não corta na mesma proporção. Um 7B Q4 com `num_ctx=8k` ainda estoura 8GB se o keep-alive segura o modelo + o cache. `ollama ps` na hora do generate: se o VRAM não cai quando você mata o chat, o keep-alive é o inquilino, não o GGUF.

Short de 01/09 (t4):
https://youtu.be/Ch7Phhu6alA

VRAM “sumindo” no Ollama (keep-alive + contexto, não o quant):
https://youtu.be/IeDaxZvVQSM

### 3. LinkedIn (PT) — post curto
**Corpo:**
Assinatura de API “porque local é complicado” às vezes é um 7B que o PC já rodava. A conta não é só tok/s: é dados que não saem da máquina + a fatura que some quando o volume é o seu, não o do provedor. O teste honesto é 20 perguntas reais do trabalho, local vs API, mesma temperatura — não o leaderboard.

Short de 01/09 (t4):
https://youtu.be/sj7Y93JISko

Long de 02/09 (GPU ociosa no treino):
https://youtu.be/glLjwSGYrlg

## ch1 Owera Software (EN)

### 1. r/LocalLLaMA — comment on a fresh thread about Copilot billing / “why did comments cost credits”
**Draft:**
Copilot suggestions in comments is not “free autocomplete.” Each keystroke in a docstring re-sends the file. 9k credits on comments is the model reading what you already wrote, on a loop. Same family as leaving Chat open and as pasting a 400-file diff: you pay for context you did not mean to send.

30-second check: disable inline in comments / markdown, keep it on in code. If the meter drops an order of magnitude, the leak was the comment buffer.

This one actually got YouTube *search* (not just Shorts) for “copilot”:
https://youtu.be/0ECc6n57iFQ

### 2. dev.to — short post
**Title:** You still think Copilot Pro is unlimited after June 1
**Body:**
The Pro “unlimited” story died with the credit meter. A second pane, a branch switch, a formatter-on-save — each one is a reindex. The dashboard lags; the invoice does not. If you run Copilot + Cursor + Claude on the same job, you are paying three meters for one function.

Throwaway check: one repo, one pane, suggestions off in comments. Compare the daily credit delta to last week.

https://youtu.be/SYBNJGgZyB0

## Comment drafts (adjacent channels)
- A PyTorch GPU-util / DataLoader thread: "70% idle is usually the DataLoader, not the model. `data_time` vs `compute_time` before you buy another GPU. https://youtu.be/glLjwSGYrlg"
- An Ollama VRAM / keep-alive thread: "O modelo Q4 não é o inquilino. É o keep-alive + KV cache. `ollama ps` na hora do generate. https://youtu.be/IeDaxZvVQSM"
- A Copilot billing / changelog thread: "Suggestions in comments is the silent 9k-credit leak. Search is already sending people to this: https://youtu.be/0ECc6n57iFQ"

## Yesterday's seeds — status
- No kits on 09-02 / 09-03 (runs skipped). 09-01 kit operator-post status unverified (agent does not post).
- v948 EXT_URL still = 9 (unchanged vs 08-17…09-01) — only frozen external spike.
- v960 EXT_URL still = 1 (converter, unchanged).
- No new EXT_URL movement attributed to the 09-01 kit.
- v1190 (not in the 09-01 kit) picked up YT_SEARCH=55 on its own — quote that, not seeding.
