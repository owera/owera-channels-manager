# Growth agent — 2026-08-12

## ⚠ Needs operator

1. **ch1 pause decision (hard review overdue since 08-01).** Still **10 subscribers** vs the 15-sub bar. Lifetime conversion near zero on volume (v785 ≈174 views / high avp loops → 0 subs; MCP v558 remains the rare +1). **Recommendation stands: PAUSE ch1** (or explicitly override with a new bar). Agent continues **zero ch1 strategy moves** (no weight/trend adopt/generate for growth) until you decide. Pipeline hygiene still allowed (done today: queued long 895 for overnight).
2. **ch1 approved buffer at observe:** **0 long + 3 shorts** after the morning 1L+4S publish — thin long bank until overnight. Hygiene: `POST /api/videos/895/produce` → `queued`.
3. **ch2 metadata backfill** (`db7a254` plan) still awaits `--apply` if still desired.
4. **BGM pool path** remains external (`MoneyPrinterTurbo/resource/songs`) — 10 tracks healthy; wipe risk still escalates to silent renders.
5. **Quota monitor (not blocking):** ch1 spent **8750/9000** after a full 5-publish day — expected (5×1750). No nudge.

---

## Observed

- **Now:** 2026-08-12 ~12:00–13:30 UTC. Kill switch off, `scheduler_paused: false`, tree clean at start.
- **ch1:** published **5/5** today, quota **8750/9000**. Mix = **1 long + 4 shorts** (v894 t5 long first @ 07:01, then v921 t18 MCP, v924/926/934 t6). **Seventh consecutive 1L+4S day** (08-06…08-12). Yesterday’s hygiene long (894) published as today’s long — long-reserve/hygiene path held.
- **ch2:** published_today **0** at observe (window opens **14:00 UTC**). rendered_today **5**. Approved = **1 long (932 t2) + 10 shorts** (t4/t27/t10) — ready for 1L+4S under the window. **08-11 did publish 1L+4S** (v911 long first) — mix still holding.
- **BGM pool:** 10 / min 5 / target 15, `is_low:false`. No music_gen errors. No `error_runs_24h`.
- **Boards (API `by_format` — #30 still running):**
  - After hygiene: ch1 draft `{long:2, short:7}` · queued `{long:1, short:0}` · approved `{long:0, short:3}`
  - After hygiene: ch2 draft `{long:2, short:8}` · approved `{long:1, short:10}` · queued `{0,0}`
  - **Pre-hygiene ch2 was draft 0 long** — high-weight t4/t27 had starved the long anchor bench (root cause of today’s code change).
- **Language (ch2):** approved/recent titles PT-BR ✓ (e.g. 932 DVC vs Git LFS; 938–941 t27).
- **traffic_json (via leaderboard API — #29 PROMOTED this run):** v542 SHORTS=385; v830 SHORTS=144; v589 SHORTS=92 + YT_SEARCH; v785 SHORTS=164; v859 SHORTS=49. No invented CTR. No BROWSE/SUGGESTED named keys on top samples (SHORTS / YT_SEARCH dominate).
- **ch1 subs-per-video:** still ~0 on post-CTA volume; **ch1 = 10 subs. ch2 = 61 subs** (unchanged vs 08-11).

### Publish log (settle evidence)

| day | ch1 | ch2 |
|-----|-----|-----|
| 08-08 | 5 (1L+4S) | 5 (1L+4S) |
| 08-09 | 5 (1L+4S) | 5 (1L+4S) |
| 08-10 | 5 (1L+4S) | 5 (1L+4S) |
| 08-11 | 5 (1L+4S) | 5 (1L+4S) long 911 first |
| 08-12 | **5 (1L+4S)** long 894 first | pending window (0 at observe; long 932 + 10 shorts banked) |

## Monetization

| metric | ch1 current | ch1 needed | ch1 % | ch2 current | ch2 needed | ch2 % |
|---|---|---|---|---|---|---|
| subscribers | 10 | 490 | 2.0% | 61 | 439 | 12.2% |
| watch hours | 6.6 | 2993.4 | 0.2% | 10.9 | 2989.1 | 0.4% |
| Shorts views | 3895 | 2,996,105 | 0.1% | 3787 | 2,996,213 | 0.1% |

**Binding constraint both channels: subscribers** (lower tier). No milestone crossed. Prefer analytics (ch2 local-AI hardware shorts; ch1 frozen) over pure milestone math.

## Triage

Digest at start: 1 issue (quota monitor only). No failed / stuck / oauth / starved / bgm_pool_low / music_gen. After displacement, 2 rejected rows appear with `suggested_action:"leave"` (age &lt;7d — leave).

| Issue | Action | Proof |
|---|---|---|
| quota ch1 8750/9000 | monitor only | full 5-publish day by design (5×1750); `daily_limit_hit:false` |
| ch1 approved 0L after morning publish | hygiene: `POST /api/videos/895/produce` | after: v895 `status=queued`; board `by_format.queued={long:1,short:0}` |
| ch2 draft 0 long (starved) | raise t2 weight 2→4; reject v922+v928 (t4); generate t2×2 | after: draft `{long:2,short:8}`; subjects 952/953 live; v922/v928 `status=rejected` |
| failed / stuck / oauth / bgm_low / pipeline_starved | none | clean lists |

## Learned

1. **#29 traffic_json API — PROMOTED (72h+).** Reports 08-10→08-12 quote `traffic_json` from the leaderboard API field without DB dive. Keep join.
2. **#30 board-inventory-by-format — leave RUNNING** (~48h since 08-10; settle ~08-13). Used live again this run.
3. **#31 auto-produce long-reserve — leave RUNNING** (shipped 08-11; &lt;72h). Today’s ch1 long was the *manual* queue from yesterday; not yet a pure auto_produce-reserve proof under approved_longs==1 overnight alone.
4. **#25 trend-displacement — leave RUNNING.** Cohort: v859=50 views / SHORTS=49 but avp=1.82 avd=0 (incomplete); v860=8; v861=0. Not enough to promote/revert vs t4. Do not invent.
5. **Winners (mature):** ch2 t4 26.9 avg views + 4 lifetime subs; t27 25.9 avg (uneven post-displace cohort); t28 25.8 @59% avp. ch1 t18 MCP 36.2 avg + only recent sub-gainer; t15 high avp loops, 0 subs on volume.
6. **Hypotheses this run:** (a) autofill long-reserve keeps ch2 draft long ≥1 without agent displacement; (b) ch2 08-12 window publishes 1L+4S with 932 leading; (c) operator pause remains the binding ch1 move.

## Acted (channels)

- **Triage hygiene (ch1, freeze-safe):** produced v895 (t5 long) → QUEUED. Verified `status=queued`.
- **Triage hygiene (ch2 long bench):** `PATCH /api/topics/2 {"weight":4}` (was 2); rejected v922+v928 (lowest-weight t4 drafts on board; reason displaced for t2 empty long bench); `POST /api/topics/2/generate {"count":2}` → drafts 952/953. Board back to draft long=2 at capacity.
- **Trends logged (no adopt/displace — boards at capacity; #25 still maturing; ch1 frozen):**
  - #66 ch2 score **81** watch — “llama.cpp -ncmoe MoE expert pinning 35B on 12GB VRAM”
  - #67 ch2 score **78** watch — “KV-cache quant below q8_0 for 128K on 6–8GB VRAM”
  - #68 ch1 score **45** reject — “Qwen3.8-Max 2.4T…” — ch1 frozen / not consumer-local
- **No BGM generate** — pool healthy (10 ≥ min 5).
- **Seeding kit:** `run/seeding/2026-08-12.md` (operator posts; agent does not).

## Engagement

**Baseline `baseline-0812`** — 4/4 clean, `visible=True`, none `used_fallback`. Scores from frames + scripts:

| Lever | score | evidence |
|---|---|---|
| R1 hook (P1) | 2 | Pain-first on frame: “RAG returns garbage — not the model” / “Your agent has amnesia” / “Reescrevendo a mesma integração?” / “Sua RAG responde tudo errado” |
| R8 thumb/title (P2) | 2 | Gaps vs titles (hook “Your Retrieval Is Sabotaging Answers” vs title “Fix Your Slow RAG…”; PT “Um Conector Para Governar Todos” / “Frases cortadas ao meio”) |
| R2 info gain (P3) | 2 | Varied mixes (ch1-code: hook/stat/term/cmp/code/stat/diagram/cta; ch2-code: term+diagram+code MCP) |
| R6 payoff (P4) | 2 | Quotable closes (“Rerank before you generate…” / “Your agent only knows what you remind it” / chunk lesson) |
| R5 retention (P5) | 2 | Open loops early on all 4 scripts |
| R3 clarity (P6) | 2 | Code legible (Cohere rerank; `@server.tool` + indented `return db.run(sql)`); CTA button clean |
| R4 pacing (P7) | 2 | Hook at t=0; durations ~35–45s shorts |
| R7 CTA (P8) | 2 | Follow/Siga + named next value 4/4 (“Tomorrow: halve retrieval latency” / memory layer / 1º servidor MCP / tamanho de chunk) |

**All-2 baseline (11th consecutive judged day) → non-engagement code path (fallback clause).**

### Shipped: autofill long-draft reserve

- **Files:** `app/services/autofill_loop.py`, `tests/verify_autofill.py`
- **Change:** when a channel has **0 long pending** (draft+queued) and an active long topic (weight&gt;0), autofill (1) holds 1 board slot back from shorts, and (2) if the board is already full of shorts, seeds **exactly one** long draft (+1 temporary overshoot). Stops high-weight short winners from permanently starving the 1L+4S draft buffer.
- **Why:** ch2 observe showed draft **0 long / 10 short** with t2 pend=0 while t4(w3)/t27(w4) held the horizon — auto_produce cannot invent a long that does not exist as a draft.
- **Gate (synchronous, observed this run):**
  - `PYTHONPATH=. .venv/bin/python tests/verify_autofill.py` → **14/14** (was 10; +4 long-reserve cases including full-board overshoot)
  - `tests/verify_storyboard.py` → **40/40**
  - `tests/verify_render.py` → **89/89**
  - `uv run python -c "import app.main"` ok
  - `launchctl kickstart` manager; `GET /api/dashboard` **200**
  - Live board after manual seed already has long drafts; overnight autofill is the residual proof → **watch next run**
- **No prompt/rubric lever change** — shipping unproven polish on an all-2 baseline would violate the gate.

### Experiments settled / opened

| id | lever | verdict |
|---|---|---|
| #25 08-03 | trend-displacement | **RUNNING** — 859 has SHORTS distribution but retention incomplete |
| #29 08-09 | traffic_json API | **PROMOTED** — 3 consecutive reports quote API field |
| #30 08-10 | board-inventory-by-format | **RUNNING** — settle ~08-13 |
| #31 08-11 | auto-produce long-reserve | **RUNNING** — &lt;72h |
| #32 08-12 | autofill long-reserve | **RUNNING** — shipped this run |

## Watch next run

1. Operator ch1 pause yes/no (still the binding decision).
2. ch2 08-12 publish composition after 14:00 UTC — expect **1L+4S** with v932 leading.
3. ch1 overnight: queued long 895 + auto_produce — does approved rebuild to ≥1L before 07:00? Quote `by_format.approved` from API.
4. **#32:** after a full autofill cycle with no agent displacement, is ch2 `draft.long >= 1` while shorts stay near capacity?
5. #25: need filled avd/avp on 859+ before promote/revert.
6. #30 settle ~08-13; #31 after ≥72h of live auto_produce-reserve nights.
7. BGM count stays ≥ min.

## Commit

`5d99367` — Growth agent 2026-08-12: autofill long-draft reserve; promote #29; ch2 long hygiene