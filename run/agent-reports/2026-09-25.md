# Growth agent 2026-09-25

Preflight: kill switch absent. `scheduler_paused` false. Branch `main`. Untracked `NEWSLETTER_DEPLOY.md` (newsletter deploy guide for another tree) left unstaged. No tracked files were dirty at start.

Code commit: `f065454`. After that commit, `launchctl kickstart -k gui/$(id -u)/com.owera.channels-manager` and `GET /api/dashboard` **200**.

## Monetization

Lower tier. Neither channel has `tier_achieved`. Binding constraint on both is **subscribers**. No metric hit 100% for the first time. Channel subs are unchanged from yesterday: ch1 **16**, ch2 **70**.

| metric | ch1 current | ch1 needed | ch1 % | ch2 current | ch2 needed | ch2 % |
|---|---|---|---|---|---|---|
| subscribers | **16** | 484 | **3.2%** | **70** | 430 | **14.0%** |
| watch hours | 11.2 | 2988.8 | 0.4% | 23.3 | 2976.7 | 0.8% |
| Shorts views | 5926 | 2,994,074 | 0.2% | 5912 | 2,994,088 | 0.2% |

ch1 subs-per-video, lifetime, unchanged: t5 long **3**, t15 **2**, t6 **2**, t18 **1**, t1 **1**. t26 Agent memory still **0** on 9 measured (57.6% avp, 19.6s avg duration). t7 CrewAI **0** on 13 (66.9% avp).

Analytics newest capture: ch1 2026-09-24T12:25Z, ch2 2026-09-24T12:27Z.

## Triage

Digest at start: **not clean** (`summary.clean: false`, `total_issues: 35`, `needs_operator: 0`). `oauth` / `quota` / `cooldown` / `bgm_pool_low` / `stuck_publishing` / `stuck_rendering` / `pipeline_starved` / `title_pattern_blocked` / `craft_gate_blocked` empty. `error_runs_24h`: 8× transient `grok.Timeout` after 600s, 3× the same timeout as a terminal error.

Both channels had 10 videos in `review`, 0 approved, `default_skip_gate=false`. stuck_review: 8 rows past 48h. Craft dry-run: `publish_craft_block_reason` **null** on all 20 review rows; `used_fallback=false` on all.

| Issue | Did | Proof (read-back) |
|---|---|---|
| stuck_review **1313** (ch1 t45, 70h, title=subject, craft clear) | `POST /approve` | GET 1313 `status=approved` `craft_review=pass` `video_path` set. next_publish_eta was 12:06Z; still approved at 12:07Z (1317 took the slot). |
| stuck_review **1317** (ch1 t26, 59h) | `POST /approve` | GET 1317 `status=published` `yt=L7U4s4K1Q8I` `published_at=2026-09-25T12:07:18Z` title `Chat saved 4pm. Prod booked 4am. · Agent memory 13`. ch1 `published_today=1`. |
| stuck_review **1298** (ch2 t27, 70h) | `POST /approve` | GET 1298 `status=approved` `craft_review=pass`. |
| stuck_review **1314** (ch2 t27, 68h) | `POST /approve` | GET 1314 `status=approved` `craft_review=pass`. |
| stuck_review **1323** (ch2 t4, 58h) | `POST /approve` | GET 1323 `status=approved` `craft_review=pass`. |
| stuck_review **1324** (ch2 t4, 57h) | `POST /approve` | GET 1324 `status=approved` `craft_review=pass`. ch2 `approved=4` `next_publish_eta=2026-09-25T14:00:00+00:00`. |
| stuck_review **1320** (ch1 t26) | `POST /reject` | GET 1320 `status=rejected`. Script after the title head: `I'll check the series notes so the script matches that story, not a generic take.` Reason quoted on the row. |
| stuck_review **1284** (ch2 t4, lowercase fragment title) | `POST /reject` | GET 1284 `status=rejected`. Title `milhão de docs no CPU não é engenharia. · IA 196`. |
| failed **1297, 1318, 1326, 1331, 1340** (retry 2/2, grok 600s, no file, `suggested_action: requeue`) | leave | GET still `failed` `retry_count=2`. `POST /requeue` does not reset `retry_count`, so another queue spends one 600s call and `_retry_or_fail` marks FAILED again. |
| rejected HOLD / CoS STOP / title-pattern leftovers | leave | Same as yesterday. Not deleted. |

BGM: `state.bgm_pool` count **30** → after generate **35** (`is_low: false`, min 5, target 15). `GET /api/music` count **59** → **64**. `POST /api/music/generate {"count":5}` returned 5 files, `errors: []`. Quoted last names: `techno_1790338013465.wav` … `techno_1790338014194.wav`. Pipeline had rendered overnight (`rendered_today=5` each) so the refresh had demand.

## Learned

Board: both channels `at_capacity: true`. ch1 `days_of_inventory` 2.0 (10 drafts, 0 queued). ch2 2.0 (10 drafts, 0 queued). `by_format` draft `{long:0, short:10}` on both. Long topics stay parked (t5 inactive, t2 weight 0). `rendered_today=5` on each. At observe `published_today=0`; after approve, ch1 **1**.

**Winners (mature, capture 09-24 ~12:25Z):** ch2 t4 still the converter (6 lifetime subs on the prior snapshot). t28 28.4 avg views / 0 subs. ch1 t15 20.2 avg / **2 subs** on 88 measured, still off (`weight=0`, `active=false`). t26 16.8 avg / 57.6% avp / **0 subs** on 9 measured.

**Traffic:** SHORTS + YT_SEARCH. BROWSE total **0** and SUGGESTED total **0** on both channels. EXT_URL sums: ch1 32, ch2 107, concentrated on old videos. v1247 (`_XGUAQrwE6s`) still **views=0**, `has_data=true`, `traffic={}`, captured **2026-09-24T12:23:42** (newer than yesterday's 09-23 02:09Z capture). v1251 still 0. v1208 still 101 views / YT_SEARCH=88.

**Yesterday's watch items:**

- v1281/1282/1283 published. GET `1281 yt=onMFEPyMAxM` 14:00Z, `1282 yt=QOLh5bkfkjo` 14:31Z, `1283 yt=U2Eik5MD2_4` 15:02Z. 1281's title is still the fragment `ligada no mês sai R$380…`.
- 1313 was not the first ch1 publish today: 1317 shipped at 12:07Z. 1313 remains approved.
- Next t4 autofill: t4 pending=0 overnight; autofill filled t27 (1347–1352). Those subjects are clean claims, no catalog-planning opener. 1336 rendered with the patched R$14 sentence (now in review, under 48h).
- v1297 still failed at retry 2.
- v1247 still 0 on the 09-24 12:23Z capture.

**Language:** ch2 review/approved/draft titles checked this run are PT-BR. 1284 was the fragment leftover; rejected.

**Hypotheses this run:** (a) 1281–1283 would publish with those YouTube ids — **held**. (b) 1313 would be the first ch1 approve past 48h — **held as an approve**, **missed as first publish**; 1317 took 12:07Z. (c) next t4 autofill would not open on catalog-number planning — **vacuous** (no new t4 drafts). (d) v1247 still 0 on a capture newer than 09-23T02:09 — **held**. (e) 1320 CoT would still be in the stored script — **held**, then rejected.

**Hypotheses next run:** (a) 1313 publishes in the 12:37Z drip; quote the YouTube id. (b) ch2 1298/1314/1323/1324 publish from 14:00Z; quote ids. (c) overnight t27/t4 subjects do not open on catalog-planning CoT and do not contain `I'll check the series notes`. (d) v1247 views on a capture newer than 2026-09-24T12:23:42. (e) a new patterned render after `f065454` does not speak series-notes CoT after the title head.

`blockingio-transient` (09-05, `48bdaa9`): no BlockingIOError in the 24h digest. Leave `running`.

`R1` preamble strips 09-21 (`5f6b8b0`) and 09-22 (`9853db8`): ≥72h but today's golden was only ch1-code (unpatterned, no CoT). Leave `running` until the matching subjects re-render.

`R1` idea-strip 09-23 (`af6dcf3`) and 09-24 (`600da30`): under 72h. New drafts 1347–1352 are clean. Leave `running`.

## Act

No weight changes. t15 stays off. No idea generation and no trend adoption: both boards at the horizon. A new weight-1 topic cannot displace t26 (weight 2) or t4 (weight 5) / t27 (weight 3) drafts.

Trends logged (no adopt):

| id | score | status | term |
|---|---|---|---|
| 126 | 62 | watching | Ollama v0.40.0-rc0 MLX default on Apple Silicon — RC 6h old, t27 genre, score <75 |

v0.34.4 stable is already trend 122. Copilot model-picker (124) and Opus 5.5 (125) stay watching. llama.cpp unnamed nightlies stay out of the log.

Long-form stays parked. Empty long bench is the operator stop.

Seeding kit: `run/seeding/2026-09-25.md`. ch2 features v1283. ch1 features v1317. r/LocalLLaMA and HN skipped. Yesterday's kit cannot be scored: the analytics capture is from before that kit was written.

## Engagement

Rubric baseline this run is **one subject**, ch1-code, label `baseline`. The command was still on ch1-concept when this report was written (ch1-code took ~17 min: grok + hyperframes). `visible=True`. Beats `hook, stat, term, cmp, term, stat, code, quote, cta`. 78w / 30.53s. Hook hold 4.747s.

| Lever | Score | What the frame showed |
|---|---|---|
| R1 hook | **2** | frame0 `Your RAG is slow and still wrong` + object `RAG`. Title and thumb hook match. |
| R8 title/thumb | **2** | same sentence. Golden subject has no `· series nn`. |
| R2 information | **2** | indented `rerank(q, hits)` / `top = ranked[:5]`; stacked cmp Spend vs Result; quote payoff. Two stats and two terms, not copies of the same card. |
| R6 payoff | **2** | quote `Retrieve wide, rerank hard, generate thin`. |
| R7 CTA | **1** | chip `Subscribe · Copilot Credits` + `same series`. Harness does not pass brand; production patterned videos on the board use the suffix (1317 VO `Subscribe — next Agent memory trap`). |
| R3 clarity | **2** | code indent visible; cmp stacked; hook four words on two lines. |
| R4 pacing | **2** | hook 4.75s, mids 3.0s, CTA 3.82s. |
| R5 retention | **2** | fetch-20 → rerank → top-5. |
| R9 audio | unverified | not listened to. |
| R10 brand | **2** | OS teal/black. |
| R11 format | **2** | one claim, 31s. |

The live R1 leak was **not** the golden: v1320 (patterned Agent memory 14) spoke series-notes CoT after a two-sentence title head. That is the lever this run targeted. Golden ch1-code is unpatterned, so `_lock_patterned_opener` is a no-op there — not claimed as an R1 frame-score increase.

**Shipped:** `_lock_patterned_opener` keeps a script that already starts with the title head (no maxsplit=1 duplicate on two-sentence heads) and drops preamble sentences from the rest, including all-preamble rest. `_PREAMBLE_BODY` also matches `series notes` / `generic take`.

Proof:

- Isolated call on GET 1320's stored script → opener `Chat cancelled order 8. Prod shipped it.` then the warehouse body; `I'll check` / `series notes` absent; `Prod shipped it.` count **1**.
- `PYTHONPATH=. .venv/bin/python tests/verify_worker.py` → **255/255**.
- `PYTHONPATH=. .venv/bin/python tests/verify_storyboard.py` → **329/329**.
- Unpatterned golden subject still a no-op (existing check).
- `uv run python -c "import app.main"` printed `import-ok`.
- Manager restarted after `f065454`. `GET /api/dashboard` **200**.

## Watch next run

1. 1313 should publish on the 12:37Z drip; quote the YouTube id. Do not wait this run.
2. ch2 1298, 1314, 1323, 1324 should publish from 14:00Z; quote ids.
3. Overnight patterned scripts must not contain `I'll check the series notes` after the title head (the running app only has today's lock after the restart below).
4. v1247 views, on a capture newer than 2026-09-24T12:23:42.
5. Golden R7 still names Copilot Credits when brand is missing. Production suffixes are fine; only revisit the harness/default if an unpatterned short ships.
