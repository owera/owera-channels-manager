# Growth agent — 2026-09-04

## ⚠ Needs operator

**This run is observation + reporting only.** `GET /api/settings` → `scheduler_paused: true`. No channel actions, no triage mutations, no code/prompt changes.

1. **Scheduler is paused** (operator). Render/publish ticks honor the flag (`render_loop.tick` / `publish_loop` return immediately). **Autofill does not** — JobRun **23748** `generate` at 2026-09-04T06:30:07 still auto-refilled 1 t4 idea (`IA`). Pause is leaky on the bench.
2. **PAUSE ch1 (hard review overdue since 08-01).** Subs still **13** (< 15). Video-attributed converters remain **six** (v558 MCP, v367 t6, v43 t1, v96 t5, v1032 t6, v960 t5 long). The 08-24→09-01 +1 is still not on a measured video. **Recommendation stands: keep ch1 paused** (or explicitly override with a new bar). Operator already cut both channels to `daily_render_budget=1` / `daily_publish_budget=1` (channel `updated_at` 2026-09-02T14:28:55) and parked the long anchors (ch1 t5 `active=false` `weight=0`; ch2 t2 `weight=0`). Agent made **zero** strategy moves this run.
3. **When you unpause, do these first (not done today):**
   - **BlockingIOError storm (09-03 00:00–00:02).** 14 videos FAILED with `BlockingIOError: [Errno 35] Resource temporarily unavailable`, `retry_count=0`, `has_file=false`, `transient: false`. 10 are still in the 7-day requeue window (ch1 t15: 1227, 1238–1241; ch2 t4: 1231, 1234–1237). Four ch2 t27 (1170–1173) are age 227.7h → delete. This signature is **not** in `_TRANSIENT`, so in-loop retry never fires. Root-cause it before the next midnight burst.
   - **v1213 is a good long sitting in REVIEW 63.1h.** `used_fallback=false`, `beat_count=11`, duration 254s, `video_path` present, `skip_gate=false`. Approve (or set `skip_gate`) — do **not** requeue. This is the 09-02 compose-timeout retry and it worked.
   - **ch1 is publish-starved even if unpaused:** approved=0, queued=0, all 5 drafts sit on parked topics (t18×3, t5×1, t43×1). Active t15/t26/t7 have `pending=0`. Auto-produce will not queue them.
4. **ch2 metadata backfill** (`db7a254` plan) still awaits `--apply` if still desired.
5. **BGM pool path** remains external (`MoneyPrinterTurbo/resource/songs`). Count 30 (target 15).

---

## Observed

- **Now:** 2026-09-04T12:01 UTC (Friday). Kill switch off, `scheduler_paused: true`, tree clean at start, `main`. Last full growth run: **2026-09-01** (09-02 and 09-03 skipped). Operator code since then: `625ac79` (Review player Range parser).
- **Cadence (operator, 09-02 14:28):** both channels `daily_render_budget=1`, `daily_publish_budget=1` (was 5/5). Directive-2 mix (4 shorts + 1 long) is structurally impossible at budget=1.
- **ch1:** published_today **0**, rendered_today **0**, quota **0/9000**. published lifetime **400**. Mix since last report: 09-01 **0L+5S**, 09-02 **0L+5S** (third and fourth straight 0L days), **09-03 none**, **09-04 none**. `oauth_status=connected`.
- **ch2:** published_today **0**, rendered_today **0**. published lifetime **375**. 09-01 **0L+5S** (prediction held — we had pulled the fallback long). 09-02 **1L+0S** (v1223 t2 long first @ 14:01 `glLjwSGYrlg`, then budgets cut at 14:28 so the remaining shorts did not go out). **09-03 none**, **09-04 none**. Next publish-plan card: **v1217 t4** @ 2026-09-04T14:00 — **will not fire while paused**.
- **ch2 subscribers: 65** (was 64 on 09-01). Lifetime converters still **seven** (v542/524/810/421/684/315 + v1022). The +1 is **not** on a measured video yet.
- **ch1 subscribers: 13** (unchanged vs 09-01). Subs-per-video on the mature leaderboard: **still 6 lifetime +1s**. t15 still **0 subs** on 66 measured / 70 published.
- **BGM pool:** 30 (min 5 / target 15; `is_low: false`). No `music_gen` errors. No generate this run.
- **Boards (API `by_format`):**
  - ch1 draft `{long:1, short:4}` · queued `{long:0, short:0}` · approved `{long:0, short:0}` · review=1 (v1213)
  - ch2 draft `{long:1, short:1}` · queued `{long:0, short:0}` · approved `{long:0, short:10}`
  - both `at_capacity: true` (ch1 days=5.0 because budget is now 1; ch2 days=2.0). `board_overflow` [].
- **Language (ch2):** 09-01 and 09-02 titles all PT-BR ✓ (1207 assinatura; 1214 4-bit; 1215 energia; 1216 VRAM/Ollama; 1002 7B vs 27B; 1223 GPU 70% ociosa). No EN leak.
- **traffic_json (via leaderboard API, no DB dive):** v542 SHORTS=389 (was 385); v785 SHORTS=165 + YT_SEARCH=10; v558 SHORTS=104 + YT_SEARCH=12; v948 SHORTS=64 + **EXT_URL=9** (still frozen vs 08-17…09-01) + NOTIFICATION=3; v884 YT_SEARCH=22 + SHORTS=77; v1022 YT_SEARCH=4 (4 views / 47.82% avp, unchanged); v1032 SHORTS=22 + YT_SEARCH=2; **v960 EXT_URL=1**. **v1190 t15** (age 147h, 70 views, avp 116.43, avd 44s): **YT_SEARCH=55 / SHORTS=14**, search_terms `copilot:4`, `copilot 365:1`, `github copilot:1` — first search-led wedge video with real volume; **0 subs**. **No BROWSE / SUGGESTED keys** on the top samples. No invented CTR.
- **compose-timeout watch (from 09-01):** both requeued longs rendered **real storyboards** at 09-02 00:00. Proof in Engagement.

### Publish log (settle evidence)

| day | ch1 | ch2 |
|-----|-----|-----|
| 08-28 | **5 (1L+4S)** long **1165** first + **4×t15** | **5 (1L+4S)** long **1179** first + **4×t4** |
| 08-29 | **5 (1L+4S)** long **1166** first + t43 + **3×t15** | **5 (1L+4S)** long **1193** first + **4×t4** |
| 08-30 | **5 (1L+4S)** long **1203** first + **4×t15** | **5 (1L+4S)** long **1202** first + **4×t4** |
| 08-31 | **5 (0L+5S)** 5×t15 — long 1213 TTS-failed | **5 (1L+4S)** long **1212** first + **4×t4** |
| 09-01 | **5 (0L+5S)** 4×t15 + t18 — long 1213 fallback→review | **5 (0L+5S)** 4×t4 + t27 (1207/1214–1216/1002) — fallback long pulled |
| 09-02 | **5 (0L+5S)** 4×t15 + t6 — v1213 good render→review again | **1 (1L+0S)** long **1223** first @14:01; budgets cut 14:28 |
| 09-03 | **0** (pause / BlockingIOError burst at 00:00) | **0** |
| 09-04 | **0** (scheduler_paused) | **0** at observe; plan v1217 @14:00 will not fire |

## Monetization

| metric | ch1 current | ch1 needed | ch1 % | ch2 current | ch2 needed | ch2 % |
|---|---|---|---|---|---|---|
| subscribers | 13 | 487 | 2.6% | 65 | 435 | 13.0% |
| watch hours | 8.7 | 2991.3 | 0.3% | 15.4 | 2984.6 | 0.5% |
| Shorts views | 5024 | 2,994,976 | 0.2% | 4815 | 2,995,185 | 0.2% |

**Binding constraint both channels: subscribers** (lower tier). No milestone crossed (no metric hit 100% for the first time). ch1 shorts_views 4819→**5024** (+205 vs 09-01); ch2 4635→**4815** (+180). ch2 watch hours 12.9→**15.4**. Prefer analytics (keep t4 as ch2 converter lead; ch1 frozen) over pure milestone math. ch2 **65 subscribers** is the first move off 64 since the 09-01 report; still 13% of lower-tier.

## Triage

Digest at start: **not clean** (`summary.clean: false`, `total_issues: 87`, `needs_operator: 0`). `oauth` / `quota` / `cooldown` / `bgm_pool_low` / `stuck_publishing` / `stuck_rendering` / `error_runs_24h` empty (the 09-03 BlockingIOError burst is **>24h**, so it dropped out of `error_runs_24h` while the 14 FAILED rows remain). **No remediation this run** (`scheduler_paused`).

| Issue | Would do (not done) | Proof (read-only) |
|---|---|---|
| 14 failed BlockingIOError | requeue 10 (age <7d); delete 1170–1173 (age 227.7h) | GET `/api/agent/issues` failed n=14, `suggested_action` requeue×10 / delete×4; `retry_count=0`, `has_file=false` |
| v1213 stuck_review 63.1h | approve (good render) | GET `/api/videos/1213` `status=review`, `skip_gate=false`, `used_fallback=false`, `beat_count=11`, `video_path` present |
| ch1 render_starved + publish_starved | unpark or generate on t15/t26/t7 | issues `producible=0`; drafts 1167–1169 t18, 1224 t5, 1232 t43 — all weight 0 |
| 70 rejected t33–t42 (age 240.3h, `suggested_action: delete`) | DELETE 10/run | GET `/api/videos/1094` still `rejected`; issues `rejected=70` (same 70 as 09-01 after that run’s 10 deletes) |
| t3 parked / t5 long-anchor | no change (and t5 is now `active=false`) | GET topics: t3 `weight=0`; t5 `weight=0` `active=false` `content_format=long` |
| v1216 t4 short `used_fallback=true` (published 09-01) | leave; too early (age 68.5h < 72) | GET `/api/videos/1216` published, `used_fallback=true`, `beat_count=0`, yt=`IeDaxZvVQSM` |

## Learned

1. **ch2 09-01 mix prediction held:** 0L+5S, 4×t4 + t27 (1207/1214–1216/1002).
2. **compose-timeout shipped 09-01 actually worked.** Midnight 09-02 both longs composed for real (`used_fallback=false`, 11 beats). ch2 published v1223. ch1’s copy is trapped in REVIEW because `skip_gate=false`.
3. **Operator cut cadence and paused** after the 09-02 14:01 ch2 long. 09-03 00:00 still rendered (pause was not holding yet) and every job hit BlockingIOError. 09-04 00:00 did not render (pause holding). Autofill still ran today.
4. **Winners (mature):** ch2 t28 27.1 avg views @ 60.1% avp / 0 subs; t4 20.2 avg + **5 lifetime subs**; t27 19.8 avg / 43.6 avp / **0 subs**. ch1 t26 18.8 avg / 64.8 avp / 0 subs; t15 18.1 avg / 48.3 avp / **0 subs on 66 measured**; t7 15.0 avg / **68.3 avp**; t6 15.0 avg + **2 lifetime subs**; **t5 long 2 lifetime subs** (v96 + v960) at 2.1 avg views. Conversion still lives on t4 (ch2) and t6/t5 (ch1), not on the Copilot-credits wedge — except v1190 is the first t15 with a real **search** mix (55 of 70 views).
5. **Hypotheses (for the first unpaused run, not acted on):** (a) ch2 14:00 today does **not** publish v1217 while `scheduler_paused=true`; if it does, the publish gate is leaky. (b) The next unpaused midnight burst will FAIL the same 10 requeue-window videos again unless BlockingIOError is classified transient or the FD/pipe is fixed. (c) Approving v1213 is the only way ch1 ships a long from the current bank.

`queue-plan-mix` (shipped 08-24, `6005571`): 11d+. Live noon queues are **empty** both channels (`GET /api/videos/queue-plan?channel_id=` → `{}`). Harder case (long behind lower-id shorts with budget remaining) still never appeared. Leave `running`.

## Acted (channels)

**None.** `scheduler_paused: true`. No requeue / retry / delete / generate / displace / produce / weight / adopt / BGM generate.

- **Trends:** read-only. #73 Qwen3.8 still score 90 adopted/parked t33. #86 Ornith 1.5 still **watching** score 83. No new persist. Live 409 check **not** run (would be an acting POST).
- **Seeding kit:** `run/seeding/2026-09-04.md` (operator posts; agent does not). Features ch2 long v1223 + 09-01 t4s that missed the 09-01 kit, and ch1 search-led v1190.

## Engagement

**No golden-set re-render this run.** Scheduler paused; no prompt change to gate. 08-31 baseline still the last scored set (every high-priority lever **2**).

**Settled:**

- **compose-timeout (`9696f3e`, 09-01) → PROMOTED.** Predicted `used_fallback=false` on the next overnight longs. GET `/api/videos/1213`: `used_fallback=false`, `beat_count=11`, `beat_types=["hook","term","lst","stat","code","diagram","cmp","lst","diagram","cmp","cta"]`, `duration=254.06`, `composition_version=storyboard`, `last_attempt_at=2026-09-02T00:00:14`. GET `/api/videos/1223`: `used_fallback=false`, `beat_count=11`, published 2026-09-02T14:01:39. Pre-change both completed `used_fallback=true` / `beat_count=0`.
- **tts-transient (`730ce56`, 08-31) → PROMOTED (mechanism) with residual.** TTS did not recur on either 09-01 or 09-02 render of v1213. Predicted mix (ch1 1 long/day) did **not** recover: v1213 landed REVIEW both days (`skip_gate=false`). Keep the `NoAudioReceived` classification.

**Not shipped:** no lever change. Next unpaused run’s highest-leverage *pipeline* work is BlockingIOError (not a rubric prompt).

## Watch next run

1. Operator unpause yes/no. Until then, every run is observation-only. Quote `scheduler_paused` first.
2. ch1 pause yes/no (still the binding decision). **13 subs does not clear 15** — do not unfreeze. Keep t3 at 0.
3. Did v1217 publish at 14:00 today despite the pause? Quote publish-plan / `published_today`.
4. On unpause: requeue the 10 BlockingIOError rows (or fix the signature first); approve v1213; do **not** unpark t33–t42; ch1 drafts on parked topics will not auto-produce.
5. `queue-plan-mix` still wants a noon where a long is queued **behind lower-id shorts** AND `approved_longs==0` AND render budget is not already spent.
6. 70 remaining rejected t33–t42 rows (age >7d) — resume deleting at 10/run when unpaused.
7. BGM count stays ≥ min (30, above target 15).
8. Quote whether v948 EXT_URL grows past 9. Quote v1022 (4 / 47.8%). Quote v1032. Quote **v960**. Quote **v1190** views / YT_SEARCH (70 / 55 today) — first search-led t15; watch whether it ever converts.
9. v1216 (fallback short, published 09-01, age 68.5h) — mature tomorrow; if avp is a disaster vs sibling t4s, treat unparseable shorts as the next compose bug.
10. Autofill vs pause: if 06:30 generate keeps firing while paused, either gate `autofill_loop` on `scheduler_paused` or accept bench drift. Not touched today.

## Commit

`635beed` — report + seeding kit + experiment verdicts only. No app code.
