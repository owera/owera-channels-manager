# Growth Agent Run — 2026-07-11

> ⚠ **Needs operator (~5 min): re-consent BOTH channels.** PR #13 (youtube.force-ssl
> scope for comment posting) was merged and deployed this run — verified live: the
> consent URL from `POST /api/channels/1/oauth/start` now carries
> `youtube.force-ssl`. Until you reconnect ch1 and ch2 (recipe in memory:
> loopback helper, keep all scope boxes checked), every publish still fails its
> first-comment step with 403 (4 errors in the last 24h). After re-consent the
> already-shipped comment machinery (engagement seed + series playlist pointer)
> starts working with no further changes.
> Also: **first seeding kit is ready** — `run/seeding/2026-07-11.md` (kit #1, ~10 min to post).

## Observed (key numbers)
| | ch1 Owera Software | ch2 Rodrigo Recio |
|---|---|---|
| subscribers | **9** (flat, 13+ days) | **55** (+1 vs 07-09; target 75 by 07-23) |
| oauth | connected ✓ | connected ✓ |
| published total | 135 | 118 |
| published today | 5 (5 short / **0 long** — directive-2 violation, root-caused + fixed below) | 6 rows incl. 00:28 UTC spillover; quota-day mix **4 short + 1 long ✓** (long 532) |
| pipeline | 12 queued / 36 approved (33s+3L) | 18 queued / 6 approved (5s+1L) |
| failed / stuck / review | 0 / 0 / 0 | 0 / 0 / 0 |
| board | 2.0d — at capacity | 3.6d — at capacity |
| quota | 8750 / 9000 (day's 5 uploads done) | 8716 / 9000 (same) |
| bgm pool | 20 / target 15 — healthy, not stale (grew this week) | (shared) |

**Traffic attribution (first run with it flowing — directive 1 checkpoint MET):**
all-time views by source from `traffic_json` (read via read-only DB query — the
API leaderboard does not expose it): ch1 **SHORTS-feed 1403 (80%)**, search 226
(13%), browse-ish (`YT_OTHER_PAGE`) 93, suggested (`RELATED_VIDEO`) 1, EXT_URL 2.
ch2 **SHORTS-feed 703 (74%)**, search 156 (16%), suggested 7, EXT_URL 7. The
algorithm IS distributing (shorts-feed dominant) — discovery works; **conversion
is the gap** (SUBSCRIBER source: 2 views ch1 / 7 ch2). Consistent with the
offensive thesis: shorts funnel exists, subscribe rationale is what's missing.

**Language integrity (directive 6): PASS** — all 21 ch2 videos published
07-08→07-11 have PT-BR titles (DB read-back).

## Triage
8 digest items, 2 marked needs-operator:
1. **`comment` 403 ×4 (recurring error signature) — root cause FIXED this run.**
   Merged PR #13 (`3da800a`, merge `4230e02`): `youtube.force-ssl` added to
   `CONSENT_SCOPES`. Deployed (restart + dashboard 200) and verified live: consent
   URL now includes the scope. Errors will continue until the operator re-consents
   (403 is a token-scope property) — hence the ⚠ block. Counts as code change 1/2.
2. **Quota near cap ×2 (`auto:false`):** 8750 & 8716 of 9000 with `daily_limit_hit:false`
   — that's the normal cost of 5 uploads+thumbnails+playlist inserts; the day's
   budget is fully published on both channels. No knob touched — monitor.
3. **Board overflow ×4 (topics 28/29 ch2, 30/31 ch1, `auto:true`, "produce or trim"):**
   deliberately left. ch2's "pending" is 16 already-queued videos (0 drafts — nothing
   to produce; render loop drains 5/day). ch1: producing more shorts would feed a
   36-video approved backlog that already means ~7 days publish latency — growing it
   makes every video staler. Root cause acted on instead: **ch1 `daily_render_budget`
   20 → 6** (`PATCH /api/channels/1`, read back `{"daily_render_budget": 6}`) — publish
   is quota-capped at 5/day, so rendering 20/day only built backlog (+15/day, 23
   rendered yesterday vs 5 publishable).
4. **Rejected 541** (pre-fix EN draft on ch2, `suggested_action:"leave"`): left; it's
   the language-fit experiment's tombstone.

## Monetization
Binding constraint everywhere: **subscribers** (needed % is by far highest).
| metric | ch1 now | lower-tier need | pct | ch2 now | lower-tier need | pct |
|---|---|---|---|---|---|---|
| subscribers | 9 | 500 | 1.8% | 55 | 500 | 11.0% |
| watch hours | 2.7 | 3000 | 0.1% | 2.5 | 3000 | 0.1% |
| shorts views | 1714 | 3M | 0.1% | 768 | 3M | 0.0% |

Subscribers binding → both of today's changes target `subscribers_gained`
(long-form daily slot; comment machinery unblock).

## Learn
- **Long-form converts, and FIFO was structurally suppressing it:** of the 5 videos
  that ever gained a subscriber, 4 are long/deep-technical. ch1 had 3 approved
  longs that could NEVER publish — `_next_approved` is FIFO by `approved_at` and 33
  shorts sit ahead of them. Today's 5/5-shorts day wasn't bad luck; it was the
  selection rule. → fixed (Engagement below).
- **Settled: language-fit `0911bed` → PROMOTED** (72h). 21/21 ch2 published videos
  PT-BR; the one EN straggler (541) was generated pre-fix and died in `rejected`.
  Retention-lift prediction stays open until t10/t4 post-fix cohorts mature.
- Leaders unchanged (ch1 t3 + t15; ch2 t4 concrete-hardware; t2 long "GPU vs CPU"
  50 views / 47% avp supports the long-anchor bet). No new mature cohort since
  yesterday → **no weight changes** (t3=3, t15=2, t5=2L on ch1; t4=3, t2=2L on ch2 —
  directive-2 long anchors ≥2 present on both ✓).
- Trends: both boards at capacity → adoption impossible, research skipped (trend 25
  ch2 / trend 27 ch1 still first in line when capacity opens ~07-13).

## Engagement
**Baseline** (golden set, 4/4 clean, `visible=True`, no fallback):
| Lever | Score | Evidence |
|---|---|---|
| R1 hook | 2 | Pain-first at t=0 ×4 ("Right answer, wrong rank"; "Sua RAG responde tudo errado"). |
| R8 thumb/title | 2 | 0/4 title echo, real curiosity gaps. |
| R2 info gain | 2 | Code beat in 4/4 + stat/diagram/cmp, varied mixes (rule 2b, 4th day). |
| R3 clarity | 2 | Indentation re-check `13be882` **3rd clean day** (ch2-concept `overlap=50)` indented under `split_by_section(doc,`); nothing clipped. Settles ~07-12. |
| R4 pacing | 1 | `93ec473` holding: worst beats 1.69s / 8.14s vs 1.02s / 8.7s pre-fix; tail floor visible (quote 1.88 / cta 2.0 twice). Stays `running`, evidence positive. |
| **R7 CTA** | **1 — weakest** | All 4 CTAs are *action* CTAs ("Try it → Add reranking today", "Use MCP → uma ponte pra tudo"). Zero follow/subscribe rationale, no series pointer — exactly the untested lever directive 4 prioritizes, and its signal IS `subscribers_gained`. |
| R5 / R6 / R10 / R11 | 2 | Open loops; quotable closes ("Retrieval finds the neighborhood. Reranking finds the door."); per-topic accents (ch1 green / ch2 red); format fit. |

**Target chosen: daily-mix publish fix (non-prompt, fallback clause) — SHIPPED `fa354c9`.**
Why not R7 today: the 2-change cap was consumed by the PR #13 merge (triage
root-cause, sanctioned by step 1.5) + one more, and an R7 prompt change would
render into queues with ~5–7 days of publish latency — its live effect is delayed
by the exact backlog problem the daily-mix fix addresses. The fix acts tomorrow
morning on 3 already-approved longs and targets the same signal (subscribers).
**State-machine trace (required):** change touches only WHICH `approved` row
`publish_loop._next_approved` returns — no status transition altered. Consumers
unchanged: `render_loop._submit_new` still takes QUEUED, publish_loop still takes
APPROVED→PUBLISHING→PUBLISHED. Until a long-form has published in the current
quota day (new `quota.published_long_today`, same Pacific-midnight boundary as
budgets), the oldest approved long (join on Topic.content_format) is returned;
otherwise plain FIFO — ch2's behavior today provably identical.
**Verification (synchronous, pre-commit):** `_next_approved(ch1)` flipped FIFO
short 545 → long 582 (`video_path` present ✓); ch2 (long 532 already out today)
returned the same 586 as before. `verify_publish` 22/22, `verify_storyboard`
40/40, import OK, restart + dashboard 200. Live publish can't fire until the
budget resets (~00:00 UTC) — logged as watch item, per playbook.
**Pre-registered for tomorrow: R7.** Baseline score 1 with frame evidence above;
the change should give the cta beat + script close ONE follow reason tied to the
delivered value / next episode ("Parte N amanhã") per directives 3+4. Predicted
signal: `subscribers_gained`.

## Actions taken (all logged in /api/runs)
- `PATCH /api/channels/1 {"daily_render_budget": 6}` (was 20) — verified in response.
- `POST /api/topics/2/generate {"count":3}` → **refused server-side**: "board at
  capacity (horizon reached)" — the capacity gate is enforced by the app, so ch2's
  long-form bench can't be refilled today (see watch list).
- Merged PR #13 → `4230e02`; shipped `fa354c9`; restarted manager; pushed to main.
- Settled `0911bed` (promoted), appended `daily-mix` experiment to `run/experiments.jsonl`.
- Wrote seeding kit #1 `run/seeding/2026-07-11.md` (TabNews + r/brdev + LinkedIn-PT
  for ch2 video 532; dev.to + LinkedIn-EN + r/LocalLLaMA comment for ch1 video 540;
  3 adjacent-channel comment drafts). EXT_URL baseline for attribution: ch1=2, ch2=7.

## Watch next run
1. **First ch1 publish after ~00:00 UTC 07-12 must be video 582 (long, "LangChain
   vs CrewAI: Which One Breaks First…")** — the daily-mix fix's first live proof.
2. Did the operator re-consent? If yes, `comment` 403s stop and first comments
   appear on new publishes; if not, re-lead with the ⚠ block.
3. **ch2 long-form supply gap ~07-13/14:** last approved ch2 long publishes 07-12;
   board opens (~pending <10) around 07-13 → generate ideas on t2 FIRST and produce
   a long immediately (directive 2 override), before any shorts/trends.
4. R7 CTA change (pre-registered above) — top candidate for tomorrow's gated change.
5. `13be882` settles 07-12 (3 clean render-days banked); `93ec473` settles 07-13.
6. ch1 approved-shorts backlog (33): with render budget now 6, it should stop
   growing; if it doesn't shrink by ~07-15, consider a deliberate drain plan.
