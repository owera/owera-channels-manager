# Growth agent — 2026-08-07

## ⚠ Needs operator

1. **ch1 pause decision (hard review overdue since 08-01).** Still **10 subscribers** vs the 15-sub bar. Lifetime conversion near zero despite real Shorts distribution (v785 = 173 views @ high avp loops; still 0 subs on that video). Recommendation stands: **PAUSE ch1** (or explicitly override with a new bar). Agent continues **zero ch1 strategy moves** until you decide.
2. **ch1 approved buffer is 3× t5 longs only** (879–881) after today's 1L+4S publish drained the stranded t15 shorts. Fine for mix if tonight's render fills shorts from queued t6 (883–887); watch that shorts don't starve under long-heavy approved weight.
3. **ch2 metadata backfill** (`db7a254` plan) still awaits `--apply` if still desired.
4. **BGM pool path** remains external (`MoneyPrinterTurbo/resource/songs`) — 10 tracks healthy today; wipe risk still escalates to silent renders.

---

## Observed

- **Now:** 2026-08-07 ~12:00–12:15 UTC. Kill switch off, `scheduler_paused: false`, tree clean at start.
- **ch1:** published **5/5** today, quota **8750/9000**. Mix = **1 long + 4 shorts** (v878 t5 long first, then v871 t6, v826–828 t15). **Early positive signal for #26 daily-mix-cap** (shipped 08-06).
- **ch2:** published_today **0** (window opens **14:00 UTC** / 11:00 America/Sao_Paulo). rendered_today **5**, approved **11 shorts / 0 longs**, queued includes **v882 long (t2)** + 4 t27 shorts. Long cannot render until budget resets — root cause fixed this run (see Engagement).
- **BGM pool:** 10 / min 5 / target 15, `is_low:false`. No music_gen errors.
- **Boards:** both `at_capacity:true` (10 pending / horizon 2.0). No failed / stuck / oauth / starved / bgm_pool_low.
- **Language (ch2):** recent titles PT-BR ✓ (no EN leak observed on 859–865 cohort titles).
- **traffic_json on video-analytics API items:** all `null` this read (DB still holds historical `videometric.traffic_json` rows — API surface not joining latest capture onto leaderboard items). Discovery signal thin in this surface; do not invent CTR.
- **ch1 subs-per-video:** still essentially 0 on post-CTA volume; lifetime sub-gainers remain thin historical set (e.g. v558 MCP +1). **ch1 = 10 subs. ch2 = 61 subs** (unchanged vs 08-06).

### Publish log (settle evidence)

| day | ch1 | ch2 |
|-----|-----|-----|
| 08-01 | 5 (1L+4S) | 5 |
| 08-02 | 5 (0L+5S) | 5 |
| 08-03 | **5 (5L+0S)** | 5 |
| 08-04 | 5 (0L+5S) | 5 |
| 08-05 | **5 (5L+0S)** | 5 |
| 08-06 | 5 (1L+4S) | 5 |
| 08-07 | **5 (1L+4S)** | pending window (0 at observe; approved all-short) |

## Monetization

| metric | ch1 current | ch1 needed | ch1 % | ch2 current | ch2 needed | ch2 % |
|---|---|---|---|---|---|---|
| subscribers | 10 | 490 | 2.0% | 61 | 439 | 12.2% |
| watch hours | 6.2 | 2993.8 | 0.2% | 10.5 | 2989.5 | 0.4% |
| Shorts views | 3339 | 2,996,661 | 0.1% | 3376 | 2,996,624 | 0.1% |

**Binding constraint both channels: subscribers** (lower tier). No milestone crossed. Prefer analytics (ch2 local-AI hardware shorts; ch1 frozen) over pure milestone math.

## Triage

Digest: 4 issues (3 rejected leave, 1 quota monitor). `summary.clean:false` only for those.

| Issue | Action | Proof |
|---|---|---|
| rejected 839–841 (disp. 08-03, age ~132h) | leave (auto) | age &lt; 7d; subjects retained |
| quota ch1 8750/9000 | monitor only | full 5-publish day by design (5×1750) |
| failed / stuck / oauth / starved / bgm_low / music_gen | none | clean |
| ch2 no approved long at noon | **code fix** (render long-first) — cannot re-render live today (budget full) | see Engagement; watch next run |

## Learned

1. **#26 daily-mix-cap — early live signal (not settled).** ch1 08-07 = 1L+4S (not 5L). Ship was 08-06 ~20:45 UTC; settle ≥08-09 with multi-day mix log. Leave `running`.
2. **#25 trend-displacement — still RUNNING.** Cohort 859/860/861 age ~45h, views still **0** (analytics lag + &lt;72h). Do not invent metrics. Settle ≥08-08 once mature.
3. **Render/submit gap diagnosed:** `_auto_produce` already long-prioritizes into QUEUED when approved longs = 0, but `_submit_new` was pure FIFO by position/id — lower-id shorts burned the 5/day budget first (ch2 live shape today).
4. **Winners (mature):** ch2 t27 32.3 avg views (still best short genre on measured set); t4 26.6; t28 25.6 @ 60% avp. ch1 t18 MCP 36.2 avg views + only recent sub-gainer; t15 high avp loops, 0 subs on volume.
5. **Hypotheses this run:** (a) long-first submit restores ch2 1L when a long is queued the same render day; (b) t27 859–865 shows SHORTS + higher avg_views vs same-window t4 once mature; (c) operator pause remains the binding ch1 move.

## Acted (channels)

- **Trends logged (no adopt/displace — boards at capacity; t27 flood awaiting metrics):**
  - #51 ch2 score **84** watch — “Claude Code + Ollama local (Qwen 3.5 backend)” — defer until #25 mature.
  - #52 ch2 score **72** watch — “VRAM-tier local LLM pick guide 2026” — below 75 bar / t27 already fed.
  - #53 ch1 score **55** reject — Copilot credit series — ch1 strategy frozen under pause recommendation.
- **No weight changes, no generates, no produces, no BGM generate** — both boards at capacity; BGM healthy (10≥5); ch1 frozen; ch2 long already queued (v882) for next budget day under the new submit order.
- **Seeding kit:** `run/seeding/2026-08-07.md` (operator posts; agent does not).

## Engagement

**Baseline `baseline-0807`** — 4/4 clean, `visible=True`, none `used_fallback`. Scores from frames + scripts:

| Lever | score | evidence |
|---|---|---|
| R1 hook (P1) | 2 | Pain-first on-screen: “RAG keeps grabbing junk” / “Your AI agent forgets you” / “Escrevendo a mesma ponte de novo” / “Sua RAG responde bobagem” |
| R8 thumb/title (P2) | 2 | Gaps hold (e.g. title rerank vs hook “Wrong answers, faster than ever”; PT hook ≠ title echo) |
| R2 info gain (P3) | 2 | Varied mixes; code (FlagReranker / @server.tool) + diagrams sequential |
| R6 payoff (P4) | 2 | Quotable closes (“Retrieval gets you close; reranking gets you right”) |
| R5 retention (P5) | 2 | Open loops early on all 4 scripts |
| R3 clarity (P6) | 2 | Code indented, diagrams readable, nothing clipped on read frames |
| R4 pacing (P7) | 2 | Hook at t=0; no flash beats observed |
| R7 CTA (P8) | 2 | Follow/Siga + named next value 4/4 (“Halve reranker latency tomorrow” / “Building the memory layer” / “Tamanho de chunk ideal”) |

**All-2 baseline (6th consecutive judged day) → non-engagement code path (fallback clause).**

### Shipped: render long-first when approved long buffer empty (`render_loop._queued_candidates` / `_submit_new`)

- **Files:** `app/services/render_loop.py`, `tests/verify_render.py`
- **Change:** when a channel has **0 approved longs**, order QUEUED candidates **longs first**, then remaining by position/id. If a long is already approved, keep FIFO. Mirrors `_auto_produce`'s long-buffer guarantee at the *submit* stage.
- **Lifecycle map:** only changes *which* `QUEUED` row `_submit_new` starts next — still QUEUED → rendering → rendered → (review\|approved). Re-render still QUEUED; re-publish still APPROVED. Does not invent paths.
- **Gate (synchronous):**
  - Crafted: lower-id shorts + higher-id long, budget 1, no approved long → **long RENDERING**, shorts stay QUEUED.
  - Banked approved long → short with lower id still wins (no force second long).
  - Isolated `_queued_candidates` order: `['long','s1','s2']`.
  - `tests/verify_render.py` **75/75** (was 70; +5 long-first checks).
  - `tests/verify_storyboard.py` **40/40**.
  - `import app.main` ok; `launchctl kickstart` manager; `GET /api/dashboard` **200**.
- **Live effect watch next run:** ch2 should render **v882 long before** remaining t27 shorts once `rendered_today` resets, then publish 1L+4S when the window opens (if long is approved pre-window). **Cannot verify live today** — ch2 render budget already 5/5 at observe.

**No prompt/rubric lever change this run** — shipping unproven polish on an all-2 baseline would violate the gate.

### Experiments settled / opened

| id | lever | verdict |
|---|---|---|
| #25 08-03 | trend-displacement | **RUNNING** — 859–861 still 0 views / &lt;72h; settle ≥08-08 |
| #26 08-06 | daily-mix-cap | **RUNNING** — early ch1 08-07 = 1L+4S; settle ≥08-09 |
| #27 08-07 | render-long-first | **RUNNING** — shipped this run; settle ~08-10 on ch2 mix |

## Watch next run

1. Operator ch1 pause yes/no.
2. ch2 08-07 publish composition after 14:00 UTC (likely 5S today — long still queued; fix helps **tomorrow**).
3. ch2 overnight: does v882 long render before t27 shorts under new order?
4. t27 859–865: views, avp, traffic once mature — settle #25.
5. ch1 multi-day mix under #26 (not just one good day).
6. BGM count stays ≥ min.

## Commit

`5ebe7e1` — Growth agent 2026-08-07: render long-first when approved long buffer empty
