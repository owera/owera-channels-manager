# Growth agent — 2026-08-15

**Status: skipped (supervisor-completed).** At 09:00:04 the wrapper saw `run/.growth-agent.lock` still held from the 08-14 abort and exited 0 without running. Supervisor verified synchronously at ~13:02 UTC / 10:02 −03.

## Supervisor finished

- Removed the stale lock (`mkdir` dir, no `growth-agent` process). Next 09:00 can start.
- Wrote this file + `2026-08-14.md` for continuity.
- Did **not** re-run the playbook, requeue videos, settle experiments, or change topics.

## Live state (~13:02 UTC)

| | Owera Software (ch1) | Rodrigo Recio (ch2) |
|---|---|---|
| Subs | 10 | 63 |
| oauth | connected | connected |
| published today | **5/5** | **0** (window 14:00 UTC — not blocked) |
| rendered today | 5 | 5 |
| approved | 1L+1S | 1L+11S |
| draft | 2L+8S (at_capacity) | 2L+8S (at_capacity) |
| queued / rendering / publishing | 0 / 0 / 0 | 0 / 0 / 0 |
| failed | 12 | 4 |
| quota | 8999/9000 | 249/9000 |

- No stuck `publishing` / `rendering`. No oauth errors. `pipeline_starved` empty. `scheduler_paused: false`. BGM 10/5/15, not low.
- ch1 quota 8999/9000 is a full 5-publish day (monitor only).
- `error_runs_24h`: 5× publish “upload stalled (no progress in 120s)”, 2× playlist_add timeout, 1× thumbnail timeout — all recovered enough that ch1 still hit 5/5.
- Manager pid serving `:7070` started **Sat Aug 15 04:38:23 −03**, after `43cfe3d` (04:37:53). Dashboard 200. Tree clean, `main` == `origin/main` before this report commit.
- `PYTHONPATH=. .venv/bin/python tests/verify_storyboard.py` → **ALL 207 CHECKS PASSED**.

## Failed (flag only — next growth run)

All `suggested_action: requeue`, `retry_count=0`:

- ch1 t18: v942, v944, v946 (`render timed out`, 85h)
- ch1 t5: v959–v963 (60.8h; v962 = Anthropic disconnect, transient)
- ch1 t6: v973, v975–v977 (`render timed out`, 30.7h)
- ch2 t2: v955–v958 (60.8h; v956 = litellm/Anthropic 600s timeout, transient)

## Experiments still `running` ≥72h (settle next growth run)

| # | shipped | age at observe | lever |
|---|---------|----------------|--------|
| **#25** | 2026-08-03 | **12d** | trend-displacement |
| **#31** | 2026-08-11 | **4d** | auto-produce-long-reserve |
| **#32** | 2026-08-12 | **72h** | autofill-long-reserve |

#33 (autofill-long-mix-cap, 2026-08-13) is ~48h — not due yet.

## ⚠ Needs operator

1. **PAUSE ch1** still the standing recommendation since the 08-01 hard review (10 subs vs the 15-sub bar). Supervisor did not pause it.
2. ch2 metadata backfill (`db7a254` plan) still awaits `--apply` if still desired.
3. BGM path still external (`MoneyPrinterTurbo/resource/songs`).
4. If you want today’s growth loop (rubric + settle + triage) instead of waiting for 09:00 08-16, kick `com.owera.channels-manager`’s agent wrapper manually — lock is clear.

## Watch next run

- ch2 14:00 UTC 1L+4S from the 1L+11S approved bank.
- Requeue / retry the 16 failed renders now that `43cfe3d` is live.
- Settle #25 / #31 / #32 (and score a rubric baseline — last scored day is 08-13).
