# Growth agent — 2026-08-22

## ⚠ Needs operator

1. **PAUSE ch1 (hard review overdue since 08-01).** Still **11 subscribers** vs the 15-sub bar (unchanged vs 08-19…08-21). Lifetime video-attributed converters remain **four** (v558 MCP +1, v367 t6 +1, v43 t1 +1, v96 t5 +1). Post-CTA / post-wedge volume still ~0 attributed subs (v785 ≈174 views / 148% avp / SHORTS=164 → 0). **Recommendation stands: PAUSE ch1** (or explicitly override with a new bar). Agent continues **zero ch1 strategy moves** (no weight/trend adopt/generate for growth). Pipeline hygiene only.
2. **ch2 metadata backfill** (`db7a254` plan) still awaits `--apply` if still desired.
3. **BGM pool path** remains external (`MoneyPrinterTurbo/resource/songs`). Count 20 (target 15); wipe risk still escalates to silent renders.

---

## Observed

- **Now:** 2026-08-22 ~12:00 UTC. Kill switch off, `scheduler_paused: false`, tree clean at start, `main` == `origin/main`. Last full growth run: 2026-08-21.
- **ch1:** published **5/5** today, quota **8750/9000** (`daily_limit_hit: false`). Mix = **1 long + 4 shorts** (v963 t5 long first @ 07:01 `_vZNWDzXMSM`, then v1031 t6, v1033 t6, v1039 t6, v1040 t6). **Seventeenth consecutive 1L+4S day** (08-06…08-22).
- **ch2:** published_today **0** at observe (window opens **14:00 UTC**). rendered_today **5**. Approved = **1 long + 11 shorts** (v1038 t2 long + v1028 t4 + 9×t27 995–1004 + leftover v709 t10 from July). **08-21 did publish 1L+4S** (v982 t2 long first @ 14:01 `Qfy-uufoNKQ`, then v1024/1025/1026/1027 **all t4**) — yesterday’s 4×t4 prediction held. That is also the first settle signal for `publish-plan-mix` (plan and live picker agreed; still <72h so left `running`).
- **ch2 subscribers: 64** (unchanged vs 08-21). Video-attributed `subscribers_gained` still the same six converters (v542/524/810/421/684/315). The 08-21 63→64 move is still unattributed — no new leaderboard `subscribers_gained`.
- **ch1 subscribers: 11** (unchanged vs 08-19…08-21). Subs-per-video on the mature leaderboard: **still 4 lifetime +1s**, none on the post-07-12 CTA / post-07-13 wedge cohort.
- **BGM pool:** 20 / min 5 / target 15 (`is_low: false`). No `music_gen` errors. No `error_runs_24h`. Last generate was 2026-08-20 — two days, not several; no refresh this run.
- **Boards (API `by_format` at observe):**
  - ch1 draft `{long:1, short:5}` · queued `{long:1, short:3}` · approved `{long:0, short:2}`
  - ch2 draft `{long:1, short:4}` · queued `{long:0, short:5}` · approved `{long:1, short:11}`
  - Both `at_capacity: true`, `days_of_inventory: 2.0`.
- **Language (ch2):** 08-21 titles all PT-BR ✓ (982 demora 3 segundos; 1024 DDR4; 1025 tamanho do arquivo; 1026 contexto longo; 1027 GPU dedicada). No EN leak.
- **traffic_json (via leaderboard API, no DB dive):** v542 SHORTS=385; v785 SHORTS=164 + YT_SEARCH=6; v558 SHORTS=104 + YT_SEARCH=12; v948 SHORTS=64 + **EXT_URL=9** (unchanged vs 08-17…08-21) + NOTIFICATION=3; v884 YT_SEARCH=22 + SHORTS=77. SHORTS / YT_SEARCH dominate. **No BROWSE / SUGGESTED keys** on the top samples. No invented CTR.
- **t18 MCP pending rose 0→4** overnight (autofill 19336). Yesterday’s watch item held. Still no manual generate (ch1 freeze).
- **Live mix risk this run fixed:** ch2 queued = 5 t4 shorts / 0 longs, with long draft v1052 sitting. After v1038 publishes at 14:00, `approved_longs` hits 0 and midnight headroom stays 0 — pre-fix that ships 0L+5S tomorrow. Proof of the shape: `GET /api/videos?status=queued` ch2 = 1029/1034–1037 all t4; draft v1052 t2; approved long = 1038 only.

### Publish log (settle evidence)

| day | ch1 | ch2 |
|-----|-----|-----|
| 08-16 | 5 (1L+4S) long 971 first | 5 (1L+4S) long 981 first + 4×t4 |
| 08-17 | 5 (1L+4S) long 972 first | 5 (1L+4S) long 955 first + 4×t4 |
| 08-18 | 5 (1L+4S) long 962 first + 3×t18 + t6 | **5 (1L+4S)** long 957 first + 4×t4 |
| 08-19 | **5 (1L+4S)** long 959 first + t18 + 3×t6 | **5 (1L+4S)** long 956 first + 3×t4 + t27 |
| 08-20 | **5 (1L+4S)** long 961 first + t18 + 3×t6 | **5 (1L+4S)** long 958 first + **4×t4** (1020–1023) |
| 08-21 | **5 (1L+4S)** long 960 first + t18 + 3×t6 | **5 (1L+4S)** long 982 first + **4×t4** (1024–1027) |
| 08-22 | **5 (1L+4S)** long 963 first + 4×t6 | pending window (0 at observe; 1L+11S banked, v1038 leading then v1028 t4) |

## Monetization

| metric | ch1 current | ch1 needed | ch1 % | ch2 current | ch2 needed | ch2 % |
|---|---|---|---|---|---|---|
| subscribers | 11 | 489 | 2.2% | 64 | 436 | 12.8% |
| watch hours | 7.5 | 2992.5 | 0.2% | 11.8 | 2988.2 | 0.4% |
| Shorts views | 4355 | 2,995,645 | 0.1% | 4253 | 2,995,747 | 0.1% |

**Binding constraint both channels: subscribers** (lower tier). No milestone crossed (no metric hit 100% for the first time). Prefer analytics (ch2 local-AI hardware shorts that convert; ch1 frozen) over pure milestone math.

## Triage

Digest at start: 1 issue (quota monitor only). `failed=[]`, `pipeline_starved` / stuck / oauth / bgm_pool_low / music_gen / `error_runs_24h` / `rejected` all empty. `summary.clean: false` only because of the quota row.

Timeout backlog remains empty (closed 08-20).

| Issue | Action | Proof |
|---|---|---|
| quota ch1 8750/9000 | monitor only | full 5-publish day (5×1750=8750); `daily_limit_hit: false` |
| failed / stuck / oauth / bgm_low / pipeline_starved / error_runs_24h / rejected | none | clean lists |

After-state board (API, unchanged by triage): ch1 queued `{long:1, short:3}` · approved `{long:0, short:2}` (1013/1014 t7); ch2 approved `{long:1, short:11}`. Failed list stays empty.

## Learned

1. **ch2 08-21 mix prediction held:** 1L+4S, long v982 first, **4×t4** (1024–1027). Weight-first is doing what it says. Today’s bank has 1 t4 approved (1028) then t27s — expect **1L + t4 + 3×t27** at 14:00 (only one t4 left in the approved pool; 5 more t4 are queued for overnight).
2. **ch2 still 64.** The 08-21 +1 did not reverse; still no video-attributed 7th converter on the leaderboard.
3. **Winners (mature):** ch2 t4 24.0 avg views + **4 lifetime subs** (still the only converter); t28 25.8 @ 59.5% avp / 0 subs; t27 20.8 avg / 43.7 avp / **0 subs** (v948 EXT_URL=9 + SHORTS=64 is still distribution without conversion). ch1 t18 MCP 14.5 avg + the only recent sub-gainer; t15 high-avp loops, 0 subs on 46 videos.
4. **t18 MCP bench refilled** (pend 0→4) without a manual generate. Freeze holds.
5. **ch2 overnight mix is one demote away from breaking.** 5 shorts queued + 0 longs + 1 long draft + today’s long about to leave the approved bank = the 08-07/08-11 residual when headroom is already 0. That is the pipeline-robustness fix this run (all-2 rubric → non-engagement path).
6. **Hypotheses this run:** (a) ch2 08-22 window publishes 1L+4S with v1038 leading then v1028 t4 then t27s; (b) after 14:00 a render tick demotes the newest queued t4 (1037) to draft, and midnight auto_produce queues v1052 — 08-23 publishes a t2 long first; (c) operator pause remains the binding ch1 move — 11 subs still does not clear the 15-sub bar.

`publish-plan-mix` (shipped 08-21, `de23128`): first live signal CONFIRMED (ch2 08-21 = v982 @ 14:01 then 4×t4, matching the post-restart plan). Still <72h — leave `running`, settle ~08-24.

## Acted (channels)

- **No requeue / retry / delete.** Failed list already empty. Cap not hit.
- **No weight changes.** t4=4 / t27=3 / t2=2 already match conversion. ch1 frozen (t15 remains w=0 as part of the freeze; not revived).
- **Trends logged (no adopt/displace — boards at capacity; every ch2 draft is w2+ so a new w1 topic cannot displace; ch1 frozen):**
  - **#78** ch2 score **88** (was 86) watch refresh — “Nemotron 3.5 Lightning 30B-A3B local (Ollama)”. Ollama library **127.6K** downloads (was 80.7K 08-21, 40.1K 08-20). Proof: `POST /api/trends` → id 78, status=watching.
  - **#81** ch2 score **81** watch **new** — “Ollama 0.32.15 cuts time-to-first-token in half” (TTFT ~995ms→524ms). First-party runtime number that fits t4 better than a new model topic. Proof: id 81, status=watching.
  - **#73** ch2 score **82** watch refresh — “Qwen3.8-27B local GGUF”. Ollama **573.9K** pulls. Proof: id 73, status=watching.
  - **#80** ch2 score **80** watch refresh — “Ollama default num_ctx silently truncates 128K models”. Proof: id 80, status=watching.
  - **#76** ch2 score **72** (was 74) watch refresh — “gpt-oss 20B/120B local MXFP4”. Proof: id 76, status=watching.
  - **#79** ch2 score **64** (was 66) watch refresh — “Ornith 1.0 9B”. Proof: id 79, status=watching.
  - **#77** ch1 score **70** reject refresh — “Copilot Max $100 / $200 credits (Aug 2026)” — *is* the wedge, rejected because ch1 is frozen. Proof: id 77, status=rejected.
- **No produce / no generate / no displace.** t4 bench has 5 queued + 0 drafts of t4 (drafts are 4×t27 + 1×t2). Auto_produce owns the overnight mix after the rebalance fix.
- **No BGM generate** — pool 20, last refresh 08-20; not stale.
- **Seeding kit:** `run/seeding/2026-08-22.md` (operator posts; agent does not).

## Engagement

**Baseline `baseline`** — 4/4 clean, `visible=True`, none `used_fallback`. ~239s. Scores from frames + scripts + beat timings:

| Lever | score | evidence |
|---|---|---|
| R1 hook (P1) | 2 | Pain-first on frame + opener ≤10w: “Your RAG returns garbage, and you blame the model.” / “Your AI agent has amnesia.” / “Cansado de escrever a mesma integração de novo?” / “Sua RAG responde qualquer coisa, menos o que você perguntou.” Hook cards: “Your RAG returns garbage” / “Your AI agent has amnesia” / “Chega de integração repetida” / “Sua RAG responde tudo errado”. Hook at t=0 all 4. |
| R8 thumb/title (P2) | 2 | Gaps vs titles (hook “Wrong answers wasting your queries” vs title “Fix Your Slow RAG…”; “Goldfish memory strikes again” vs “Why Your AI Agent Forgets…”; PT “Uma conexão substitui cinquenta” / “Você quebra tudo errado”). Zero title-word echo. |
| R2 info gain (P3) | 2 | Varied mixes (ch1-code: hook/stmt/stat/term/lst/code/cmp/cta + `scores = ce.predict(pairs)`; ch1-concept: list + cmp Model alone vs External system + `db.save(user, msg)`; ch2-code fanout Servidor MCP → Ferramentas/Bancos/Arquivos + `srv.tool(buscar_db)`; ch2-concept diagram Chunk quebrado → Busca ruim → IA alucina + `chunk = titulo + "\\n"`). |
| R6 payoff (P4) | 2 | Quotable closes (“Stop stuffing more chunks in. Rank them better.” / “An AI without memory isn't an assistant—it's a stranger you re-hire daily.” / “Padroniza uma vez, integra pra sempre.” / “Chunk ruim entra, resposta ruim sai. Simples assim.”) |
| R5 retention (P5) | 2 | Open loops early on all 4 (“It's not the model.” / “Here's the truth: it's not broken, it's built that way.” / “O MCP muda esse jogo de vez.” / “O problema não é o modelo, é o seu chunking.”). |
| R3 clarity (P6) | 2 | Code legible; CMP stacked + VS in flow (More chunks vs Rank better; Model alone vs External system; Sem MCP vs Com MCP); fanout on ch2-code truthful. List midpoint frames show all 3 items (ch1-code 5.59s; ch1-concept 7.05s; ch2-concept 4.87s). |
| R4 pacing (P7) | 2 | Hook at t=0. Min beat 1.80s (ch1-concept / ch2-concept term). **Known residual returned:** ch1-code code **8.887s DRAG**. Prompt-level R4 is exhausted (07-13/07-16 verdicts) — do not ship another R4 prompt rule. Drift still owns the long holds. |
| R7 CTA (P8) | 2 | Follow/Siga + named next value 4/4 (“Cache reranking for scale” / “Build the memory layer” / “Amanhã: seu 1º servidor MCP” / “Tamanho de chunk ideal amanhã”). Spoken ask present on all 4 scripts. |

**All-2 baseline (19th consecutive judged day) → non-engagement path (fallback clause).** No prompt change. The only soft spot is the known R4 residual (8.9s code hold), which prior verdicts already closed at the prompt layer. Recurring render-timeouts are gone (failed=[]). Shipping unproven polish on an all-2 set would violate the gate.

**Shipped instead (pipeline-robustness):** `_rebalance_queued_mix` now frees one short slot when `approved_longs==0` and the queue is full of shorts so a waiting long draft can produce.

Lifecycle (written before the change, as required):

```
draft ─produce→ queued ─render_loop._submit_new→ rendering ─render_loop._advance→
   rendered → (review | approved)
approved ─publish_loop→ publishing → published
```

Rebalance sets QUEUED → DRAFT (undo of produce; the short is not deleted). `_auto_produce` (same tick, after rebalance) then DRAFT → QUEUED on the long. `_submit_new` with `approved_longs==0` prefers that queued long. Publish still only consumes APPROVED.

| | before (crafted 08-22 midnight shape) | after |
|---|---|---|
| queued | 5 shorts, 0 longs | **1 long + 4 shorts** |
| long draft | waiting | **QUEUED** |
| newest short | QUEUED | **DRAFT** (subject retained) |

Proof: `PYTHONPATH=. .venv/bin/python tests/verify_render.py` → **117/117** (new cases: demote newest short; auto_produce fills with the long; no-op if a long is in flight / no long draft / headroom already exists). `uv run python -c "import app.main"` ok. Manager restarted (`launchctl kickstart`); `GET /api/dashboard` = 200. Live demote is **not** claimed — at observe `approved_longs=1` (v1038 still banked), so the new branch does not fire until after 14:00. Watch next run.

### Experiments settled / opened

- Settled: none (publish-plan-mix shipped 08-21, <72h; first signal confirmed, leave running).
- Opened: `rebalance-short-for-long` commit `d2d158f`, status=running. Settle ~08-25 from the 08-23 publish log: ch2 14:00 slot should be a t2 long (v1052 if overnight produce picks it).

## Watch next run

1. Operator ch1 pause yes/no (still the binding decision). 11 subs does not clear the 15-sub bar — do not unfreeze.
2. ch2 08-22 publish composition after 14:00 UTC — expect **1L+4S** with t2 long v1038 leading then **v1028 t4** then t27s (only 1 t4 approved). This is also more evidence for `publish-plan-mix`.
3. After 14:00, a render tick should demote the newest queued t4 (**v1037**) to draft (`rebalance: demoted queued short → draft so a long can fill the 1L mix`). Midnight auto_produce should queue **v1052** t2. 08-23 first publish should be that long. If v1037 is still QUEUED at tomorrow’s observe with 0 approved longs, the fix did not take (escalate).
4. ch1 noon approved shorts=2 (t7 leftover) is thin; queued already has 1 long + 3 t6. Auto_produce after midnight should refill without a manual produce.
5. Failed list stays empty.
6. BGM count stays ≥ min (20, above target 15). Next refresh only if stale again *and* renders landed.
7. #78 Nemotron (88, 127.6K pulls) / #81 Ollama TTFT: adopt only as a t27/t4 episode via displacement if a w1-or-lower draft exists — not a new topic while t27 converts 0.
8. Quote whether v948 EXT_URL grows past 9 (seeding signal) and **which video, if any, now shows `subscribers_gained` for the ch2 63→64 move**.

## Commit

`d2d158f` (rebalance dual). Report SHA pinned in the follow-up commit.
