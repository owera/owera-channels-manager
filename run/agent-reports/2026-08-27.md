# Growth agent — 2026-08-27

## ⚠ Needs operator

1. **PAUSE ch1 (hard review overdue since 08-01).** Still **13 subscribers** vs the 15-sub bar. Video-attributed converters remain **five** (v558 MCP, v367 t6, v43 t1, v96 t5, v1032 t6). The 12→13 move is still **unattributed**. **Recommendation stands: PAUSE ch1** (or explicitly override with a new bar). Agent continues **zero ch1 strategy moves** (no trend adopt / generate for growth). Hygiene only: re-parked t3 (was unparked w=2 at 15:48 UTC 08-26) and restored t5 long-anchor w=1→2.
2. **ch2 metadata backfill** (`db7a254` plan) still awaits `--apply` if still desired.
3. **BGM pool path** remains external (`MoneyPrinterTurbo/resource/songs`). Count 25 (target 15); wipe risk still escalates to silent renders.

---

## Observed

- **Now:** 2026-08-27 ~12:00 UTC. Kill switch off, `scheduler_paused: false`, tree clean at start, `main`. Last full growth run: 2026-08-26.
- **ch1:** published **5/5** today, quota **8750/9000** (`daily_limit_hit: false`). Mix = **1 long + 4 shorts** (v1068 t5 long first @ 07:00 `XVxt14baX4o`, then v1053 t7 leftover, then v1080/1081/1082 **3×t6**). **Twenty-second consecutive 1L+4S day** (08-06…08-27).
- **ch2:** published_today **0** at observe (window opens **14:00 UTC**). rendered_today **5**. **08-26 did publish 1L+4S** (v1073 t2 long first @ 14:01 `Z7DjWH1DGk4`, then v998/1000/1001/1003 **all t27**) — yesterday’s 1L+4×t27 prediction held.
- **ch2 subscribers: 64** (unchanged). Lifetime converters still **seven** (v542/524/810/421/684/315 + v1022). v1022 now 4 views / 47.82% avp / YT_SEARCH=4 / +1 (was 3 views / 65.1% / YT_SEARCH=3).
- **ch1 subscribers: 13.** Subs-per-video on the mature leaderboard: **still 5 lifetime +1s**. None on the post-07-13 Copilot-credits wedge (t15 still 0 subs on 46 videos). Do not invent which video converted 12→13.
- **BGM pool:** 25 (min 5 / target 15; `is_low: false`). No `music_gen` errors. Last generate 08-24; pool healthy, not refreshed this run.
- **Boards (API `by_format`):**
  - ch1 draft `{long:2, short:8}` · queued `{long:0, short:0}` · approved `{long:0, short:2}` (1072 t18 + 1083 t6)
  - ch2 draft `{long:1, short:9}` · queued `{long:0, short:0}` · approved `{long:1, short:11}` (v1164 t2 long leading)
  - Both `at_capacity: true`, `days_of_inventory: 2.0`.
- **Language (ch2):** 08-26 titles all PT-BR ✓ (1073 Batch vs tempo real; 998 70B sem 32GB; 1000 VRAM; 1001 Ollama descarrega; 1003 Q4). Leading approved long v1164 also PT-BR (“Por Que Seu Modelo de IA Fica Mais Lento Depois de Semanas em Produção”). Queued-then-rendered 1074–1077 t4 all PT-BR. No EN leak.
- **traffic_json (via leaderboard API, no DB dive):** v542 SHORTS=385; v785 SHORTS=164 + YT_SEARCH=6; v558 SHORTS=104 + YT_SEARCH=12; v948 SHORTS=64 + **EXT_URL=9** (unchanged vs 08-17…08-26) + NOTIFICATION=3; v884 YT_SEARCH=22 + SHORTS=77; v1022 YT_SEARCH=4; v1032 SHORTS=22 + YT_SEARCH=1. SHORTS / YT_SEARCH dominate. **No BROWSE / SUGGESTED keys** on the top samples. No invented CTR.
- **Overnight mix (operator-adjacent commit `a7a3c96` 02:18 -0300):** treat empty/"LONG" topic formats as shorts in `_next_approved` after the day's long is out. Latent on live topics (all canonical short/long). Not this agent's change.

### Publish log (settle evidence)

| day | ch1 | ch2 |
|-----|-----|-----|
| 08-20 | **5 (1L+4S)** long 961 first + t18 + 3×t6 | **5 (1L+4S)** long 958 first + **4×t4** (v1022 converted) |
| 08-21 | **5 (1L+4S)** long 960 first + t18 + 3×t6 | **5 (1L+4S)** long 982 first + **4×t4** |
| 08-22 | **5 (1L+4S)** long 963 first + 4×t6 | **5 (1L+4S)** long 1038 first + t4 1028 + 3×t27 |
| 08-23 | **5 (1L+4S)** long 999 first + 3×t6 + t18 | **5 (1L+4S)** long **1052** first + **4×t4** |
| 08-24 | **5 (1L+4S)** long 1005 first + 3×t18 + t6 | **5 (1L+4S)** long **1054** first + **4×t4** |
| 08-25 | **5 (1L+4S)** long 1056 first + 3×t6 + t18 | **5 (1L+4S)** long **1055** first + **4×t4** (1064–1067) |
| 08-26 | **5 (1L+4S)** long 1079 first + 2×t18 + 2×t7 | **5 (1L+4S)** long **1073** first + **4×t27** (998/1000/1001/1003) |
| 08-27 | **5 (1L+4S)** long 1068 first + t7 + 3×t6 | pending window (0 at observe; plan **1164 t2 long @14:00 then 1074–1077 all t4**) |

**Midnight 08-27 rebalance CONFIRMED** (the other half of `rebalance-headroom-zero`, already PROMOTED 08-26 on the 14:00 evidence): JobRun **21097** `rebalance: demoted queued short → draft so a long can fill the 1L mix` on **v1078** (t4, now draft); JobRun **21098** `auto-produced: draft queued to fill free render capacity` on **v1164** (t2 long). 1164 rendered after a litellm timeout retry (21126 error → 21131 success).

## Monetization

| metric | ch1 current | ch1 needed | ch1 % | ch2 current | ch2 needed | ch2 % |
|---|---|---|---|---|---|---|
| subscribers | 13 | 487 | 2.6% | 64 | 436 | 12.8% |
| watch hours | 7.7 | 2992.3 | 0.3% | 12.3 | 2987.7 | 0.4% |
| Shorts views | 4509 | 2,995,491 | 0.2% | 4460 | 2,995,540 | 0.1% |

**Binding constraint both channels: subscribers** (lower tier). No milestone crossed (no metric hit 100% for the first time). Prefer analytics (keep t4 as ch2 converter lead; ch1 frozen) over pure milestone math.

## Triage

Digest: **not clean** (`summary.clean: false`, `total_issues: 84`, `needs_operator: 1`). `failed=[]`, `pipeline_starved` / stuck / oauth / bgm_pool_low empty. Quota monitor + 80 rejected (flood, age 48.3h, `suggested_action: leave`).

| Issue | Action | Proof |
|---|---|---|
| 80 rejected t33–t42 (age 48.3h) | leave | `suggested_action: leave`; age < 7d. Do not delete. |
| quota ch1 8750/9000 (`auto: false`) | monitor only | full 5-publish day (5×1750=8750); `daily_limit_hit: false` |
| `error_runs_24h` render timeout ×3 + litellm timeout ×1 | none — all retried and succeeded | v1072/1082/1083/1164 all `render/success` after retry 1/2. Transient, not a recurring code signature. |
| `error_runs_24h` analytics HttpError 500 | none — YouTube backendError, count=1 | not a code bug |
| failed / stuck / oauth / bgm_low / pipeline_starved / board_overflow | none | empty lists |
| t3 unparked w=2 at 15:48 UTC 08-26 | **re-parked w=0** | `PATCH /api/topics/3 {"weight":0}` → read-back `t3 w=0` (post-restart still 0) |
| t5 long-anchor w=1 (directive-2 wants ≥2) | **restored w=2** | `PATCH /api/topics/5 {"weight":2}` → read-back `t5 w=2 fmt=long` |

Failed list stays empty. The 80 rejected rows are **not deleted**.

## Learned

1. **ch2 08-26 mix prediction held:** 1L+4S, long v1073 first, then **4×t27**.
2. **Midnight 08-27 demote+produce held:** v1078 demoted, v1164 long auto-produced and rendered. `rebalance-headroom-zero` midnight half is now live, not just 14:00.
3. **Today’s ch2 14:00 plan is 1L + 4×t4** (1164 then 1074–1077). Leftover t27 (1002/1004/1048…) sit behind w4.
4. **Winners (mature):** ch2 t4 22.3 avg views + **5 lifetime subs** (the only converter that still moves); t28 25.8 @ 59.5% avp / 0 subs; t27 20.3 avg / 44.2 avp / **0 subs**. ch1 t15 22.3 avg / 62.3 avp / 0 subs on 46 videos; t6 13.3 avg + **2 lifetime subs**; t18 11.9 avg + 1 sub.
5. **15:48 UTC 08-26 bulk topic update** (not this agent): created t43 (Copilot invoice series, w4) and t44 (Claude Code series, w4), reweighted ch1 including **unparking t3**. Standing wedge says t3 stays parked; this run re-parked it. Left t43/t44 (wedge/series; operator-or-other-agent created them 20h ago).
6. **Hypotheses this run:** (a) ch2 08-27 window publishes 1L+4S with v1164 leading then 4×t4; (b) midnight 08-28 auto_produce queues one ch1 long (approved_longs=0 after today’s 1L, drafts 1165/1166 t5 waiting); (c) a later at_capacity adopt still 409s.

`queue-plan-mix` (shipped 08-24, `6005571`): 72h+ but live noon queue is `{}` both channels (budget already spent). Leave `running`.

`adopt-board-cap` (shipped 08-25, `21fab8c`): live `POST /api/trends/84/adopt` → **409** `board at capacity (horizon reached) — displace drafts first`; pending stayed 10. 48h, not yet 72h. Leave `running`.

`R2` code-ok retry (shipped 08-26, `8188c17`): 24h. Re-render-judged on today’s baseline: **all 4 golden subjects carry a code beat**. Leave `running`.

## Acted (channels)

- **t3 w=2→0** (Prompt & Context short re-parked; standing wedge). Proof: GET `/api/topics/3` `weight=0` after restart.
- **t5 w=1→2** (long-anchor mix hygiene, directive-2). Proof: GET `/api/topics/5` `weight=2` `content_format=long`.
- **No produce / no generate / no displace.** Boards at capacity; ch2 lowest-weight draft is t2 long w2 (must not displace) then t27 w3 — no w1-or-lower seat for a ≥75 trend.
- **Trends logged (no adopt):**
  - **#73** ch2 score **88** (was 87) — Qwen3.8-27B Ollama **924.7K** (was 853.6K, **+71K/day**). Proof: id 73, status=adopted, topic 33 parked.
  - **#83** ch2 score **84** (was 82) — Ollama **0.33.1** is now latest stable (GitHub 2026-08-26 22:55; 窓の杜 covered Claude Desktop toggle today). Proof: id 83, topic 40 parked.
  - **#82** ch2 score **83** (was 84) — Muse Glimmer **169.8K** (was 167.6K, +2.2K, stalled vs Qwen). Proof: id 82, topic 35.
  - **#85** ch1 score **76** (was 74) **rejected** — Copilot Business/Enterprise promo credits end **2026-09-01** (5 days). *Is* the wedge; rejected because ch1 is frozen. Proof: id 85, status=rejected, `adopted_topic_id: null`.
  - **#86** ch2 score **72** **watching** — Ornith 1.5 35B-A3B GGUF (new vs parked #79 Ornith 1.0). Proof: id 86, status=watching, topic=null.
- **Live 409 check:** `POST /api/trends/84/adopt` → http 409, both boards pending=10.
- **No BGM generate** — pool 25, refreshed 08-24.
- **Seeding kit:** `run/seeding/2026-08-27.md` (operator posts; agent does not). Features 08-26 t27 1001/998 + t2 long 1073 + v1032 converter.

## Engagement

**Baseline `baseline`** — 4/4 clean, `visible=True`, none `used_fallback`. ~521s. Scores from frames + scripts + beat timings:

| Lever | score | evidence |
|---|---|---|
| R1 hook (P1) | 2 | Pain-first on frame + opener ≤~11w: “Your RAG returns garbage even when the answer's right there.” / “Your AI agent has amnesia, and it's not your fault.” / “Cada API nova te faz reescrever tudo do zero.” / “Sua RAG responde besteira, e a culpa não é do modelo.” Hook cards at t=0: “Right answer, wrong order” / “Your agent has amnesia” / “Cada API te faz recomeçar” / “Sua RAG responde lixo”. |
| R8 thumb/title (P2) | 2 | Gaps vs titles (hook “Wrong answers hiding in retrieval” vs title “Fix Your Slow RAG…”; “Memory Resets Every Time” vs “Why Your AI Agent Forgets…”; PT “Um conector para dominar todos” / “Você Cortou no Lugar Errado”). Zero distinctive title-word echo. |
| R2 info gain (P3) | 2 | **R2 code-ok retry HOLDS.** 4/4 mixes include a code beat: ch1-code `reranker.score` + `top_k`; ch1-concept `db.save`/`db.query`; ch2-code `@mcp.tool()` indented `return db.find(q)`; ch2-concept `split(doc, by="section", overlap=80)`. Fanout on ch2-code (Servidor MCP → Modelos/Ferramentas/Dados). CMP stacked + VS. |
| R6 payoff (P4) | 2 | Quotable closes (“Don't let good chunks die in bad order.” / “The model doesn't remember. You have to make it remember.” / “Pare de codar cola, comece a codar uma vez só.” / “Chunk ruim entra, lixo sai — simples assim.”) |
| R5 retention (P5) | 2 | Open loops early on all 4. |
| R3 clarity (P6) | 2 | Code indented (ch2-code `return` under `def buscar`). CMP stacked + VS in flow. Fanout on ch2-code. ch2-concept list midpoint shows all 3 rows. Edge labels beside connectors (`save`/`query` on ch1-concept). |
| R4 pacing (P7) | **1** | **Weakest lever.** No sub-1.7s flashes (min 1.68–2.14). **8s+ DRAG:** ch2-concept list **8.93s**; ch2-code CTA **8.14s**. Prompt-level “split the span” is exhausted (07-13 verdict); this is the content-aware / deterministic gap. |
| R7 CTA (P8) | 2 | Follow/Siga + named next value 4/4 (“Halve reranker latency tomorrow” / “Exact memory setup tomorrow” / “Servidor MCP do zero” / “Tamanho ideal por documento”). Spoken ask present on all 4 scripts. |

**Targeted R4** (weakest lever; P7). Not another prompt rule — a deterministic max-hold, the counterpart of the 07-16 min floor.

**Shipped (`ca37863`):** `_MID_MAX=7.5` / `_TAIL_MAX=8.0` in `align_storyboard`. After the min-floor pass, pull the next start earlier so a mid-body hold is ≤7.5s, but never dump enough into the successor that *it* exceeds its cap. Last beat (CTA) is **not** shortened — the follow card should stay through the spoken ask.

Lifecycle: this change does not set `Video.status`. `compose()` still returns HTML or None; render_loop still consumes QUEUED. No state-machine touch.

| | ch2-concept (list drag) | ch2-code (CTA drag) |
|---|---|---|
| baseline max | **8.93s** list | **8.14s** CTA |
| after max | **6.37s** list | **7.30s** CTA |
| after min | 2.09s | 1.98s |

**Gate (after ch2-concept + after ch2-code):**
- ch2-concept after: list 6.367s; mix hook/stmt/stat/lst/diagram/**code**/cmp/quote/cta; `split(text, by="section", overlap=50)` indented; CTA “Siga → Amanhã: overlap exato”; hook “Sua RAG responde qualquer coisa”; `visible=True`, no fallback.
- ch2-code after: max 7.295s; mix hook/lst/term/diagram/**code**/cmp/stat/stmt/cta; `@mcp.tool()` / `return db.get(q)` indented; fanout Servidor MCP → GPT/Claude/Seus dados; CTA “Siga → Amanhã: servidor MCP do zero”; hook “Cada API custa uma semana”; `visible=True`, no fallback.
- R4 1→2 both. R1/R2/R7/R8 unchanged. verify_storyboard **229/229**. Exact baseline timings: list **8.93→7.5**, `valid=True`. `uv run python -c "import app.main"` ok. Manager restarted (`launchctl kickstart`); `GET /api/dashboard` = 200.

### Experiments settled / opened

- Left running: `queue-plan-mix` (noon queue still `{}`); `adopt-board-cap` (409 confirmed again, 48h); `R2` code-ok (holds on fresh baseline, 24h).
- Opened: `R4` max-hold commit `ca37863`, status=running.

## Watch next run

1. Operator ch1 pause yes/no (still the binding decision). 13 subs does not clear 15 — do not unfreeze. Keep t3 at 0.
2. ch2 08-27 publish composition after 14:00 UTC — expect **1L+4S** with t2 long **v1164** leading then **4×t4** (1074–1077).
3. Midnight 08-28: ch1 `approved_longs=0`, drafts 1165/1166 t5 long. Expect auto_produce to queue **one long** into tomorrow’s 07:00 slot (long-reserve). Quote the JobRun.
4. `queue-plan-mix` live signal still needs a noon where a long is queued behind lower-id shorts **and** `approved_longs==0` **and** render budget is not already spent.
5. `adopt-board-cap` hits 72h on 08-28 — promote if a later at_capacity adopt still 409s (already confirmed twice).
6. Failed list stays empty. The 80 rejected t33–t42 rows age toward the 7-day delete cap (age 48.3h today) — do not delete before that.
7. Do not unpark t33–t42. Qwen3.8 (#73, 924.7K, +71K/day) is still the only watching-class term rising; serve it as a **t4 episode via displacement** if a w1-or-lower draft exists, not by unparking t33.
8. BGM count stays ≥ min (25, above target 15). Next refresh only if stale again *and* renders landed.
9. Quote whether v948 EXT_URL grows past 9 (seeding signal). Quote v1022 views/avp (4 / 47.8% today). Quote v1032.
10. Re-render-judge the R4 max-hold on a fresh baseline (no mid-body beat >7.5s; CTA may still hold ~8s through the ask).

## Commit

`ca37863` (R4 max-hold). Report `f2b2570`.
