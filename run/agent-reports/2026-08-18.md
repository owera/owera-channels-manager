# Growth agent — 2026-08-18

## ⚠ Needs operator

1. **PAUSE ch1 (hard review overdue since 08-01).** Still **10 subscribers** vs the 15-sub bar. Lifetime attributed converters remain four videos (v558 MCP +1, v367 t6 +1, v43 t1 +1, v96 t5 +1). Post-CTA / post-wedge volume still ~0 subs (v785 ≈174 views / 148% avp / SHORTS=164 → 0). **Recommendation stands: PAUSE ch1** (or explicitly override with a new bar). Agent continues **zero ch1 strategy moves** (no weight/trend adopt/generate for growth). Pipeline hygiene only.
2. **ch2 metadata backfill** (`db7a254` plan) still awaits `--apply` if still desired.
3. **BGM pool path** remains external (`MoneyPrinterTurbo/resource/songs`). Count holds at 15 = target; wipe risk still escalates to silent renders.
4. **1 failed render still sitting** after today's cap of 5 requeues: **v977** (ch1 t6, `render timed out`, `suggested_action: requeue`). Next run should drain it. Yesterday's leftover 6 is now 1.

---

## Observed

- **Now:** 2026-08-18 ~12:01 UTC. Kill switch off, `scheduler_paused: false`, tree clean at start, `main` == `origin/main`. Last full growth run: 2026-08-17.
- **ch1:** published **5/5** today, quota **9000/9000** (full 5-publish day, monitor only, `daily_limit_hit: false`). Mix = **1 long + 4 shorts** (v962 t5 long first @ 07:01, then v944 t18, v1006 t6, v946 t18, v992 t18). **Thirteenth consecutive 1L+4S day** (08-06…08-18). v946 is yesterday's requeued MCP timeout — recovered and published same morning.
- **ch2:** published_today **0** at observe (window opens **14:00 UTC**). rendered_today **5**. Approved = **1 long + 11 shorts** (v957 t2 long + 4×t4 + 6×t27 + leftover t10) — ready for 1L+4S, weight-first t4 (w4) ahead of t27 (w3). **08-17 did publish 1L+4S** (v955 t2 long first @ 14:01, then v964/965/968/969 **all t4**). Yesterday's prediction held.
- **ch2 subscribers: 63** (unchanged vs 08-13/08-16/08-17). Video-attributed `subscribers_gained` still the same six known converters. Do not invent which video converted.
- **ch1 subscribers: 10.** Subs-per-video on the mature leaderboard: **4 lifetime +1s**, none on the post-07-12 CTA / post-07-13 wedge cohort.
- **BGM pool:** 15 / min 5 / target 15 (`is_low: false`). No `music_gen` errors. No `error_runs_24h`. Last generate 2026-08-16 (2 days, not several) — no refresh this run.
- **Boards (API `by_format` at observe, before triage):**
  - ch1 draft `{long:3, short:7}` · queued `{long:0, short:0}` · approved `{long:1, short:1}`
  - ch2 draft `{long:3, short:7}` · queued `{long:0, short:0}` · approved `{long:1, short:11}`
  - Both `at_capacity: true`, `days_of_inventory: 2.0`.
- **Language (ch2):** 08-17 titles all PT-BR ✓ (955 notebook→prod; 964 SSD NVMe; 965 local→prod; 968 bateria; 969 API vs local). No EN leak.
- **traffic_json (via leaderboard API, no DB dive):** v542 SHORTS=385; v785 SHORTS=164 + YT_SEARCH=6; v558 SHORTS=104 + YT_SEARCH=12; v948 SHORTS=50 + **EXT_URL=9** (unchanged vs 08-17; views 21→61 via Shorts, not more external); v891 YT_SEARCH=14 + SHORTS=6; v884 YT_SEARCH=19 + SHORTS=77. SHORTS / YT_SEARCH dominate. **No BROWSE / SUGGESTED keys** on the top samples. No invented CTR.

### Publish log (settle evidence)

| day | ch1 | ch2 |
|-----|-----|-----|
| 08-12 | 5 (1L+4S) long 894 first | 5 (1L+4S) long 932 first |
| 08-13 | 5 (1L+4S) long 895 first | 5 (1L+4S) long 952 first |
| 08-14 | 5 (1L+4S) long 897 first | **4 (1L+3S)** long 953 first |
| 08-15 | 5 (1L+4S) long 896 first | 5 (1L+4S) long 954 first |
| 08-16 | 5 (1L+4S) long 971 first | 5 (1L+4S) long 981 first + 4×t4 |
| 08-17 | 5 (1L+4S) long 972 first | **5 (1L+4S)** long 955 first + 4×t4 |
| 08-18 | **5 (1L+4S)** long 962 first + 3×t18 + t6 | pending window (0 at observe; 1L+11S banked, v957 leading) |

## Monetization

| metric | ch1 current | ch1 needed | ch1 % | ch2 current | ch2 needed | ch2 % |
|---|---|---|---|---|---|---|
| subscribers | 10 | 490 | 2.0% | 63 | 437 | 12.6% |
| watch hours | 7.3 | 2992.7 | 0.2% | 11.5 | 2988.5 | 0.4% |
| Shorts views | 4294 | 2,995,706 | 0.1% | 4112 | 2,995,888 | 0.1% |

**Binding constraint both channels: subscribers** (lower tier). No milestone crossed (no metric hit 100% for the first time). Prefer analytics (ch2 local-AI hardware shorts that convert; ch1 frozen) over pure milestone math.

## Triage

Digest at start: 7 issues (6 failed requeue + 1 quota monitor). `pipeline_starved` / stuck / oauth / bgm_pool_low / music_gen / `error_runs_24h` all empty.

Yesterday's 5 requeues (watch item): **all 5 recovered off `failed`.** v946 → **published** today (t18 MCP); v957 → approved (t2 long, has `video_path`) — leads today's ch2 window; v959 → approved (t5 long, has `video_path`); v958 / v960 stayed draft (mix-rebalance, expected). Hung-retry leftovers keep draining.

| Issue | Action | Proof |
|---|---|---|
| failed v961 (ch1 t5 long, timeout 132h) | `POST /requeue` | after: `status=queued`, `error=None`, `retry_count=0`. Still queued after mix-rebalance (the 1 long reserve). |
| failed v963 (ch1 t5 long, timeout 132h) | `POST /requeue` | first after: `status=queued`. Mix-rebalance then: **`status=draft`**, `error=None`, subject retained (approved already had 1 long + 1 queued long). |
| failed v973 (ch1 t6, timeout 102h) | `POST /requeue` | after: `status=queued`, `error=None` |
| failed v975 (ch1 t6, timeout 102h) | `POST /requeue` | after: `status=queued`, `error=None` |
| failed v976 (ch1 t6, timeout 102h) | `POST /requeue` | after: `status=queued`, `error=None` |
| remaining failed (1) | leave (cap 5) | digest after: **v977** still `failed` / `suggested_action: requeue` |
| quota ch1 9000/9000 | monitor only | full 5-publish day by design (5×1750); `daily_limit_hit: false` |
| stuck / oauth / bgm_low / pipeline_starved / error_runs_24h / rejected | none | clean lists |

After-state board (API): ch1 queued `{long:1, short:3}` (961 + 973/975/976) · draft `{long:4, short:7}` (v963 rejoined the long bench); ch2 unchanged. `rendered_today=5` both — requeues will not start until the UTC-midnight budget reset. Watch that overnight, not now.

## Learned

1. **Yesterday's timeout requeues rendered / recovered.** 5/5 left `failed`. No new `failed` rows since 08-15. The remaining 1 (v977) is a pre-fix leftover, not a live hang bug — drain tomorrow, don't recode.
2. **t4 led 08-17 shorts (4/4)** after the w4 rebalance. Leave weights (t4=4 / t27=3 / t2=2).
3. **Winners (mature):** ch2 t4 25.3 avg views + **4 lifetime subs** (still the only converter); t28 25.8 @ 59% avp / 0 subs; t27 20.4 avg / 44.8 avp / **0 subs** (v948 EXT_URL=9 + SHORTS=50 is distribution without conversion). ch1 t18 MCP 24.9 avg + the only recent sub-gainer; t15 high-avp loops, 0 subs on 46 videos.
4. **Hypotheses this run:** (a) the 4 still-queued timeouts (961/973/975/976) render overnight the same way yesterday's did; (b) ch2 08-18 window publishes 1L+4S with v957 leading then t4 shorts from the 1L+11S bank; (c) operator pause remains the binding ch1 move.

No running experiments in `experiments.jsonl` to settle (all prior lines already `promoted` as of 08-16).

## Acted (channels)

- **Requeue 5** — see Triage. Cap hit. 1 failed remains (v977).
- **No weight changes.** t4=4 / t27=3 / t2=2 already match conversion. ch1 frozen.
- **Trends logged (no adopt/displace — boards at/over capacity after requeue; every ch2 draft is w2+ so a new w1 topic cannot displace; ch1 frozen):**
  - **#78** ch2 score **84** watch — “Nemotron 3.5 Lightning 30B-A3B local (Ollama)” (Ollama 08-11, 30B MoE / 3B active). Proof: `POST /api/trends` → id 78, status=watching.
  - **#73** ch2 score **82** watch refresh — “Qwen3.8-27B local GGUF (Ollama / llama.cpp) 2026-08” (Willison 08-16). Proof: id 73, status=watching.
  - **#76** ch2 score **80** watch refresh — “gpt-oss 20B/120B local MXFP4”. Proof: id 76, status=watching.
  - **#77** ch1 score **70** reject refresh — “Copilot Max $100 / $200 credits (Aug 2026)” — *is* the wedge, rejected because ch1 is frozen. Proof: id 77, status=rejected.
- **No produce / no generate / no displace.** Queued after requeue = tomorrow's first render slots. t4 bench has 3 drafts.
- **No BGM generate** — pool 15 = target, last refresh 08-16 (2 days).
- **Seeding kit:** `run/seeding/2026-08-18.md` (operator posts; agent does not).

## Engagement

**Baseline `baseline`** — 4/4 clean, `visible=True`, none `used_fallback`. ~235s. Scores from frames + scripts + beat timings:

| Lever | score | evidence |
|---|---|---|
| R1 hook (P1) | 2 | Pain-first on frame + opener ≤11w: “Your RAG answers are junk” / “Your agent has amnesia” / “Chega de reescrever integrações” / “Sua RAG responde tudo errado”. Hook at t=0 all 4. |
| R8 thumb/title (P2) | 2 | Gaps vs titles (hook “Why your answers arrive late” vs title “Fix Your Slow RAG…”; PT “Um conector no lugar de dez” / “Cortou a frase no lugar errado”). Zero title-word echo. |
| R2 info gain (P3) | 2 | Varied mixes (ch1-code: hook/cmp/stmt/term/code/stat/lst/cta + indented `scores = ce.predict(pairs)`; ch2-code fanout Servidor MCP → Banco/Arquivos/APIs + indented `@mcp.tool()` / `return db.run(sql)`). ch2-concept has code (`split_by("section", overlap=50)`) plus two lists. |
| R6 payoff (P4) | 2 | Quotable closes (“Stop stuffing your prompt with near-misses—rank first, then generate.” / “Your agent isn't broken. It was never designed to remember.” / “Pare de reinventar a roda: um servidor, infinitas conexões.” / “Contexto ruim entra, resposta ruim sai — simples assim.”) |
| R5 retention (P5) | 2 | Open loops early on all 4 (“That's not a retrieval problem—it's a ranking problem” / “Here's the truth nobody tells you” / “O MCP muda isso de vez” / “O problema quase nunca é o modelo”). |
| R3 clarity (P6) | 2 | Code legible + indented; CMP stacked + VS in flow (Retrieval vs Ranking; Chat window vs Real memory; Sem MCP vs Com MCP; Por tamanho vs Por significado); diagram elabels “save” / “feed back” beside the Chat→DB→Reply arrows, no strikethrough. Fanout on ch2-code truthful. |
| R4 pacing (P7) | 2 | Hook at t=0. ch1 subjects clean (min 1.88–1.98s, max 6.13–7.03s). **Known residual returned on ch2:** ch2-concept lst 9.105s + cmp 1.68s (floor infeasible — predecessor guard); ch2-code term 8.343s. Drift still owns the long holds. Prompt-level R4 is exhausted (07-13/07-16 verdicts) — do not ship another R4 prompt rule. |
| R7 CTA (P8) | 2 | Follow/Siga + named next value 4/4 (“Halve reranker latency tomorrow” / “Exact memory setup tomorrow” / “Servidor MCP do zero” / “Tamanho ideal por documento”). Spoken ask present on all 4 scripts. |

**All-2 baseline (15th consecutive judged day) → non-engagement path (fallback clause).** No prompt change. The only soft spot is the known R4 residual (1.68s floor-infeasible + 9.1s list hold), which prior verdicts already closed at the prompt layer. Recurring render-timeouts are already owned by `43cfe3d` (live; yesterday's 5 requeues produced files). Today's work is the next 5 requeues, not another timeout-heuristic or pacing prompt. Shipping unproven polish on an all-2 set would violate the gate.

### Experiments settled / opened

None. No running lines left to settle. No new engagement experiment (no file touch).

## Watch next run

1. Operator ch1 pause yes/no (still the binding decision).
2. ch2 08-18 publish composition after 14:00 UTC — expect **1L+4S** with t2 long v957 leading then t4 shorts from the 1L+11S bank.
3. Overnight render of requeued v961 / v973 / v975 / v976 (and whether mix-rebalanced draft v963 becomes the next long auto-produce, not a lost subject).
4. Drain the last failed (v977 t6). After that the timeout backlog should be empty.
5. BGM count stays ≥ min (now 15 = target).
6. #78 Nemotron 3.5 Lightning: adopt only as a t27/t4 episode via displacement if a w1-or-lower draft exists — not a new topic while t27 converts 0.
7. Quote whether v948 EXT_URL grows past 9 (seeding signal) and whether any new ch2 video attributes a subscriber.

## Commit

`779b532` — Growth agent 2026-08-18: drain 5 more timeouts; all-2 baseline
