# Growth agent — 2026-08-21

## ⚠ Needs operator

1. **PAUSE ch1 (hard review overdue since 08-01).** Still **11 subscribers** vs the 15-sub bar (unchanged vs 08-19/08-20). Lifetime video-attributed converters remain **four** (v558 MCP +1, v367 t6 +1, v43 t1 +1, v96 t5 +1). Post-CTA / post-wedge volume still ~0 attributed subs (v785 ≈174 views / 148% avp / SHORTS=164 → 0). **Recommendation stands: PAUSE ch1** (or explicitly override with a new bar). Agent continues **zero ch1 strategy moves** (no weight/trend adopt/generate for growth). Pipeline hygiene only.
2. **ch2 metadata backfill** (`db7a254` plan) still awaits `--apply` if still desired.
3. **BGM pool path** remains external (`MoneyPrinterTurbo/resource/songs`). Count 20 (target 15); wipe risk still escalates to silent renders.

---

## Observed

- **Now:** 2026-08-21 ~12:00 UTC. Kill switch off, `scheduler_paused: false`, tree clean at start, `main` == `origin/main`. Last full growth run: 2026-08-20.
- **ch1:** published **5/5** today, quota **8750/9000** (`daily_limit_hit: false`). Mix = **1 long + 4 shorts** (v960 t5 long first @ 07:01 `XkAt6r9Zxyo`, then v1008 t6, v1019 t18, v1030 t6, v1032 t6). **Sixteenth consecutive 1L+4S day** (08-06…08-21).
- **ch2:** published_today **0** at observe (window opens **14:00 UTC**). rendered_today **5**. Approved = **1 long + 11 shorts** (v982 t2 long + 4×t4 1024–1027 + 6×t27 995–1001 + leftover v709 t10 from July). **08-20 did publish 1L+4S** (v958 t2 long first @ 14:00, then v1020/1021/1022/1023 **all t4**) — yesterday’s 4×t4 prediction held.
- **ch2 subscribers: 64** (was 63 on 08-13…08-20). **First move in 8 days.** Video-attributed `subscribers_gained` still the same six known converters (v542/524/810/421/684/315). Analytics lag — do not invent which video converted the 7th.
- **ch1 subscribers: 11** (unchanged vs 08-19/08-20). Subs-per-video on the mature leaderboard: **still 4 lifetime +1s**, none on the post-07-12 CTA / post-07-13 wedge cohort.
- **BGM pool:** 20 / min 5 / target 15 (`is_low: false`). No `music_gen` errors. No `error_runs_24h`. Last generate was 2026-08-20 (yesterday, 15→20) — not stale; no refresh this run.
- **Boards (API `by_format` at observe):**
  - ch1 draft `{long:2, short:8}` · queued `{long:0, short:0}` · approved `{long:1, short:1}`
  - ch2 draft `{long:1, short:9}` · queued `{long:0, short:0}` · approved `{long:1, short:11}`
  - Both `at_capacity: true`, `days_of_inventory: 2.0`.
- **Language (ch2):** 08-20 titles all PT-BR ✓ (958 versionamento de dados; 1020 português; 1021 lento depois de 10 min; 1022 2 GPUs; 1023 demora 30s). No EN leak.
- **traffic_json (via leaderboard API, no DB dive):** v542 SHORTS=385; v785 SHORTS=164 + YT_SEARCH=6; v558 SHORTS=104 + YT_SEARCH=12; v948 SHORTS=64 + **EXT_URL=9** (unchanged vs 08-17…08-20) + NOTIFICATION=3; v884 YT_SEARCH=21 + SHORTS=77. SHORTS / YT_SEARCH dominate. **No BROWSE / SUGGESTED keys** on the top samples. No invented CTR.
- **Live picker vs plan (the bug this run fixed):** `_next_approved(ch2)` = v982 t2 long. Pre-change `GET /api/videos/publish-plan?channel_id=2` listed **709 first** and **982 last**. Post-restart plan: **982 @ 14:00**, then 1024–1027 (t4 w4), leftover 709 last. Proof quoted in Engagement / Acted.

### Publish log (settle evidence)

| day | ch1 | ch2 |
|-----|-----|-----|
| 08-15 | 5 (1L+4S) long 896 first | 5 (1L+4S) long 954 first |
| 08-16 | 5 (1L+4S) long 971 first | 5 (1L+4S) long 981 first + 4×t4 |
| 08-17 | 5 (1L+4S) long 972 first | 5 (1L+4S) long 955 first + 4×t4 |
| 08-18 | 5 (1L+4S) long 962 first + 3×t18 + t6 | **5 (1L+4S)** long 957 first + 4×t4 |
| 08-19 | **5 (1L+4S)** long 959 first + t18 + 3×t6 | **5 (1L+4S)** long 956 first + 3×t4 + t27 |
| 08-20 | **5 (1L+4S)** long 961 first + t18 + 3×t6 | **5 (1L+4S)** long 958 first + **4×t4** (1020–1023) |
| 08-21 | **5 (1L+4S)** long 960 first + t18 + 3×t6 | pending window (0 at observe; 1L+11S banked, v982 leading) |

## Monetization

| metric | ch1 current | ch1 needed | ch1 % | ch2 current | ch2 needed | ch2 % |
|---|---|---|---|---|---|---|
| subscribers | 11 | 489 | 2.2% | 64 | 436 | 12.8% |
| watch hours | 7.4 | 2992.6 | 0.2% | 11.7 | 2988.3 | 0.4% |
| Shorts views | 4340 | 2,995,660 | 0.1% | 4203 | 2,995,797 | 0.1% |

**Binding constraint both channels: subscribers** (lower tier). No milestone crossed (no metric hit 100% for the first time). ch2 63→64 is the first subscriber move since 08-13; still far from lower-tier 500. Prefer analytics (ch2 local-AI hardware shorts that convert; ch1 frozen) over pure milestone math.

## Triage

Digest at start: 1 issue (quota monitor only). `failed=[]`, `pipeline_starved` / stuck / oauth / bgm_pool_low / music_gen / `error_runs_24h` / `rejected` all empty. `summary.clean: false` only because of the quota row.

Timeout backlog remains empty (closed 08-20).

| Issue | Action | Proof |
|---|---|---|
| quota ch1 8750/9000 | monitor only | full 5-publish day (5×1750=8750); `daily_limit_hit: false` |
| failed / stuck / oauth / bgm_low / pipeline_starved / error_runs_24h / rejected | none | clean lists |

After-state board (API, unchanged by triage): ch1 queued `{long:0, short:0}` · approved `{long:1, short:1}` (963 long + 1031 short); ch2 approved `{long:1, short:11}`. Failed list stays empty.

## Learned

1. **ch2 08-20 mix prediction held:** 1L+4S, long v958 first, **4×t4** (1020–1023). Weight-first is doing what it says. Today’s bank has 4 approved t4 again + v982 long leading — expect the same shape at 14:00.
2. **ch2 +1 subscriber (63→64).** First channel-level move since 08-13. Video-attributed converters still the same six; do not assign the 7th until a leaderboard row shows `subscribers_gained`.
3. **Winners (mature):** ch2 t4 23.6 avg views + **4 lifetime subs** (still the only converter); t28 25.8 @ 59% avp / 0 subs; t27 20.7 avg / 43.6 avp / **0 subs** (v948 EXT_URL=9 + SHORTS=64 is still distribution without conversion). ch1 t18 MCP 14.8 avg + the only recent sub-gainer; t15 high-avp loops, 0 subs on 46 videos.
4. **t18 MCP bench is empty** (pend=0, w=2). Overnight autofill filled t6 (lower id, same weight) when t6 was under threshold; t6 is now at 6 pending (≥ skip), so the *next* tick with board space should fill t18 without a code change. Do not generate (ch1 frozen). Watch tomorrow’s t18 pending.
5. **publish-plan was lying.** FIFO ETAs ≠ live picker. That is the pipeline-robustness fix this run (all-2 rubric → non-engagement path).
6. **Hypotheses this run:** (a) ch2 08-21 window publishes 1L+4S with v982 leading then 4×t4; (b) ch1 noon approved shorts=1 is thin — auto_produce after midnight refills without a manual produce; (c) operator pause remains the binding ch1 move — 11 subs still does not clear the 15-sub bar.

No running experiments in `experiments.jsonl` to settle at start of run (all prior lines already `promoted` as of 08-16). New line opened for the publish-plan fix.

## Acted (channels)

- **No requeue / retry / delete.** Failed list already empty. Cap not hit.
- **No weight changes.** t4=4 / t27=3 / t2=2 already match conversion. ch1 frozen (t15 remains w=0 as part of the freeze; not revived).
- **Trends logged (no adopt/displace — boards at capacity; every ch2 draft is w2+ so a new w1 topic cannot displace; ch1 frozen):**
  - **#78** ch2 score **86** (was 84) watch refresh — “Nemotron 3.5 Lightning 30B-A3B local (Ollama)”. Ollama library **80.7K** downloads (was 40.1K 08-20); 25GB Q4 tag advertises 1M context. Proof: `POST /api/trends` → id 78, status=watching, `updated_at=2026-08-21T12:14:44`.
  - **#73** ch2 score **82** watch refresh — “Qwen3.8-27B local GGUF”. Unsloth Dynamic V3.0 GGUF landed today. Proof: id 73, status=watching.
  - **#80** ch2 score **80** watch refresh — “Ollama default num_ctx silently truncates 128K models”. 1M-on-25GB Nemotron tag makes the silent 4k default worse this week. Proof: id 80, status=watching.
  - **#76** ch2 score **74** (was 76) watch refresh — “gpt-oss 20B/120B local MXFP4”. Proof: id 76, status=watching.
  - **#79** ch2 score **66** (was 68) watch refresh — “Ornith 1.0 9B”. Proof: id 79, status=watching.
  - **#77** ch1 score **70** reject refresh — “Copilot Max $100 / $200 credits (Aug 2026)” — *is* the wedge, rejected because ch1 is frozen. Proof: id 77, status=rejected.
- **No produce / no generate / no displace.** t4 bench has 6 drafts; t27 has 3; t2 long has 1. Auto_produce owns the overnight mix.
- **No BGM generate** — pool 20, refreshed yesterday; not stale.
- **Seeding kit:** `run/seeding/2026-08-21.md` (operator posts; agent does not).

## Engagement

**Baseline `baseline`** — 4/4 clean, `visible=True`, none `used_fallback`. ~261s. Scores from frames + scripts + beat timings:

| Lever | score | evidence |
|---|---|---|
| R1 hook (P1) | 2 | Pain-first on frame + opener ≤10w: “Your RAG returns garbage answers, right?” / “Your AI forgets you the second you close the tab.” / “Você escreve a mesma integração de novo, toda semana.” / “Sua RAG responde qualquer coisa, menos o que você perguntou.” Hook cards: “Your RAG returns garbage” / “Your AI forgets you instantly” / “A mesma integração, de novo” / “Sua RAG responde errado”. Hook at t=0 all 4. |
| R8 thumb/title (P2) | 2 | Gaps vs titles (hook “Your retrieval is dragging results” vs title “Fix Your Slow RAG…”; “Goldfish memory is fixable” vs “Why Your AI Agent Forgets…”; PT “Um conector, mil ferramentas” / “Você corta seus textos errado”). Zero title-word echo. |
| R2 info gain (P3) | 2 | Varied mixes (ch1-code: hook/stmt/stat/term/lst/code/cmp/cta + indented `scores = ce.predict(pairs)`; ch1-concept: diagram Chat→Store→Retrieve→Context + `def save(msg):` / indented `db.insert(msg)`; ch2-code fanout Servidor MCP → API/Banco/Ferramenta + indented `return db.run(sql)` under `def query`; ch2-concept has stat + cmp + lst + quote, no code this render — still not statement-only). ch2-concept list 4.117s / 3 items, last row +2.45s (midpoint frame shows 2/3 — residual of extract timing, card completes before beat end). |
| R6 payoff (P4) | 2 | Quotable closes (“Retrieval gets you close; reranking gets you right.” / “Your AI isn't dumb—it's amnesiac, and memory is a feature you engineer.” / “Pare de escrever integrações e comece a escrever o que importa.” / “Contexto quebrado gera resposta quebrada — simples assim.”) |
| R5 retention (P5) | 2 | Open loops early on all 4 (“Here's the truth: your retriever isn't broken” / “That's not a bug—it's how these models work” / “O MCP resolve isso de verdade” / “O problema não é o modelo. É o seu chunking burro”). |
| R3 clarity (P6) | 2 | Code legible + indented; CMP stacked + VS in flow (Retrieval vs Reranking; Smarter model vs External memory; Sem MCP vs Com MCP; Corte fixo vs Por significado); diagram elabels not striking through. Fanout on ch2-code truthful. ch1-code 4.967s list midpoint frame shows all 3 items. |
| R4 pacing (P7) | 2 | Hook at t=0. **Known residual returned:** ch1-code term **1.68s FLASH** (floor) + cmp 1.88s; ch2-code code **8.892s DRAG** + drift. Prompt-level R4 is exhausted (07-13/07-16 verdicts) — do not ship another R4 prompt rule. Drift still owns the long holds. |
| R7 CTA (P8) | 2 | Follow/Siga + named next value 4/4 (“Halve reranker latency tomorrow” / “Build the memory layer tomorrow” / “Seu 1º servidor MCP amanhã” / “Amanhã: overlap exato”). Spoken ask present on all 4 scripts. |

**All-2 baseline (18th consecutive judged day) → non-engagement path (fallback clause).** No prompt change. The only soft spot is the known R4 residual (1.68s floor + 8.9s code hold), which prior verdicts already closed at the prompt layer. Recurring render-timeouts are gone (failed=[]). Shipping unproven polish on an all-2 set would violate the gate.

**Shipped instead (pipeline-robustness):** `publish-plan` now drains in `_next_approved` order.

| | before (live GET) | after (live GET, post-restart) |
|---|---|---|
| ch2 first ETA | v709 t10 leftover | **v982 t2 long @ 14:00** |
| ch2 shorts today | t27 (995…) then t4 | **t4 1024–1027** (w4), then t27, v709 last |
| ch1 08-22 07:00 | v1031 short then v963 long | **v963 long @ 07:00** then v1031 |

Proof: `GET /api/videos/publish-plan` after `launchctl kickstart` + dashboard 200. `uv run python -c "import app.main"` ok. `tests/verify_publish.py` **114/114** (new cases: long-first vs oldest leftover; shorts-first once a long is already out today).

### Experiments settled / opened

- Settled: none (no running lines at start).
- Opened: `publish-plan-mix` commit `de23128`, status=running. Settle ~08-24 from the 08-21/08-22 publish log: ch2 14:00 slot should be v982, then 4×t4.

## Watch next run

1. Operator ch1 pause yes/no (still the binding decision). 11 subs does not clear the 15-sub bar — do not unfreeze.
2. ch2 08-21 publish composition after 14:00 UTC — expect **1L+4S** with t2 long v982 leading then **4×t4** (plan and `_next_approved` now agree). This is also the first settle signal for `publish-plan-mix`.
3. ch1 noon approved shorts=1 is thin; auto_produce after midnight should refill the 1L+4S overnight queue. Do not manual-produce.
4. t18 MCP pending should rise off 0 on the next autofill tick that has board space (t6 is now at skip threshold). Still no manual generate (freeze).
5. Failed list stays empty.
6. BGM count stays ≥ min (20, above target 15). Next refresh only if stale again *and* renders landed.
7. #78 Nemotron (86, 80.7K pulls) / #80 num_ctx: adopt only as a t27/t4 episode via displacement if a w1-or-lower draft exists — not a new topic while t27 converts 0.
8. Quote whether v948 EXT_URL grows past 9 (seeding signal) and **which video, if any, now shows `subscribers_gained` for the ch2 63→64 move**.

## Commit

`de23128` (publish-plan mix). Report `5ba306b`.
