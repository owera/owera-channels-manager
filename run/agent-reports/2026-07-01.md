# Growth Agent Run — 2026-07-01

**Kill switch:** absent · **scheduler_paused:** false · **tree:** clean · **prev report:** 2026-06-30

First full run of the redesigned engagement engine (the earlier 07-01 run was baseline-only).
No operational issues needing the operator. One gated engagement improvement shipped (R8).

## Observe (key numbers)
- **Owera Software (ch1)** — 9 subs · 84 published · 17 queued, 0 draft/failed/rejected · rendered_today 9, published_today 4 · quota 6800/9000 · oauth connected.
- **Rodrigo Recio (ch2)** — 52 subs · 78 published · 7 queued, 3 draft, 2 approved (waiting on tomorrow's publish slot) · rendered_today 7, published_today 5/5 · quota 8500/9000 · oauth connected · cooldown expired (2026-06-22).
- **BGM pool:** 15/15 (target), not low — no music action needed.
- **Board inventory:** ch1 `days_of_inventory` 3.4 (**at_capacity**), ch2 2.0 (**at_capacity**). Both channels have ≥ horizon days of render work queued.

## Triage
Digest essentially clean: no `failed`, `rejected`, `stuck_*`, `oauth`, or `error_runs_24h` entries; `bgm_pool_low` empty.
- **Only flagged item — ch2 quota 8500/9000** (`daily_limit_hit: false`, `auto: false`). All 5 of today's publishes are already done (next publish 2026-07-02); the quota self-resets daily. **Action: monitor only** — no budget/drip change warranted since the cap was not hit and there is no more publishing queued for ch2 today.
- No videos in `failed`/`rejected`/`review`/stuck → no requeue/retry/delete/approve actions this run.

## Monetization
Targeting **Lower Tier** for both (neither `tier_achieved`). Binding constraint = the metric furthest from goal; at this scale everything but subscribers is ~0%, and **discovery** (getting watched at all) is the real bottleneck — which is exactly what the engagement gate optimizes.

| Channel | Subs (cur/need/pct) | Watch hrs (cur/need/pct) | Shorts views (cur/need/pct) |
|---|---|---|---|
| Owera Software (ch1) | 9 / 491 / 1.8% | 1.7 / 2998.3 / 0.1% | 1005 / 2,998,995 / 0.0% |
| Rodrigo Recio (ch2) | 52 / 448 / 10.4% | 0.8 / 2999.2 / 0.0% | 352 / 2,999,648 / 0.0% |

No metric hit 100% this run. No milestone-driven reweighting is justified with signal this thin.

## Learn (winners / losers + hypotheses)
Only mature (`measured`) videos considered. **CTR/impressions are 0 across every topic and format on both channels** — that data is not flowing, so thumbnail/title levers can only be judged on the render, not measured signal.

- **Format (clearest, replicated signal):** shorts dominate long-form on both channels.
  - ch1: shorts avg 16.0 views @ 32.9% retention vs long 2.0 views @ 29.8%.
  - ch2: shorts avg 12.6 views @ 19.9% retention vs long 3.2 views @ 13.8%.
- **Topics (ch1):** best shorts by retention are "GitHub Copilot Credits 2026" (42%, n=8) and "LangChain Tips" (35%, n=11); "Prompt & Context Engineering" short carries the volume (752 views, 41 measured, weight already 3). "CrewAI Tips" short is dead (0 views, n=4).
- **Topics (ch2):** "Fundamentos da Engenharia de IA" short leads (15.1 views @ 23%, weight 3); all long-form topics weak (13–22% retention, 1–4 views).
- **Weights already aligned:** ch1 has **all long-form topics at weight 0** (soft-paused); shorts winner at weight 3. ch2 still carries weight 2 on two long-form topics (a defensible watch-hours hedge). **No reweighting this run** — both channels at capacity (a reweight has no effect until the bench drains), and I won't thrash a working setup.

**Hypotheses this run:**
1. (Shipped, R8) Thumbnail hooks that stop echoing the title and open a curiosity gap will raise CTR once impressions flow.
2. (Watch) ch2 long-form (topics 2, 13 at weight 2) keeps underperforming shorts; if the next cohort confirms, down-weight 2→1.
3. Discovery — not format or topic — is the binding constraint at <60 subs; engagement-quality levers (hook, thumbnail, retention) compound it.

## Engagement (the core of the run)
Baseline: rendered the full golden set through the real pipeline (`--label baseline`, 4/4 clean, all `visible=True`, none `used_fallback`) and READ the frames + scripts + hooks.

**Rubric baseline scores (per lever):**
| Lever | Score | Note |
|---|---|---|
| R1 hook | 2 | Pain-first, on-screen at t=0, punchy, high contrast ("Right answer, buried in noise", "Your AI forgot you already"). Scripts open pain-first. |
| **R8 thumbnail/title** | **1** | **Weakest high-priority lever.** Thumbnail hook echoes the title instead of opening a complementary gap. |
| R2 info gain | 2 | Very varied beats (code/diagram/compare/term/stat/list/quote); ≤2 statements. Clean syntax-highlit code, clear pipeline diagram. |
| R3 clarity | 2 | ≤8-word headlines, no clipping/overflow, high contrast. |
| R4 pacing | 2 | Word-synced; 83–101 words over 34–45s. |
| R5 retention | 2 | Open loops present ("Here's the fix", "Want it to actually recall…"). |
| R6 payoff | 2 | Quotable closers ("Retrieval finds candidates. Reranking finds answers."). |
| R7 CTA | 2 | Single specific CTA tied to value ("Segue → / Arruma teu chunking"). |
| R10 brand | 2 | Per-topic accent palettes distinct (green RAG, orange memory, red MCP). |
| R11 format | 2 | Shorts stay tight. |

**Target lever: R8 (priority 2, the only weak lever).** Chosen because CTR data isn't flowing, so R8 can *only* improve via render-and-judge — a perfect signal-scarce fit — and it's a one-file prompt change (the safe class).

**Change:** sharpened `thumbnail._hook_text`'s prompt (`app/services/thumbnail.py`) to forbid reusing the title's distinctive tool/technique/number and require a genuine curiosity gap (withhold the resolution / fresh metaphor).

**Gate — SHIPPED (all three passed):**
- *Controlled A/B on the 4 fixed baseline titles (3 samples each):* title-word echo dropped **9/12 → ~1/12**. Examples:
  - ch1-code: "5 Lines Fix Slow RAG" → "Your Answers Were Wrong" / "Your Retrieval Is Lying"
  - ch2-code: "Um Servidor Para Tudo" (literal title slice) → "Uma conexão substitui todas"
  - ch2-concept: "Seu Chunking Está…" ×3 → "Você está cortando errado"
  - ch1-concept: "Your AI Has Amnesia" ×3 → "Goldfish memory, solved" / "It Has No Memory"
- *Real end-to-end renders (`rubric_review --only ch2-code`, `--only ch1-code`, `--label after`):* both `visible=True`, `used_fallback=False`. New hooks in context: ch2-code "Um Conector Para Governar Todos", ch1-code "One tweak, instant speed" — both strong gaps, neither repeats its title. Beat mixes still varied → no other lever dropped.
- *Regression:* `verify_storyboard.py` 38/38 green.
- Target lever R8 up; no other lever's output changes (single-file thumbnail path). **Commit `62ecec6`.**

**Experiments settled:** none — the log had only the seed line before today. This run's R8 experiment is `running` (predicts avg_ctr up; settle once ch1/ch2 impressions begin reporting, or re-run render-and-judge if still signal-scarce).

## Actions taken
- **Code:** 1 prompt change — `app/services/thumbnail.py` `_hook_text` (commit `62ecec6`). Within the ≤2 cap.
- **Channel API:** none warranted (both channels at board capacity → no idea generation / trend adoption; weights already aligned to the shorts>long signal; triage clean).
- **Experiment log:** appended the R8 experiment (`run/experiments.jsonl`).

## Watch next time
- **Impressions/CTR:** still 0 everywhere — the whole R8 hypothesis needs impressions to settle on real signal. If still absent next run, re-run render-and-judge to keep R8 promoted; otherwise compare CTR before/after commit `62ecec6`.
- **ch2 long-form** (topics 2, 13 @ weight 2): confirm underperformance next cohort → down-weight 2→1.
- **ch2 quota:** watch it doesn't hard-hit 9000 on a burst publish day; nudge drip only if `daily_limit_hit` flips true.
- **Board capacity:** both channels at capacity today; when `days_of_inventory` drops < 0.5, refill via the winners (ch1 "Prompt & Context Engineering" short w3; ch2 "Fundamentos" short w3).
- **Next weakest lever** (once R8 settles): R1 and R8 both strong → the frontier moves to R2/R6 sharpening or measured-signal work as analytics mature.
