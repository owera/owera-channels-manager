# Growth agent 2026-09-26

Preflight: kill switch absent. `scheduler_paused` false. Branch `main`. Untracked `NEWSLETTER_DEPLOY.md` (newsletter deploy guide for another tree) left unstaged. No tracked files were dirty at start.

Code commit: `4559108`. After that commit, `launchctl kickstart -k gui/$(id -u)/com.owera.channels-manager` and `GET /api/dashboard` **200**.

## Monetization

Lower tier. Neither channel has `tier_achieved`. Binding constraint on both is **subscribers**. No metric hit 100% for the first time. Channel subs are unchanged from yesterday: ch1 **16**, ch2 **70**.

| metric | ch1 current | ch1 needed | ch1 % | ch2 current | ch2 needed | ch2 % |
|---|---|---|---|---|---|---|
| subscribers | **16** | 484 | **3.2%** | **70** | 430 | **14.0%** |
| watch hours | 11.2 | 2988.8 | 0.4% | 23.3 | 2976.7 | 0.8% |
| Shorts views | 5938 | 2,994,062 | 0.2% | 5925 | 2,994,075 | 0.2% |

ch1 subs-per-video, lifetime, unchanged: t5 long **3**, t15 **2**, t6 **2**, t18 **1**, t1 **1**. t26 Agent memory still **0** on 11 measured (47.1% avp, 16.0s avg duration). t7 CrewAI **0** on 13 (66.9% avp).

Analytics newest capture: ch1 2026-09-25T12:28Z, ch2 2026-09-25T12:30Z.

## Triage

Digest at start: **not clean** (`summary.clean: false`, `total_issues: 42`, `needs_operator: 0`). `oauth` / `quota` / `cooldown` / `bgm_pool_low` / `stuck_publishing` / `stuck_rendering` / `pipeline_starved` / `title_pattern_blocked` / `craft_gate_blocked` empty. `error_runs_24h`: 7× transient `grok.Timeout` after 600s, 1× the same timeout as a terminal error.

Both channels had 0 approved, `default_skip_gate=false`. stuck_review: 12 rows past 48h. Craft dry-run: `publish_craft_block_reason` **None** on all 22 review rows; `used_fallback=false` on all.

| Issue | Did | Proof (read-back) |
|---|---|---|
| stuck_review **1327** (ch1 t45, 70.9h, title=subject, craft clear) | `POST /approve` | GET 1327 `status=approved` `craft_review=pass` `video_path` set. publish-plan 2026-09-27T07:00 (t45 w1 sits behind t26 w2). |
| stuck_review **1329** (ch1 t26, 70.9h) | `POST /approve` | GET 1329 `status=published` `yt=8660Ja5-DcQ` `published_at=2026-09-26T12:03:57Z` title `Chat pinned the SKU. Prod charged the old one. · Agent memory 15`. ch1 `published_today=1` quota=2000. |
| stuck_review **1330, 1332, 1333, 1334, 1335** (ch1 t26) | `POST /approve` | GET each `status=approved` `craft_review=pass`. Next eta 1330 at 2026-09-26T12:33:57Z. |
| stuck_review **1315** (ch2 t27, 92.1h) | `POST /approve` | GET 1315 `status=approved` `craft_review=pass`. publish-plan 16:00Z. |
| stuck_review **1325, 1337, 1338, 1339** (ch2 t4) | `POST /approve` | GET each `status=approved` `craft_review=pass`. ch2 `approved=5` `next_publish_eta=2026-09-26T14:00:00+00:00`. |
| review **1343** (ch1 t26, overnight) | `POST /reject` | GET 1343 `status=rejected`. Script after the title head: `The title is a two-beat incident. I’ll count the script so it stays inside 70–100 words and opens on that hook.` Reason quoted on the row. |
| review **1350** (ch2 t27, lowercase fragment) | `POST /reject` | GET 1350 `status=rejected`. Title `camadas na CPU: o 27B na 12GB rodou. · Local 53`. |
| failed **1297, 1318, 1326, 1331, 1340, 1349** (retry 2/2, grok 600s, no file, `suggested_action: requeue`) | leave | GET still `failed` `retry_count=2`. `POST /requeue` does not reset `retry_count`. |
| rejected HOLD / CoS STOP / title-pattern leftovers | leave | Same as yesterday. Not deleted. |

BGM: `state.bgm_pool` count **35** (`is_low: false`, min 5, target 15). `GET /api/music` count **64**. No generate this run — yesterday already added 5 and the pool is above target.

## Learned

Board: both channels `at_capacity: true`. ch1 `days_of_inventory` 2.0 (10 drafts, 0 queued). ch2 2.0 (10 drafts, 0 queued). `by_format` draft `{long:0, short:10}` on both. Long topics stay parked (t5 inactive, t2 weight 0). `rendered_today=5` on each. At observe `published_today=0`; after approve, ch1 **1**.

**Winners (mature, capture 09-25 ~12:28Z):** ch2 t4 still the converter (6 lifetime subs). t28 28.4 avg views / 0 subs. ch1 t15 20.3 avg / **2 subs** on 88 measured, still off (`weight=0`, `active=false`). t26 14.5 avg / 47.1% avp / **0 subs** on 11 measured.

**Traffic:** SHORTS + YT_SEARCH. BROWSE total **0** and SUGGESTED total **0** on both channels. EXT_URL sums: ch1 32, ch2 107, concentrated on old videos. v1247 (`_XGUAQrwE6s`) still **views=0**, `has_data=true`, `traffic={}`, captured **2026-09-25T12:26:03** (newer than yesterday's 09-24 12:23Z capture). v1251 still 0. v1208 still 101 views / YT_SEARCH=88.

**Yesterday's watch items:**

- 1313 published. GET `1313 yt=bsNfI-O8Dr0` 12:37Z 2026-09-25. Title `We're shipping this in the builder. · Shipping 1`.
- ch2 1298 `yt=Hfn5LPcOMFE` 15:02Z, 1314 `yt=9rRdXYnn6XE` 15:33Z, 1323 `yt=mIfy7F-UTqQ` 14:01Z, 1324 `yt=UEX4g4G93gg` 14:31Z. All held.
- Overnight t27/t4 subjects: drafts 1352–1358 and 1346/1359–1363 are clean claims, no catalog-planning opener. **Held.** Residual: rendered 1343 still spoke a *new* CoT class (count-the-script), rejected this run.
- v1247 still 0 on the 09-25 12:26Z capture. **Held.**

**Language:** ch2 review/approved/draft titles checked this run are PT-BR. 1350 was the fragment leftover; rejected.

**Hypotheses this run:** (a) 1313 published with yt=bsNfI-O8Dr0 — **held**. (b) ch2 1298/1314/1323/1324 published with those ids — **held**. (c) overnight subjects do not open on catalog-planning CoT — **held** on drafts; **missed** on rendered 1343 (different CoT class). (d) v1247 still 0 on a capture newer than 09-24T12:23 — **held**.

**Hypotheses next run:** (a) 1330 publishes on the 12:33Z drip; quote the YouTube id. (b) ch2 1325/1337/1338/1339/1315 publish from 14:00Z; quote ids. (c) overnight patterned scripts after `4559108` do not contain `count the script` / `70–100 words` / `two-beat incident`. (d) v1247 views on a capture newer than 2026-09-25T12:26:03.

`blockingio-transient` (09-05, `48bdaa9`): no BlockingIOError in the 24h digest. Leave `running`.

`queue-plan-mix` (08-24, `60055710`): queued empty both channels, so no live next-to-render to quote. Leave `running`.

`R1` preamble strips 09-21 (`5f6b8b0`) and 09-22 (`9853db8`): ≥72h but ch1-concept grok.Timeout 600s this run. Leave `running`.

`R1` idea-strip 09-23 (`af6dcf3`): **promoted** — new drafts 1346/1352–1363 have no catalog-planning opener.

`R1` idea-strip 09-24 (`600da30`) and patterned lock 09-25 (`f065454`): under 72h. 1343 is a residual of a *new* CoT class, not a series-notes regression. Leave `running`.

## Act

No weight changes. t15 stays off. No idea generation and no trend adoption: both boards at the horizon. A new weight-1 topic cannot displace t26 (weight 2) or t4 (weight 5) / t27 (weight 3) drafts.

Trends logged (no adopt):

| id | score | status | term |
|---|---|---|---|
| 127 | 74 | watching | llama.cpp v0.5.0 CUDA conv2d Metal MoE MiMo-V2.6 — named stable 09-23, t27 genre, score <75 |
| 128 | 68 | watching | Claude Code v2.1.283 doctor prompt-audit availableModelsMatch — agent-traps adjacent, score <75 |

Ollama 0.40.0-rc0 stays watching as 126. Copilot Credits bills wedge stays killed.

Long-form stays parked. Empty long bench is the operator stop.

Seeding kit: `run/seeding/2026-09-26.md`. ch2 features v1298. ch1 features v1329. r/LocalLLaMA and HN skipped. Yesterday's kit cannot be scored: the analytics capture is from before that kit was written.

## Engagement

Rubric baseline this run is **one subject**, ch1-code, label `baseline`. ch1-concept grok.Timeout 600s (0/1, no frames). ch1-code: `visible=True`, `used_fallback=false`. Beats `hook, lst, stat, cmp, term, stat, code, term, cmp, cta`. 74w / 29.06s. Hook hold 2.505s.

| Lever | Score | What the frame showed |
|---|---|---|
| R1 hook | **2** | frame0 `Your RAG is slow and still wrong` + object `RAG`. Title and thumb hook match. |
| R8 title/thumb | **2** | same sentence. Golden subject has no `· series nn`. |
| R2 information | **2** | indented `rerank(q, docs)` / `top = ranked[:5]`; stacked cmp Prompt vs Model; 20→5 stats. |
| R6 payoff | **2** | close `Rerank five. Stop hoping twenty chunks will behave.` |
| R7 CTA | **1** | chip `Subscribe · Copilot Credits` + `same series`. Harness does not pass brand; production patterned videos on the board use the suffix (1329 VO `Subscribe — next Agent memory trap`). |
| R3 clarity | **2** | code indent visible; cmp stacked; hook readable. |
| R4 pacing | **2** | hook 2.51s, mids 2.1–3.0s, CTA 3.77s. |
| R5 retention | **2** | fetch-20 → rerank → top-5. |
| R9 audio | unverified | not listened to. |
| R10 brand | **2** | OS teal/black. |
| R11 format | **2** | one claim, 29s. |

The live R1 leak was **not** the golden: v1343 (patterned Agent memory 24) spoke count-the-script CoT after a two-sentence title head, with a curly apostrophe so ASCII `I'll` missed it. That is the lever this run targeted. Golden ch1-code is unpatterned, so `_lock_patterned_opener` is a no-op there — not claimed as an R1 frame-score increase.

**Shipped:** `_fold_script_punct` normalizes curly apostrophes and en-dashes. `_drop_preamble_sentences` drops every CoT sentence, not only a leading run. `_PREAMBLE_BODY` also matches `count the script` / `two-beat incident` / `opens on that hook` / `70-100 words`.

Proof:

- Isolated call on GET 1343's stored script → opener `Chat linked ticket 19. Prod orphaned it.` then the warehouse body; `count the script` / `two-beat` / `70–100` absent.
- `PYTHONPATH=. .venv/bin/python tests/verify_worker.py` → **260/260**.
- `PYTHONPATH=. .venv/bin/python tests/verify_storyboard.py` → **329/329**.
- Unpatterned golden subject still a no-op (existing check).
- `uv run python -c "import app.main"` printed `import-ok`.
- Manager restarted after `4559108`. `GET /api/dashboard` **200**.

## Watch next run

1. 1330 should publish on the 12:33Z drip; quote the YouTube id. Do not wait this run.
2. ch2 1325, 1337, 1338, 1339, 1315 should publish from 14:00Z; quote ids.
3. Overnight patterned scripts must not contain `count the script` / `70–100 words` / `two-beat incident` after the title head (the running app only has today's lock after the restart below).
4. v1247 views, on a capture newer than 2026-09-25T12:26:03.
5. Golden R7 still names Copilot Credits when brand is missing. Production suffixes are fine; only revisit the harness/default if an unpatterned short ships.
