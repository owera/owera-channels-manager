# Growth agent — 2026-07-28 (Tuesday)

**Commits:** 540d45d (fanout diagram layout, engagement), c4bac85 (window-aware dashboard ETA),
+ this report/log commit.

> ## ⚠ Needs operator
> 1. **Seeding kits: posted or not? (5th day asking.)** EXT_URL lifetime is ch1 7 / ch2 19 —
>    small but nonzero. Without posted-status I can't tell "kits don't work" from "kits never
>    ran". One word unblocks the read.
> 2. **ch2 metadata backfill is ready to apply**: `run/agent-reports/backfill-ch2-metadata-plan.json`
>    (top-20 plan, committed today) awaits the operator `--apply` (10/day quota cap) — shipped
>    db7a254 last night.
> 3. Quota: ch1 8700/9000 before its analytics pass — same deterministic ceiling as all week.
>    Monitor only, no action.

## Pre-flight note
Tree had one untracked file: the backfill plan JSON above. Verified it is exactly the
artifact last night's code-agent cycle documented (ch2 / pt-BR / top-20 `retag_only`
entries preserving proven titles), so I proceeded and committed it with today's run
rather than aborting on a known artifact.

## The day's main read: ch2 publish-window gate is live and holding
First live read of yesterday's activation — **the gate works**:
- ch1 (no windows): 5 publishes on the normal drip, 07:01–09:04 UTC.
- ch2 (11:00–22:00 BRT): **0 publishes by 12:01 UTC with 9 approved videos held** —
  vs 07:02–09:05 UTC publishes yesterday. The publish tick is firing, hitting
  `_window_ok`, and waiting for the 14:00 UTC open.
- Order sim for the open (verified against `_next_approved`'s mix logic + live weights):
  1 forced long (t2 w2 "Fundamentos" long) then 4 t4-w3 shorts — directive-2 mix holds.
- Residual found and fixed (commit c4bac85): the dashboard `next_publish_eta` ignored
  windows and claimed ch2 would publish "now". It now routes through `next_window_open`;
  verified live after restart: ch2 ETA reads exactly `2026-07-28T14:00:00Z`, ch1 unchanged.
- Quota interaction checked (the window flip makes ch2's analytics pass run BEFORE its
  publishes now): analytics costs ≤ 2 units/video ≈ 400 for ch2's catalog — publishes keep
  ~8600 headroom. **No conflict today**; becomes real at a ~500-video catalog (500×2 + 8250
  > 9000) — noted for the future, no action now.

**Day-2 board-mix signal (ed719f8, settles tomorrow): even stronger than day 1.** ch1
published 1 long (766, t5 CrewAI-vs-LangChain) + 4 shorts **all t15 wedge** (784–787,
Copilot-credits series). Language integrity ch2: all pending titles PT-BR, clean.

## Triage
Digest: 16 entries, **none actionable** — 12 `rejected` are the 07-26 deliberate board
rebalance (`leave`, 59.7h old, delete gate is >7d); quota ch1 is the monitor item above;
`publish_starved` ch1 (3 approved) is the known steady state tonight's 00:00 autofill
refills (verified it did exactly that the last two nights). Two **one-off** `error_runs_24h`
signatures (playlist_add + comment, both HTTP 404 "Video not found", count 1 each — likely
raced a deleted/private video). Not recurring → no code fix; if they repeat tomorrow,
root-cause them. BGM pool 20/15, healthy. **No remediation actions taken — nothing needed one.**

## Observed (key numbers)
- **Subs: ch1 10, ch2 60 — flat through 07-28** (channelmetric daily). The 07-20→27 week
  cohort earned **0 subscribers on either channel** (ch1 35 videos / 249 views; ch2 34 / 231).
- **Traffic delta (one day of movement, 07-26→07-27 captures):** ch1 SHORTS **+18**, SEARCH
  +17 — the shorts feed has nearly stopped testing ch1 (was ~58/day in the 07-09→23 cohort).
  ch2 SHORTS **+81**, SEARCH +18 — alive, roughly at its recent pace. BROWSE/SUGGESTED ≈ 0
  on both. Today's pass lands ~12:16 UTC (after this run started).
- **Wedge distribution proof-point:** ch1 643 "You're Comparing AI Coding Prices Wrong —
  Claude vs Copilot" sits at **102 views mature** (291h) — a comparison-format video with
  real discovery, on the exact wedge the strategy bets on. Conversion still 0 for the mature
  cohort (only 558, the MCP long, ever gained a sub: 1).
- ch2 search terms stay tool-shaped: qlora 3, langgraph 3, fine tuning 2, claude code 2.
- Analytics freshness: 07-27 pass captured ch1 125 / ch2 130 rows.

## Monetization
| Metric (Lower tier) | ch1 current / needed / pct | ch2 current / needed / pct |
|---|---|---|
| Subscribers | 10 / 490 / **2.0%** | 60 / 440 / **12.0%** |
| Watch hours | 4.4 / 2995.6 / 0.1% | 7.5 / 2992.5 / 0.2% |
| Shorts views | 2804 / ~3M / 0.1% | 2566 / ~3M / 0.1% |

Subscribers remain the binding constraint on both; no threshold crossed. Conversion
machinery stays the priority — and as of today every distribution precondition (mix, wedge
lead, ch2 windows) is finally live simultaneously. **The falsifier clock effectively starts
now: another flat week under restored preconditions triggers the directive-7 distribution
escalation.**

## Learned / hypotheses
1. The diagram beat could not express one-to-many topology at all (see Engagement) — any
   "one X for every Y" concept rendered as a misleading chain. Fixed; hypothesis logged that
   truthful diagrams lift comprehension-driven retention (settles on render evidence + later
   avg_view_pct).
2. ch1's discovery problem is narrowing to conversion-at-the-margin: distribution collapsed
   (+18 shorts-views/day) while its one comparison video that DID get distribution (643) still
   converted 0. The 07-30 t15-cohort subs read and the 08-01 review carry the decision.
3. ch2 remains the growth asset: retention leaders are all hardware-reality (589 avp 76.3;
   660 avp 99), search is tool-shaped, and windows now put publishes in live hours.

## Actions taken
1. **Engagement change (1 of 2): fanout diagram layout — 540d45d** (details in Engagement).
   Manager restarted; dashboard 200.
2. **Ops truthfulness (2 of 2): window-aware `next_publish_eta` — c4bac85.** Verified live
   (ch2 ETA = 14:00:00Z exactly; ch1 untouched). Manager restarted; dashboard 200.
3. **Trends logged (no adoption — both boards `at_capacity:true` 2.0d):** #40 "GPT-5.6
   family pricing (Luna/Terra/Sol)" (ch1, score 86, watch — pricing-comparison wedge fit,
   adopt with #38 Opus 5 when bench frees) and #41 "DeepSeek V4 Pro open-weight local"
   (ch2, score 74, watch — t27 hardware-reality vein). API quirk reconfirmed: `momentum`
   must be a string.
4. **Seeding kit** `run/seeding/2026-07-28.md`: ch2 TabNews WDDM-VRAM-reservation post +
   r/brdev comment + adjacent-channel comment (all feature 781, live from 11:00 BRT); ch1
   Copilot-indexing-credits comment (787), dev.to CrewAI-vs-LangChain post (766), adjacent
   Copilot-tips comment. Operator posts; ch2 items only after 11:00 BRT.
5. No weight/budget/topic changes — weights already positioned, digest clean, boards full.
6. Committed the pending backfill plan artifact (operator item 2).

## Engagement
**Baseline (full golden set, 4/4 clean, visible=True, no fallback):**
| Lever | score | evidence |
|---|---|---|
| R1 hook (P1) | 2 | 4/4 pain-first ≤7w at t=0 ("Your RAG answers are garbage" / "Semanas perdidas por integração") |
| R8 thumb/title (P2) | 2 | zero title echo 4/4, genuine gaps |
| R2 info gain (P3) | **1** | varied 8–9 beat mixes, real code/stat/cmp — BUT ch2-code drew the one-hub-many-clients MCP concept as a linear chain "Servidor MCP→GPT→Claude→Gemini" (reads as GPT feeding Claude) on a 7.4s-held beat; root cause: `_diagram_svg` accepted `layout` and ignored it, while the prompt advertised `fanout` |
| R6 payoff (P4) | 2 | quotable closes 4/4 |
| R5 retention (P5) | 2 | truth-pivots present, no sag |
| R3 clarity (P6) | 2 | code ≤30 chars indented, nothing clipped |
| R4 pacing (P7) | 1 | min beat 1.68–1.88s (floor holds); drift now covers every >5.5s hold (3/3/3/4) — content-level drag stays parked |
| R7 CTA (P8) | 2 | follow-ask + named next-value in-language 4/4 |

**Targeted:** R2 (weakest at highest priority — P3 beats parked R4 at P7).
**Shipped: 540d45d** — implemented the advertised-but-ignored `fanout` layout
(deterministic hub-first side-rail elbows, model edges = labels only so no arrow can cross
a box; draw-on dash scaled 300→1200 for rail paths) + prompt line stating when each layout
applies. **Gate: PASSED** — unit-exercised geometry (portrait/landscape/labels/2-node
fallthrough/pipeline byte-identical); `verify_storyboard` 40/40; gated re-render of
ch2-code: model **chose fanout**, frame shows hub "Servidor MCP" → Banco/Arquivos/APIs
with clean elbows (and semantically better spokes than the baseline's model-name chain),
hook/CTA/floor/drift all unchanged, 1/1 clean render. Logged as running experiment
(predicted: avg_view_pct up).

**Settled: R4 long-hold drift (70f5320, 07-25) → PROMOTED** (72h, SIGNAL-SCARCE — subs
flat, no retention cohort; render evidence is the verdict). Fresh baseline: every >5.5s
beat carries exactly one drift tween (3/3/3/4 across subjects, none on short beats), min
beats at/above floor; the after-render showed 6/6. No card fully freezes anymore. Kept.

**Running:** board-mix (ed719f8) settles ~07-29 — day-2 signal fired in full (t15 led all
4 ch1 shorts; forced-long mix held). R2 fanout (540d45d) settles ~07-31.

## Watch next run (07-29)
1. **ch2 publish rows in the DB must show ≥ 14:00 UTC today** — completes the live window
   proof (ETA + held buffer already confirm the gate blocks; the publishes themselves happen
   after this run exits).
2. **Settle board-mix (ed719f8)** — two full days of mix data by then.
3. **Spot-check the first production render that carries a diagram** — confirm fanout
   appears in real videos, not just the golden set.
4. Wedge-cohort `subscribers_gained` first read ~07-30; **08-01 ch1 hard review is Friday** —
   if ch1 is still <15 subs, lead with the pause recommendation per directive 9.
5. If bench space frees: adopt #38 (Opus 5) into the ch1 wedge first; #40 (GPT-5.6) is the
   fresher pricing angle — pick ONE.
6. One-off 404 error_runs (playlist_add/comment) — root-cause only if they recur.
