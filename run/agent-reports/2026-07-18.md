# Growth agent report — 2026-07-18

**No operator action required.** Both channels connected; analytics flowing; clean-ish digest
(only routine rejected/quota noise). One engagement change shipped + gated + live.

## Observed (key numbers)
- **Subscribers:** ch1 **10** (was 9 for 11+ days — moved to 10 yesterday, holding); ch2 **59**
  (target 75 by 07-23 checkpoint).
- **Board:** both channels `at_capacity` (10 pending / 2.0 days each) → no idea generation, no
  trend adoption this run.
- **BGM pool:** 20 tracks (target 15, min 5) — healthy, `is_low:false`. No action.
- **Analytics:** ch1 135 videos with view data, ch2 109. `has_data` is the live field (the older
  `measured` name is gone).
- **Distribution vs conversion (the core read):** aggregated `traffic_json` latest-snapshot per
  video across the whole cohort:
  - ch1: **SHORTS 1767**, YT_SEARCH 286, YT_OTHER_PAGE 122, YT_CHANNEL 16, EXT_URL 3.
  - ch2: **SHORTS 1637**, YT_SEARCH 250, YT_OTHER_PAGE 49, YT_CHANNEL 35, RELATED 11, EXT_URL 10.
  - **The Shorts feed is distributing both channels heavily** (1767/1637 views) with strong search
    pickup. BROWSE/SUGGESTED are ~0, but for shorts the **SHORTS feed IS the algorithmic test
    signal** and it is firing. → **Directive-7 falsifier does NOT trigger** (distribution is not
    the wall). The gap remains **conversion**: ch1 gets ~1767 shorts-feed views and converts ~0
    subs. This is exactly the wedge-repositioning thesis (directive 9).

### Per-video signal
- **ch1 top mature:** 519 (t3, 145v, 38.7% avp, 0 subs), 526 (t6, 78v, 39.9%), **482 (t15 Copilot,
  57v, 47.6% avp)** — the wedge topic shows the best retention among recent. Lifetime sub-gainers
  (3): 367 (t6 short), 43 (t1 long), 96 (t5 long) — all old.
- **ch2 top mature:** **542 (t4, 404v, 47.6% avp, 1 sub)**, 589 (t27 Ollama, 96v, 56.6% avp), 524
  (t4, 93v, 42.3%, 1 sub). Sub-gainers: 542, 524 (t4 hardware shorts), 421/315 (t2 longs). **t4
  hardware is the proven ch2 converter.**

## Monetization
| Metric | ch1 current / needed (Lower) | pct | ch2 current / needed (Lower) | pct |
|---|---|---|---|---|
| Subscribers | 10 / 490 | 2.0% | 59 / 441 | 11.8% |
| Watch hours | 3.3 / 2996.7 | 0.1% | 5.0 / 2995.0 | 0.2% |
| Shorts views | 2162 / 2.997M | 0.1% | 1822 / 2.998M | 0.1% |

Binding constraint both channels = **subscribers** (highest realistic pct; watch-hours/shorts-views
thresholds are ~3M and years away at this scale). Subscriber conversion is where content investment
pays — matches the standing subscriber-offensive directives. No metric hit 100%.

## Triage
- **Deleted 5 dead rejected videos** (625–629, ch1): rejected 181.6h ago (7.6 days > 7d), no
  `video_path`, digest `suggested_action: delete`, all t3-parked (banned per directive 9).
  After: `GET /api/agent/issues` rejected count **8 → 3**; `GET /api/videos/625` → **404**. ✓
- Remaining 3 rejected (661/675/679) are <7d (`leave`) — untouched.
- **Quota:** ch1 & ch2 both at 8750/9000 today (`daily_limit_hit:false`, `auto:false`) — under cap,
  self-resets at the Pacific reset. Monitor only; no budget/drip change.
- No `failed`, no stuck rendering/publishing/review, no oauth issues, no music-gen errors,
  `error_runs_24h` empty. Clean otherwise.

## Learn (hypotheses)
1. **Conversion, not distribution, is the ch1 wall** (SHORTS feed = 1767 views → ~0 subs). The
   R7/CTA follow-ask is the highest-leverage conversion lever — targeted this run.
2. ch1 wedge (t15 Copilot, 482 = 47.6% avp) retains better than legacy t3 (38.7%) — repositioning
   is directionally right; needs a matured conversion cohort to confirm.
3. ch2 t4 hardware remains the converter; keep it the w3 lead (already is).

## Engagement
**Baseline (07-18 golden set, 4/4 clean renders, all visible=True/no-fallback):**
- R1 hook: **2** — all four pain-first, concrete, on-screen at t=0 ("Your AI forgets you instantly",
  "Sua RAG responde com lixo", "Chega de reescrever integrações", "The answer's at position 7").
- R8 thumbnail/title: **2** — anchored hooks, no title echo (07-14 change holding).
- R2 info-gain: **2** — varied mixes with code/stat/diagram/cmp on all subjects.
- R7 CTA: **weakest, 2/4 weak** — ch1-code **"Try it → Add 5 lines today"** (action CTA, no follow
  ask); ch2-concept **"Siga → Conserte seu chunking"** (follow verb OK but sub topic-echoes this
  video). ch1-concept/ch2-code strong. R7 is the subscriber-conversion lever (directive 4 priority;
  signal = subscribers_gained) → **this run's target.**

**Shipped (commit `70861ef`): deterministic follow-verb guarantee on the CTA.**
The storyboard prompt already says "text is a follow verb" but the LLM violated it ~half the time —
the same failure mode the R4 pacing prompt-rules hit, and the same fix: code owns the structural
guarantee. `compose()` now forces the cta beat's `text` to the follow verb for the narration
language (Follow / Siga / Sigue), threaded from the voice via `language_from_voice` in **both** the
production path (`worker._generate_composition`) and the gate harness (`rubric_review.py`). The LLM
keeps authoring only the `sub`. Also kills a latent English "Subscribe" default that could leak onto
a PT channel.

**Gate (draft `--only` renders, foreground):**
- ch1-code CTA **"Try it → Add 5 lines today" → "Follow → Daily RAG fixes"** (R7 **1→2**: follow ask
  now guaranteed; the fresh draft also produced a next-content sub).
- ch2-code (PT) **"Siga → Como montar seu MCP amanhã"** — correct PT verb (forced), **no English
  leak**, next-content sub. PT path confirmed.
- Change touches only `cta.text`, so **no other lever can be affected**; after-render hook
  ("Right docs, dumb answers") and beat variety unchanged/healthy.
- `verify_storyboard` **40/40**; both renders visible=True, no fallback.
- Made **LIVE**: manager restarted (`launchctl kickstart`), `GET /api/dashboard` = 200, `compose`
  signature confirms the `language` param loaded.
- **Residual (open):** the sub-line topic-echo (ch2-concept "Conserte seu chunking") is
  content-level and stays LLM-authored — revisit with a sub-focused lever if it recurs.

**Settled experiment — 5858fd0 (publish-order weight-first, shipped 07-15): PROMOTED.**
Mechanism live-verified via DB read-back: since 07-16 ch1 publishes weight-first — t15 Copilot wedge
(w3) + t5 anchor (w2) lead daily (07-16: 4×t15+t5; 07-17: 2×t15+2×t5+t6; 07-18: t5/t6), replacing the
FIFO backlog of 34 non-wedge shorts that buried the wedge cohort pre-fix; ch2 → t4 (w3) leads.
Predicted signal (subs up): ch1 moved 9→10 (checkpoint directive-7 "ch1 finally moves past 9" met);
attribution still SIGNAL-SCARCE (post-07-16 cohort <72h). Kept.
**Still running:** 14b1979 (R4 deterministic beat-floor, 07-16) — only ~48h old, <72h; leave.

## Channel actions
- Triage deletes (625–629). No topic weight/budget changes (both at capacity; wedge weights already
  set 07-13). No idea generation / trend adoption (capacity gate).
- **Trend research:** Copilot backlash (#198015 now 400+ comments/~900 downvotes; promo credits
  expire **end of Aug 2026**) is already the adopted t15 wedge — used as the seeding angle, not a new
  topic. Logged one fresh ch2 watch candidate: **trend id 35** `qwen3-coder:30b MoE local (19GB Q4,
  256K)` — fits t4/t27 but ch2 at capacity, no adoption.
- **Seeding kit #8** written → `run/seeding/2026-07-18.md`. Featured ch1 646 (20k-credit multi-file
  edit, deadline angle) + ch2 687 (R$300/mês API vs local). Prior seeds: EXT_URL totals ch1=3/ch2=10
  (small, mostly older ch2 videos) — no new spike; seeds still appear largely unposted by operator.

## Watch next run
- Does the R7 follow-ask force hold live on the next published cohort? (deterministic, so yes by
  construction — but confirm a real published video's CTA is "Follow"/"Siga".)
- Settle 14b1979 (R4 beat-floor) at 72h next run.
- ch1 subs 10 → ? and whether any wedge (t15) video starts showing per-video `subscribers_gained`.
- **07-23 hard checkpoint** approaching: ch2 59→75 target; ch1 off 9. If BROWSE/SUGGESTED stays 0
  but SHORTS keeps firing, the story stays "conversion, not distribution."
- The CTA sub-line topic-echo residual (ch2-concept) if R7 needs another pass.
