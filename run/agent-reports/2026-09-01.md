# Growth agent — 2026-09-01

## ⚠ Needs operator

1. **PAUSE ch1 (hard review overdue since 08-01).** Subs moved **12 → 13** (first tick since 08-24). Still **< 15**. Video-attributed converters remain **six** (v558 MCP, v367 t6, v43 t1, v96 t5, v1032 t6, v960 t5 long) — the +1 is **not** on a measured video yet (analytics lag). **Recommendation stands: PAUSE ch1** (or explicitly override with a new bar). Agent continues **zero ch1 strategy moves**. Hygiene only: t3 stays w=0, t5 long-anchor stays w=2.
2. **Two consecutive 0L+5S days on ch1 (08-31 and 09-01).** 08-31 was TTS (`NoAudioReceived` on v1213). 09-01 was the retry: v1213 rendered at 00:00 into **REVIEW** (`skip_gate=false`) with `used_fallback=true` (kinetic-text 274s). Same night ch2 long **v1223** also fell back and was sitting on the 14:00 slot. Root cause: `grok -p` timed out at 300s on the ~10k-char long storyboard; the worker swallowed it into fallback. This run requeued both longs and stopped swallowing `GrokCLIError`.
3. **ch2 metadata backfill** (`db7a254` plan) still awaits `--apply` if still desired.
4. **BGM pool path** remains external (`MoneyPrinterTurbo/resource/songs`). Count 30 (target 15); wipe risk still escalates to silent renders.
5. **Copilot Business/Enterprise promo credits ended today (2026-09-01).** Logged as trend #91 rejected (ch1 frozen). Deadline passed.

---

## Observed

- **Now:** 2026-09-01 ~12:00 UTC (Tuesday). Kill switch off, `scheduler_paused: false`, tree clean at start, `main`. Last full growth run: 2026-08-31.
- **ch1:** published **5/5** today, quota **8750/9000**. Mix = **0 long + 5 shorts** (v1211/1218/1219/1220 **t15** + v1072 **t18**). Second straight 0L day. Cause: overnight t5 long **v1213** rendered but landed REVIEW with fallback HTML.
- **ch2:** published_today **0** at observe (window **14:00 UTC**). rendered_today **5**. **08-31 did publish 1L+4S** (v1212 t2 long first @ 14:00 `yt8Pe-9xZmA`, then v1197/1204/1205/1206 **all t4**) — yesterday’s prediction held.
- **ch2 14:00 plan after this run’s requeue of v1223:** 1207/1214/1215/1216 t4 then 1002 t27 — **0L+5S today**, chosen because v1223 was a 278s kinetic-text fallback. Overnight 09-02 queue-plan already labels **v1223** next (budget full until 00:00).
- **ch2 subscribers: 64** (unchanged). Lifetime converters still **seven** (v542/524/810/421/684/315 + v1022). v1022 still 4 views / 47.82% avp / YT_SEARCH=4 / +1.
- **ch1 subscribers: 13** (was 12 on 08-24…08-31). Subs-per-video on the mature leaderboard: **still 6 lifetime +1s**. None on the post-07-13 Copilot-credits wedge (t15 still 0 subs on 57 measured / 66 published).
- **BGM pool:** 30 (min 5 / target 15; `is_low: false`). Last generate 08-28. No `music_gen` errors.
- **Boards (API `by_format`) after requeue:**
  - ch1 draft `{long:1, short:9}` · queued `{long:1, short:0}` · approved `{long:0, short:1}` (1083 t6)
  - ch2 draft `{long:1, short:9}` · queued `{long:1, short:0}` · approved `{long:0, short:11}`
  - both `at_capacity: true`, `days_of_inventory: 2.0`, pending 11 (queued long is the extra seat). `board_overflow` [].
- **Language (ch2):** 08-31 titles all PT-BR ✓ (1212 Trava em Produção; 1197 fonte 1000W; 1204 RAM 32GB; 1205 modelo antigo; 1206 IA offline). No EN leak.
- **traffic_json (via leaderboard API, no DB dive):** v542 SHORTS=385; v785 SHORTS=164 + YT_SEARCH=6; v558 SHORTS=104 + YT_SEARCH=12; v948 SHORTS=64 + **EXT_URL=9** (unchanged vs 08-17…08-31) + NOTIFICATION=3; v884 YT_SEARCH=22 + SHORTS=77; v1022 YT_SEARCH=4; v1032 SHORTS=22 + YT_SEARCH=2; **v960 EXT_URL=1**. SHORTS / YT_SEARCH dominate. **No BROWSE / SUGGESTED keys** on the top samples. No invented CTR.
- **Midnight 09-01:** auto_produce queued t5 long **v1213** (the 08-31 TTS failure, requeued yesterday). Render **succeeded** at 00:00 (`retry_count=0`, `video_path` present) but `used_fallback=true` / `beat_count=0`. Manager log 21:10:03: `storyboard compose failed for 'Your Agents Ignore Fallback Models…': grok.Timeout: grok -p timed out after 300s`. Same signature on ch2 v1223 at 21:15:15.

### Publish log (settle evidence)

| day | ch1 | ch2 |
|-----|-----|-----|
| 08-25 | **5 (1L+4S)** long 1056 first + 3×t6 + t18 | **5 (1L+4S)** long **1055** first + **4×t4** |
| 08-26 | **5 (1L+4S)** long 1079 first + 2×t18 + 2×t7 | **5 (1L+4S)** long **1073** first + **4×t27** |
| 08-27 | **5 (1L+4S)** long 1068 first + t7 + 3×t6 | **5 (1L+4S)** long **1164** first + **4×t4** |
| 08-28 | **5 (1L+4S)** long **1165** first + **4×t15** | **5 (1L+4S)** long **1179** first + **4×t4** |
| 08-29 | **5 (1L+4S)** long **1166** first + t43 + **3×t15** | **5 (1L+4S)** long **1193** first + **4×t4** |
| 08-30 | **5 (1L+4S)** long **1203** first + **4×t15** | **5 (1L+4S)** long **1202** first + **4×t4** |
| 08-31 | **5 (0L+5S)** 5×t15 — long 1213 TTS-failed | **5 (1L+4S)** long **1212** first + **4×t4** (1197, 1204–1206) |
| 09-01 | **5 (0L+5S)** 4×t15 + t18 — long 1213 fallback→review | pending window; plan after requeue = **0L + 4×t4 + t27** |

## Monetization

| metric | ch1 current | ch1 needed | ch1 % | ch2 current | ch2 needed | ch2 % |
|---|---|---|---|---|---|---|
| subscribers | 13 | 487 | 2.6% | 64 | 436 | 12.8% |
| watch hours | 8.3 | 2991.7 | 0.3% | 12.9 | 2987.1 | 0.4% |
| Shorts views | 4819 | 2,995,181 | 0.2% | 4635 | 2,995,365 | 0.2% |

**Binding constraint both channels: subscribers** (lower tier). No milestone crossed (no metric hit 100% for the first time). ch1 shorts_views held 4819 (no new measured Shorts views vs 08-31); ch2 held 4635. Prefer analytics (keep t4 as ch2 converter lead; ch1 frozen) over pure milestone math.

ch1 **13 subscribers** is the first move off 12 since 08-24. Still 2.6% of lower-tier. Do not unfreeze.

## Triage

Digest at start: **not clean** (`summary.clean: false`, `total_issues: 81`, `needs_operator: 1`). `failed=[]`, `pipeline_starved` / stuck / oauth / bgm_pool_low empty. Quota monitor + **80 rejected** (flood, age **168.3h**, `suggested_action: delete`). `error_runs_24h` [].

| Issue | Action | Proof |
|---|---|---|
| 80 rejected t33–t42 (age 168.3h, `suggested_action: delete`, `auto: true`) | `DELETE` 1084–1093 (cap 10) | GET `/api/videos/1084` → **404**; GET `/api/videos/1094` still `rejected`; issues `rejected=70` (80→70). 70 remain for later runs. |
| v1213 t5 long in REVIEW, `used_fallback=true`, `skip_gate=false`, age ~12h (not stuck_review) | `PATCH skip_gate=null` then `POST /requeue` | after: `status=queued`, `skip_gate=None`, `error=null`; queue-plan `{"1213":{"reason":"render budget full (5/5)","eta":"2026-09-02T00:00:00+00:00"}}`. Lifecycle: re-render → QUEUED (render_loop._submit_new), not APPROVED. |
| v1223 t2 long APPROVED, `used_fallback=true` (same grok.Timeout, would publish 14:00) | `POST /requeue` | after: `status=queued`; publish-plan first card flipped **1223 → 1207** t4 short. queue-plan `{"1223":{"reason":"render budget full (5/5)","eta":"2026-09-02T00:00:00+00:00"}}`. |
| quota ch1 8750/9000 (`auto: false`) | monitor only | 5 publishes; `daily_limit_hit: false`. |
| t3 parked / t5 long-anchor | no change | GET `/api/topics/3` `weight=0`; GET `/api/topics/5` `weight=2` `content_format=long` |
| v1216 t4 short `used_fallback=true` (unparseable storyboard, 21:25 log) | leave | 36s short, already in today’s 15:30 slot behind three good t4s. Not a 277s kinetic long. |

Failed list stayed empty. 70 rejected rows remain (age >7d, will continue deleting at 10/run).

## Learned

1. **ch2 08-31 mix prediction held:** 1L+4S, long v1212 first, then **4×t4**.
2. **The 08-31 TTS fix worked on the retry — and exposed the next bug.** v1213 did not fail TTS at 00:00; it completed. The composition timed out and the worker **completed anyway** with fallback. `grok.Timeout` was already in `_TRANSIENT` and never fired, because `STATE_FAILED` was never set.
3. **Both overnight longs hit the same 300s grok cap** (prompt_chars 9957 / 9890). Same-night shorts (~6k-char prompts) mostly composed. v1216 was the short that unparsed (different failure: `storyboard: unparseable`).
4. **Winners (mature):** ch2 t28 25.8 avg views @ 59.5% avp / 0 subs; t4 20.9 avg + **5 lifetime subs** (the only converter that still moves); t27 19.3 avg / 43.5 avp / **0 subs**. ch1 t26 18.8 avg / 64.8 avp / 0 subs; t15 18.4 avg / 52.2 avp / 0 subs on 57 measured; t6 14.6 avg + **2 lifetime subs**; **t5 long 2 lifetime subs** (v96 + v960).
5. **Hypotheses this run:** (a) ch2 09-01 window publishes 0L+5S (we pulled the fallback long) — 4×t4 + t27; (b) midnight 09-02 `_submit_new` takes queued longs **v1213** / **v1223** first (queue-plan already labels them next, budget-full until then); (c) a future grok timeout on compose **fails** the job (`retry_count` 1 then QUEUED) instead of completing `used_fallback=true`.

`queue-plan-mix` (shipped 08-24, `6005571`): 192h+. Live noon queues are **not empty**: each channel shows its long as the only card, `approved_longs=0`, budget already 5/5. Still missing the harder case (long behind lower-id shorts with budget remaining). Leave `running`.

`tts-transient` (shipped 08-31, `730ce56`): <72h. The TTS flake did not recur; the retry rendered. Leave `running` until 09-03.

## Acted (channels)

- **Deleted 1084–1093** (see Triage). **Requeued v1213 + v1223**. No generate / no displace / no produce. ch2 lowest-weight draft is t2 long w2 (must not displace) then t27 w3 / t4 w4 — no w1-or-lower seat for a ≥75 trend. ch1 frozen.
- **Trends logged (no adopt):**
  - **#73** ch2 score **90** — Qwen3.8 **1.3M** (was 1.2M, +100K). Proof: id 73, status=adopted, topic 33 parked.
  - **#86** ch2 score **83** (was 82) **watching** — Ornith 1.5 **281.1K** (was 232.9K, **+48K**). Still ~5× below Qwen. Do not adopt (no w1 seat). Proof: id 86, status=watching, topic=null.
  - **#90** ch2 score **68** **watching** (NEW) — Qwen3.8-27B Unsloth Dynamic V3 1-bit ~6.7GB / 16GB RAM. Quant of parked t33, below 75-bar. Proof: id 90, status=watching, topic=null.
  - **#91** ch1 score **70** **rejected** (NEW term; old #85 kept) — Copilot promo credits **ended today**. *Is* the wedge; rejected because ch1 is frozen at 13. Proof: id 91, `adopted_topic_id: null`.
- **Live 409 check:** `POST /api/trends/84/adopt` → http 409 `board at capacity (horizon reached) — displace drafts first`; pending stayed 11 (queued longs).
- **BGM:** no generate (refreshed 08-28; count 30 unchanged). Proof: GET `/api/music` count=30.
- **Seeding kit:** `run/seeding/2026-09-01.md` (operator posts; agent does not). Features 08-31 t4 1197/1204/1205 + t2 long 1212 + ch1 t15 1211/1218.

## Engagement

**No golden-set re-render this run.** 08-31 baseline scored every high-priority lever **2**. This run’s defect is not a prompt lever: overnight longs completed with `used_fallback=true` because compose timed out and the worker swallowed it. Re-rendering the short-form golden set would not have shown the long-form timeout. Pipeline robustness instead (counts as the one change).

**Target:** stop shipping kinetic-text fallbacks when `grok -p` hits the 300s cap.

1. `grok_timeout_seconds` 300 → **600** (long storyboard prompts are ~10k chars; shorts ~6k finished in ~1–2 min last night).
2. `_generate_composition` **re-raises `GrokCLIError`** (timeout/nonzero). Generic exceptions still return `""` → fallback. `run_job` then sets `STATE_FAILED` with `grok.Timeout` in the error, which `_TRANSIENT` already classifies → QUEUED retry.

Lifecycle (this change does not set `Video.status` itself; it changes whether `run_job` completes or fails when compose times out):

```
queued ─_submit_new→ rendering ─worker.run_job compose GrokCLIError→
  STATE_FAILED error contains grok.Timeout
  ─_advance_in_flight poll FAILED→ transient + retry_count<2 → QUEUED
  else → FAILED
```

`_submit_new` is the consumer of the QUEUED retry. APPROVED is never the target (no usable storyboard yet). Fallback HTML is no longer a successful complete for this signature.

**Proof (synchronous, crafted state — not a wait for midnight):**
- Isolated `uv run python -c`: `settings.grok_timeout_seconds == 600`; compose `GrokCLIError` **re-raises**; generic `RuntimeError` still returns `""`; `"grok.Timeout"` still in `render_loop._TRANSIENT`; lockstep with `issues.TRANSIENT_SIGNATURES`.
- `tests/verify_worker.py` **202/202** (compose GrokCLIError re-raises; run_job → `STATE_FAILED` with `grok.Timeout` in error, no `final.mp4`).
- `tests/verify_llm.py` **47/47** (default timeout is 600s).
- `tests/verify_render.py` **135/135** (`grok.Timeout` still QUEUED).
- `tests/verify_storyboard.py` **234/234** (untouched).
- `uv run python -c "import app.main"` ok; manager restarted (`launchctl kickstart`); `GET /api/dashboard` HTTP **200**; v1213 and v1223 still `queued` / `skip_gate=None` after restart.

Watch next run: midnight 09-02 JobRuns for 1213/1223 — expect `used_fallback=false` and a real beat mix, **or** a transient retry JobRun naming `grok.Timeout` (not a successful fallback complete).

### Experiments settled / opened

- Left running: `queue-plan-mix` (live noon shows longs 1213/1223 first with `approved_longs=0`, still no lower-id shorts behind them with budget remaining).
- Left running: `tts-transient` (`730ce56`, <72h; TTS did not recur on the 09-01 retry).
- Opened: compose-timeout / no-swallow (`PENDING` until this commit’s sha).

## Watch next run

1. Operator ch1 pause yes/no (still the binding decision). **13 subs does not clear 15** — do not unfreeze. Keep t3 at 0. Quote whether 13 holds or bounces; quote whether the +1 ever appears on a measured `subscribers_gained`.
2. ch2 09-01 publish composition after 14:00 UTC — expect **0L+5S** (1207/1214–1216 t4 + 1002 t27) because we pulled the fallback long.
3. Midnight 09-02: queued longs **v1213** (ch1) and **v1223** (ch2) are labeled next-to-render. Quote `creation_config.used_fallback` and beat_count. If grok times out, expect `STATE_FAILED` → QUEUED retry, not a successful fallback.
4. `queue-plan-mix` live signal still wants a noon where a long is queued **behind lower-id shorts** AND `approved_longs==0` AND render budget is not already spent.
5. 70 remaining rejected t33–t42 rows (age >7d) — continue deleting at 10/run.
6. Do not unpark t33–t42. Qwen3.8 (#73, 1.3M) is still the only watching-class term at 1M-scale; Ornith 1.5 (#86, 281.1K, +48K) is the one accelerating. Serve either as a **t4 episode via displacement** if a w1-or-lower draft exists, not by unparking.
7. BGM count stays ≥ min (30, above target 15). Next refresh only if stale again *and* renders landed.
8. Quote whether v948 EXT_URL grows past 9 (seeding signal). Quote v1022 views/avp (4 / 47.8% today). Quote v1032. Quote **v960** (converter, EXT_URL=1).
9. v1216 (fallback short, publishing today) — if avp is a disaster vs sibling t4s once mature, treat unparseable shorts as the next compose bug.

## Commit

this commit — grok compose timeout no longer ships fallback longs; report + seeding + experiment log.
