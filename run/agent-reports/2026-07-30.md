# Growth agent — 2026-07-30

## ⚠ Needs operator

**1. The 08-01 ch1 pause review is the day after tomorrow, and the case got stronger
again.** The mature post-07-16 wedge cohort is now **60 videos, 501 views, 0 subscribers**
(was 55/476/0 yesterday). t15's post-wedge retention fell another step: **22.4% avg avp
across 9 mature videos** vs 58.1% for the 17 pre-wedge ones (yesterday: 33.7% on 6) — same
topic, same weights, monotonically worse. ch1 sits at 10 subs against the 15-sub bar with
one day of data left. Unless tomorrow's numbers surprise, Friday's report will lead with
the pause recommendation and the redirect-budget-to-ch2 case.

**2. Seeding: third consecutive flat day — please answer the standing question.**
Across every tracked video, EXT_URL moved by exactly **+1 view (video 689)** since
yesterday; 568/683/741 are byte-identical again. If the 07-27→29 kits were posted, seeding
is not working and we should cut it; if they weren't, I'll stop drafting daily kits until
that changes. Either answer redirects tomorrow's effort. (Today's kit is still written:
`run/seeding/2026-07-30.md`.)

**3. (carry-over)** ch2 metadata backfill (`db7a254`, plan file written 07-28) still
awaits the operator `--apply` (10/day cap). Not re-verified today.

Everything else healthy: both channels `oauth_status: connected`, zero failed videos,
nothing stuck, `error_runs_24h` empty, BGM pool 20 (min 5 / target 15).

## Observed

- **Now:** 2026-07-30 ~12:00 UTC. Kill switch absent, scheduler running, tree clean.
- **ch1 Owera Software:** 230 published, **10 subs (flat)**. Today's 5 published 07:01–09:04
  UTC: 1 long (768, t5 LangChain-vs-CrewAI) + 4 shorts (798, 797, 804, 805 — all t15).
  Quota 8750/9000 again (`daily_limit_hit: false`), same shape as yesterday: 5 uploads +
  playlist/comment/thumbnail work. Nothing was blocked.
- **ch2 Rodrigo Recio:** 206 published, **60 subs (flat)**. Yesterday's watch item
  confirmed: the publish-window gate works in production — 5 rows published 14:01–16:05 UTC
  (11:01–13:05 BRT), all inside the window. Today's batch is staged: 10 approved, and the
  publish loop's reserved long slot will lead with **720 (t11 LangGraph long, approved since
  07-18)** at 14:00 UTC, then 4 t4 shorts.
- **Directive-2 mix: day 4 holding on both channels** (publish log): ch1 = 1L+4S with t15
  taking every short slot; ch2 = 1L+4S yesterday, same shape staged today.
- **Language integrity (directive 6):** all 5 newest ch2 titles PT-BR (751, 782, 783,
  789, 790). ✓
- **Boards:** both `at_capacity: true` (10 drafts, 2.0 days) → no idea generation, no trend
  adoption this run.
- **Traffic:** ch2 shorts keep getting SHORTS-feed distribution (683: 25, 741: 21); ch1
  remains low-volume. Conversion, not distribution, is still the gap.

## Monetization

| metric | ch1 current | ch1 needed | ch1 % | ch2 current | ch2 needed | ch2 % |
|---|---|---|---|---|---|---|
| subscribers | 10 | 490 | 2.0% | 60 | 440 | 12.0% |
| watch hours | 4.5 | 2995.5 | 0.1% | 9.1 | 2990.9 | 0.3% |
| Shorts views | 2858 | 2,997,142 | 0.1% | 2911 | 2,997,089 | 0.1% |

No metric moved meaningfully. Subscribers remain the only live constraint; ch2 remains the
asset (6× ch1's subs, now on 24 fewer published videos).

## Triage

Digest at run start: 14 issues, 1 needs-operator, `clean: false`. After investigation:

- **`publish_starved` (ch1, auto) — DIAGNOSED AS A FALSE ALARM and fixed at the root
  instead of obeyed.** The suggested action ("produce + render more drafts") is what I did
  yesterday — and the audit log proves it was pointless: a **midnight auto-produce**
  (`render_loop._auto_produce`, "fill free render capacity") queues active-topic drafts
  every night at 00:00 and the render loop refills the approved buffer before the 07:00
  publish window. On 07-30 00:00 it produced 5 for ch2 and skipped ch1 *only because my
  manual produce had already filled its queue* — the same 5 slots, same mix. The digest
  predates auto-produce (its comment still said "nothing in the app promotes
  DRAFT → QUEUED") and read the buffer mid-cycle. Fixed in code — see Code change below.
  **After-state proof:** `GET /api/agent/issues` post-restart returns
  `pipeline_starved: []` (was 1 entry), total_issues 14 → 13.
- **12 × `rejected`, `suggested_action: "leave"`** — the 07-26 board-rebalance rows
  (757–765, 771–773), age ~108h, under the 7-day delete floor. Untouched by design.
- **`quota` (ch1, escalate)** — 8750/9000, identical non-blocking shape two days running;
  resets 07:00 UTC. No budget change (nothing was blocked; lowering budget costs
  distribution to fix a non-problem).
- **No manual produce today — deliberately.** Tonight's auto-produce is predicted to queue
  ch1: 1 t5 long + 3 t15 + 1 t6 shorts (bench holds no approved long, so the long-slot
  branch fires), ch2: 1 t2 long + 4 t4 shorts. Verifying this prediction tomorrow IS the
  live test of both the fix and the no-produce decision.

## Learned

1. **The pipeline is fully self-sustaining end-to-end — and yesterday's manual produce
   was a no-op.** Discovery of the day: `_auto_produce` (render loop, every tick) +
   the publish loop's reserved daily long slot + autofill's weight-first idea refill
   together already implement the entire directive-2 mix mechanically. The agent's
   production job is now *editorial* (which subjects, which topics), not *operational*
   (keeping the belt fed). This also reframes the daily triage table: `pipeline_starved`
   was the one "auto-fix" I performed daily, and it was theater.
2. **t15 is saturating on retention, not just conversion.** 58.1% → 22.4% avp pre/post
   wedge is a monotone slide across three daily measurements (58.2 → 33.7 → 22.4 as the
   cohort matured 0 → 6 → 9 videos). 34 published videos on one pricing angle appears to
   be past the vein's yield.
3. **ch2's two strongest veins by retention are t27 (53.1% avp, 43.2 avg views) and t28
   Claude Code (60.0% avp)** — both new-ish topics with 8 videos each. t4 carries volume
   (30.9 avg views) but converts at 3 subs / 73 videos. The growth thesis for August:
   feed t27/t28, keep t4 as the discovery funnel.

**Hypotheses:** (a) unchanged — ch1's failure is niche-level; (b) t15's decay is
saturation, not execution (same system, same weights, falling avp); (c) t27/t28's high
avp at low volume will convert better per-video than t4 once fed — testable by feeding
them when the board opens.

## Engagement

**Baseline** (full golden set `baseline-0730`, 4/4 clean, `visible=True`, none
`used_fallback`):

| Lever | score | evidence |
|---|---|---|
| R1 hook (P1) | 2 | 4/4 pain-first at t=0 ("Perfect embeddings, garbage results" / "Sua RAG despeja lixo"); openers ≤10w except ch2-code at 13w (pain still front-loaded) |
| R8 thumb/title (P2) | 2 | zero echo 4/4, real gaps ("Goldfish memory, fixed" vs "Why Your AI Agent Forgets…") |
| R2 info gain (P3) | 2 | **both running R2 fixes visibly holding in fresh renders**: fanout diagram drew hub→3-targets (ch2-code), list midpoint frames show ALL items (ch2-concept b3, 5.3s); real stat/code/cmp beats throughout |
| R6 payoff (P4) | 2 | quotable closes 4/4 ("Chunk ruim entra, resposta ruim sai. Simples assim.") |
| R5 retention (P5) | 2 | truth-pivot in all 4 ("A verdade é dura: …") |
| R3 clarity (P6) | 2 | code ≤30 chars indented w/ highlight, nothing clipped |
| R4 pacing (P7) | **1** | ch1-concept hook held 8.69s (23% of runtime); ch2-code code-beat 8.86s; min beat 1.68s (floor holds) |
| R7 CTA (P8) | 2 | spoken follow-ask + named next value, in-language, 4/4 |

**Targeted: none — a deliberate no-op day, and here is the evidence.** The only weak
lever is R4, and I forensically checked its one bad number before touching anything:
`silencedetect` on ch1-concept's narration shows **no silence ≥0.6s anywhere** — the
8.69s hook hold is genuinely slow TTS delivery of a 10-word opener (~1.15 w/s vs the
2.7 w/s video average), not dead air and not a cue-alignment bug. Prompt-level pacing was
declared exhausted twice (07-16, 07-19 verdicts: "revisit only with a content-aware
mechanism, not another prompt rule"), the drift tween (70f5320) already keeps long holds
alive, and TTS delivery-rate surgery is R9-risk territory too large for one gated day.
Shipping a cosmetic tweak to claim the daily improvement would be exactly the
ship-on-faith the gate exists to prevent. Two R2 experiments are also still unsettled —
stacking a third visual change would muddy both settles.

**Settled: none matured** (fanout `540d45d` settles ~07-31; list-reveal `ce46b43`
~08-01). Today's baseline is early positive evidence for both (see R2 row).

**Production artifact note (yesterday's watch item 3):** production renders keep only
`video.mp4`/thumbs — no storyboard `index.html` — so the list-cap can't be re-verified
from today's published files; the golden-set render (where 3 rows land by +2.45s on an
8.855s beat) remains the render-level proof.

## Code change (1 of ≤2): `14f6471` — pipeline_starved digest false alarm

`app/services/issues.py` + `tests/verify_issues.py`. The two starvation checks predate
`_auto_produce` and misfired daily (see Triage). Now: `publish_starved` projects the
buffer forward (approved + in-flight + producible active-topic drafts capped at one
render-budget-day) and fires only when that still misses a publish day; `render_starved`
fires only when waiting drafts are all on parked/inactive/missing topics — the same
inner-join filter `_auto_produce` applies, i.e. the one stall the loops cannot heal.
**No `Video.status` transition is touched — detection only** (the lifecycle map is
unchanged; nothing new consumes or sets any status).

**Verification (all synchronous, this run):**
- `verify_issues.py` rewritten to the post-auto-produce contract: the exact 07-29/30
  false-positive shape (ready 3 + 10 producible drafts vs 5/day) stays **silent**; the
  parked-bench deadlock (10 weight-0 drafts) still fires **both** kinds; an empty bench
  fires `publish_starved` only. 77 checks green.
- All 15 suites green, **638 total checks**.
- Live read-back: direct `issues.detect()` against the real DB and then
  `GET /api/agent/issues` after restart both return `pipeline_starved: []` with ch1 at
  approved=3, producible=10 — the digest that cried wolf yesterday is silent on the same
  facts, with 13 real issues still reported.
- Manager restarted (`launchctl kickstart`), `/api/dashboard` 200 — live.

Logged in `run/experiments.jsonl` (`triage-signal`, settle by 08-02 from the publish log:
no missed publish days, no manual produce needed, and any true starvation still caught).

## Acted

1. **No manual produce** (the fix's point — see Triage; prediction to verify tomorrow).
2. **Code:** `14f6471` (above). One change total; the engagement slot was a deliberate
   no-op.
3. **Trends logged (no adoption — both boards at capacity):** **#44** "Copilot spending
   limits + usage-based billing (July 2026)" (ch1, score 74, watch — serves the t15
   credits wedge; held because the 08-01 review may pause ch1) and **#45** "Qwen 3.6 27B
   local em GPU 24GB (Q4)" (ch2, score 75, watch — t27 hardware-reality vein; adopt as
   t27 subjects together with #42 when the bench opens). First POST attempt failed
   validation (`momentum` must be a string) — resent, both persisted with ids.
4. **Seeding kit:** `run/seeding/2026-07-30.md` — ch2: TabNews quantization-tradeoffs
   post + two r/brdev comment drafts (791, 720); ch1: dev.to spending-caps post +
   r/github comment + LinkedIn post (805, 804-adjacent). Leads with the 3rd-day
   no-pickup question.
5. **No weight/budget/settings changes** — two days before the ch1 review, repositioning
   would only blur the evidence.

## Watch next run (07-31)

1. **Verify tonight's auto-produce prediction** (ch1: 1×t5 long + 3×t15 + 1×t6; ch2:
   1×t2 long + 4×t4) from the produce JobRuns and publish log — this is the live proof
   for `14f6471` and for retiring manual produces.
2. **Settle `540d45d` (R2 fanout)** — matures 07-31; today's fresh render already shows
   fanout holding.
3. **08-01 is the ch1 hard review** — tomorrow's report should pre-assemble the final
   numbers (subs, cohort views/avp, t15 slide) so Friday's is decision-ready.
4. **t15 avp** — a fourth consecutive decline (below ~22%) would justify parking on
   saturation grounds regardless of the pause decision.
5. **Seeding answer** from the operator; check EXT_URL on 778, 742, 794, 787.
6. **ch2 board** — if it drops below capacity, adopt #42 and feed #45 into t27.
