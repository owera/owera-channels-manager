# Growth agent — 2026-08-08

## ⚠ Needs operator

1. **ch1 pause decision (hard review overdue since 08-01).** Still **10 subscribers** vs the 15-sub bar. Lifetime conversion near zero on volume (e.g. v785 = 174 views @ high avp loops → 0 subs on that video). Recommendation stands: **PAUSE ch1** (or explicitly override with a new bar). Agent continues **zero ch1 strategy moves** (no weight/trend adopt/generate for growth) until you decide. Pipeline hygiene still allowed (today's mix rebalance is hygiene).
2. **ch1 approved buffer** after today's 1L+4S: remaining **880/881 t5 longs + 887 t6 short**. Overnight rebalance demoted excess t5 queue so shorts can fill tomorrow (see Engagement) — watch that the 1L+4S mix holds on 08-09.
3. **ch2 metadata backfill** (`db7a254` plan) still awaits `--apply` if still desired.
4. **BGM pool path** remains external (`MoneyPrinterTurbo/resource/songs`) — 10 tracks healthy; wipe risk still escalates to silent renders.
5. **t27 displacement cohort still 0 views** (859–866, ages ~44–70h). Either analytics lag or zero distribution — do not invent traction. Revisit #25 only with measured views/avp.

---

## Observed

- **Now:** 2026-08-08 ~12:00–12:15 UTC. Kill switch off, `scheduler_paused: false`, tree clean at start.
- **ch1:** published **5/5** today, quota **8750/9000**. Mix = **1 long + 4 shorts** (v879 t5 long first, then v883–886 t6). **Third consecutive 1L+4S day** after #26 (08-06, 08-07, 08-08).
- **ch2:** published_today **0** at observe (window opens **14:00 UTC**). rendered_today **5**. Approved includes **2 longs (v882 t2 @ 00:06, v892 t2 @ 00:07)** + shorts — **early live signal for #27 render-long-first** (yesterday the long sat queued behind shorts that filled the budget).
- **BGM pool:** 10 / min 5 / target 15, `is_low:false`. No music_gen errors.
- **Boards:** both `at_capacity:true` (10 pending / horizon 2.0).
- **Language (ch2):** recent titles PT-BR ✓.
- **traffic_json on video-analytics API items:** still not joined onto leaderboard items this read. Do not invent CTR.
- **ch1 subs-per-video:** still ~0 on post-CTA volume; lifetime sub-gainers remain thin (e.g. v558 MCP +1). **ch1 = 10 subs. ch2 = 61 subs** (unchanged vs 08-07).

### Publish log (settle evidence)

| day | ch1 | ch2 |
|-----|-----|-----|
| 08-01 | 5 (1L+4S) | 5 (1L+4S) |
| 08-02 | 5 (0L+5S) | 5 (1L+4S) |
| 08-03 | **5 (5L+0S)** | 5 (1L+4S) |
| 08-04 | 5 (0L+5S) | 5 (1L+4S) |
| 08-05 | **5 (5L+0S)** | 5 (1L+4S) |
| 08-06 | 5 (1L+4S) | 5 (1L+4S) |
| 08-07 | 5 (1L+4S) | **5 (0L+5S)** ← long stranded (#27 target) |
| 08-08 | **5 (1L+4S)** | pending window (0 at observe; **2 longs approved**) |

## Monetization

| metric | ch1 current | ch1 needed | ch1 % | ch2 current | ch2 needed | ch2 % |
|---|---|---|---|---|---|---|
| subscribers | 10 | 490 | 2.0% | 61 | 439 | 12.2% |
| watch hours | 6.3 | 2993.7 | 0.2% | 10.7 | 2989.3 | 0.4% |
| Shorts views | 3421 | 2,996,579 | 0.1% | 3573 | 2,996,427 | 0.1% |

**Binding constraint both channels: subscribers** (lower tier). No milestone crossed. Prefer analytics (ch2 local-AI hardware shorts; ch1 frozen) over pure milestone math.

## Triage

Digest: 4 issues (3 rejected leave, 1 quota monitor). No failed / stuck / oauth / starved / bgm_pool_low / music_gen.

| Issue | Action | Proof |
|---|---|---|
| rejected 839–841 (disp. 08-03, age ~156h) | leave (auto) | age &lt; 7d (168h); subjects retained; suggested_action=leave |
| quota ch1 8750/9000 | monitor only | full 5-publish day by design (5×1750) |
| ch1 queue pure-long while short drafts idle | **code fix** (rebalance + short-prefer) | before: queued 893–897 all t5 long, drafts 905–909 t6 short → after: queued **893 only**; 894–897 demoted to draft (see Engagement) |
| failed / stuck / oauth / bgm_low | none | clean |

## Learned

1. **#26 daily-mix-cap — strong multi-day signal, not yet 72h.** ch1 08-06/07/08 = 1L+4S (vs 08-03/05 5L). Ship was 08-06; settle ≥08-09. Leave `running`.
2. **#27 render-long-first — early live signal.** ch2 overnight approved **v882 + v892 longs** (00:06–00:07 UTC) after 08-07's 0L day. Publish mix under the window is the settle metric (~08-10). Leave `running`.
3. **#25 trend-displacement — still RUNNING.** Cohort 859–866 views still **0** at ages ~44–70h (oldest ~69.5h, just under 72h maturity). Do not invent metrics. Settle when views/avp exist and age ≥72h.
4. **New defect (dual of #27):** when approved long buffer is healthy, a pure-long QUEUED bench blocks short drafts forever (`headroom = budget − rendered − active = 0`). ch1 live shape at noon proved it (5× t5 long queued, 5× t6 short drafts).
5. **Winners (mature):** ch2 t27 32.3 avg views (best short genre on measured set) but **new** t27 videos at 0 views; t4 28.6 avg + 4 lifetime subs; t28 25.8 @ 59% avp. ch1 t18 MCP 36.2 avg + only recent sub-gainer; t15 high avp loops, 0 subs on volume.
6. **Hypotheses this run:** (a) short-prefer + rebalance restores ch1 short queue under banked longs without starving the long reserve; (b) ch2 08-08 publishes 1L+4S once the window opens; (c) operator pause remains the binding ch1 move.

## Acted (channels)

- **Trends logged (no adopt/displace — boards at capacity; t27 flood awaiting metrics; ch1 frozen):**
  - #54 ch2 score **76** watch — “Qwen3-Coder 30B local em 24GB VRAM” — t27-adjacent coding angle; defer until #25 mature.
  - #55 ch2 score **72** watch — “Gemma 4 multimodal local (Ollama)” — below 75 bar / novel multimodal.
  - #56 ch1 score **45** reject — “Gemini 3.6 Flash agentic coding (EN)” — ch1 strategy frozen + outside wedge.
- **No weight changes, no generates, no produces, no BGM generate** — boards at capacity; BGM healthy; ch1 frozen; ch2 longs already approved for the window.
- **Seeding kit:** `run/seeding/2026-08-08.md` (operator posts; agent does not).
- **Live hygiene (via code path, not strategy):** rebalance demoted ch1 excess queued longs (proof above).

## Engagement

**Baseline `baseline-0808`** — 4/4 clean, `visible=True`, none `used_fallback`. Scores from frames + scripts:

| Lever | score | evidence |
|---|---|---|
| R1 hook (P1) | 2 | Pain-first on-screen: “Your RAG returns garbage” / “Your Bot Has Amnesia” style hooks; ch2 “Sua RAG responde tudo errado” |
| R8 thumb/title (P2) | 2 | Gaps hold (e.g. title rerank vs hook “Wrong answers, faster than ever”; PT hook ≠ title echo) |
| R2 info gain (P3) | 2 | Varied mixes; CrossEncoder code beat legible; diagrams present |
| R6 payoff (P4) | 2 | Quotable closes (“Retrieve wide, rerank narrow, answer sharp” / stateless memory line) |
| R5 retention (P5) | 2 | Open loops early on all 4 scripts |
| R3 clarity (P6) | 2 | Code indented, nothing clipped on read frames |
| R4 pacing (P7) | 2 | Hook at t=0; beat spans sane |
| R7 CTA (P8) | 2 | Follow/Siga + named next value 4/4 (“Halve RAG latency tomorrow” / “Tamanho ideal com overlap”) |

**All-2 baseline (7th consecutive judged day) → non-engagement code path (fallback clause).**

### Shipped: short-prefer + rebalance when approved long buffer healthy

- **Files:** `app/services/render_loop.py`, `tests/verify_render.py`
- **Change:**
  1. `_queued_candidates`: if approved longs ≥ 1, order **shorts first** then longs (dual of #27 long-first).
  2. `_rebalance_queued_mix` (runs in `tick` before `_auto_produce`): if approved longs ≥ 1 **and** promotable short drafts exist, demote excess queued longs beyond **1 reserve** back to DRAFT so headroom can refill with shorts.
- **Lifecycle map:** demote is QUEUED → DRAFT (undo of produce). Re-render still starts at QUEUED; re-publish still APPROVED. `_submit_new` still only picks QUEUED → RENDERING. No new terminal states.
- **Gate (synchronous):**
  - Crafted: banked approved long + lower-id queued long + higher-id short, budget 1 → **short RENDERING**.
  - Rebalance: 5 queued longs + 4 short drafts + banked long → **1 long queued**, 4 demoted.
  - Rebalance no-op without short drafts.
  - Full rebalance + `_auto_produce` (rendered_today=0): queue = 1 long + 4 shorts.
  - `tests/verify_render.py` **85/85** (was 75; +10 mix checks).
  - `tests/verify_storyboard.py` **40/40**.
  - `import app.main` ok; `launchctl kickstart` manager; `GET /api/dashboard` **200**.
- **Live effect (observed this run):** ch1 before queued **893–897** (all t5 long), drafts **905–909** (t6 short). After direct `_rebalance_queued_mix` + `_auto_produce`: queued **893 only**; 894–897 draft. Auto-produce correctly **did not** promote shorts today because `rendered_today=5` → headroom negative — expected. **Watch next run:** after midnight budget reset, auto_produce should queue 4 t6 shorts into the freed slots; overnight render should prefer those shorts under banked longs.

**No prompt/rubric lever change this run** — shipping unproven polish on an all-2 baseline would violate the gate.

### Experiments settled / opened

| id | lever | verdict |
|---|---|---|
| #25 08-03 | trend-displacement | **RUNNING** — 859–866 still 0 views; oldest ~69.5h; settle when measured |
| #26 08-06 | daily-mix-cap | **RUNNING** — ch1 08-06..08 three 1L+4S days; settle ≥08-09 (≥72h) |
| #27 08-07 | render-long-first | **RUNNING** — ch2 overnight banked 2 longs; settle on 08-08+ publish mix |
| #28 08-08 | render-short-prefer + rebalance | **RUNNING** — shipped this run; live rebalance proof above |

## Watch next run

1. Operator ch1 pause yes/no.
2. ch2 08-08 publish composition after 14:00 UTC — expect **1L+4S** with v882/v892 available.
3. ch1 overnight: after rebalance, does auto_produce fill 4 shorts and submit shorts-first under banked longs?
4. t27 859–866: any views once age ≥72h — settle #25.
5. #26 settle ≥08-09 if ch1 mix stays 1L+4S.
6. BGM count stays ≥ min.

## Commit

_(filled after push)_
