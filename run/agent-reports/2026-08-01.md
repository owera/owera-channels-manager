# Growth agent — 2026-08-01

## ⚠ Needs operator — RECOMMENDATION: PAUSE ch1 (hard review due today)

The 2026-07-13 wedge directive set today as the hard review: *"if ch1 is still <15 subs,
lead the report with a recommendation to PAUSE ch1."* **ch1 is at 10.** The case, all
numbers read from the DB this run:

| evidence | value |
|---|---|
| ch1 subscribers vs the 15-sub bar | **10** (single move off 9 was 07-15, pre-wedge-restart) |
| Mature post-07-16 wedge cohort (final) | **70 videos, 799 views, 0 subscribers** |
| ch1 lifetime conversion | 4 sub-gaining videos ever (1 sub each); none since 07-15 |
| Wedge lead t15 | 39 published on the one proven angle, 0 subs lifetime, avp collapsed 58.2→21.2 (parked at w0 yesterday) |
| The control | ch2: **60 subs** on 24 fewer videos — with *fewer* Shorts views than ch1 (3048 vs 3156). Distribution is comparable; **conversion is the entire gap** |

Everything fixable was fixed and given time to work: the daily 1L+4S mix has held six
consecutive days, weight-first publishing, series titling, deterministic CTA + spoken
follow-ask, all-2 rubric baselines two days running — and the niche still converts at
zero. The failure is channel/positioning-level, not mechanical.

**Recommended action (operator call, I did not execute it):**
`PATCH /api/channels/1 {"paused":true}`. Honest framing of what pausing buys: it does
NOT let ch2 publish more (each channel has its own 9000-unit quota; ch2 is already at
the 5/day ceiling). It stops ~9000 units/day + 5 renders/day + half the improvement
loop's attention going to a surface with zero measured yield, and concentrates
everything on the channel where content quality actually converts. ch1's bench, OAuth,
and analytics stay intact for a possible relaunch under a different positioning
(today's reserve fix explicitly gives paused channels the full analytics cap, so its
history keeps refreshing). If you overrule, say what ch1 should try next — drift is
the one option directive 9 forbids.

**Also for the operator:**
1. **Seeding kits are PAUSED as pre-committed** — 5th consecutive flat day (EXT_URL +0
   across all tracked videos; 778 plateaued at 53 views). No kit written today. Resume
   by request if you start posting them.
2. **(carry-over)** ch2 metadata backfill (`db7a254`, plan file 07-28) still awaits
   `--apply`.

## Observed

- **Now:** 2026-08-01 ~12:00 UTC (Saturday). Kill switch absent, scheduler running,
  tree clean. Boards: both channels `at_capacity:true` (10 drafts / 2.0 days) → no idea
  generation, no trend adoption.
- **ch1:** 240 published, **10 subs**. Today: 1 long (770 t5) + 4 t6 shorts
  (749/750/814/816), 07:01–09:04 UTC — directive-2 mix day 6.
- **ch2:** 215 published, **60 subs** (flat ~12 days). Yesterday it published **4/5**
  — root-caused below and fixed. 11 approved staged; window opens 14:00 UTC; the
  reserved long slot should lead with 753 (t2 long).
- **⭐ Auto-produce day-2 exact again (#23):** JobRuns 12448–12457 at 00:00 produced
  ch1 1×t5 long + 4×t6 shorts, ch2 1×t2 long + 4×t4 shorts — right mix, right
  weights, `pipeline_starved: []` stayed silent. Settles tomorrow.
- **Language integrity (directive 6):** 5/5 staged ch2 titles PT-BR ✓.
- **Correction to 07-31 report:** video 778 ("rebar on vs off" search demand) is a
  **t4** video, not t27 — t27's own videos all date 07-11→13. The concrete-hardware
  search vein lives inside t4's subject mix; trend #46 should feed that vein (via t27
  or t4) when boards open.
- **t4 saturation watch (yesterday's hypothesis b):** no t15 pattern — mature avp by
  publish day 44.3 / 31.7 / 34.0 / 43.0 / 122.8 / 35.8: noisy, not monotone decline.

## Monetization

| metric | ch1 current | ch1 needed | ch1 % | ch2 current | ch2 needed | ch2 % |
|---|---|---|---|---|---|---|
| subscribers | 10 | 490 | 2.0% | 60 | 440 | 12.0% |
| watch hours | 4.9 | 2995.1 | 0.2% | 9.7 | 2990.3 | 0.3% |
| Shorts views | 3156 | 2,996,844 | 0.1% | 3048 | 2,996,952 | 0.1% |

Subscribers remain the binding constraint; ch2 remains the only compounding asset.

## Triage

Digest at run start: 13 issues, 1 needs-operator. Disposition:

- **ch2 published 4/5 on 07-31 — root-caused to a quota-coexistence defect and FIXED
  (the run's one code change, `f190d52`).** Mechanism: a full 5-publish day costs 8750
  of the 9000 cap; the analytics pass capped itself at the full 9000. Snapshots gate
  per **UTC** day but quota bills per **Pacific** day, so a 00:xx pass cut short by the
  old day's cap rolls its remainder into the 12:xx pass — which bills the *new* quota
  day before ch2's 14:00 window opens. DB proof: ch2 analytics units/publishes by quota
  day — 07-28 **788u → 4/5**, 07-29 248u → 5/5, 07-30 248u → 5/5, 07-31 **740u → 4/5**;
  at the missed 16:05 slot spent was 7318 and the publish gate needs +1700 ≤ 9000.
  ch1 was never bitten only because its unset window publishes at 07:01, right after
  reset. Fix: `_publish_reserve` holds back remaining-publishes × 1750 from the
  analytics cap (0 when budget spent / channel paused; over-cap budgets clamped).
  **Verified:** 12-check `tests/verify_analytics_reserve.py`; all 17 suites green;
  manager restarted (dashboard 200); live read-back: ch1 reserve 0 → cap 9000, ch2
  reserve **8750 → cap 250**. Logged as experiment **#24**, settles 08-04 on the
  publish log.
- **12 × `rejected`, `leave`** — the 07-26 board-rebalance rows, age 155.7h; the 7-day
  delete floor clears **tomorrow** — triage-delete on 08-02.
- **`quota` (ch1, escalate-class)** — 8750/9000 is exactly 5×1750, i.e. the designed
  cost of a full publish day, not a fault. Monitor; dissolves if ch1 pauses.
- Zero failed / stuck / oauth rows; `error_runs_24h` empty; BGM 20 (target 15) healthy,
  ≥1 video rendered since last run but pool count stable and varied — no generation
  needed. No requeues, retries, deletes, or produces (pipeline self-sustains, day 2).

## Learned

1. **The publish drip and the analytics pass compete for the same quota budget, and
   the loser is silent.** Publish-before-noon channels never see it; window channels
   lose their last slot ~every 3rd day (the rollover accumulates until a dropped
   publish resets it). Generalization: two quota consumers with different day
   boundaries (UTC gate vs Pacific billing) will always drift into collision —
   coexistence must be reserved, not hoped for.
2. **ch1's failure is niche-level** — the pause case above is the terminal statement
   of three weeks of evidence; nothing mechanical remains to try.
3. **t4 is not saturating** (yet) — the t15 yield-curve rule (~30 videos/angle) does
   not generalize automatically; it needs per-topic confirmation.

**Hypotheses:** (a) with the reserve live, ch2 publishes 5/5 every day through 08-04
(settles #24); (b) tonight's auto-produce repeats the exact mix (settles #23);
(c) if the operator pauses ch1, ch2's sub growth stays flat absent a *content* change
— the next lever there is conversion depth (the long-form converters), not volume.

## Engagement

**Baseline `baseline-0801`** (full golden set, 4/4 clean, `visible=True`, none
`used_fallback`) — **second consecutive all-2 baseline:**

| Lever | score | evidence |
|---|---|---|
| R1 hook (P1) | 2 | 4/4 pain-first ≤7-word cards at t=0 ("Your RAG returns garbage" / "Chega de reescrever integrações") |
| R8 thumb/title (P2) | 2 | zero title echo; real gaps ("Wrong answers, faster retrieval" vs "Fix Your Slow RAG…") |
| R2 info gain (P3) | 2 | truthful fanout (MCP→Banco/API/Arquivos); lists/stats/code all information-bearing |
| R6 payoff (P4) | 2 | quotable closes 4/4 ("Vector search finds candidates. A reranker finds answers.") |
| R5 retention (P5) | 2 | open loop in all 4 ("The problem isn't your embeddings…" / "O problema quase nunca é o modelo") |
| R3 clarity (P6) | 2 | list rows ≤4 words, nothing clipped in any frame read |
| R4 pacing (P7) | 2 | min beat 1.68s (floor), max hold 7.46s, hooks at t=0, no drags |
| R7 CTA (P8) | 2 | spoken ask + named next value + matching card, both languages ("Siga → Chunking semântico amanhã") |

**Targeted: none** — no weak lever exists; the change slot went to the
quota-coexistence fix above (the playbook's non-engagement fallback), which today's
triage evidence made the clear highest-leverage work.

**Settled: experiment #22 (`ce46b43`, R2 list-reveal cap) → PROMOTED** (72h,
SIGNAL-SCARCE). Midpoint frames show ALL rows on both list subjects (ch1-code 5.405s:
3/3; ch2-concept 6.167s: 3/3) — the pre-fix shape (third row arriving 7.25s in) is
gone from fresh renders, with no drag reintroduced. Kept in code.

**Still running:** #23 (starvation digest — day-2 live evidence exact, settles 08-02);
#24 (quota reserve — shipped today, settles 08-04).

## Acted

1. **Code (1 of ≤2): `f190d52`** — analytics publish-quota reserve + 12-check suite
   (see Triage). Restarted live; behavior read back on real state.
2. **Settled #22 promoted; appended #24** to `run/experiments.jsonl`.
3. **No channel API actions** — boards at capacity (no ideas/trends/produces), no
   weight changes warranted (t15 parked yesterday; t4 healthy), triage rows all
   `leave`-class until tomorrow's delete floor.
4. **No seeding kit** — paused per the 07-31 pre-commitment (5th flat day).
5. Trend research: light pass only — nothing new and channel-fitting surfaced (generic
   2026-landscape content; OpenCode already t24); #45/#46/#47 remain queued for when
   boards open. No log entries forced.

## Watch next run (08-02)

1. **Operator's pause decision** — if paused, verify analytics still refreshes ch1
   (the reserve's paused-branch) and note the freed render capacity; if overruled,
   ch1 needs a new positioning decision from the operator, not more of the same.
2. **Settle #23** (starvation digest, 08-02): third exact auto-produce + no missed
   publish day + digest silent.
3. **#24 day-1 evidence:** ch2 must publish 5/5 today (753 long leads at 14:00 UTC);
   check the analytics pass stopped ≤250 units pre-window and completed post-publish.
4. **Delete the 12 07-26 rejected rows** — 7-day floor clears; cap ≤10/run, so 10 + 2.
5. **ch2 approved-pool drain:** 11 staged with 5/day — pool should shrink to ~6-7
   with the drip restored; if it grows instead, something else is eating slots.
6. **EXT_URL stays on watch** (no kit): if 778/683 move without seeding, that's organic
   — log it before crediting anything.
