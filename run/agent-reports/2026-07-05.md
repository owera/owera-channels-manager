# Growth Agent Run — 2026-07-05

⚠ **Needs operator (advisory, not blocking): publish the ch2 Google app to Production.**
ch2 OAuth is **connected** again (reconnected last night) and uploads work — but this token has
died every ~7 days because the app is in *Testing* mode (see memory
`ch2-oauth-recurring-revocation-testing-mode`). **Expected next revocation ≈ 2026-07-12.**
Publishing the app to Production in the Google Cloud Console ends the weekly reconnect cycle.

## Observed (key numbers)
| | ch1 Owera Software | ch2 Rodrigo Recio |
|---|---|---|
| oauth | connected ✓ | **connected ✓ (was expired 3 days)** |
| published (total) | 101 | **87** (was 78 — backlog drained + 449) |
| published today | 1 | 4 (3 drip + video 449 after my retry) |
| quota spent today | 1700 / 9000 | 5100 / 9000 |
| failed / approved / queued | 0 / 0 / 0 | 0 / 0 / 0 (449 resolved → published) |
| board inventory | 2.0 d — **at capacity** | 2.0 d — **at capacity** |
| analytics measured | 95 / 101 | 73 / 86 |
| subscribers | 9 | 53 (+1) |

- CTR is still 0 everywhere (impressions not populating) — R8 stays signal-scarce.
- BGM pool 15/15, `is_low:false` — healthy, no action (at target, so no variety batch).

## Triage
- **Video 449 (ch2 long, "O Erro de Retry…") — FIXED, now `published` (yt: tDthxx8wsD0).**
  Diagnosis before acting: 7 of its 8 publish attempts happened in the dead-token era; the 8th
  (04:09 today, live token) was a transient stall that happened to be the terminal strike, while
  videos 450–456 published fine minutes around it. File verified valid (`ffprobe`: 256.7s mp4,
  19MB — same size class as today's successful uploads). Digest said `auto:false` (retry-cap hit
  = judgment call); judgment: root cause demonstrably gone → `POST /videos/449/retry` →
  read back `approved`, error null → publish loop uploaded it within the drip.
  **After-state proof: `{"status":"published","yt_video_id":"tDthxx8wsD0","error":null}`.**
- **ch2 OAuth reconnect held**: the 8 parked `approved` videos from the outage all published
  (78 → 86 before my retry; 87 after). Last-publish stall from 07-01 is cleared.
- `error_runs_24h`: single publish-stall signature (count 1) = the 449 event above. Not
  recurring; no code fix warranted.
- Nothing else: no stuck/rejected/review rows, no cooldown, no quota wall, boards merely full.

## Learn
- **ch2 retention pattern found (H3 answered):** within ch2's measured videos, concrete
  "your machine / your wallet" topics hold dramatically better than abstract infra topics —
  avp 95.5 (MacBook trava rodando IA local), 79.5 (MacBook+eGPU), 48.9 (alugar GPU na nuvem)
  vs 13.9 (versionamento de modelos), 27.2 (OpenShift vs K8s). Same pipeline, same channel —
  it's the subject concreteness. Acted on this twice (t4 theme + trend log, below).
- ch1 t15 (*GitHub Copilot Credits*) still the retention leader among high-view topics
  (avp 59.2, 13.4 avg views, 12 measured) — weight 2 set yesterday stands; watch its refills.
- Shorts remain the discovery vehicle on both channels (ch1 13.8 vs 2.2 avg views; ch2 14.1
  vs 3.1). Consistent with current weighting; no change.
- **Hypotheses:** (H4) stacked cmp cards lift avp on shorts carrying cmp beats (experiment
  `7e1d20d`, running). (H5) t4 refills biased to concrete local-AI/cost subjects will lift
  ch2's short avp above ~25% as the new cohort matures (settle ≥72h after refills publish).

## Engagement
**Baseline** (golden set on HEAD, 4/4 clean renders, frames read against the rubric):

| Lever | Score | Note |
|---|---|---|
| R1 hook | 2 | Hook beat at t=0 both channels; pain-first lines ("Right docs, garbage answers" / "Sua RAG responde bobagem"). |
| R8 thumb/title | 2 | 0/4 title-echo; genuine curiosity gaps (07-01 change still holding). |
| R2 info gain | 2 | Real Python, real diagrams, stats, varied beat mixes (9-beat shorts, ≤2 stmt). |
| R6 payoff | 2 | Quotable closes on all four ("Retrieval finds the haystack. Reranking hands you the needle."). |
| **R3 mobile clarity** | **1** | **Weakest.** Diagram now fills well (07-04 fix holds) and code beats fill width, but `cmp` renders two 42%-wide columns in a narrow middle band (~70% dead frame, small phone text) and the centered VS badge touches card text (ch2-concept baseline frame) — the exact collision flagged 07-04. |

**Targeted R3 → shipped `7e1d20d`.** Portrait-only CSS in `storyboard._base_css`: `.cmp-cols`
stacks the two cards vertically at 88% width with the VS badge in normal flow between them
(the DOM already had badge-between-cards order). Landscape untouched; no other beat's CSS in scope.
- **Gate (all three passed):** after-renders `--only ch2-concept` + `--only ch1-code` — cmp cards
  now full-width with ~2× larger text, VS badge cleanly between cards (collision structurally
  impossible), block fills ~35% of frame height vs ~15%; diagram/hook/code frames unchanged-good;
  both renders `visible=True`, no fallback; `verify_storyboard` **40/40**. **Made live**: committed →
  manager restarted → dashboard 200.
- **Experiments settled: none matured.** R3 diagram-fill (`9268c6f`, 07-04) is <72h old — still
  `running`, settle ~07-07+ on the ch1 post-fix cohort. R8 already promoted.
- Logged today's change as a new R3 experiment in `run/experiments.jsonl` (`status:running`,
  predicted `avg_view_pct` up; settle ~07-08).

## Monetization
Lower Tier for both; nothing crossed 100%. Binding constraint on both channels is still Shorts
views / watch hours (~0%); subscribers is the closest proxy (ch2 10.6%). Shorts volume ×
retention is the lever — exactly what today's R3 change and the t4 re-steer target. No override
of the analytics-driven plan.

| Metric | ch1 current / needed / % | ch2 current / needed / % |
|---|---|---|
| Subscribers | 9 / 500 / 1.8% | 53 / 500 / **10.6%** |
| Watch hours | 1.8 / 3000 / 0.1% | 0.9 / 3000 / 0.0% |
| Shorts views | 1143 / 3M / 0.0% | 436 / 3M / 0.0% |

## Channel actions (all via REST API, all read back)
- `POST /videos/449/retry` → **published** (proof above).
- `PATCH /topics/4` — **re-pointed t4's theme_prompt at the proven-retention pattern**: priority
  to concrete local-AI/hardware/cost subjects (avp 79–95 exemplars), explicitly demoting abstract
  infra (versionamento/K8s, avp 14–27) unless anchored in a concrete error/cost. Read back OK.
  Steers future refills only — safe at board capacity.
- **Trends logged (no adoption — both boards `at_capacity:true`):**
  - id 25 (ch2, pt-BR): *Rodar IA local em 2026 (Ollama, VRAM, quantização)* — score 88, watching.
    Perfect fit with the ch2 retention pattern; adopt when inventory frees.
  - id 26 (ch1, en): *Agent memory and state in production* — score 84, watching. ch1's all-time
    best video is memory-themed; industry momentum toward agent memory/ops.
  (First POST attempt 422'd — `momentum` must be a string; fixed and verified ids 25/26.)
- No idea generation (capacity gate). BGM: no action (pool at target).

## Watch next run
- **Video 449 stayed published** — nothing to do; but if ch2 long-form uploads stall again with a
  live token, investigate the upload chunking/timeout path rather than retrying blind.
- **ch2 token: expected revocation ≈ 07-12** (Testing mode). If `oauth` flips to expired, lead
  with the reconnect escalation + the Production-publish durable fix.
- **Settle R3 diagram-fill (`9268c6f`)** — ch1 cohort matures ~07-07; compare avp of videos
  rendered after 07-04 vs before (SIGNAL-RICH now that retention flows).
- **Settle R3 cmp-stack (`7e1d20d`)** ~07-08 the same way (shorts carrying cmp beats).
- **t4 re-steer (H5):** check refill ideas skew concrete; compare new-cohort avp vs the 24.6 baseline.
- **Adopt trends 25/26** when a board drops below capacity.
- CTR/impressions: still waiting; R8 re-settles on real CTR when they flow.
- Small R3 polish candidate (not shipped, one-change cap): diagram connector line strikes through
  its edge labels ("top 20"/"best") — give `.elabel` a background halo or offset.

**Commits:** `7e1d20d` (cmp portrait stack) + this report/experiment-log commit.
