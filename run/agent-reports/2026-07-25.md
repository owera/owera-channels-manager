# Growth agent — 2026-07-25

**Commit:** 70f5320 (storyboard long-hold drift — R4 drag mechanism)

> ## ⚠ Needs operator
> **Carried from 07-23/07-24, still unanswered:** are the seeding kits being posted?
> EXT_URL is ch1 3 / ch2 5 cohort-wide (ch1 +1 — first movement ever, too small to read).
> One word in tomorrow's run notes ("posted" / "not posted") changes how I interpret this
> number. Directive-3 (named "Parte N" series) also remains unimplemented since 07-09.
> Optional cleanup of the duplicate playlists from the pre-07-24 recreate loop is still
> available in YouTube Studio (I can't delete playlists — guardrail).

## Both 07-24 structural fixes verified live today

1. **Auto-produce's first day-start cycle fired and worked.** Observed: exactly 5
   `produce` JobRuns per channel at 00:00:14 UTC ("auto-produced: draft queued to fill
   free render capacity"), rendered_today 5/5 on both channels, **no `render_starved`
   in today's digest**, and both channels published 5/5 with zero agent hand-production.
   The morning batch existed without me — the starvation class is closed.
2. **Playlist recreate loop: no recreation since the fix.** Playlist table max id is
   still 50 (the last dupe row predates the fix); 13 publishes since, all 13
   `playlist_add` JobRuns `success`, zero "auto-created playlist" runs. Caveat kept
   honest: none of today's publishes hit a 13-char-id topic (today was topics 2/4/5/6,
   all 34-char), so the specific "13-char id kept and used" live case still pends —
   **watch next run**, but the loop behavior (a new playlist per publish) is gone.

## Observed (key numbers)
- **Subs: ch1 10, ch2 60** (both unchanged). Binding constraint: subscribers, both.
- **Conversion (07-09→ cohort, latest snapshots):** ch1 **1 sub / 75 measured**
  (0.013/video); ch2 **3 / 74** (0.042/video). Unchanged — the R1+R7-carrying cohort
  (first published 07-23/24) matures tomorrow→07-27; that's the next real evidence drop
  before the 08-01 ch1 review.
- **ch1 traffic UNFROZE** (watch item closed): cohort SHORTS 626→675 (+49), SEARCH
  69→81 (+12) — the two byte-identical days were staleness, not zero distribution.
  ch1's freshest capture is 07-24 12:17 vs ch2's 07-25 00:17 (~12h lag under the quota
  cap) — acceptable; no per-channel floor change needed yet.
- **ch2 search keeps compounding:** 226 cohort search views (+15/day), SHORTS 1314.
  Terms stay tool-shaped (langgraph, claude code) — supports the 07-24 hypothesis that
  searchable tool names in ch2 titles earn search pickup.
- **Daily mix held on both channels:** long published first (ch1 719 t5 07:23, ch2 722
  t2 07:24), then 4 shorts each — directive-2 clean. **Language integrity ch2:** all 5
  new titles PT-BR, no EN leak.
- Quota 8750/9000 (same as yesterday — consistent with 10 publishes + analytics; monitor).
  BGM pool 20/min 5, healthy, no music_gen errors. Both channels `connected`.

## Monetization
| Metric (Lower tier) | ch1 current / needed / pct | ch2 current / needed / pct |
|---|---|---|
| Subscribers | 10 / 490 / **2.0%** | 60 / 440 / **12.0%** |
| Watch hours | 4.2 / 2995.8 / 0.1% | 7.1 / 2992.9 / 0.2% |
| Shorts views | 2611 / ~3M / 0.1% | 2420 / ~3M / 0.1% |

No metric at 100%. ch2 watch hours +0.2h day-over-day; the long-form drumbeat continues.

## Triage
- **`publish_starved` ch2 (1 approved vs 5/day)** — no action needed and none possible
  today: ch2 already published 5/5, its render budget is spent (5/5), and the bench holds
  10 drafts incl. 4 longs. Tonight's 00:00 auto-produce cycle (verified live this morning)
  refills the buffer; the 1 banked approved covers the first drip slot. This is the
  auto-produce steady state, not a fault. ch1's long buffer emptied today (30 approved,
  all shorts) but its bench has 5 long drafts and the auto-produce long-buffer guarantee
  queues one first — watch tomorrow's first ch1 publish is a long.
- **Quota 8750/9000 both channels** (`daily_limit_hit:false`, auto:false) — monitor only.
- **`error_runs_24h`: empty.** No failed/rejected/stuck/oauth entries. Clean day
  operationally; both boards at capacity (10 drafts, 2.0 days) → no idea generation or
  trend adoption this run (board gate).

## Learned
1. **The R4 hook-drag residual is dead** — closed by the combination of the 07-20 opener
   cap and the 07-24 ending re-anchor: today's baseline hooks held 2.99–4.74s on 4/4
   subjects (was 8.3–10.0s a week ago). The drag moved mid-body (term/cmp/lst holds).
2. **The remaining drag is a renderer property, not an LLM property:** every beat type
   except `lst` lands its full content within ~1s of beat start (fixed offsets/staggers),
   so long-held cards freeze. `render_list` already solved this locally with its
   window-spread reveal — generalizing motion-through-the-hold is the content-aware
   mechanism the 07-19 verdict called for. Shipped today (below).
3. ch1's "frozen analytics" scare was refresh staleness under quota pressure, not lost
   distribution — check capture timestamps before concluding anything from flat numbers.

## Engagement
**Baseline (full golden set, 4/4 clean, visible=True, no fallback):**
| Lever | score | note |
|---|---|---|
| R1 hook (P1) | **2** | 4/4 pain-first ≤11w at t=0; holds now 2.99–4.74s (drag gone) |
| R8 thumb/title (P2) | **2** | anchored, no title echo (one partial: ch2-code "Pra Tudo"/"Para Tudo") |
| R2 info gain (P3) | **2** | 8–10 varied beats, real code/diagram/stat (note: chain-style diagram slightly misdraws MCP's hub topology; watch) |
| R6 payoff (P4) | **2** | quotable closes 4/4 ("Retrieval finds candidates; reranking finds the winner") |
| R5 retention (P5) | 2 | truth-pivot mid-script, no sag |
| R3 clarity (P6) | 2 | code ≤30 chars indented + highlighted, nothing clipped |
| R4 pacing (P7) | **1 → targeted** | ch2-code lst 8.43s + term 9.37s consecutive static holds (39% of the video); floors hold (min 1.68s), hooks clean |
| R7 CTA (P8) | **2** | spoken ask + named next-value 4/4; visual card states the same promise |

**Shipped: R4 long-hold drift (70f5320).** Any beat held >5.5s gets a subtle linear
scale drift (1.0→1.045) from reveal-settle to exit-fade, so no card ever fully freezes —
the deterministic successor to the exhausted prompt-level pacing rules. Gate evidence:
- Unit-exercised `_wrap` on the exact baseline timings: drift fires only >5.5s
  (term 9.37→tween 14.1s–21.92s, no fade overlap), short beats byte-identical, last-beat
  fade interplay correct.
- Gated re-render (ch2-code): 4 beats >5.5s each carry exactly one drift tween, none
  elsewhere; frames intact (term/cmp/cta layouts unchanged, stacked cmp + VS-in-flow
  preserved); visible=True, no fallback. `verify_storyboard` 40/40.
- Manager restarted post-commit; dashboard 200 — live for tonight's renders.
Logged in `run/experiments.jsonl` (settle ≈07-28, SIGNAL-SCARCE re-render-judge).

**Settled: nothing** — the R7 spoken-CTA experiment (f79fa33, 07-24) is ~34h old; it
settles ≈07-27. Left running, untouched, for clean attribution.

## Actions taken
- Code: storyboard long-hold drift (70f5320) — the one gated engagement change; manager
  restarted after commit, dashboard 200.
- Seeding kit `run/seeding/2026-07-25.md` (ch2: M1-vs-RTX regime TabNews post, dual
  memory-gotcha r/brdev comment, feature-store adjacent-channel comment; ch1: LangChain
  memory token-bill r/LocalLLaMA comment, deadline-comparison dev.to post + LinkedIn,
  streaming-UX adjacent-channel comment).
- No weight/topic/trend/settings/video-state changes: boards at capacity, digest clean,
  weights already positioned (t15 w3 / t5 w2 / t4 w3), and the maturing R1+R7 cohort
  argues for holding the mix steady one more day.

## Watch next run (07-26)
1. **R1+R7 cohort first maturity reads** (published 07-23→24, 72h from tomorrow):
   per-video `subscribers_gained` on ch1's wedge cohort — the key input before the
   08-01 pause decision.
2. **Playlist 13-char live case:** next publish of an affected topic → `playlist_add`
   success with stable `topic.playlist_id`, no create.
3. **ch1's first publish should be a long** (long buffer empty; auto-produce must queue
   one of drafts 726–730 first). If ch1 publishes 5 shorts, the long-buffer guarantee
   has a bug.
4. **Drift in production:** tomorrow's rendered videos should carry drift tweens on
   long beats (check a rendered index.html in the job dir); rubric baseline should stop
   flagging static-hold drag.
5. R7-spoken settles ≈07-27; R4-drift ≈07-28.
