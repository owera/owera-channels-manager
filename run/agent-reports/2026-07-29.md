# Growth agent — 2026-07-29

## ⚠ Needs operator

**1. ch1 hard review (directive 9) is Saturday 08-01 — the evidence is already conclusive.**
ch1 has published **55 mature videos since the 07-16 wedge repositioning went live and gained
zero subscribers** (476 views, 30.8% avp, `subscribers_gained` = 0 across every ch1 topic in
that window). The wedge lead t15 "GitHub Copilot Credits 2026" has **34 published / 26 measured
videos and has never gained a single subscriber**, and its retention has fallen — the mature
pre-07-16 t15 cohort averaged 58.2% avp vs 33.7% for the post-07-16 cohort. ch1 sits at 10 subs
against the 15-sub bar. Unless something moves in the next two days, my 08-01 recommendation
will be to **pause ch1** and redirect its render/publish budget to ch2, which converts.

**2. Long-form is getting almost no distribution on either channel — directive 2 needs a
decision.** Mature videos, last 6 weeks:

| format | channel | videos | views | views/video | avp | subs |
|---|---|---|---|---|---|---|
| long | ch1 | 24 | 36 | **1.5** | 11.0% | 0 |
| long | ch2 | 81 | 185 | **2.3** | 15.6% | 2 |
| short | ch1 | 171 | 2688 | 15.7 | 43.8% | 2 |
| short | ch2 | 93 | 2771 | 29.8 | 40.3% | 3 |

Long-form takes ~20% of the daily render and publish budget (and the matching quota) and returns
~2 views per video — 221 views from 105 videos, about 4% of all views. Spot-checking recent longs,
their `traffic_json` is **100% `NO_LINK_OTHER`**: no BROWSE, no SUGGESTED, no SEARCH, no SHORTS
feed. The algorithm is not testing them at all. Directive 2 mandates one long per channel per day
on the premise that "long-form is the proven subscriber converter" — that premise came from the
early lifetime data and does not hold now. **Caveat, stated plainly:** subscriber counts are far
too small to claim longs convert *worse* (2 subs/105 longs vs 5 subs/264 shorts is the same
per-video rate within noise). The defensible case is about **distribution and cost**, not
conversion. I did not change directive 2 unilaterally — it's operator-approved and the 08-01
review is the right place to decide.

**3. Seeding has produced no measurable traffic in three days.** `EXT_URL` in `traffic_json` is
1 view each on videos 683, 689, 741 (ch2) and 568 (ch1), and those counts are **byte-identical
across the 07-27, 07-28 and 07-29 captures**. Were the last three kits actually posted? If they
were, seeding isn't working and should be cut rather than drafted daily.

Everything else is healthy: both channels `oauth_status: connected`, no failed videos, nothing
stuck, no error runs in 24h, BGM pool 20 (min 5 / target 15).

## Observed

- **Now:** 2026-07-29 12:00 UTC. Scheduler running, kill switch absent, tree clean at start.
- **ch1 Owera Software:** 225 published, 10 subs. 5 rendered / 5 published today, quota
  8750/9000 (`daily_limit_hit: false`; resets 07-30 07:00 UTC). 3 approved, 10 drafts, 0 queued
  at Observe time.
- **ch2 Rodrigo Recio:** 201 published, 60 subs. 5 rendered / 0 published today — correct, its
  publish window (11:00–22:00 America/Sao_Paulo) opens at 14:00 UTC and `next_publish_eta` reads
  exactly `2026-07-29T14:00:00Z`. 10 approved waiting.
- **Boards:** both `at_capacity: true` (10 pending, 2.0 days of inventory) → no idea generation
  and no trend adoption this run, per the capacity gate.
- **Traffic sources (directive 1):** ch2 shorts are being distributed — video 732 shows 51 SHORTS
  feed views plus a real search term (`"rateio de ia"`), 742 shows 25 SHORTS views at **84.0% avp**,
  731 shows 32 SHORTS views at 75.3%. ch1 shorts get SHORTS + YT_SEARCH + YT_OTHER_PAGE but at
  much lower volume. Distribution exists on shorts; conversion is still the gap on ch1.

## Monetization

| metric | ch1 current | ch1 needed | ch1 % | ch2 current | ch2 needed | ch2 % |
|---|---|---|---|---|---|---|
| subscribers | 10 | 490 | 2.0% | 60 | 440 | 12.0% |
| watch hours | 4.5 | 2995.5 | 0.1% | 8.1 | 2991.9 | 0.3% |
| Shorts views | 2837 | 2,997,163 | 0.1% | 2782 | 2,997,218 | 0.1% |

Neither channel has reached Lower Tier on any metric. By the letter of the rule the binding
constraint is Shorts views (99.9% remaining on both), but that framing isn't useful at this scale —
subscribers is the only metric moving at all and the only one with a plausible near-term path, so
it stays the focus. ch2 is the asset: it holds 6× ch1's subscribers on 24% fewer published videos.

## Triage

Digest: 14 issues, 1 needs operator, `clean: false`.

- **`pipeline_starved` (ch1, auto)** — only 3 approved against a 5/day publish budget (0.6 days of
  publish inventory). **Fixed:** produced 5 drafts → queued, matching directive 2's mix (4 wedge
  shorts t15: 797, 798, 804, 805 + 1 long t5: 768), all `200`. **After-state read back from the DB:**
  ch1 `draft` 10 → 5, `queued` 0 → 5. These render against tomorrow's budget (today's 5 renders
  were already spent before this run).
- **12 × `rejected`, `suggested_action: "leave"`** — the 07-26 board-rebalance rejections (ids
  757–765 ch2, 771–773 ch1), age 83.7h. Action is "leave" and they're under the 7-day deletion
  floor, so untouched by design.
- **`quota` (ch1, escalate)** — 8750/9000 spent, `daily_limit_hit: false`. That's just 5 uploads
  at ~1600 units plus playlist work; it resets 07-30 07:00 UTC. No budget change: nothing was
  actually blocked, and lowering the publish budget would cost distribution to fix a non-problem.
- **BGM pool** — count 20 vs min 5 / target 15, `is_low: false`. No action.
- **`error_runs_24h`** — empty. The one-off 404s flagged on 07-28 did not recur.

## Learned

1. **The 07-26 board-mix fix worked, and it revealed that the mix was never the real problem.**
   The publish log now shows ch1 at 1 long + 4 shorts for three straight days with t15 leading
   every short slot (07-27: 3×t15 + t6; 07-28: 4×t15; 07-29: 4×t15) against 07-26's broken 4 longs
   + 1 short and zero t15. The wedge is on air exactly as designed — and it converted nobody. That
   separates two hypotheses cleanly: this is not a scheduling or bench-management failure, it's a
   content/positioning failure.
2. **t15's retention is degrading, not just its conversion.** 58.2% avp (17 mature pre-07-16
   videos) → 33.7% (6 mature post-07-16). Same topic, same weight, worse watch-through. Worth
   watching: if it keeps falling, the Copilot-credits angle may simply be exhausted after 34 videos.
3. **ch2's hardware-reality vein keeps producing the channel's best retention** — 84.0% and 75.3%
   avp on the two VRAM/quantization shorts, versus a 40.3% channel average for shorts. This is the
   clearest content signal in the portfolio and it argues for feeding t27.

**Hypotheses for next runs:** (a) ch1's problem is the niche, not the execution — the same
production system converts on ch2 and not on ch1; (b) long-form's zero distribution is
format-level, not quality-level, since the same pipeline's shorts get 10× the views; (c) t15 is
saturating and its remaining upside is smaller than t27's.

## Engagement

**Baseline** (full golden set, 4/4 clean, `visible=True`, none `used_fallback`):

| Lever | score | evidence |
|---|---|---|
| R1 hook (P1) | 2 | 4/4 pain-first ≤10w at t=0 ("Your RAG returns garbage chunks, and you know it." / "Sua RAG responde qualquer coisa, menos o que você perguntou.") |
| R8 thumb/title (P2) | 2 | zero title↔thumb echo 4/4, real curiosity gaps ("Fix Your Slow RAG…" vs "Wrong answers keep sneaking through") |
| R2 info gain (P3) | **1** | varied mixes with real diagram/code/stat — but list beats sat incomplete for most of their span (below) |
| R6 payoff (P4) | 2 | quotable closes 4/4 ("Retrieval finds candidates; reranking finds the truth.") |
| R5 retention (P5) | 2 | truth-pivot present in all 4 ("A verdade? Isso acabou.") |
| R3 clarity (P6) | 2 | code ≤30 chars, indented, highlight intact, nothing clipped |
| R4 pacing (P7) | **1** | four beats ≥9s; ch2-code held 49% of its 43.6s runtime on two cards (cmp 10.14s + lst 11.29s) |
| R7 CTA (P8) | 2 | spoken follow-ask + named next value, in-language, 4/4 |

**Targeted: R2** (weakest lever at the highest priority — P3 beats R4 at P7).

**A false start worth recording.** I first read the baseline frames as showing *2-item* lists and
edited the storyboard list prompt to forbid them. Reading `index.html` instead of the PNG showed
the model had authored **3 items all along** — and the after-render was no better. That change was
**reverted un-shipped** (`git checkout`) before any commit. The real defect was in the opposite
place: `render_list` spread its row reveals across the *whole* beat window (`step = (dur-0.8)/n`),
and a beat's span comes from cue spacing, not from how long the enumeration takes to say. On the
baseline's 11.29s list the third item didn't appear until **+7.25s** — the frame was honest, my
inference wasn't. Lesson for future runs: **item counts must be read from `index.html`; a
mid-reveal PNG is not evidence of what the model authored.**

**Shipped: `ce46b43`** — cap the inter-row reveal step at 1.1s in `render_list`.
**Gate: PASSED.**
- Unit-exercised on the exact failing timings: 11.29s/3 items → last item **+7.25s becomes
  +2.45s**; tight cases byte-comparable (4.0s/3 items reveals at +0.25/+1.32/+2.38, 3.0s/2 at
  +0.25/+1.35), so narration tracking on well-fitted beats is preserved.
- Gated re-render of ch2-code drew an 8.855s list beat; rows land at +0.25/+1.35/+2.45 and the
  **midpoint frame now shows all 3 items** ("Cada API nova": outro adaptador / outra autenticação /
  código quebrado) where the baseline midpoint showed 2 of 3. `visible=True`, no fallback.
- **All 15 verify suites green (609 checks)**, including `verify_storyboard` 40/40.
- No other lever dropped: hook 9w pain-first at t=0, title↔thumb gap intact, min beat 2.04s
  (floor holds), longest hold **11.29s → 8.855s**, CTA carries the follow ask + named next value.
- Manager restarted (`launchctl kickstart`), import OK, `/api/dashboard` 200 — the change is live.

**Settled: board-mix (`ed719f8`, 07-26) → PROMOTED.** Predicted signal (`t15_published_per_day`
up) confirmed from the publish log rather than analytics: 0 t15 on the bug day, then 3, 4, 4. The
directive-2 mix has held three consecutive days on both channels. Kept in code — with the caveat
recorded in the experiment line that the mix is fixed but is not converting.

**Running:** R2 fanout (`540d45d`, 07-28) settles ~07-31. R2 list-reveal (`ce46b43`, today)
settles ~08-01.

## Acted

1. **Triage:** produced ch1 videos 797, 798, 804, 805 (t15 shorts) + 768 (t5 long) — draft→queued,
   verified in the DB.
2. **Code (1 of ≤2):** `ce46b43`, gated and live (above). A second change was started and reverted
   un-shipped, so one shipped change total.
3. **Trends logged (no adoption — both boards `at_capacity`):** #42 "Ollama v0.32 modo agente +
   flash attention em GPU antiga" (ch2, score 82, watch — strong t27 hardware-reality fit; adopt
   first when the ch2 bench frees) and #43 "Langflow 1.9.2 — agent orchestrators as must-patch
   infrastructure" (ch1, score 58, watch — only adjacent to the wedge).
4. **Seeding kit:** `run/seeding/2026-07-29.md` — ch2 TabNews post on the Ollama v0.32 flash-
   attention angle tied to the VRAM-bandwidth video (778), two r/brdev comment drafts (778, 742);
   ch1 r/github comment + dev.to post on Copilot credit auditing (794, 787), with an explicit
   "skip r/LocalLLaMA unless genuinely relevant" note. Includes the no-pickup question above.
5. **No weight, budget, or settings changes.** Weights are already positioned for the wedge and
   the 08-01 review should decide ch1's fate before anything is re-tuned.

## Watch next run (07-30)

1. **ch1 subs.** If still 10 on 08-01, lead with the pause recommendation and the case above.
2. **Did the 5 produced ch1 videos render?** They were queued after today's render budget was
   spent, so they should pick up on tomorrow's budget — confirm `queued` drained.
3. **First production render carrying a list beat** — confirm the 1.1s reveal cap shows up in a
   real video, not just the golden set.
4. **ch2 publishes after 14:00 UTC today** — the window gate is proven at the ETA level; the rows
   themselves land after this run exits.
5. **t15 retention** — if the post-07-16 avp keeps sliding below 33.7%, treat the topic as
   saturated regardless of the pause decision.
6. **Adopt trend #42 (Ollama v0.32)** into t27 the moment ch2's board drops below capacity.
