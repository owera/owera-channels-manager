# Growth agent — 2026-08-13

## ⚠ Needs operator

1. **ch1 pause decision (hard review overdue since 08-01).** Still **10 subscribers** vs the 15-sub bar. Lifetime conversion still ~0 on volume (v785 ≈174 views / 148% avp loops → 0 subs; MCP v558 remains the rare +1). **Recommendation stands: PAUSE ch1** (or explicitly override with a new bar). Agent continues **zero ch1 strategy moves** (no weight/trend adopt/generate for growth). Pipeline hygiene only.
2. **ch1 approved buffer at observe:** **2 long + 0 shorts** after the morning 1L+4S publish. Overnight queue is already **5 shorts** — no extra produce (would exceed render budget). Watch that those 5 render before 07:00.
3. **ch2 metadata backfill** (`db7a254` plan) still awaits `--apply` if still desired.
4. **BGM pool path** remains external (`MoneyPrinterTurbo/resource/songs`) — 10 tracks healthy; wipe risk still escalates to silent renders.
5. **Quota monitor (not blocking):** ch1 spent **8750/9000** after a full 5-publish day — expected (5×1750). No nudge.

---

## Observed

- **Now:** 2026-08-13 ~12:00 UTC. Kill switch off, `scheduler_paused: false`, tree clean at start.
- **ch1:** published **5/5** today, quota **8750/9000**. Mix = **1 long + 4 shorts** (v895 t5 long first @ 07:01, then v933/935/936/937 t6). **Eighth consecutive 1L+4S day** (08-06…08-13).
- **ch2:** published_today **0** at observe (window opens **14:00 UTC**). rendered_today **5**. Approved = **2 long + 9 shorts** — ready for 1L+4S. **08-12 did publish 1L+4S** (v932 long first @ 14:01, then v938–941 t27).
- **ch2 subscribers: 61 → 63 (+2).** First channel-level move since 08-11. Video-attributed `subscribers_gained` still totals **6** across the same known converters (no new per-video +1 in the leaderboard this run — analytics lag). Do not invent which video converted.
- **BGM pool:** 10 / min 5 / target 15, `is_low:false`. No music_gen errors. No `error_runs_24h`.
- **Boards (API `by_format` — #30 PROMOTED this run):**
  - ch1 draft `{long:5, short:0}` · queued `{long:0, short:5}` · approved `{long:2, short:0}`
  - ch2 draft `{long:5, short:0}` · queued `{long:0, short:5}` · approved `{long:2, short:9}`
  - Inverse of yesterday: **draft benches are all-long**. Queued shorts cover tonight’s render. Root cause of today’s code change.
- **Language (ch2):** 08-12 titles PT-BR ✓ (932 DVC vs Git LFS; 938–941 t27). No EN leak.
- **traffic_json (via leaderboard API):** v542 SHORTS=385; v830 SHORTS=144; v589 SHORTS=92 + YT_SEARCH=25; v785 SHORTS=164; v859 SHORTS=49; v890 SHORTS=71. No invented CTR. SHORTS / YT_SEARCH dominate; no BROWSE/SUGGESTED named keys on top samples.
- **ch1 subs-per-video:** still ~0 on post-CTA volume; **ch1 = 10 subs.**

### Publish log (settle evidence)

| day | ch1 | ch2 |
|-----|-----|-----|
| 08-09 | 5 (1L+4S) | 5 (1L+4S) |
| 08-10 | 5 (1L+4S) | 5 (1L+4S) |
| 08-11 | 5 (1L+4S) | 5 (1L+4S) |
| 08-12 | 5 (1L+4S) long 894 first | **5 (1L+4S)** long 932 first |
| 08-13 | **5 (1L+4S)** long 895 first | pending window (0 at observe; 2L+9S banked) |

## Monetization

| metric | ch1 current | ch1 needed | ch1 % | ch2 current | ch2 needed | ch2 % |
|---|---|---|---|---|---|---|
| subscribers | 10 | 490 | 2.0% | 63 | 437 | 12.6% |
| watch hours | 6.7 | 2993.3 | 0.2% | 11.0 | 2989.0 | 0.4% |
| Shorts views | 4102 | 2,995,898 | 0.1% | 3844 | 2,996,156 | 0.1% |

**Binding constraint both channels: subscribers** (lower tier). No milestone crossed. Prefer analytics (ch2 local-AI hardware shorts; ch1 frozen) over pure milestone math.

## Triage

Digest at start: 3 issues (quota monitor + 2 rejected rows). No failed / stuck / oauth / starved / bgm_pool_low / music_gen.

| Issue | Action | Proof |
|---|---|---|
| quota ch1 8750/9000 | monitor only | full 5-publish day by design (5×1750); `daily_limit_hit:false` |
| rejected v922, v928 (ch2, age 84h / 83.6h) | leave | `suggested_action:"leave"` (age &lt;7d); displaced 08-12, subjects retained |
| ch1 approved 0S after morning publish | none (queue already 5 shorts) | `by_format.queued.short=5`; producing more would exceed `daily_render_budget` |
| failed / stuck / oauth / bgm_low / pipeline_starved | none | clean lists |

## Learned

1. **#30 board-inventory-by-format — PROMOTED (72h+).** Reports 08-11→08-13 quote `by_format` from the issues/state API. Keep join.
2. **#31 auto-produce long-reserve — leave RUNNING** (~48h since 08-11; settle ~08-14). ch1 noon approved = 2L (not the 0L gap). Not yet a pure auto_produce-reserve proof under approved_longs==1 overnight alone.
3. **#32 autofill long-reserve — leave RUNNING** (&lt;72h). Predicted `draft.long >= 1` HELD (ch2 draft long=5) but **overshot**: #32 reserved a slot *from shorts*, then t2 (raised to w4 yesterday) walked first and filled all leftover seats with longs. Residual shipped today as #33.
4. **#25 trend-displacement — leave RUNNING.** Mature cohort still mixed — do not invent a verdict:
   - v859: 52 views / avp 32.63 / avd 14 / SHORTS=49 (age 189h)
   - v860 (Qwen 24GB): 8 views / avp 51.49 / YT_SEARCH=7 (age 189h)
   - v861 (Q4 vs Q8): 0 views (age 188h)
   - same-window t4 still the conversion leader (4 lifetime subs, 27.4 avg views, 45.0 avp)
5. **Winners (mature):** ch2 t4 27.4 avg views + 4 lifetime subs; t28 25.8 @ 59% avp; t27 22.0 avg / 29.2 avp / 0 subs (volume without conversion). ch1 t18 MCP 36.2 avg + only recent sub-gainer; t15 high-avp loops, 0 subs on volume.
6. **Hypotheses this run:** (a) long mix-cap of `horizon_days` stops the all-long draft overshoot without starving the 1 long/day reserve; (b) ch2 08-13 window publishes 1L+4S with a t2 long leading; (c) operator pause remains the binding ch1 move.

## Acted (channels)

- **t2 weight 4→2:** long bench is overstocked (5 drafts + 2 approved). Directive-2 only needs the long anchor ≥2. Proof: `GET /api/topics/2` → `weight=2`.
- **Trends logged (no adopt/displace — boards at_capacity; drafts are w2/w4 so a new w1 topic cannot displace; ch1 frozen; #25 still mixed):**
  - #69 ch2 score **80** watch — “vLLM OOM vs llama.cpp RAM-spill on 24GB”
  - #70 ch2 score **74** watch — “Qwen 3.5 9B Q4_K_M default for 8GB VRAM”
  - #71 ch1 score **72** reject — “Copilot Business/Enterprise promo credits expire end of August 2026” — ch1 frozen / overlaps #32/#44
- **No produce / no generate / no displace.** Queued already = render budget. t4 empty bench is a valid displacement candidate, skipped this run to keep the action cap on the mix-cap fix + t2 weight.
- **No BGM generate** — pool healthy (10 ≥ min 5).
- **Seeding kit:** `run/seeding/2026-08-13.md` (operator posts; agent does not).

## Engagement

**Baseline `baseline`** — 4/4 clean, `visible=True`, none `used_fallback`. Scores from frames + scripts:

| Lever | score | evidence |
|---|---|---|
| R1 hook (P1) | 2 | Pain-first on frame: “Right docs, wrong answers” / “Your AI forgets you instantly” / “Reescrevendo a mesma integração?” / “Sua RAG responde tudo errado”. Openers ≤10w. |
| R8 thumb/title (P2) | 2 | Gaps vs titles (hook “Wrong answers buried in retrieval” vs title “Fix Your Slow RAG…”; PT “Um Conector Que Substitui Todos” / “Cortou no meio da resposta”) |
| R2 info gain (P3) | 2 | Varied mixes (ch1-code: hook/term/stat/diagram/code/cmp/cta; ch2-code fanout MCP→Bancos/Arquivos/APIs + indented `@mcp.tool`) |
| R6 payoff (P4) | 2 | Quotable closes (“Retrieval finds candidates; reranking finds the answer.” / “Memory isn’t a feature you prompt—it’s an architecture you design.” / chunk lesson) |
| R5 retention (P5) | 2 | Open loops early on all 4 scripts |
| R3 clarity (P6) | 2 | Code legible (CrossEncoder rerank; `return db.run(sql)` indented; `split_by` paragraphs). CMP stacked + VS in flow. Diagram elabels beside arrows. |
| R4 pacing (P7) | 2 | Hook at t=0; durations 34.9–44.5s shorts |
| R7 CTA (P8) | 2 | Follow/Siga + named next value 4/4 (“Cache trick: half the latency” / memory layer tomorrow / 1º servidor MCP / overlap ideal) |

**All-2 baseline (12th consecutive judged day) → non-engagement code path (fallback clause).**

### Shipped: autofill long mix-cap (1 long per horizon day)

- **Files:** `app/services/autofill_loop.py`, `tests/verify_autofill.py`
- **Change:** when a live short topic exists, longs may occupy at most `board_horizon_days` pending seats. Stops a high-weight long that walks first (equal weight, lower id) from draining every leftover slot — the 08-13 failure mode after yesterday’s #32 reserve + t2 w4 bump.
- **Why:** both channels at observe had draft `{long:5, short:0}`. #32 reserved a slot *from shorts* but did not cap how many longs fill open space. Crafted replay of the live shape (horizon=2, 5 queued shorts, 0 long, equal w4) pre-fix filled 5 longs; post-fix fills 2.
- **Gate (synchronous, observed this run):**
  - `PYTHONPATH=. .venv/bin/python tests/verify_autofill.py` → **20/20** (was 14; +6 including the exact 08-13 queued-short shape)
  - `tests/verify_storyboard.py` → **40/40**
  - `tests/verify_render.py` → **89/89**
  - `uv run python -c "import app.main"` ok
  - Crafted in-memory tick: 5 queued shorts + 0 long + horizon 2 → long drafts **2** (not 5)
  - Manager restart + `GET /api/dashboard` 200 happens after this commit (see Commit)
- **No prompt/rubric lever change** — shipping unproven polish on an all-2 baseline would violate the gate.

### Experiments settled / opened

| id | lever | verdict |
|---|---|---|
| #25 08-03 | trend-displacement | **RUNNING** — 859 has SHORTS+real avp; 860/861 still weak; t4 still converts |
| #30 08-10 | board-inventory-by-format | **PROMOTED** — 3 consecutive reports quote API field |
| #31 08-11 | auto-produce long-reserve | **RUNNING** — ~48h |
| #32 08-12 | autofill long-reserve | **RUNNING** — `draft.long>=1` held but overshot; residual is #33 |
| #33 08-13 | autofill long mix-cap | **RUNNING** — shipped this run |

## Watch next run

1. Operator ch1 pause yes/no (still the binding decision).
2. ch2 08-13 publish composition after 14:00 UTC — expect **1L+4S** with a t2 long leading.
3. ch1 overnight: 5 queued shorts + 2 approved longs — does `by_format.approved` rebuild to ≥1L+4S before 07:00?
4. **#33:** after overnight autofill, do long pending stay ≤ `horizon_days` (2) while shorts refill? Quote `by_format.draft`.
5. #32 settle ~08-15 (draft long ≥1 without agent displacement — now true, but only together with #33).
6. #25: still need a cleaner 859/860/861 vs same-window t4 comparison before promote/revert.
7. #31 after ≥72h of live auto_produce-reserve nights.
8. BGM count stays ≥ min.

## Commit

_pending — filled after `git push`_
