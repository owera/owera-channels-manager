# Growth agent report — 2026-07-15

⚠ **Needs operator (carried, not new):** seeding kits #1–#5 remain unposted as far as
traffic shows (no EXT_URL views on any seeded video through today). Highest-leverage
single item in today's kit (`run/seeding/2026-07-15.md`): the TabNews Ollama-slow post
for ch2 — the hardware-BR niche is the only thing converting subscribers right now.

## Observed
- **Subs:** ch1 **9** (flat), ch2 **59** (flat day-over-day; +5 since the 07-09 baseline
  of 54, target 75 by 07-23).
- **First mini-viral:** ch2 #542 ("32GB de RAM mas o gargalo era a VRAM") hit **401
  views — 387 from the SHORTS feed — and +1 measured subscriber** (topic t4, w3). Its
  topic-mate #589 (Ollama slow on a good GPU, t27) is at 80 views / **62.8% avg-view-pct**
  at 74h — the best fresh retention on either channel. The concrete-hardware PT vein
  keeps being the portfolio's only reliable converter.
- **Daily mix held on BOTH channels** (DB read-back): ch1 long 632 + 4 shorts; ch2 long
  654 + 4 shorts, long first-in-slot both. Yesterday's watch item (ch2 missing its long
  on 07-13) did not recur.
- **Language integrity (directive 6):** all 5 ch2 videos published today have PT-BR
  titles. One blemish: #601's title says "Pare Agora em 2024" — a stale year reference
  (idea-quality, not language; already published, no action possible).
- **ch1 subs-per-video (directive 9):** still 3 lifetime subs / 155 published. The
  post-07-13 wedge cohort has 0 measured views yet (<72h) — but see Actions: until
  today's fix, **zero t15 (wedge lead) videos had reached publication at all**, because
  publish order ignored topic weight.
- Boards: both channels `at_capacity` (ch1 2.2 days, ch2 2.0) → no idea generation, no
  trend adoption. BGM pool 20 tracks vs min 5 / target 15 — healthy, and rendering is
  active (8/channel yesterday), no refresh needed today.

## Monetization
| Channel | Subs | Watch hours | Shorts views | Lower-tier binding constraint |
|---|---|---|---|---|
| ch1 Owera Software | 9 / 500 (1.8%) | 3.0 / 3000 (0.1%) | 1,961 / 3M (0.1%) | subscribers |
| ch2 Rodrigo Recio | 59 / 500 (11.8%) | 4.2 / 3000 (0.1%) | 1,587 / 3M (0.1%) | subscribers |

Subscribers remain the binding constraint on both channels — exactly what the wedge
(ch1) and the hardware-niche weighting (ch2) target. No budget changes.

## Triage
7 issues in the digest; all handled without needing the operator:
- **Deleted rejected #541** (pre-language-fix EN straggler, 178h > 7d, digest said
  delete). Verified: row gone from the DB after the call.
- **Rejected the 6 remaining t3 (parked topic) pipeline videos** — approved 625–629 +
  queued 661. Directive 9 parked t3 (68 published, 0 subs ever) but these pre-park
  renders were still going to consume ~1.2 days of ch1 publish slots ahead of the wedge.
  Verified: all 6 read back `rejected`; ch1 approved backlog 52 → 47. Recoverable via
  `/retry` (video_path kept) if the operator disagrees.
- **Quota digest showed 9000/9000 spent on both channels** (`daily_limit_hit:false`).
  Both channels still published their full 5/5 today, so it isn't blocking — same
  self-resetting daily pattern as yesterday (was 8700–8750). Monitor only.
- **board_overflow t32** (6 pending vs ceiling 5): left as-is — yesterday's judgment
  already vetted these 6 as wedge-fit and they are queued (produced); no new information
  to re-trim on.
- **playlist_add 404, count 1** — same single-occurrence signature as yesterday (one 404
  then success). Not recurring-recurring yet; if it shows tomorrow again, find the stale
  playlist id (code-fix candidate).
- **Rejected #675/#679:** digest says leave; left (deletable >7d on 07-21).
- No failed/stuck/oauth issues. Scheduler running.

## Learned
- **The weight knob didn't reach the publish queue.** The 07-13 wedge repositioning
  changed idea generation and rendering, but `_next_approved` was strict approved_at
  FIFO — 34 non-wedge shorts (approved 07-08→07-10) stood ahead of the entire wedge
  cohort, which would have first published ~07-23: the checkpoint date itself, and the
  operator would have judged the wedge on data that contained no wedge videos. Fixed
  today (see Actions).
- **Conversion evidence keeps concentrating in concrete-hardware PT:** 542 (+1 sub,
  401 views), 524 (+1 sub), and the freshest high-retention video 589 are all in that
  vein; ch1's generic-EN videos with similar view counts convert 0. Consistent with
  directive 9's diagnosis.
- Hypothesis going forward: with weight-first publishing, ch1's 07-16/07-17 slots fill
  with t15/t5/t6 — if the wedge converts, per-video `subscribers_gained` should show it
  before the 07-23 review; if t15 stays at 0 while t5/t6 comparisons convert, rotate the
  wedge lead to comparisons.

## Actions
- **Code (1 of ≤2): `5858fd0` — publish order now respects topic weight** (both the
  long-reserve pick and the general pick order by `Topic.weight DESC`, then FIFO).
  Verified synchronously before commit by calling `_next_approved` on the live DB:
  ch1 flips 560 (t20, w1, approved 07-08) → **641 (t15 Copilot Credits, w3 wedge
  lead)**; ch2 → 655 (t4, w3 winner). verify_publish 22/22, verify_storyboard 40/40,
  manager restarted, dashboard 200 — the change is live for tonight's publishes.
  Logged as a running experiment (predicted: subscribers_gained up).
- **Triage:** deleted #541; rejected 625–629 + 661 (proof above).
- **Weights:** ch2 t27 (Rodar IA local — Ollama/VRAM/quantização) 1 → 2, on 589's
  62.8% avp + the t4 hardware twin converting. Read back: `weight: 2`.
- **Trends:** researched (web); logged **#31 CrewAI 1.14 pluggable memory backends** as
  `watch` (score 58 — minor release, fits existing CrewAI topics, boards at capacity).
  Nothing else new vs. the log (MS Agent Framework #28, ADK #29, smolagents #30 stand).
  Note: `POST /api/trends` rejects numeric `momentum` (422 — wants a string); known
  schema gotcha, worked around.
- **Seeding kit #5:** `run/seeding/2026-07-15.md` — TabNews Ollama-diagnosis post +
  r/brdev comment (589), LinkedIn vector-DB post (654) for ch2; dev.to failure-path
  checklist + LinkedIn post (632) for ch1; 3 adjacent-channel comment drafts.

## Engagement
**Baseline (07-15 golden set `baseline-0715`, 4/4 clean renders, visible=True, no
fallback):** R1 hook **2** (4/4 pain-first at t=0: "The best chunk is buried", "Your AI
forgot you again", "Cada API é um pesadelo", "Sua RAG responde lixo") · R2 info-gain
**2** (all 4 carry code + stat; varied mixes) · R3 clarity **2** (code frames indented +
line-highlighted, diagram fills frame, edge label clean beside connector) · **R4 pacing
1** — ch1-concept hook beat runs 9.28s then two 1.93s flashes; ch1-code has a 1.26s stat
flash; ch2 subjects clean (2.6–8.4s) · R6 payoff **2** (quotable closes 4/4) · R7 CTA
**2** (Follow/Segue + concrete next-payoff sub-line 4/4) · R8 thumb/title **2** (4/4
anchored to the specific problem, zero title echo — 1f70497 holding on day 1).

**Target lever:** R4 is the only weak lever, and it is owned by running experiment
`1ba57be` (settles tomorrow with fresh renders). Per the playbook's fallback rule, with
the rubric baseline otherwise strong, this run's improvement went to the clearly
higher-leverage pipeline gap instead: the weight-blind publish queue (`5858fd0`, gated
by the synchronous behavior check + both suites — details in Actions). **No prompt files
were touched**, so no render-gate delta to report; the 07-15 baseline stands as the
"before" for whatever ships next.

**Settled:** `b8238d5` (07-12, R7 follow-CTA + what-you-get-next) → **PROMOTED**,
SIGNAL-SCARCE. Its predicted signal is subscribers_gained, but the post-07-12 cohort is
too thin/young to attribute (both sub-gaining videos predate it). Render-judged on
today's baseline: 4/4 CTAs still carry the Follow verb + concrete next-payoff sub-line.
Kept; re-check on measured subs as the cohort matures.

**Still running:** `1ba57be` (R4 cue-gap, settles 07-16 — today's data: flashes persist
on ch1 subjects, drag mostly gone on ch2; if both flash and drag persist tomorrow, the
next R4 change should be a deterministic mid-beat floor in `align_storyboard`, not
another prompt rule) · `1f70497` (R8 anchored hooks, settles 07-17, holding) ·
`5858fd0` (publish-order, new today).

## Watch next run
- **Did weight-first publishing actually deliver?** ch1's 07-16 short slots should all
  be t15 videos (641 first). Read back the published list — this is the experiment's
  mechanism check.
- **Settle `1ba57be` (R4)** with fresh renders; decision guidance above.
- First wedge conversion data lands once t15 videos have ≥72h — quote per-video
  `subscribers_gained` (directive 9).
- ch2 at 59 with 8 days to the 75-sub checkpoint — needs ~2/day; the t27/t4 weighting
  and the unposted TabNews seed are the levers. Flag in Monday's growth review if the
  slope doesn't move.
- playlist_add 404: third day in a row → dig for the stale playlist id.
- Quota 9000/9000 pattern: verify it self-resets and publishes proceed on schedule.
