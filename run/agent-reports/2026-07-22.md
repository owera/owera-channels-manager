# Growth agent — 2026-07-22

**Commit:** 036bbb3

No operational issues found. Rubric baseline strong across every lever; shipped a
gate-robustness fix to `rubric_review.py` (stale-frame cleanup) instead of an engagement
lever. One analytics-justified weight bump (ch1 t18 MCP 1→2).

## Observed (key numbers)
- **Subs:** ch1 **10** (flat, 11+ days off 9→10; directive-9 hard review is 08-01), ch2 **60**.
  Binding constraint = subscribers on both channels (ch1 2% / ch2 12% of Lower Tier).
- **Distribution is abundant, conversion is the gap** (confirms directive-9 diagnosis). Traffic
  mix 07-18→07-22: ch1 SHORTS 5971/7319 (82%) + YT_SEARCH 824; ch2 SHORTS 6768/8285 (82%) +
  YT_SEARCH 955. The directive-7 falsifier (BROWSE/SUGGESTED/SHORTS ~0 → channel-level problem)
  is **NOT** triggered — the algorithm is testing the videos hard.
- **Analytics flowing:** ch1 measured 180, ch2 163.
- Both channels: 5/5 published today, at render-board capacity (10 pending = 2.0 days each),
  quota near cap (ch1 9000/9000, ch2 8950/9000). BGM pool healthy (20 / target 15).

### Per-video / per-topic winners (mature ≥72h)
- **ch1:** "MCP Isn't Magic — USB-C for AI" 123 views / **58.9% avp** / 21s avd / **+1 sub** —
  the standout ch1 explainer. Topic t18 MCP: 3 vids, 54.2% avp, +1 sub (best retention on ch1).
  t15 Copilot Credits (wedge lead, w3): 23 vids, 50.2% avp, 0 subs — healthy distribution, no
  conversion yet. "Cursor Feels Cheap—Until the Bill Hits" 47.6% avp (pricing wedge).
- **ch2:** "Você comprou 32GB de RAM — o gargalo era a VRAM" **405 views / +1 sub** (runaway
  hardware hit); "GPU usada barata pra IA" 69.8% avp. Topic converters: t4 Fundamentos-short
  (48 vids, +3 subs, w3), t2 Fundamentos-long (+2 subs, w2), t27 Rodar IA local (w2). ch2 weights
  already tuned to the sub-gainers — left unchanged.

## Monetization
| Metric | ch1 current / needed / pct | ch2 current / needed / pct |
|---|---|---|
| Subscribers (Lower) | 10 / 500 / **2.0%** | 60 / 500 / **12.0%** |
| Watch hours (Lower) | 4.2 / 3000 / 0.1% | 5.9 / 3000 / 0.2% |
| Shorts views (Lower) | 2,545 / 3.0M / 0.1% | 2,232 / 3.0M / 0.1% |
Binding constraint = **subscribers** both channels. No metric hit 100% this run.

## Triage
**No operational issues found.** Digest clean except two near-cap quota rows (both
`auto:false`, `daily_limit_hit:false` → monitor only; self-resets at UTC midnight). No
failed/rejected/stuck/oauth/error-run entries. BGM pool not low. Both channels at board
capacity → **no idea generation or trend adoption this run** (per the capacity gate).

## Learned (winners / hypotheses)
- ch1's highest-engagement, only-recently-converting topic is **MCP (t18)** — an "agents-explained"
  wedge pillar (directive 9) — yet it was weighted 1 while generic wedge t15 (0 subs / 23 vids)
  sits at 3. Hypothesis: refilling t18 more will surface more high-retention MCP explainers and is
  the best conversion bet on ch1 right now. **Action taken:** `PATCH /api/topics/18 {"weight":2}`
  (verified via read-back: weight=2). t15 left at 3 (operator-set wedge lead, directive 9).
- ch2 hardware/Fundamentos concentration holds: the runaway (405 views) and the sub-gainers are
  all concrete-hardware / fundamentals. ch2 weights already reflect this — no change.

## Engagement
**Rubric baseline (golden set, HEAD, label `baseline`) — all four subjects rendered clean
(visible=True, no fallback):**

| Lever | ch1-code | ch1-concept | ch2-code | ch2-concept |
|---|---|---|---|---|
| R1 hook (P1) | 2 | 2 | 2 | 2 |
| R2 info gain (P3) | 2 | 2 | 2 | 2 |
| R3 clarity (P6) | 2 | 2 | 2 | 2 |
| R6 payoff (P4) | 2 | 2 | 2 | 2 |
| R7 CTA (P8, elevated) | 2 | 2 | 2 | 2 |
| R8 thumbnail (P2) | 2 | 2 | 2 | 2 |

Hooks all pain-first at t=0 ("Your RAG grabs the wrong chunks", "Your AI agent has amnesia",
"Chega de reescrever integrações", "Sua RAG responde lixo"); diagrams dense and readable
(Query→Retrieve 20→Rerank→Top 5 with wide/tight edge labels); CTAs all carry the deterministic
follow verb + concrete what-you-get-next sub (Follow→Daily RAG fixes; Siga→Amanhã: como subir
seu MCP). **No weak engagement lever this run** — consistent with the last ~10 promoted
experiments having optimized the prompt levers.

**⚠ Scoring hazard caught (and fixed).** While scoring R7 I first read `ch1-code/b9_cta.png`
showing a weak "Try it → Add 5 lines today" (action CTA). The manifest showed ch1-code has only
9 beats (b0–b8) — `b9_cta.png` was a **stale frame from a prior render** left in the label dir.
The correct canonical frame `b8_cta.png` was "Follow → Daily RAG fixes" (R7=2). This documented
gotcha (`rubric_review` dirs keep stale frames) nearly produced a wrong verdict and threatens the
integrity of the mandatory quality gate — so this run's code change fixes the gate itself.

**Shipped (gate robustness, not a lever — rubric baseline already strong so this is the
higher-leverage work per the step-4 fallback):**
- `run/rubric_review.py`: clear stale `b*.png` beat frames from a label's subject dir before each
  render (`out_dir.glob("b*.png") → unlink`). Beat count varies run-to-run, so leftover frames
  (e.g. an old `b9_cta`) no longer match the manifest and can be mis-scored.
- **Verified behavior (not on faith):** planted stale `b9_cta.png` + `b12_stmt.png` into a subject
  dir, re-rendered `--only ch1-code` (produced 8 beats b0–b7) → both planted frames removed, only
  canonical frames remained ("PLANTED STALE FRAMES REMOVED ✓"). Render stayed visible=True /
  no-fallback. `tests/verify_storyboard.py` **40/40 green**. Change touches only a dev tool
  (its own process) — no manager restart needed; dashboard still 200.

**Experiment settled:** none matured this run. R1 (07-20, 42e4f07) stays `running` — shipped 48h
ago, needs ≥72h; re-judge on the 07-23 run.

## Actions taken
- `PATCH /api/topics/18 {"weight":2}` — ch1 MCP topic 1→2 (analytics + wedge justified; verified).
- Code: `run/rubric_review.py` stale-frame cleanup (committed; verified above).
- Wrote `run/seeding/2026-07-22.md` (ch1 = MCP USB-C explainer + Copilot pricing wedge; ch2 =
  32GB-RAM/VRAM hardware hit + used-GPU guide; 3 adjacent-channel comment drafts).
- No idea generation / trend adoption (both channels at board capacity).

## Watch next run (07-23 — directive-7 hard checkpoint)
- **Directive-7 checkpoint (07-23):** distribution is confirmed strong (SHORTS 82% both), so the
  falsifier does not fire — the gap is conversion, keep the engagement + wedge focus. ch1 still 10
  subs; the 08-01 ch1 pause decision (directive 9) is the next hard gate.
- Does bumping t18 MCP (w2) actually pull more MCP explainers into the ch1 render mix, and do they
  convert (per-video subscribers_gained)? First check when new t18 videos mature.
- R1 experiment (42e4f07) matures 07-23 — re-render-judge and settle.
- Confirm the stale-frame fix keeps future label dirs clean (no b*_ mismatch on the next baseline).
