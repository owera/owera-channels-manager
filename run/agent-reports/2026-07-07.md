# Growth Agent Run — 2026-07-07

No operational issues found. No operator action needed.

## Observed (key numbers)
| | ch1 Owera Software | ch2 Rodrigo Recio |
|---|---|---|
| oauth | connected ✓ | connected ✓ |
| published (total) | 111 (was 110) | 97 (was 93) |
| published today (by 09:00) | 1 | 4 |
| failed / stuck / review | 0 / 0 / 0 | 0 / 0 / 0 |
| board inventory | 2.0 d — at capacity | 2.0 d — at capacity |
| analytics measured | 104 / 111 | 88 / 97 |
| subscribers | 9 | 53 |

- **Yesterday's quota watch item: confirmed clean.** The 07-06 approved backlog (1 ch1 +
  4 ch2) published on schedule after the 07:00 UTC reset with **zero `failed` rows** — the
  publish loop deferred cleanly through the quota window (observed: counts above +
  `published_today` from `/api/agent/state` at 09:00 local).
- Quota 8500/9000 today (1700 ch1 + 6800 ch2), no `daily_limit_hit`.
- CTR/impressions still 0 on every measured video — R8 stays signal-scarce.
- BGM pool 15/15, `is_low:false`; `error_runs_24h` empty.

## Triage
**No operational issues found.** Issues digest `summary.clean:true` — zero
failed/rejected/stuck/review rows, no OAuth or cooldown entries, no music-gen errors.
No remediation API calls this run. (ch2 token: the 07-06 Production-publish fix is in;
watch date stays 07-12/13 — if `oauth` flips after that, rediagnose rather than assume
the weekly Testing-mode cycle.)

## Learn
- **ch2 concrete-topic pattern keeps holding** (mature leaderboard unchanged in shape):
  MacBook-trava 95.5 avp / MacBook+eGPU 79.5 / alugar-cluster 54.2 vs abstract-infra
  12–27. **H5 (t4 re-steer) still open** — videos 491–493 are <72h with no measured
  views yet (analytics lag); verdict expected ~07-08/09.
- **ch1**: mature avp leaders remain loop-friendly rewatch shorts (225.1 / 139.4 / 125.0)
  across Prompt-Engineering and Copilot-Credits topics; no weight changes warranted.
- **Settled: R3 diagram-fill (`9268c6f`, 07-04) → PROMOTED** (SIGNAL-SCARCE). The fix
  shipped ~12:00 UTC 07-04 and today's 72h mature cutoff is that same hour, so the mature
  post-fix retention cohort is ~empty — no SIGNAL-RICH verdict possible. Render-judge
  evidence (3rd consecutive day): today's baseline diagrams still fill the portrait frame
  (ch1-code 4-node ~62% width, ch1-concept large 3-node, nothing clipped). Kept in code;
  re-check retention once the post-07-04 cohort matures.
- Still running (too young, as planned): R3 cmp-stack `2092365` (~07-08), R3 elabel
  `7ec526f` (~07-09).
- Hypothesis (H7, new, from today's change): tool/API/protocol videos that SHOW a concrete
  code beat retain better than text-only beat mixes — measurable later via
  `creation_config` beat-mix attribution once those videos mature.

## Engagement
**Baseline** (golden set on HEAD, 4/4 clean renders `visible=True`, frames read):

| Lever | Score | Note |
|---|---|---|
| R1 hook | 2 | All 4 hooks at t=0, pain-first ("Your RAG pulls the wrong chunks" / "Sua RAG responde bobagem"). |
| R8 thumb/title | 2 | 0/4 title-echo; genuine gaps ("Why Your Retrieval Feels Broken" vs "Fix Your Slow RAG With a Reranker in 5 Lines"). |
| **R2 info gain** | **1** | **Weakest.** ch2-code — an MCP explainer, a *code* subject — rendered with NO code and NO stat beat (hook,lst,term,cmp,diagram,stmt,quote,cta), and its diagram chained entities incoherently (Servidor MCP → Modelo A → Modelo B → Ferramenta). Root cause: prompt rule 2 only "translates" the narration; a conceptual script that never reads code aloud gives the model nothing to translate. |
| R3 mobile clarity | 2 | All three shipped fixes hold: diagrams fill the frame, edge labels sit beside the connector with halo (ch1-code "top 20"/"scored", ch1-concept "save"/"feed"), cmp full-width stacked, code ≤~40 chars readable. Only the known frame-timing arrowhead nit (unchanged). |
| R4/R5/R6/R7/R10/R11 | 2 | Word-synced 89–99w scripts; early turns; quotable closes ("Lixo entra, lixo sai — começa no chunking."); specific CTA ("Try it → Add reranking today"); per-topic accents; shorts tight. Stat beats grounded ("0 memory by default", "10-20% sobreposição"). |

**Targeted R2 → shipped `160afde`.** Added rule 2b to `storyboard._system_prompt`: if the
video is ABOUT a tool, API, protocol, or coding technique, include at least one
`code`/`command` beat with a minimal realistic snippet (≤5 lines, ≤30 chars/line) — even
when the narration never reads code aloud.
- **Gate (all three passed):**
  - `--only ch2-code` after-render: mix became hook,**stat**,term,diagram,**code**,cmp,quote,cta —
    a correct `@mcp.tool()` Python snippet, a grounded "1x — escreva só uma vez" stat, hook
    still at t=0, diagram direction improved (IA → MCP → systems). **R2 1→2.**
  - `--only ch1-concept` control: gained a *sensible* memory-loop snippet
    (`ctx = db.search(query)` → `llm` → `db.save`), diagram + edge labels clean — no lever
    dropped (the rule triggering on a concept-adjacent subject produced added info gain,
    not a forced snippet).
  - `verify_storyboard` **40/40**; both gated renders `visible=True`, no fallback.
- **Made live:** committed → `launchctl kickstart` restart → `/api/dashboard` **200**
  (observed).
- **Settled:** R3 diagram-fill `9268c6f` → **promoted** (see Learn). Logged the new R2
  experiment in `run/experiments.jsonl` (`status:running`, predicted `avg_view_pct` up;
  render-judge re-check ~07-10).
- Two nits observed and left alone (candidates for a future run, not blockers): the code
  renderer flattens leading indentation (ch2-code's `return` renders flush with `def`),
  and hub-and-spoke relationships still render as a linear chain (IA→MCP→Slack→Banco→CRM).

## Monetization
Lower Tier for both; nothing crossed 100%. Binding constraints unchanged (Shorts views /
watch hours ~0%); ch2 subscribers remains the closest milestone (10.6%). Shorts
volume × retention stays the lever — today's R2 change targets info-gain-per-second,
i.e. retention. No override of the analytics-driven plan.

| Metric | ch1 current / needed / % | ch2 current / needed / % |
|---|---|---|
| Subscribers | 9 / 500 / 1.8% | 53 / 500 / **10.6%** |
| Watch hours | 2.1 / 3000 / 0.1% | 1.7 / 3000 / 0.1% |
| Shorts views | 1319 / 3M / 0.0% | 553 / 3M / 0.0% |

## Channel actions
**None this run — by design.** Both boards `at_capacity:true` (capacity gate: no idea
generation, no trend adoption), zero triage rows, BGM pool at target. Trend research
skipped for the same reason as 07-06: nothing could be adopted, and the watching bench
already holds two high-scoring matched candidates (25 *Rodar IA local em 2026* ch2 s88;
26 *Agent memory and state in production* ch1 s84) for the first board that frees up.

## Watch next run
- **Settle R3 cmp-stack (`2092365`)** — matures ~07-08 (render-judge; retention if the
  shorts cohort carrying cmp beats is thick enough).
- **Settle R3 elabel (`7ec526f`)** — ~07-09 (render-judge re-check).
- **R2 code-beat (`160afde`)** — render-judge re-check ~07-10; later, attribute via
  `creation_config` beat mix (code-carrying vs not) on mature videos.
- **H5 / t4 re-steer:** 491–493 mature ~07-08/09 — compare avp vs the ~24.6 abstract-era
  baseline.
- **BGM staleness:** pool 15/15 since 07-05; if still unchanged tomorrow (07-08), generate
  the variety batch of 5 (pipeline demand exists: 5 published today).
- **ch2 token:** watch date 07-12/13 post-Production-publish; expect NO revocation now.
- Code-beat indentation flattening + linear-chain diagrams: promote to the run's target if
  they recur in the next baseline.
- CTR/impressions: still 0; R8 re-settles on real CTR when impressions flow.

**Commits:** `160afde` (R2 rule 2b) + this report/experiment-log commit.
