# Growth agent — 2026-08-23

## ⚠ Needs operator

1. **PAUSE ch1 (hard review overdue since 08-01).** Now **12 subscribers** (was 11 on 08-19…08-22) vs the 15-sub bar. Lifetime video-attributed converters remain **four** (v558 MCP +1, v367 t6 +1, v43 t1 +1, v96 t5 +1) — the 11→12 move is **unattributed** (no new `subscribers_gained` on the leaderboard). Post-CTA / post-wedge volume still ~0 attributed subs (v785 ≈174 views / 148% avp / SHORTS=164 → 0). **Recommendation stands: PAUSE ch1** (or explicitly override with a new bar). Agent continues **zero ch1 strategy moves** (no weight/trend adopt/generate for growth). Pipeline hygiene only.
2. **ch2 metadata backfill** (`db7a254` plan) still awaits `--apply` if still desired.
3. **BGM pool path** remains external (`MoneyPrinterTurbo/resource/songs`). Count 20 (target 15); wipe risk still escalates to silent renders.

---

## Observed

- **Now:** 2026-08-23 ~12:00 UTC. Kill switch off, `scheduler_paused: false`, tree clean at start, `main`. Last full growth run: 2026-08-22.
- **ch1:** published **5/5** today, quota **8750/9000** (`daily_limit_hit: false`). Mix = **1 long + 4 shorts** (v999 t5 long first @ 07:01 `fL-pRLx94_0`, then v1041 t6, v1042 t6, v1043 t6, v1044 t18). **Eighteenth consecutive 1L+4S day** (08-06…08-23).
- **ch2:** published_today **0** at observe (window opens **14:00 UTC**). rendered_today **5**. Approved = **1 long + 11 shorts** (v1052 t2 long + 4×t4 1029/1034–1036 + 6×t27 998–1004 + leftover v709 t10 from July). **08-22 did publish 1L+4S** (v1038 t2 long first @ 14:01 `7HwG4gYvg1s`, then v1028 t4, then v995/997/996 t27) — yesterday’s 1L + t4 + 3×t27 prediction held.
- **ch2 subscribers: 64** (unchanged vs 08-21/08-22). Video-attributed `subscribers_gained` still the same six converters (v542/524/810/421/684/315). The 08-21 63→64 move is still unattributed.
- **ch1 subscribers: 12** (was 11). First move since 08-19. Subs-per-video on the mature leaderboard: **still 4 lifetime +1s**, none on the post-07-12 CTA / post-07-13 wedge cohort. Do not invent which video converted the 5th.
- **BGM pool:** 20 / min 5 / target 15 (`is_low: false`). No `music_gen` errors. No `error_runs_24h`. Last generate was 2026-08-20 — three days, not several; no refresh this run.
- **Boards (API `by_format` at observe):**
  - ch1 draft `{long:2, short:8}` · queued `{long:0, short:0}` · approved `{long:0, short:2}` (1013/1014 t7 leftover)
  - ch2 draft `{long:2, short:8}` · queued `{long:0, short:0}` · approved `{long:1, short:11}`
  - Both `at_capacity: true`, `days_of_inventory: 2.0`.
- **Language (ch2):** 08-22 titles all PT-BR ✓ (1038 monitoramento de modelo; 1028 travou 5 segundos; 995 GPU 24GB modelo errado; 997 chat travando; 996 Q4 vs Q8). 08-23 leading approved long v1052 also PT-BR (“Como Blindar seu Pipeline de Dados Contra Mudança de Schema”). No EN leak.
- **traffic_json (via leaderboard API, no DB dive):** v542 SHORTS=385; v785 SHORTS=164 + YT_SEARCH=6; v558 SHORTS=104 + YT_SEARCH=12; v948 SHORTS=64 + **EXT_URL=9** (unchanged vs 08-17…08-22) + NOTIFICATION=3; v884 YT_SEARCH=22 + SHORTS=77. SHORTS / YT_SEARCH dominate. **No BROWSE / SUGGESTED keys** on the top samples. No invented CTR.
- **Rebalance residual (this run’s fix):** JobRuns 19621–19629 at 14:00–14:01 UTC 08-22 demoted **all five** queued t4 shorts (1037, 1036, 1035, 1034, 1029), not one. Midnight recovered: produce 19647 queued v1052 long; 19648–19651 re-queued 1029/1034–1036; 1037 stayed draft (subject retained: “Seu Ryzen sem gráfico integrado rouba VRAM da IA? A verdade”). v1052 rendered 00:09 and is the 14:00 lead today.

### Publish log (settle evidence)

| day | ch1 | ch2 |
|-----|-----|-----|
| 08-16 | 5 (1L+4S) long 971 first | 5 (1L+4S) long 981 first + 4×t4 |
| 08-17 | 5 (1L+4S) long 972 first | 5 (1L+4S) long 955 first + 4×t4 |
| 08-18 | 5 (1L+4S) long 962 first + 3×t18 + t6 | **5 (1L+4S)** long 957 first + 4×t4 |
| 08-19 | **5 (1L+4S)** long 959 first + t18 + 3×t6 | **5 (1L+4S)** long 956 first + 3×t4 + t27 |
| 08-20 | **5 (1L+4S)** long 961 first + t18 + 3×t6 | **5 (1L+4S)** long 958 first + **4×t4** (1020–1023) |
| 08-21 | **5 (1L+4S)** long 960 first + t18 + 3×t6 | **5 (1L+4S)** long 982 first + **4×t4** (1024–1027) |
| 08-22 | **5 (1L+4S)** long 963 first + 4×t6 | **5 (1L+4S)** long 1038 first + t4 1028 + 3×t27 |
| 08-23 | **5 (1L+4S)** long 999 first + 3×t6 + t18 | pending window (0 at observe; 1L+11S banked, v1052 leading then 4×t4) |

## Monetization

| metric | ch1 current | ch1 needed | ch1 % | ch2 current | ch2 needed | ch2 % |
|---|---|---|---|---|---|---|
| subscribers | 12 | 488 | 2.4% | 64 | 436 | 12.8% |
| watch hours | 7.5 | 2992.5 | 0.2% | 11.9 | 2988.1 | 0.4% |
| Shorts views | 4386 | 2,995,614 | 0.1% | 4305 | 2,995,695 | 0.1% |

**Binding constraint both channels: subscribers** (lower tier). No milestone crossed (no metric hit 100% for the first time). ch1 11→12 is the first subscriber move since 08-19; still far from the 15-sub pause bar and from lower-tier 500. Prefer analytics (ch2 local-AI hardware shorts that convert; ch1 frozen) over pure milestone math.

## Triage

Digest at start: 1 issue (quota monitor only). `failed=[]`, `pipeline_starved` / stuck / oauth / bgm_pool_low / music_gen / `error_runs_24h` / `rejected` all empty. `summary.clean: false` only because of the quota row.

Timeout backlog remains empty (closed 08-20).

| Issue | Action | Proof |
|---|---|---|
| quota ch1 8750/9000 | monitor only | full 5-publish day (5×1750=8750); `daily_limit_hit: false` |
| failed / stuck / oauth / bgm_low / pipeline_starved / error_runs_24h / rejected | none | clean lists |

After-state board (API, unchanged by triage): ch1 queued `{long:0, short:0}` · approved `{long:0, short:2}` (1013/1014 t7); ch2 approved `{long:1, short:11}` (v1052 leading). Failed list stays empty.

## Learned

1. **ch2 08-22 mix prediction held:** 1L+4S, long v1038 first, then **v1028 t4** then 3×t27. Weight-first + long-reserve did what they said. Today’s plan: **1052 @ 14:00 then 1029/1034/1035/1036 all t4** (`GET /api/videos/publish-plan?channel_id=2`).
2. **ch1 moved 11→12.** Unattributed. Still 4 lifetime video +1s. Freeze holds; 12 does not clear 15.
3. **Winners (mature):** ch2 t4 23.6 avg views + **4 lifetime subs** (still the only converter); t28 25.8 @ 59.5% avp / 0 subs; t27 21.0 avg / 45.5 avp / **0 subs** (v948 EXT_URL=9 + SHORTS=64 is still distribution without conversion). ch1 t18 MCP 14.1 avg + the only recent sub-gainer; t15 high-avp loops, 0 subs on 46 videos.
4. **`rebalance-short-for-long` mechanism confirmed with a residual.** Midnight *did* queue v1052 (JobRun 19647) and it is approved, leading 14:00 — the predicted 08-23 long. But 14:00 demoted **five** shorts, not one, because `headroom < 0` (rendered_today=5) so auto_produce could not fill and every tick demoted another. Overnight recovered the mix; the afternoon queue was emptied for nothing.
5. **Hypotheses this run:** (a) ch2 08-23 window publishes 1L+4S with v1052 leading then 4×t4; (b) after 14:00, with queued currently empty, the new `headroom != 0` gate is a no-op today — first live “do not drain” signal is the next day a long publishes while shorts are still queued and rendered_today==budget; (c) operator pause remains the binding ch1 move — 12 subs still does not clear the 15-sub bar.

`publish-plan-mix` (shipped 08-21, `de23128`): second live day CONFIRMED (ch2 08-22 = v1038 @ 14:01 then t4 1028 then t27s, matching the picker). Still <72h — leave `running`, settle ~08-24.

`rebalance-short-for-long` (shipped 08-22, `d2d158f`): first live signal CONFIRMED (v1052 approved, plan lead). Residual closed same-run by `headroom == 0`. Leave `running`, settle ~08-25 from the 08-23 publish log.

## Acted (channels)

- **No requeue / retry / delete.** Failed list already empty. Cap not hit.
- **No weight changes.** t4=4 / t27=3 / t2=2 already match conversion. ch1 frozen (t15 remains w=0 as part of the freeze; not revived).
- **Trends logged (no adopt/displace — boards at capacity; every ch2 draft is w2+ so a new w1 topic cannot displace; ch1 frozen):**
  - **#82** ch2 score **85** watch **new** — “Muse Glimmer 30B local (Ollama / GGUF)”. Meta 30B dense, ~18GB Q4, Ollama **157.7K** downloads. Proof: `POST /api/trends` → id 82, status=watching.
  - **#73** ch2 score **84** (was 82) watch refresh — “Qwen3.8-27B local GGUF”. Ollama **632.2K** (was 573.9K). Proof: id 73, status=watching.
  - **#78** ch2 score **86** (was 88) watch refresh — “Nemotron 3.5 Lightning”. Ollama **129.2K** (was 127.6K; growth stalled). Proof: id 78, status=watching.
  - **#81** ch2 score **81** watch refresh — “Ollama 0.32.15 TTFT halved”. Still latest stable (v0.33 is rc). Proof: id 81, status=watching.
  - **#80** ch2 score **80** watch refresh — “Ollama default num_ctx silently truncates 128K models”. Proof: id 80, status=watching.
  - **#76** ch2 score **70** (was 72) watch refresh — “gpt-oss 20B/120B local MXFP4”. Proof: id 76, status=watching.
  - **#79** ch2 score **62** (was 64) watch refresh — “Ornith 1.0 9B”. Proof: id 79, status=watching.
  - **#77** ch1 score **70** reject refresh — “Copilot Max $100 / $200 credits (Aug 2026)” — *is* the wedge, rejected because ch1 is frozen. Proof: id 77, status=rejected.
- **No produce / no generate / no displace.** Auto_produce owns the overnight mix. ch1 approved 0L+2S is the known noon gap; rendered_today=5 so a manual produce cannot fill today.
- **No BGM generate** — pool 20, last refresh 08-20; not stale.
- **Seeding kit:** `run/seeding/2026-08-23.md` (operator posts; agent does not).

## Engagement

**Baseline `baseline`** — 4/4 clean, `visible=True`, none `used_fallback`. ~235s (first attempt hung 10 min on the storyboard LLM call — killed; retry completed). Scores from frames + scripts + beat timings:

| Lever | score | evidence |
|---|---|---|
| R1 hook (P1) | 2 | Pain-first on frame + opener ≤10w: “Your RAG returns garbage, and you don't know why.” / “Your AI agent has amnesia.” / “Escrever integração pra cada API tá te consumindo, né?” / “Sua RAG responde qualquer coisa, menos o que você perguntou.” Hook cards: “Your RAG returns garbage” / “Your AI agent has amnesia” / “Uma integração por API te suga” / “Sua RAG responde tudo errado”. Hook at t=0 all 4. |
| R8 thumb/title (P2) | 2 | Gaps vs titles (hook “Wrong answers, faster” vs title “Fix Your Slow RAG…”; “Its Memory Resets Every Time” vs “Why Your AI Agent Forgets…”; PT “Uma conexão substitui todas as outras” / “Você está cortando texto errado”). Zero title-word echo. |
| R2 info gain (P3) | 2 | Varied mixes (ch1-code: hook/lst/stat/stmt/term/code/diagram/cmp/cta + `scores = ce.predict(pairs)`; ch1-concept: cmp Default vs With memory + `db.save(q, reply)`; ch2-code fanout IA → Banco/API/Arquivos + `@mcp.tool()` indented; ch2-concept cmp + cmd `eval retrieval --top-k 5`). |
| R6 payoff (P4) | 2 | Quotable closes (“Retrieval gets you close; reranking gets you right.” / “Memory isn't automatic—it's architecture.” / “Pare de costurar integrações. Construa uma ponte e atravesse todas.” / “Chunk ruim, resposta ruim, sempre.”) |
| R5 retention (P5) | 2 | Open loops early on all 4 (“It's not your embeddings.” / “That's not a bug—it's how these models work.” / “E a verdade é simples: você não precisa disso.” / “O problema não é o modelo. É o seu chunking.”). |
| R3 clarity (P6) | 2 | Code legible + indented (ch2-code `return db.find(id)` under `def`); CMP stacked + VS in flow (Retrieval vs Reranking; Default vs With memory; Sem MCP vs Com MCP; Você culpa vs A culpa real); fanout on ch2-code truthful. List midpoint frames show all items (ch1-code lst 3.96s: embeddings + the model; ch2-concept lst 5.21s: lixo semântico + IA alucina). |
| R4 pacing (P7) | 2 | Hook at t=0. Min beat 1.80s (ch1-code stmt/term + ch2-code code — floor holding). **Known residual returned:** ch1-code diagram **10.98s DRAG**; ch2-code term 8.80s. Prompt-level R4 is exhausted (07-13/07-16 verdicts) — do not ship another R4 prompt rule. Drift still owns the long holds. |
| R7 CTA (P8) | 2 | Follow/Siga + named next value 4/4 (“Halve reranking latency tomorrow” / “Agent memory setup tomorrow” / “Servidor MCP do zero” / “Amanhã: tamanho de chunk exato”). Spoken ask present on all 4 scripts. |

**All-2 baseline (20th consecutive judged day) → non-engagement path (fallback clause).** No prompt change. The only soft spot is the known R4 residual (10.98s diagram hold), which prior verdicts already closed at the prompt layer. Recurring render-timeouts are gone (failed=[]). Shipping unproven polish on an all-2 set would violate the gate.

**Shipped instead (pipeline-robustness):** `_rebalance_queued_mix` only demotes a queued short when `headroom == 0` (midnight, budget reset, queue full). `headroom < 0` (render budget already spent) is now a no-op.

Lifecycle (written before the change, as required):

```
draft ─produce→ queued ─render_loop._submit_new→ rendering ─render_loop._advance→
   rendered → (review | approved)
approved ─publish_loop→ publishing → published
```

Rebalance still sets QUEUED → DRAFT (undo of produce; the short is not deleted), but only when auto_produce can actually use the freed slot this tick. `_auto_produce` (same tick, after rebalance) then DRAFT → QUEUED on the long when headroom becomes 1. `_submit_new` with `approved_longs==0` prefers that queued long. Publish still only consumes APPROVED.

| | 14:00 shape (rendered_today=5, 5 queued shorts) | midnight shape (rendered_today=0, 5 queued shorts) |
|---|---|---|
| before | 5 shorts QUEUED, long DRAFT | 5 shorts QUEUED, long DRAFT |
| after (two ticks) | **still 5 shorts QUEUED**, long DRAFT | **1 short DRAFT + 4 shorts QUEUED**, long **QUEUED** |

Proof: `PYTHONPATH=. .venv/bin/python tests/verify_render.py` → **121/121** (new cases: first tick no demote; second tick still no drain; long stays draft; auto_produce no-op while budget spent). Midnight cases unchanged (still demote exactly 1). `uv run python -c "import app.main"` ok. Manager restarted (`launchctl kickstart`); `GET /api/dashboard` = 200. Live 14:00 “do not drain” is **not** claimed today — ch2 queued is already empty at observe. Watch next run.

### Experiments settled / opened

- Settled: none (`publish-plan-mix` shipped 08-21, <72h, two live days confirmed; `rebalance-short-for-long` shipped 08-22, mechanism confirmed, residual tightened same-run, leave running).
- Opened: `rebalance-headroom-zero` commit `22056ea7bdd2e5c13e77986842a055cc8eefacdf`, status=running. Settle from the next 14:00 where ch2 has queued shorts and `approved_longs` just hit 0 with `rendered_today==budget` — those shorts should still be QUEUED at the following noon.

## Watch next run

1. Operator ch1 pause yes/no (still the binding decision). 12 subs does not clear the 15-sub bar — do not unfreeze. Quote whether any leaderboard row now shows `subscribers_gained` for the 11→12 move.
2. ch2 08-23 publish composition after 14:00 UTC — expect **1L+4S** with t2 long **v1052** leading then **4×t4** (1029, 1034, 1035, 1036). This is also more evidence for `publish-plan-mix` and the first publish-log settle point for `rebalance-short-for-long`.
3. After 14:00 today queued is empty, so the new gate is vacuously a no-op. First live “do not drain” signal: a later day when a long publishes at 14:00 while shorts are still QUEUED and `rendered_today==budget`. Those shorts should still be queued at the next noon (not five produce-demote JobRuns).
4. ch1 noon approved shorts=2 (t7 leftover) + 0 longs is thin; drafts already have 2 t5 longs + 4 t6 + 3 t18. Auto_produce after midnight should refill without a manual produce.
5. Failed list stays empty.
6. BGM count stays ≥ min (20, above target 15). Next refresh only if stale again *and* renders landed.
7. #82 Muse Glimmer (85, 157.7K) / #73 Qwen3.8 (84, 632.2K): adopt only as a t27/t4 episode via displacement if a w1-or-lower draft exists — not a new topic while t27 converts 0.
8. Quote whether v948 EXT_URL grows past 9 (seeding signal).

## Commit

`22056ea` (rebalance headroom==0). Report follows.
