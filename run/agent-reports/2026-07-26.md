# Growth agent — 2026-07-26

**Commits:** ed719f8 (autofill weight-priority + idea-batch clamp), + this report/log commit.

> ## ⚠ Needs operator
> 1. **You deleted ~35 ch1 videos yesterday via the UI** (external client, log-verified:
>    the stale 07-11→07-17 non-wedge approved shorts AND the 5 fresh 07-25 renders
>    736–740). I treated it as backlog cleanup and restored the standing directive-2 mix
>    on that assumption. **If it was a strategy signal instead (e.g. "fewer shorts on
>    ch1"), say so in tomorrow's run notes** and I'll re-plan — right now the standing
>    orders (4 shorts + 1 long, wedge-first) remain my instructions.
> 2. **Seeding kits: posted or not?** Third day asking — EXT_URL is ch1 3 / ch2 6
>    cohort-wide. One word settles how I read that number. If they ARE posted, I'll drop
>    link-posts for comment-only seeding.
> 3. Quota 8750/9000 daily on both channels (deterministic: 5 publishes + analytics).
>    No action needed; flagging closeness to the cap.

## The day's main event: both idea benches had collapsed into all-long topics
The operator deletion (ch1) plus normal bench turnover (ch2) emptied both boards
yesterday; at 00:19 the autofill loop refilled them and exposed **two real bugs**
(root-caused in `autofill_loop.py` / `video_gen.py`, fixed, shipped as ed719f8):

1. **id-order refill walk** — topics iterate with no weight priority, so the lowest-id
   long anchors drained the whole shared board horizon: ch1's bench became 8× t5 longs
   (wedge lead **t15 (w3): 0 pending — and dark since ~07-18** for exactly this reason);
   ch2's became 15× t2 longs (t4 w3 starved).
2. **No clamp on generator output** — t2 was asked for 8 ideas (board_space) and got 15
   (the LLM over-returned; every line was added) → the `board_overflow` 15/10 in today's
   digest.

Consequence observed live: **ch1 published 4 longs + 1 short today** (directive-2
inverted) because the approved pool held almost nothing else after the deletion.

**Fix (ed719f8):** refill walk now sorts weight-DESC (NULL=1, ties by id); idea batches
clamped to the ask at the root (`generate_ideas`) and re-clamped in `_refill_topic`.
Verified end-to-end: new `tests/verify_autofill.py` (10 checks — weight priority,
remainder flow-down, overshoot clamp, weight-0 parked, NULL weight) exercises the real
`tick()` on an in-memory DB; all 13 suites green (524 checks); manager restarted,
dashboard 200.

## Triage (all fixes read back from the DB/API — proofs quoted)
- **`board_overflow` t2 15/10 (auto:true, "produce or trim drafts")** + the mirror-image
  all-long ch1 bench: **rejected 12 surplus long drafts** via the documented reject
  endpoint (ch2: 757–765, ch1: 771–773; reason string names this run; subjects retained —
  delete is barred by guardrail, reject is the reversible trim available to me).
  Read-back: all 12 → `rejected`; drafts now ch2 = 6× t2, ch1 = 5× t5.
- **Refilled the starved w3 short topics inside the freed board space:**
  `POST /api/topics/15/generate {"count":5}` → `{"generated":3}` (clamped to board space,
  exactly as designed) — drafts 774–776, all Copilot-credits wedge subjects;
  `POST /api/topics/4/generate` → `{"generated":2}` — drafts 777–778, PT-BR hardware.
  Read-back quoted above. Auto-produce is shorts-first weight-DESC, so these queue ahead
  of the longs at 00:00 → **tomorrow both channels should publish 1L+4S again, with t15
  leading ch1's shorts.**
- `publish_starved` both (auto): the known auto-produce steady state — approved buffers
  (ch1 3 / ch2 4) + tonight's renders cover tomorrow; no action.
- `error_runs_24h` empty; no failed/rejected(pre-existing)/stuck/oauth rows; both channels
  `connected`; BGM 20/min 5 healthy (no music_gen errors; no refresh needed — pool
  unchanged is fine, ≥1 video rendered but pool isn't stale: tracks added 07-2x).
- **New (for the code agent): render budget overshoot** — both channels rendered **8/5**
  in the 00:15–00:24 UTC burst. Root cause: `JobRun.created_at` is local (GMT-3) but
  `quota._day_start()` compares against UTC midnight, so early-UTC renders count as
  "yesterday". Local-throttle only (no YT quota impact) → filed as BACKLOG #15 rather
  than spending the change cap here.

## Observed (key numbers)
- **Subs: ch1 10, ch2 60** (both unchanged; ch2 flat 3 days). Binding constraint:
  subscribers, both channels.
- **The mature R1+R7 cohort read (published 07-23, ≥72h): 0 subscribers gained on
  either channel.** ch1's whole 07-20→07-23 mature slice: ~208 views, 0 subs. Caveat for
  the 08-01 ch1 review: **the wedge lead t15 was dark the entire test window** (bench
  empty since ~07-18 — the autofill bug above), so the wedge hypothesis has NOT actually
  been tested by this cohort. Today's refill restarts the test with ~5 days of runway.
- **Traffic (07-09→ cohort, latest snapshots):** ch1 SHORTS 819 / SEARCH 98 (+17) /
  EXT_URL 3 (flat); ch2 SHORTS 1349 / SEARCH 244 (+18/day pace holds) / EXT_URL 6 (+1).
  BROWSE+SUGGESTED ≈ 0 on both — but SHORTS-feed distribution is real and growing, so
  the directive-7 falsifier (no algorithmic testing at all) is NOT triggered.
- **ch2 search terms stay tool-shaped** (langgraph, claude code, crewai) — the
  searchable-tool-title hypothesis keeps accumulating support.
- **Language integrity ch2: clean** — all 5 of today's titles PT-BR.
- Daily mix: ch2 clean (1 long 723 first, then 4 shorts). ch1 inverted (4L+1S) — cause
  and fix above.
- Publish windows (last night's backlog-12 feature): still unset on both channels
  (`publish_windows: null`) — publishes ran 07:01–09:05 UTC = **04:01–06:05 in Brazil,
  audience-dead hours for ch2**. Activation planned tomorrow (see Watch), kept out of
  today so tomorrow's mix-restoration read stays single-variable.

## Monetization
| Metric (Lower tier) | ch1 current / needed / pct | ch2 current / needed / pct |
|---|---|---|
| Subscribers | 10 / 490 / **2.0%** | 60 / 440 / **12.0%** |
| Watch hours | 4.3 / 2995.7 / 0.1% | 7.3 / 2992.7 / 0.2% |
| Shorts views | 2685 / ~3M / 0.1% | 2470 / ~3M / 0.1% |

No metric at 100%. Subscribers remain the binding constraint everywhere → wedge/series
conversion work stays the priority, and the wedge is finally re-stocked.

## Learned
1. **A weight is only a priority if every consumer honors it.** Publish order (5858fd0)
   and auto-produce (07-24) went weight-first; autofill didn't — and the bench is
   upstream of both, so the id-order walk silently defeated the whole weight system
   whenever the board was tight. The wedge going dark for a week was invisible until the
   bench collapsed entirely.
2. **The 08-01 ch1 decision needs the caveat:** conversion is still ~0 (10 subs), but
   the wedge-first strategy was only actually running 07-16→07-18. Today restarts it;
   07-27→31 is the real test window.
3. Operator UI actions are invisible to the audit log (`DELETE /api/videos` writes no
   JobRun) — diagnosing the deletion took the uvicorn access log. Filed mentally; if it
   recurs, a one-line JobRun on delete is a cheap code-agent item.

## Engagement
**Baseline (full golden set, 4/4 clean, visible=True, no fallback): all 8 levers score 2
— first all-strong baseline.**
| Lever | score | evidence |
|---|---|---|
| R1 hook (P1) | 2 | 4/4 pain-first ≤10w at t=0 ("Your RAG retrieves garbage…", "Sua RAG responde tudo, menos o certo"); holds 4.99–6.17s |
| R8 thumb/title (P2) | 2 | anchored hooks, zero title echo 4/4 |
| R2 info gain (P3) | 2 | 8–9 varied beats; real code/diagram/stat/cmp per subject |
| R6 payoff (P4) | 2 | quotable closes 4/4 ("Memory is engineered, not assumed") |
| R5 retention (P5) | 2 | truth-pivot mid-script, no sag |
| R3 clarity (P6) | 2 | stacked cmp intact, nothing clipped |
| R4 pacing (P7) | 2 | min beat 1.68s, max non-lst hold 6.9s; **every >5.5s beat carries exactly one drift tween (4/1/4/3)** |
| R7 CTA (P8) | 2 | spoken follow-ask + named next-value 4/4; visual card states the same promise |

**Shipped engagement change: none — deliberate no-op** per the playbook fallback clause:
the baseline is all-strong and the autofill bug was unambiguously the higher-leverage
work. The change cap went there (1 of 2 used).

**Running experiments (none matured):**
- **R7 spoken-CTA (f79fa33, 07-24)** — settles ≈07-27. Today's render evidence positive:
  4/4 closes carry the spoken ask + named next-value.
- **R4 long-hold drift (70f5320, 07-25)** — settles ≈07-28. **Verified live in
  production today** (yesterday's watch item): today's real rendered videos carry 4–12
  drift tweens each (`scale:1.045` in the job-dir index.html), long-forms more, plus the
  golden set counts above. No layout regression on any read frame.
- **board-mix (ed719f8, today)** — logged in `run/experiments.jsonl`; settles on the
  DB-readable publish mix (t15 cadence + 1L+4S) from 07-27.

## Actions taken
- Code: ed719f8 — autofill weight-priority + idea-batch clamp (+ tests/verify_autofill.py,
  10 checks); manager restarted, dashboard 200.
- API: 12 draft rejects (757–765, 771–773) + t15 gen (3 ideas: 774–776) + t4 gen
  (2 ideas: 777–778). All read back and quoted above.
- BACKLOG.md #15 filed (render-budget timezone accounting, evidence attached).
- Seeding kit `run/seeding/2026-07-26.md` (ch2: acurácia-vs-precisão TabNews post,
  OOM/KV-cache r/brdev comment, quantização adjacent-channel comment; ch1: dev.to
  same-system-twice post, r/LangChain comparison comment, LangGraph-checkpoint
  adjacent-channel comment, LinkedIn variant).
- No weight/trend/settings changes: weights already positioned, boards at capacity by
  design after the rebalance, and the digest is otherwise clean.

## Watch next run (07-27)
1. **Mix restoration is the experiment read:** ch1 should publish 1 long + 4 shorts with
   t15 leading (drafts 774–776 queue first — shorts-first weight-DESC); ch2 1L+4S. If
   ch1 publishes >1 long again, the board-mix hypothesis is wrong — investigate before
   re-litigating weights.
2. **Autofill's first live weight-first walk** (~00:19): with boards at cap it should
   SKIP both channels (no overshoot into the rejected-drafts space); when space frees,
   t15/t4 must be first to refill. Check the generate JobRuns' detail lines.
3. **R7 spoken-CTA settles** (72h from 07-24 ~09:00): re-render-judge per protocol.
4. **Publish-window activation for ch2** (planned): PATCH `publish_windows`
   ~"11:00-22:00" `publish_tz America/Sao_Paulo` so publishes stop landing 04:00–06:00
   audience time. Do it AFTER reading the mix restoration so the two changes don't
   confound; ch1 stays unset (global audience, no measured peak).
5. Operator answers on the deletion intent + seeding (block above).
6. Wedge conversion restart: first t15-cohort `subscribers_gained` reads land ~07-30 —
   two days before the 08-01 review, so 07-31's report must lead with the wedge verdict.
